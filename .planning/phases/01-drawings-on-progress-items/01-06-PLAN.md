---
phase: 01-drawings-on-progress-items
plan: 06
type: execute
wave: 3
depends_on: [01-04]
files_modified:
  - src/components/drawings/DrawingThumbnail.tsx
  - src/components/drawings/DrawingUploadSheet.tsx
  - src/components/drawings/DrawingViewer.tsx
  - src/components/drawings/DrawingVersionHistory.tsx
  - src/components/drawings/PdfPageNavigator.tsx
autonomous: true
requirements: [DRW-05, DRW-06, DRW-07, DRW-08, DRW-09, DRW-10, DRW-11]

must_haves:
  truths:
    - "DrawingThumbnail tile renders title (truncated 2 lines), revision label, and a status badge (現行 green / 已取代 grey strikethrough / 已撤回 red strikethrough)"
    - "Status badges are min text-lg (≥16pt) per D-22 + PITFALLS M2"
    - "DrawingUploadSheet bottom-sheet has 3 options (📷 拍攝 / 🖼️ 從相簿選擇 / 📁 從檔案選擇) and accepts up to 5 files per batch (D-02)"
    - "Soft warning toast appears when any picked file is >5 MB (`檔案較大，可能會慢`)"
    - "Hard block alert appears for any picked file >25 MB with the canonical Chinese error string (see Task 2 read_first for exact byte sequence — ISSUE-04 pin); that file is NOT uploaded"
    - "DrawingViewer is a full-screen modal with X-button close (D-09)"
    - "DrawingViewer lazy-loads viewer-pdf and viewer-zoom chunks (React.lazy)"
    - "DrawingViewer assumes DrawingsProvider is mounted upstream; it is only instantiated from DrawingsSection (which itself early-returns when no provider is present) — ISSUE-11"
    - "PDF viewer renders page-by-page with 1/N indicator and prev/next (D-12)"
    - "Pinch-zoom + pan works for both image and PDF modes via react-zoom-pan-pinch (DRW-05)"
    - "PdfWorker self-host module is imported by DrawingViewer before first render"
    - "Version-history button '📋 版本記錄' in viewer top-right opens DrawingVersionHistory listing all versions with effective-from/until dates (DRW-09)"
    - "DrawingVersionHistory resolves uploader display names via DrawingsContext.uploaderNameById[v.uploaded_by] || '未知' (ISSUE-03 fix — no raw UUIDs surfaced)"
    - "DrawingViewer root element has data-testid='drawing-viewer-zoom' for Playwright (per RESEARCH.md line 739, 761)"
  artifacts:
    - path: src/components/drawings/DrawingThumbnail.tsx
      provides: "One tile in the grid"
    - path: src/components/drawings/DrawingUploadSheet.tsx
      provides: "Bottom sheet with 3 upload options + 25MB hard block"
    - path: src/components/drawings/DrawingViewer.tsx
      provides: "Full-screen viewer with lazy-loaded pdf/zoom chunks"
      contains: "data-testid=\"drawing-viewer-zoom\""
    - path: src/components/drawings/DrawingVersionHistory.tsx
      provides: "Version list with status badges + effective dates + uploader display name from uploaderNameById"
    - path: src/components/drawings/PdfPageNavigator.tsx
      provides: "Prev/next + page indicator"
  key_links:
    - from: "DrawingViewer"
      to: "src/lib/pdfWorker.ts"
      via: "import './pdfWorker' side effect before first <Page>"
      pattern: "from '\\.\\./\\.\\./lib/pdfWorker'"
    - from: "DrawingUploadSheet"
      to: "useDrawings().uploadDrawing / uploadVersion"
      via: "context hook call"
      pattern: "useDrawings\\(\\)"
    - from: "DrawingVersionHistory"
      to: "useDrawings().uploaderNameById"
      via: "context map lookup keyed by version.uploaded_by"
      pattern: "uploaderNameById"
---

<objective>
Build the 5 view-layer components for drawings, each with a single clear responsibility. They consume the DrawingsContext from Plan 05 and ship as lazy-loaded chunks so the entry bundle stays under 800 KB.

Depends on:
- Plan 04 (react-pdf + react-zoom-pan-pinch installed; pdfWorker.ts ready; types in src/types.ts)
- Plan 05 is technically a Wave 3 sibling — components in this plan import from `'../../contexts/DrawingsContext'` and `'../../lib/drawings'`. Plan 05's <interfaces> block is the dependency, not the implementation.

Purpose: All visible UI for the drawings feature. Plan 07 wires them into ProjectDetail/ProgressItemCard.
Output: Five self-contained components ready to be composed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-drawings-on-progress-items/01-CONTEXT.md
@.planning/phases/01-drawings-on-progress-items/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@src/components/Modal.tsx
@src/components/CreateIssueModal.tsx
@src/components/IssueCard.tsx
@src/components/ProgressItemCard.tsx
@src/types.ts
@src/lib/pdfWorker.ts

<interfaces>
<!-- Component prop contracts — define here so Plan 07 can compose without exploring code. -->

// DrawingThumbnail.tsx
export interface DrawingThumbnailProps {
  drawing: Drawing
  currentVersion: DrawingVersion | null
  thumbUrl: string | null  // pre-resolved signed URL or null → render category icon
  onClick(): void
}
export function DrawingThumbnail(props: DrawingThumbnailProps): JSX.Element

// DrawingUploadSheet.tsx
export interface DrawingUploadSheetProps {
  open: boolean
  leafItemId: string                          // for create-new-drawing flow
  existingDrawingId?: string                  // for upload-new-version flow; if set, replaces title input with version-label input
  onClose(): void
  onUploaded?(drawingId: string): void
}
export function DrawingUploadSheet(props: DrawingUploadSheetProps): JSX.Element

// DrawingViewer.tsx
export interface DrawingViewerProps {
  version: DrawingVersion                     // the version currently displayed
  drawing: Drawing                            // for title + version-history wiring
  allVersions: DrawingVersion[]               // for the 版本記錄 modal
  onClose(): void
  onSelectVersion(version: DrawingVersion): void  // user picked a different version in history modal
}
export function DrawingViewer(props: DrawingViewerProps): JSX.Element

// DrawingVersionHistory.tsx (inner modal opened from viewer)
export interface DrawingVersionHistoryProps {
  open: boolean
  drawing: Drawing
  versions: DrawingVersion[]                  // sorted newest-first
  currentVersionId: string | null
  onClose(): void
  onSelect(version: DrawingVersion): void
}
export function DrawingVersionHistory(props: DrawingVersionHistoryProps): JSX.Element

// PdfPageNavigator.tsx
export interface PdfPageNavigatorProps {
  current: number
  total: number
  onChange(next: number): void
}
export function PdfPageNavigator(props: PdfPageNavigatorProps): JSX.Element
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create DrawingThumbnail + DrawingVersionHistory + PdfPageNavigator (the small components)</name>
  <files>src/components/drawings/DrawingThumbnail.tsx, src/components/drawings/DrawingVersionHistory.tsx, src/components/drawings/PdfPageNavigator.tsx</files>
  <read_first>
    - src/components/IssueCard.tsx (the visual model — thumbnail grid + badge styling)
    - src/components/Modal.tsx (base modal for VersionHistory)
    - src/types.ts (DRAWING_STATUS_ZH from Plan 04)
    - src/contexts/DrawingsContext.tsx (Plan 05 — uploaderNameById map shape)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Pitfall M2" lines 918-924 (badge sizing + colors)
    - .planning/codebase/CONVENTIONS.md §"Tailwind Usage" §"Status color palette"
  </read_first>
  <action>
    A) src/components/drawings/DrawingThumbnail.tsx:
       - Named export `function DrawingThumbnail(props: DrawingThumbnailProps)`.
       - Renders a tile: square (aspect-square), rounded-xl, shadow-card, white bg.
       - Top: 256x256 square area; if thumbUrl, `<img src={thumbUrl} className="w-full h-full object-cover rounded-t-xl" />`. If null, render a Lucide icon (`FileText` for PDF mime, `Image` for image mime) centered in the square with `text-site-400`.
       - Bottom: padding p-2. Title `<div className="text-sm font-medium text-site-900 line-clamp-2">`. Revision label `<div className="text-xs text-site-500">{revisionLabelOrDefault(currentVersion?.revision_label, currentVersion?.version_no ?? 1)}</div>`. Status badge below.
       - Badge component inline (or pull from DrawingVersionHistory if shared):
         - 'current' → `<span className="text-lg font-bold bg-green-100 text-green-700 px-2 py-0.5 rounded">現行</span>` (Tailwind text-lg = 18px, satisfies ≥16pt)
         - 'superseded' → `<span className="text-lg bg-gray-100 text-gray-500 line-through px-2 py-0.5 rounded">已取代</span>`
         - 'withdrawn' → `<span className="text-lg bg-red-50 text-red-600 line-through px-2 py-0.5 rounded">已撤回</span>`
       - Click handler: `<button onClick={onClick} className="w-full text-left">`. Apple HIG: min-h-44 globally enforced in CSS.
       - Use DRAWING_STATUS_ZH from src/types.ts (for accessibility, also set aria-label).

    B) src/components/drawings/DrawingVersionHistory.tsx:
       - Built on src/components/Modal.tsx base (which provides scrim + center positioning).
       - Title: '版本記錄' (D-13).
       - **Read DrawingsContext via useDrawings(): const { uploaderNameById } = useDrawings()** to resolve uploader display names (ISSUE-03 fix).
       - Body: a vertical list of versions (sorted newest-first via props.versions).
       - Each row:
         - `v{version_no}` (large, bold)
         - Status badge (same component as DrawingThumbnail — extract to a tiny `DrawingStatusBadge` sub-component in this file)
         - Effective dates: 'current' → `上載 {uploaded_at}` ; 'superseded' → `{uploaded_at} ~ {superseded_at}`; 'withdrawn' → `上載 {uploaded_at} / 撤回 {withdrawn_at}` (DRW-09)
         - Revision label
         - Uploader display name: `{uploaderNameById[v.uploaded_by] || '未知'}` (NEVER render the raw UUID — ISSUE-03 fix)
         - Tap row → onSelect(version) AND close
         - For non-current versions, dim the row (`opacity-60`) but still tappable
       - Format dates via toLocaleDateString('zh-HK') (CONVENTIONS.md) e.g. `new Date(version.uploaded_at).toLocaleDateString('zh-HK')`.
       - Close button: top-right X (Lucide X icon), large tap target.

    C) src/components/drawings/PdfPageNavigator.tsx:
       - Bottom-of-viewer floating bar.
       - Layout: `<div className="flex items-center justify-center gap-4 bg-black/70 text-white rounded-full px-4 py-2">`.
       - Left button: Lucide `ChevronLeft`, disabled when current === 1.
       - Middle: `<span className="text-base">{current} / {total}</span>`.
       - Right button: Lucide `ChevronRight`, disabled when current === total.
       - Clicks: onChange(current - 1) / onChange(current + 1).
       - Min-h-44 inherited from buttons.

    All three files: no semicolons, single quotes, named exports, inline Chinese. Each <300 lines.
  </action>
  <verify>
    <automated>test -f src/components/drawings/DrawingThumbnail.tsx && test -f src/components/drawings/DrawingVersionHistory.tsx && test -f src/components/drawings/PdfPageNavigator.tsx && grep -q "現行" src/components/drawings/DrawingThumbnail.tsx && grep -q "text-lg" src/components/drawings/DrawingThumbnail.tsx && grep -q "版本記錄" src/components/drawings/DrawingVersionHistory.tsx && grep -q "uploaderNameById" src/components/drawings/DrawingVersionHistory.tsx && grep -q "'未知'" src/components/drawings/DrawingVersionHistory.tsx && ! grep -qE "v\\.uploaded_by[^A-Za-z_]" src/components/drawings/DrawingVersionHistory.tsx || grep -q "uploaderNameById\\[" src/components/drawings/DrawingVersionHistory.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Three components exist; tsc passes; badges use text-lg + correct colors per M2; Chinese labels match D-22; DrawingVersionHistory uses uploaderNameById lookup (never renders raw UUID for uploaded_by).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create DrawingUploadSheet with 3 options + 25MB hard block + per-file progress</name>
  <files>src/components/drawings/DrawingUploadSheet.tsx</files>
  <read_first>
    - src/components/CreateIssueModal.tsx (lines 52-79 — onPickFiles state machine; the direct template per RESEARCH.md line 200)
    - src/components/Modal.tsx (base modal)
    - src/contexts/DrawingsContext.tsx (Plan 05 — the contract for uploadDrawing/uploadVersion)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Existing photo-upload pattern" lines 178-200
    - .planning/phases/01-drawings-on-progress-items/01-CONTEXT.md D-01..D-04

    **CANONICAL CHINESE STRING (ISSUE-04 pin) — copy EXACTLY byte-for-byte:**

    Hard-block error:  `檔案太大 (>25MB)，請壓縮後再上載`
                         ^^^^      ^             ^
                         |         |             full-width comma (U+FF0C)
                         |         half-width '>' (U+003E)
                         half-width parens (U+0028 / U+0029)

    Soft warning:      `檔案較大，可能會慢`
                            ^
                            full-width comma (U+FF0C)

    DO NOT substitute full-width parens (U+FF08 / U+FF09) or full-width '>' (U+FF1E). The Plan 09 manual UAT step (SC4) compares against the half-width form. The DrawingsContext error message in Plan 05 also uses this exact form.
  </read_first>
  <action>
    Create src/components/drawings/DrawingUploadSheet.tsx as a bottom-sheet modal:

    1. Open state controlled by props.open; render-null when !open.
    2. Layout: full-screen scrim + bottom-anchored white sheet with rounded-t-2xl, max-height 80vh, p-4. Use `<div className="fixed inset-0 z-50">` scrim + inner sheet.

    3. Top: title.
       - If existingDrawingId: '上載新版本'
       - Else: '新增圖則'
       - Right: close X button.

    4. Three big buttons (D-01). Each min-h-16, full-width, rounded-xl, border, with icon + label:
       - 📷 拍攝 — opens Capacitor Camera.getPhoto({ source: 'CAMERA' })
       - 🖼️ 從相簿選擇 — opens Capacitor Camera.getPhoto({ source: 'PHOTOS' })
       - 📁 從檔案選擇 — triggers a hidden `<input type="file" accept="image/*,application/pdf" multiple />` click

       On native (Capacitor.isNativePlatform() === true), use the @capacitor/camera plugin. On web, the camera/album buttons fall through to the same `<input type="file">` with `accept` and `capture="environment"` for camera variant.

       Import camera plugin lazily: `const { Camera } = await import('@capacitor/camera')` inside the click handler so the import doesn't block the entry bundle.

    5. File state machine (mirror CreateIssueModal.tsx pattern):
       ```ts
       interface FileSlot {
         id: string                       // local uuid
         file: File
         status: 'pending' | 'uploading' | 'done' | 'error'
         progress: number                 // 0..100
         error?: string
         tooLarge: boolean                // >25 MB hard-blocked
         softWarn: boolean                // >5 MB but ≤25 MB
       }
       const [slots, setSlots] = useState<FileSlot[]>([])
       ```
       Up to 5 slots (D-02). Reject excess with toast `'每批最多 5 個檔案'`.

    6. For each picked file:
       - if size > 25 * 1024 * 1024: setSlots add with tooLarge=true, status='error', and error set to the EXACT canonical string from <read_first>: `檔案太大 (>25MB)，請壓縮後再上載` (half-width parens, half-width '>', full-width comma). Display the row with red bg. DO NOT call upload.
       - else if size > 5 * 1024 * 1024: softWarn=true, status='pending'. Show toast with EXACT canonical string: `檔案較大，可能會慢` once per batch.
       - else: status='pending'.

    7. Optional title input (only for new-drawing flow): `<input placeholder="圖則標題" maxLength={120}>`.

    8. Optional revision label input: `<input placeholder="例如: Rev A 或 V1.2" maxLength={16}>` per D-07.

    9. Submit button '上載': disabled until valid; calls useDrawings().uploadDrawing or uploadVersion per slot in parallel; updates slot.progress/status from onProgress callback.

    10. Each slot row shows file name, size, progress bar, status label, and remove button.

    11. Cancel button '取消' (when nothing uploading) → onClose.
  </action>
  <verify>
    <!-- ISSUE-04 fix: tolerate either half-width or full-width parens via regex (the canonical form is half-width but downstream tests/UAT must accept either). -->
    <automated>test -f src/components/drawings/DrawingUploadSheet.tsx && grep -q "拍攝" src/components/drawings/DrawingUploadSheet.tsx && grep -q "從相簿選擇" src/components/drawings/DrawingUploadSheet.tsx && grep -q "從檔案選擇" src/components/drawings/DrawingUploadSheet.tsx && grep -qE "檔案太大\s*[\(（]>25MB[\)）]，請壓縮後再上載" src/components/drawings/DrawingUploadSheet.tsx && grep -q "檔案較大，可能會慢" src/components/drawings/DrawingUploadSheet.tsx && grep -q "25 \* 1024 \* 1024" src/components/drawings/DrawingUploadSheet.tsx && grep -q "@capacitor/camera" src/components/drawings/DrawingUploadSheet.tsx && grep -q "useDrawings" src/components/drawings/DrawingUploadSheet.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Component exists; supports 3 input modes; per-file slot UI works; hard-blocks >25 MB with the canonical Chinese string (parens half-width or full-width both acceptable to the verify regex, but the source MUST use half-width per the read_first pin); soft-warns >5 MB; calls useDrawings hook for actual upload; tsc passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create DrawingViewer (lazy-loaded full-screen modal w/ pinch-zoom + PDF)</name>
  <files>src/components/drawings/DrawingViewer.tsx</files>
  <read_first>
    - src/lib/pdfWorker.ts (from Plan 04 — must be imported FIRST before any <Page>)
    - src/components/drawings/DrawingVersionHistory.tsx (from Task 1)
    - src/components/drawings/PdfPageNavigator.tsx (from Task 1)
    - src/contexts/DrawingsContext.tsx (Plan 05 — getViewerUrl)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"PDF.js Worker Setup Gotcha" + §"Pattern 2: Lazy-loaded viewer"
    - .planning/phases/01-drawings-on-progress-items/01-CONTEXT.md D-09..D-13
  </read_first>
  <action>
    Create src/components/drawings/DrawingViewer.tsx as a full-screen modal. THIS COMPONENT itself is lazy-loaded by Plan 07's consumer — but it ALSO lazy-loads its own dependencies on react-pdf and react-zoom-pan-pinch via the manualChunks split.

    Approach: put the heavy library imports inline (top-of-file). They live in the viewer-pdf and viewer-zoom chunks per vite.config.ts manualChunks, so a static `import { TransformWrapper } from 'react-zoom-pan-pinch'` here goes into the viewer-zoom chunk automatically. The CALLER (Plan 07 / DrawingsSection) lazy-loads THIS file via React.lazy().

    **INVARIANT (ISSUE-11):** DrawingViewer ASSUMES DrawingsProvider is mounted upstream. It calls useDrawings() unconditionally. DrawingViewer must ONLY be instantiated from DrawingsSection, which itself early-returns when no DrawingsProvider is in scope (Plan 07 Task 2 enforces this via useDrawingsOptional check). Documented in success_criteria below.

    Module structure:

    1. Static imports at top:
       ```ts
       import { useState, useEffect, useMemo } from 'react'
       import { X, History as HistoryIcon } from 'lucide-react'
       import { TransformWrapper, TransformComponent } from 'react-zoom-pan-pinch'
       import { Document, Page } from 'react-pdf'
       import '../../lib/pdfWorker'  // side-effect: sets pdfjs.GlobalWorkerOptions.workerSrc — MUST be before any <Page>
       import 'react-pdf/dist/Page/AnnotationLayer.css'
       import 'react-pdf/dist/Page/TextLayer.css'
       import { useDrawings } from '../../contexts/DrawingsContext'
       import { DrawingVersionHistory } from './DrawingVersionHistory'
       import { PdfPageNavigator } from './PdfPageNavigator'
       import { revisionLabelOrDefault } from '../../lib/drawings'
       import type { Drawing, DrawingVersion } from '../../types'
       ```

    2. `export function DrawingViewer({ version, drawing, allVersions, onClose, onSelectVersion }: DrawingViewerProps)`:
       - State: signedUrl (string | null), urlError (string | null), pageNum (number, default 1), totalPages (number, default 0), historyOpen (boolean, false).
       - useEffect on version.id: call useDrawings().getViewerUrl(version) → setSignedUrl. On error, setUrlError.
       - Layout (full-screen modal with data-testid='drawing-viewer-zoom'): top bar with title + revision label + 📋 版本記錄 button + X close; body with TransformWrapper around <Document>/<Page> for PDFs or <img> for images; PdfPageNavigator at bottom for PDFs with totalPages > 1; DrawingVersionHistory mounted conditionally on historyOpen.

    3. Swipe-down close (D-09): minimal — X button is primary; swipe optional for v1.

    4. Safe-area inset: pb-safe-bottom or style env(safe-area-inset-bottom).

    5. NO console.error noise; surface url errors via the urlError banner.
  </action>
  <verify>
    <automated>test -f src/components/drawings/DrawingViewer.tsx && grep -q "data-testid=\"drawing-viewer-zoom\"" src/components/drawings/DrawingViewer.tsx && grep -q "TransformWrapper" src/components/drawings/DrawingViewer.tsx && grep -q "from 'react-pdf'" src/components/drawings/DrawingViewer.tsx && grep -q "'\\.\\./\\.\\./lib/pdfWorker'" src/components/drawings/DrawingViewer.tsx && grep -q "📋 版本記錄" src/components/drawings/DrawingViewer.tsx && grep -q "PdfPageNavigator" src/components/drawings/DrawingViewer.tsx && npx tsc --noEmit && npm run build:check</automated>
  </verify>
  <done>DrawingViewer exists with TransformWrapper for both PDF and image modes; lazy pdfWorker import at top; data-testid present; tsc passes; build:check still under 800 KB entry.</done>
</task>

</tasks>

<verification>
- All 5 components compile (`npx tsc --noEmit`).
- `npm run build:check` passes — entry chunk remains <800 KB; new `viewer-pdf` and `viewer-zoom` chunks present.
- Visual badge contrast ≥16pt verified by grep `text-lg` on status badges (M2).
- `grep -q getPublicUrl src/components/drawings/` returns nothing (defence-in-depth on top of Plan 05).
- DrawingVersionHistory never displays raw UUIDs for uploaded_by (uses uploaderNameById map).
- DrawingUploadSheet error string for >25MB matches the canonical pinned form (half-width parens, half-width '>', full-width comma).
</verification>

<success_criteria>
- Components self-contained — each has clear props, no codebase exploration required by Plan 07.
- DRW-05 (pinch-zoom), DRW-06 (PDF page-by-page), DRW-08 (size limits), DRW-09 (version history with dates + uploader names), DRW-10 (badges), DRW-11 (withdraw not hard-delete) all satisfied by components or wiring.
- **Invariant (ISSUE-11):** DrawingViewer assumes DrawingsProvider is mounted upstream; it is only instantiated from DrawingsSection (which itself early-returns when no provider is present). If DrawingsSection is ever rendered outside a DrawingsProvider, it must hide silently rather than crash. This invariant is enforced by Plan 07 Task 2's useDrawingsOptional guard.
- Bundle discipline preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-drawings-on-progress-items/01-06-SUMMARY.md` covering: component prop contracts as-built, lazy-load boundaries, uploaderNameById consumption pattern, canonical Chinese error-string pinning, any deferred polish (swipe-down close).
</output>
