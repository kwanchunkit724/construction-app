---
phase: 01-drawings-on-progress-items
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/v8-drawings.sql
  - supabase/v8-private-bucket-template.sql
  - supabase/tests/rls-smoke.sql
autonomous: false
requirements: [INF-01, INF-02, INF-03, INF-04, INF-05, DRW-12, DRW-13]

must_haves:
  truths:
    - "drawings and drawing_versions tables exist with leaf-only trigger enforced"
    - "project-drawings bucket exists and is PRIVATE (public=false)"
    - "Helper can_upload_drawing(uid, project_id) excludes subcontractor"
    - "Postgres RPC supersede_drawing_version(p_drawing_id, p_version_no, p_file_path, p_thumb_path, p_mime_type, p_size_bytes, p_revision_label, p_uploaded_by) exists; single-transaction insert+supersede+update; returns new version uuid"
    - "RLS policies on drawings/drawing_versions/storage.objects use security-definer helpers; no recursive policies"
    - "demo_feedback select policy is admin-only (m8 fix)"
    - "rls-smoke.sql exists and asserts 3 personas (admin / MC of A / subcon of B) get correct counts AND fails loudly if persona role-switch silently failed"
    - "Schema is pushed to live Supabase (verified via SQL: \\dt drawings)"
  artifacts:
    - path: supabase/v8-drawings.sql
      provides: "Schema, RLS, bucket, helper, supersede_drawing_version RPC, demo_feedback ride-along"
      contains: "create table drawings"
    - path: supabase/v8-private-bucket-template.sql
      provides: "Reusable comment-only template for Phase 2/3 (INF-02)"
    - path: supabase/tests/rls-smoke.sql
      provides: "INF-04 3-persona harness with role-switch assertion"
  key_links:
    - from: "drawings table"
      to: "progress_items.id (leaf-only trigger)"
      via: "assert_progress_item_is_leaf trigger"
      pattern: "create trigger drawings_leaf_only"
    - from: "storage.objects policies"
      to: "project_id"
      via: "(storage.foldername(name))[1]::uuid"
      pattern: "storage.foldername"
    - from: "DrawingsContext.uploadVersion (Plan 05)"
      to: "supersede_drawing_version RPC"
      via: "supabase.rpc('supersede_drawing_version', ...)"
      pattern: "create function supersede_drawing_version"
---

<objective>
Create the v8-drawings.sql migration that introduces the drawings/drawing_versions schema, the project-drawings private bucket, the can_upload_drawing helper, the supersede_drawing_version RPC (single-transaction atomicity for new-version flow), RLS policies for tables + storage.objects, the leaf-only trigger, the demo_feedback RLS fix ride-along, the reusable private-bucket template (INF-02), and the 3-persona RLS smoke harness (INF-04).

Purpose: Foundation that every later wave depends on. Without this, no upload code can run and no test can pass.
Output: One canonical migration file + two companion files; schema PUSHED to live Supabase via the developer (BLOCKING checkpoint).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-drawings-on-progress-items/01-CONTEXT.md
@.planning/phases/01-drawings-on-progress-items/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@supabase/v3-progress-schema.sql
@supabase/v4-issues-schema.sql
@supabase/v6-account-deletion.sql

<interfaces>
<!-- Existing helpers to reuse verbatim (do NOT redefine). From supabase/v3-progress-schema.sql:33-71 -->
can_view_project(p_user_id uuid, p_project_id uuid) returns boolean
  -- admin OR assigned_pm OR approved project_member
can_edit_project_progress(p_user_id uuid, p_project_id uuid) returns boolean
  -- admin OR assigned_pm OR approved member with role in ('pm','main_contractor','subcontractor')

<!-- NEW helper to add (D-19, D-25, excludes 'subcontractor'): -->
can_upload_drawing(p_user_id uuid, p_project_id uuid) returns boolean
  -- admin OR assigned_pm OR approved member with role in ('pm','main_contractor')

<!-- NEW RPC (ISSUE-09 fix): single-transaction supersede.
     Plan 05's uploadVersion calls supabase.rpc('supersede_drawing_version', { ... }). -->
supersede_drawing_version(
  p_drawing_id uuid,
  p_version_no int,
  p_file_path text,
  p_thumb_path text,           -- nullable
  p_mime_type text,
  p_size_bytes bigint,
  p_revision_label text,       -- nullable
  p_uploaded_by uuid
) returns uuid
  -- Atomic body:
  --   1. INSERT new drawing_versions row with status='current' RETURNING id → newId
  --   2. UPDATE drawing_versions SET status='superseded', superseded_at=now()
  --        WHERE drawing_id = p_drawing_id AND id <> newId AND status = 'current'
  --   3. UPDATE drawings SET current_version_id = newId WHERE id = p_drawing_id
  --   4. RETURN newId
  -- language plpgsql, NOT security definer (RLS still applies — caller must have can_upload_drawing).

<!-- progress_items table exists; FK target is progress_items(id). Leaf = no row with parent_id = this id. -->
<!-- demo_feedback existing policy from scripts/create-feedback-table.sql: "Authenticated read feedback" using (true) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write supabase/v8-private-bucket-template.sql (INF-02 reusable template)</name>
  <files>supabase/v8-private-bucket-template.sql</files>
  <read_first>
    - supabase/v3-progress-schema.sql (lines 33-71 for helper signature pattern)
    - supabase/v4-issues-schema.sql (existing bucket creation pattern at lines 126-128 — the ANTI-PATTERN to deviate from)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Migration File Outline" lines ~415-425
    - .planning/codebase/CONVENTIONS.md §"Supabase Migration File Naming"
  </read_first>
  <action>
    Create supabase/v8-private-bucket-template.sql as a COMMENT-ONLY documentation file (no executable SQL — it is INF-02 reference template for Phase 2/3 to copy). Contents:

    1. Header comment block: "Reusable PRIVATE bucket + RLS template. Phase 1 (drawings), Phase 2 (si-vo), Phase 3 (ptw) all instantiate this pattern. DO NOT execute this file — it is documentation."

    2. Documented sections (all as SQL comments `--`):
       - Bucket creation pattern: `insert into storage.buckets (id, name, public) values ('<name>', '<name>', false) on conflict (id) do nothing;`
       - Path convention: `{scope_id}/...` so `(storage.foldername(name))[1]::uuid = scope_id`
       - Two storage.objects policies template (select via can_view_X, insert via can_edit_X)
       - Rule: NO update/delete policies on storage.objects (immortal evidence)
       - Reference: helpers must be `language sql security definer stable set search_path = public`
       - Note: client always passes path starting with scope_id (defence-in-depth alongside RLS)

    Use single quotes per CONVENTIONS.md, no semicolons in JS code (this is SQL — semicolons required), inline Chinese comments OK.
  </action>
  <verify>
    <automated>test -f supabase/v8-private-bucket-template.sql && grep -q "PRIVATE bucket" supabase/v8-private-bucket-template.sql && grep -q "security definer set search_path = public" supabase/v8-private-bucket-template.sql && grep -q "storage.foldername" supabase/v8-private-bucket-template.sql</automated>
  </verify>
  <done>File exists; contains all five documentation sections; contains the literal strings "PRIVATE bucket", "security definer set search_path = public", and "storage.foldername".</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write supabase/v8-drawings.sql (full schema + RLS + bucket + supersede_drawing_version RPC + ride-along)</name>
  <files>supabase/v8-drawings.sql</files>
  <read_first>
    - supabase/v3-progress-schema.sql (lines 33-71: copy helper SHAPE for can_upload_drawing)
    - supabase/v3-progress-schema.sql (for progress_items columns + parent_id field)
    - supabase/v4-issues-schema.sql (lines 126-147 for bucket+policy pattern — note this uses public=true, we deviate)
    - scripts/create-feedback-table.sql (existing demo_feedback "Authenticated read feedback" policy to drop)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Migration File Outline" lines 268-420
    - .planning/phases/01-drawings-on-progress-items/01-CONTEXT.md (D-05, D-17..D-19, D-32, D-33)
  </read_first>
  <action>
    Create supabase/v8-drawings.sql as a single atomic migration applied via Supabase Dashboard SQL Editor. Required sections IN ORDER:

    1. Header comment: "v8-drawings.sql — Phase 1 of 工地控制系統 milestone. Skips contested v5/v6/v7 namespace per D-33. Introduces drawings + drawing_versions tables, project-drawings private bucket, can_upload_drawing helper, supersede_drawing_version RPC, leaf-only trigger, and demo_feedback RLS fix (INF-05/D-32 ride-along)."

    2. Defensive drops:
       ```sql
       drop function if exists supersede_drawing_version(uuid, int, text, text, text, bigint, text, uuid) cascade;
       drop trigger if exists drawings_leaf_only on drawings;
       drop function if exists assert_progress_item_is_leaf() cascade;
       drop function if exists can_upload_drawing(uuid, uuid) cascade;
       drop table if exists drawing_versions cascade;
       drop table if exists drawings cascade;
       ```

    3. Create PRIVATE bucket (D-17, public=false):
       ```sql
       insert into storage.buckets (id, name, public)
       values ('project-drawings', 'project-drawings', false)
       on conflict (id) do nothing;
       ```

    4. Create tables per RESEARCH.md §Migration File Outline step 5 (drawings + drawing_versions with current_version_id deferred FK, status check `in ('current','superseded','withdrawn')`, mime check `in ('application/pdf','image/jpeg','image/png')`, `unique(drawing_id, version_no)`).

    5. Leaf-only trigger (RESEARCH.md step 6) using `assert_progress_item_is_leaf()` function — `language plpgsql security definer set search_path = public`, raises exception 'drawings can only attach to leaf progress items' if `exists (select 1 from progress_items where parent_id = new.leaf_item_id)`. Attach as `before insert or update on drawings`.

    6. Indexes (RESEARCH.md step 7): idx_drawings_leaf_item, idx_drawings_project, idx_drawing_versions_drawing, idx_drawing_versions_status.

    7. New helper `can_upload_drawing(p_user_id uuid, p_project_id uuid) returns boolean language sql security definer stable set search_path = public` — body excludes 'subcontractor' role (admin + assigned_pm OR approved member with role in ('pm','main_contractor')). Copy SHAPE from can_edit_project_progress in v3-progress-schema.sql.

    8. NEW: supersede_drawing_version RPC (ISSUE-09 fix). Body MUST be a single atomic plpgsql block:
       ```sql
       create function supersede_drawing_version(
         p_drawing_id uuid,
         p_version_no int,
         p_file_path text,
         p_thumb_path text,
         p_mime_type text,
         p_size_bytes bigint,
         p_revision_label text,
         p_uploaded_by uuid
       ) returns uuid
       language plpgsql
       set search_path = public
       as $$
       declare
         new_id uuid;
       begin
         insert into drawing_versions (drawing_id, version_no, file_path, thumb_path, mime_type, size_bytes, revision_label, status, uploaded_by, uploaded_at)
         values (p_drawing_id, p_version_no, p_file_path, p_thumb_path, p_mime_type, p_size_bytes, p_revision_label, 'current', p_uploaded_by, now())
         returning id into new_id;

         update drawing_versions
           set status = 'superseded', superseded_at = now()
           where drawing_id = p_drawing_id
             and id <> new_id
             and status = 'current';

         update drawings
           set current_version_id = new_id
           where id = p_drawing_id;

         return new_id;
       end;
       $$;
       ```
       Note: NOT security definer — RLS still applies, caller must satisfy can_upload_drawing on the drawing_versions insert and the drawings update. Single transaction → either all three writes commit, or none do (no zero-current-version window).

       Grant: `grant execute on function supersede_drawing_version(uuid, int, text, text, text, bigint, text, uuid) to authenticated;`

    9. Enable RLS on both tables.

    10. Table policies (RESEARCH.md step 9):
        - drawings: "Members view drawings" (select using can_view_project); "Editors insert drawings" (insert with check can_upload_drawing); "Editors update drawings" (update using can_upload_drawing). NO delete policy.
        - drawing_versions: "Members view versions" (select using exists join via drawings + can_view_project); "Editors insert versions" (insert with check via drawings + can_upload_drawing); "Uploader or admin withdraws" (update using uploaded_by = auth.uid() OR admin).

    11. Storage bucket RLS (RESEARCH.md step 10): drop existing same-named policies, create "Members read drawings" (select where bucket_id='project-drawings' AND can_view_project(auth.uid(), (storage.foldername(name))[1]::uuid)), create "Editors upload drawings" (insert with check using can_upload_drawing). NO update/delete policies.

    12. Realtime publication:
        ```sql
        alter publication supabase_realtime add table drawings;
        alter publication supabase_realtime add table drawing_versions;
        ```

    13. demo_feedback RLS fix ride-along (D-32, INF-05, m8):
        ```sql
        drop policy if exists "Authenticated read feedback" on demo_feedback;
        create policy "Admin reads feedback" on demo_feedback for select to authenticated
          using (exists (select 1 from user_profiles where id = auth.uid() and global_role = 'admin'));
        ```

    Style: lowercase SQL keywords (match v3-progress-schema.sql style). All SECURITY DEFINER functions MUST include `set search_path = public` (PITFALLS C6).
  </action>
  <verify>
    <automated>grep -q "create table drawings" supabase/v8-drawings.sql && grep -q "create table drawing_versions" supabase/v8-drawings.sql && grep -q "'project-drawings', 'project-drawings', false" supabase/v8-drawings.sql && grep -q "can_upload_drawing" supabase/v8-drawings.sql && grep -q "create function supersede_drawing_version" supabase/v8-drawings.sql && grep -q "grant execute on function supersede_drawing_version" supabase/v8-drawings.sql && grep -c "security definer set search_path = public" supabase/v8-drawings.sql | awk '{exit ($1 < 2)}' && grep -q "drawings_leaf_only" supabase/v8-drawings.sql && grep -q "Admin reads feedback" supabase/v8-drawings.sql && grep -q "storage.foldername(name)" supabase/v8-drawings.sql && grep -q "alter publication supabase_realtime add table drawings" supabase/v8-drawings.sql</automated>
  </verify>
  <done>File compiles in Postgres syntax (developer pastes into Supabase SQL Editor without errors — see Task 4 BLOCKING checkpoint); contains all 13 sections above; can_upload_drawing body explicitly excludes 'subcontractor'; supersede_drawing_version function defined with grant to authenticated; demo_feedback policy is admin-only.</done>
</task>

<task type="auto">
  <name>Task 3: Write supabase/tests/rls-smoke.sql (INF-04 3-persona harness with role-switch assertion)</name>
  <files>supabase/tests/rls-smoke.sql</files>
  <read_first>
    - supabase/v8-drawings.sql (the file just authored — for table names + bucket id)
    - supabase/v3-progress-schema.sql (for project_members structure + can_view_project signature)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Migration File Outline" line 425 (rls-smoke spec)
    - .planning/codebase/STRUCTURE.md (note: supabase/tests/ is new directory)
  </read_first>
  <action>
    Create supabase/tests/rls-smoke.sql — a SQL script that exercises RLS from 3 distinct user perspectives and asserts row counts. Manual run via Supabase SQL Editor.

    Structure:
    1. Header comment: "INF-04 RLS smoke harness — paste into Supabase SQL Editor. Runs three personas via `set local request.jwt.claims = '{...}'` and asserts select count(*) on drawings + drawing_versions returns the expected numbers. ABORTs on mismatch using `raise exception`."

    2. Seed minimal fixture data inside a transaction (begin / rollback at end so test is idempotent):
       - Two fake projects (project-A uuid hardcoded, project-B uuid hardcoded)
       - Three fake users: admin (global_role='admin'), mc_of_a (project_members role='main_contractor' on project-A approved), subcon_of_b (project_members role='subcontractor' on project-B approved)
       - Insert one leaf progress_item per project
       - Insert one drawings row per project — bypass RLS via service-role context (`set role postgres;`)
       - Insert one drawing_versions row per drawing

    3. Three persona blocks. **ISSUE-05 fix: each block MUST begin with a role-switch assertion to catch silent failure.** Structure for each:
       ```sql
       set local request.jwt.claims = '{"sub": "<uid>", "role": "authenticated"}';
       set local role authenticated;
       do $$ begin
         if current_user <> 'authenticated' then
           raise exception 'persona did not switch to authenticated (current_user = %)', current_user;
         end if;
       end $$;
       do $$
       declare cnt int;
       begin
         select count(*) into cnt from drawings;
         if cnt <> <expected> then
           raise exception 'persona X: expected <N> drawings, got %', cnt;
         end if;
       end $$;
       ```
       - Expected counts:
         - admin: 2 drawings, 2 versions (sees all)
         - mc_of_a: 1 drawing (project-A only), 1 version
         - subcon_of_b: 1 drawing (project-B only — subcon can VIEW but not upload per can_upload_drawing exclusion), 1 version

    4. `rollback;` at end to leave DB clean.

    5. Final `raise notice 'rls-smoke.sql passed (3 personas verified)';`

    Note: This is a developer-run harness, not yet a Playwright test.
  </action>
  <verify>
    <automated>test -f supabase/tests/rls-smoke.sql && grep -c "request.jwt.claims" supabase/tests/rls-smoke.sql | awk '{exit ($1 < 3)}' && grep -c "persona did not switch to authenticated" supabase/tests/rls-smoke.sql | awk '{exit ($1 < 3)}' && grep -q "raise exception" supabase/tests/rls-smoke.sql && grep -q "rollback" supabase/tests/rls-smoke.sql</automated>
  </verify>
  <done>File exists; contains 3 `request.jwt.claims` blocks (admin / mc_of_a / subcon_of_b); each block opens with a role-switch assertion that raises exception if current_user ≠ 'authenticated'; contains at least one raise-exception count assertion per persona; transaction-wrapped with rollback at end.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: [BLOCKING] Developer pushes v8-drawings.sql to live Supabase</name>
  <what-built>
    The migration file supabase/v8-drawings.sql is complete and locally validated. The schema is NOT yet on live Supabase — there is no automated `supabase db push` configured (verified: package.json has no migration script; codemagic.yaml does not invoke supabase CLI). Pasting into the Supabase Dashboard SQL Editor is the only path.
  </what-built>
  <how-to-verify>
    1. Open https://app.supabase.com → CK工程 project → SQL Editor → New query.
    2. Paste the ENTIRE contents of `supabase/v8-drawings.sql` and Run.
    3. Expect: "Success. No rows returned" (table/bucket creation has no row output).
    4. Verify in same SQL Editor: `select table_name from information_schema.tables where table_name in ('drawings','drawing_versions');` — must return 2 rows.
    5. Verify bucket: `select id, public from storage.buckets where id = 'project-drawings';` — must return 1 row with `public = false`.
    6. Verify helper: `select proname from pg_proc where proname = 'can_upload_drawing';` — must return 1 row.
    7. Verify RPC: `select proname from pg_proc where proname = 'supersede_drawing_version';` — must return 1 row.
    8. Verify demo_feedback fix: `select policyname from pg_policies where tablename = 'demo_feedback';` — must include 'Admin reads feedback' and MUST NOT include 'Authenticated read feedback'.
    9. (Optional but recommended) Paste `supabase/tests/rls-smoke.sql` into SQL Editor — must end with `NOTICE: rls-smoke.sql passed (3 personas verified)`.
  </how-to-verify>
  <resume-signal>Type "pushed" (with all 7 verifications passing) or paste error output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → Storage API | Untrusted authenticated user uploads/reads drawing blobs |
| client → Postgres (drawings/drawing_versions) | Untrusted authenticated user reads/writes rows |
| auth.uid() ↔ (storage.foldername(name))[1]::uuid | RLS bridges auth identity to project scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | I (Information disclosure) | storage.objects on project-drawings | mitigate | Bucket created with `public = false`; only `createSignedUrl` minted; RLS policy requires `can_view_project(auth.uid(), (storage.foldername(name))[1]::uuid)` (PITFALLS C1). |
| T-01-02 | E (Elevation of privilege) | drawings/drawing_versions INSERT | mitigate | `can_upload_drawing` EXCLUDES `subcontractor` role; RLS policies use it on both table inserts AND storage.objects insert (defence-in-depth). |
| T-01-03 | D (DoS) | RLS recursive-policy meltdown | mitigate | All helpers `security definer set search_path = public` (PITFALLS C6); `drawing_versions` policy joins via `drawings` table with helper, not raw subquery on itself; rls-smoke.sql asserts non-empty + correct counts AND role-switch state to detect recursion or silent persona failure. |
| T-01-04 | T (Tampering) | drawings hard-delete | mitigate | No DELETE policies on drawings, drawing_versions, or storage.objects; UI must not call .delete(); only soft `status='withdrawn'` via UPDATE policy (DRW-11). |
| T-01-05 | I (Information disclosure) | demo_feedback over-permissive RLS | mitigate | Drop "Authenticated read feedback" and replace with admin-only select (INF-05, D-32, m8). |
| T-01-06 | S (Spoofing) | Path manipulation to access cross-project blobs | mitigate | Client-side path validator in src/lib/drawings.ts (Plan 05) refuses any path not starting with active projectId; server-side `(storage.foldername(name))[1]::uuid` RLS independently enforces same constraint. |
| T-01-07 | R (Repudiation) | Drawing version supersession audit | mitigate | New version = single-transaction RPC `supersede_drawing_version` (INSERT new + UPDATE old to superseded + UPDATE current_version_id); old rows retain status='superseded' + superseded_at timestamp; never UPDATE in place. Partial failure rolls back the whole txn — no zero-current-version window (ISSUE-09 fix). |
| T-01-08 | I (Information disclosure) | Trigger function source-path injection | mitigate | `assert_progress_item_is_leaf()` declared `security definer set search_path = public` per PITFALLS C6. |

</threat_model>

<verification>
- v8-drawings.sql parses + applies cleanly in Supabase SQL Editor (Task 4).
- All 7 verification queries in Task 4 return expected results.
- rls-smoke.sql runs to NOTICE without raising any exception (and asserts persona role-switch).
- supersede_drawing_version RPC is grantable to authenticated.
- No DROP TABLE statements outside the initial defensive-drop block (storage data is sacred).
- Grep `getPublicUrl` across the repo returns ONLY the legacy issue-photos callsite (no drift into drawings code).
</verification>

<success_criteria>
- Migration file present at supabase/v8-drawings.sql with all 13 required sections including supersede_drawing_version RPC.
- Companion template at supabase/v8-private-bucket-template.sql (INF-02).
- RLS smoke harness at supabase/tests/rls-smoke.sql with role-switch assertion per persona (INF-04).
- Developer has pushed schema to live Supabase and confirmed all 7 verification queries.
- demo_feedback RLS hardened (INF-05).
</success_criteria>

<output>
After completion, create `.planning/phases/01-drawings-on-progress-items/01-01-SUMMARY.md` covering: files created, schema decisions, supersede_drawing_version semantics, how to extend the bucket template in Phase 2/3, gotchas encountered during the push.
</output>
