---
phase: 01-drawings-on-progress-items
plan: 07
type: execute
wave: 4
depends_on: [01-05, 01-06]
files_modified:
  - src/components/drawings/DrawingsSection.tsx
  - src/components/ProgressItemCard.tsx
  - src/pages/ProjectDetail.tsx
autonomous: true
requirements: [DRW-01, DRW-02, DRW-03, DRW-04, DRW-09, DRW-11, DRW-14, DRW-15]

must_haves:
  truths:
    - "DrawingsProvider is mounted at ProjectDetail level (scoped to projectId)"
    - "Each leaf progress item shows a 圖則 (N) action button in its action row (N = count of drawings)"
    - "Tapping the action button reveals an inline DrawingsSection with grid + search + + 新增圖則 button"
    - "Grid is 2 cols on mobile, 4 cols on md+ (BlueStacks tablet)"
    - "Search input filters by title substring case-insensitive, debounced 200ms (D-24)"
    - "Default sort: created_at DESC (D-23) — newest first"
    - "+ 新增圖則 button is visible ONLY when ['admin','pm','main_contractor'].includes(profile.global_role) (D-25 — ISSUE-07 fix: array.includes, NOT JS shorthand-OR)"
    - "Overflow menu provides: 上載新版本, 查看版本記錄, 撤回 (撤回 visible only to original uploader or admin per D-26)"
    - "DrawingViewer is lazy-loaded via React.lazy on first viewing"
    - "Empty state shows 尚未有圖則 placeholder + + 新增圖則 button"
    - "DrawingsSection imports DrawingsContext directly from Plan 05 (raw context export is guaranteed by Plan 05 — no fallback path needed)"
  artifacts:
    - path: src/components/drawings/DrawingsSection.tsx
      provides: "Inline section: search + grid + upload button + viewer trigger; lazy-loads DrawingViewer"
      contains: "lazy(() => import"
    - path: src/components/ProgressItemCard.tsx
      provides: "Modified to add 圖則 (N) action button + inline DrawingsSection slot"
    - path: src/pages/ProjectDetail.tsx
      provides: "Mounts DrawingsProvider around the page content"
  key_links:
    - from: "ProjectDetail.tsx"
      to: "DrawingsProvider"
      via: "<DrawingsProvider projectId={...}> wrapping the page's main content"
      pattern: "DrawingsProvider"
    - from: "ProgressItemCard"
      to: "DrawingsSection"
      via: "inline conditional render when isLeaf and section toggled open"
      pattern: "DrawingsSection"
    - from: "ProgressItemCard.useDrawingsOptional"
      to: "Plan 05's DrawingsContext named export"
      via: "useContext(DrawingsContext) — returns null when no provider in scope"
      pattern: "import \\{ DrawingsContext \\} from '../contexts/DrawingsContext'"
---

<objective>
Wire the drawings feature into the live UI. CONTEXT.md D-21 says "圖則 section appears ABOVE the existing issues section" — but RESEARCH.md §"Existing Code Analysis" lines 166-174 shows there is NO leaf-item detail screen; leaf items are inline ProgressItemCard. We adopt Option A from RESEARCH.md: add a 圖則 (N) toggle button to the leaf card's action row; tapping reveals the DrawingsSection inline directly below the card content. Matches the codebase's modal-orchestration idiom and is strictly less risk than introducing a new route.

Depends on: Plan 05 (DrawingsContext + raw `DrawingsContext` named export) and Plan 06 (5 components).

Purpose: User-facing feature complete.
Output: A PM can upload, view, version, and withdraw drawings on any leaf progress item in a project.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-drawings-on-progress-items/01-CONTEXT.md
@.planning/phases/01-drawings-on-progress-items/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@src/pages/ProjectDetail.tsx
@src/components/ProgressItemCard.tsx
@src/contexts/AuthContext.tsx
@src/contexts/ProgressContext.tsx
@src/contexts/IssuesContext.tsx
@src/contexts/DrawingsContext.tsx
@src/components/drawings/DrawingThumbnail.tsx
@src/components/drawings/DrawingUploadSheet.tsx
@src/components/drawings/DrawingViewer.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/components/drawings/DrawingsSection.tsx</name>
  <files>src/components/drawings/DrawingsSection.tsx</files>
  <read_first>
    - src/contexts/DrawingsContext.tsx (Plan 05 — useDrawings hook API + uploaderNameById)
    - src/components/drawings/DrawingThumbnail.tsx (Plan 06)
    - src/components/drawings/DrawingUploadSheet.tsx (Plan 06)
    - src/contexts/AuthContext.tsx (useAuth().profile.global_role for role gating)
    - .planning/phases/01-drawings-on-progress-items/01-CONTEXT.md D-21..D-27
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Pattern 2: Lazy-loaded viewer"
  </read_first>
  <action>
    Create DrawingsSection.tsx:

    1. Lazy import DrawingViewer: `const DrawingViewer = lazy(() => import('./DrawingViewer').then(m => ({ default: m.DrawingViewer })))`. Wrap viewer mount in `<Suspense fallback={<FullPageSpinner label="載入中..." />}>`.

    2. Props: `{ leafItemId: string }`.

    3. Read `useAuth().profile` and `useDrawings()`. Filter drawings by `leaf_item_id === leafItemId`.

    4. Search state with 200ms debounce (D-24). Filtering: case-insensitive title substring.

    5. Sort filtered list by `new Date(created_at).getTime()` DESC (D-23).

    6. **canUpload (ISSUE-07 fix — explicit, correct expression):**
       ```ts
       const canUpload = !!profile && ['admin', 'pm', 'main_contractor'].includes(profile.global_role)
       ```
       Do NOT write `profile?.global_role === 'admin' || 'pm' || 'main_contractor'` — that JavaScript expression is buggy (it evaluates to truthy `'pm'` for everyone). UI gating is convenience; RLS is the server-side truth.

    7. Thumbnail URLs: useEffect fetches getThumbUrl for each drawing's currentVersion; stores in `Record<versionId, string | null>` state.

    8. Section header: `<h3>圖則 ({itemDrawings.length})</h3>` + `+ 新增圖則` button (when canUpload).

    9. Search input below header. Debounced.

    10. Empty state: `尚未有圖則` text + `+ 新增圖則` button (when canUpload).

    11. Grid: `grid grid-cols-2 md:grid-cols-4 gap-3`. For each drawing, render `<DrawingThumbnail>` with thumbUrl from state. Overlay an `⋯` overflow button (top-right) when canUpload; opens a menu with three items: `上載新版本`, `查看版本記錄`, `撤回`. `撤回` only visible when `currentVersion.uploaded_by === profile?.id || profile?.global_role === 'admin'` (D-26). Wrap `撤回` action in `confirm('確定撤回此版本?')`.

    12. DrawingUploadSheet mounted conditionally (open state holds mode='new' or {mode:'version', drawingId}).

    13. DrawingViewer mounted conditionally inside Suspense when `viewing` state set; close handler clears.

    Style per CONVENTIONS.md: no semicolons, single quotes, inline Chinese.
  </action>
  <verify>
    <!-- ISSUE-07 fix: enforce array.includes pattern, not shorthand-OR. -->
    <automated>test -f src/components/drawings/DrawingsSection.tsx && grep -q "lazy(() => import('./DrawingViewer')" src/components/drawings/DrawingsSection.tsx && grep -q "新增圖則" src/components/drawings/DrawingsSection.tsx && grep -q "尚未有圖則" src/components/drawings/DrawingsSection.tsx && grep -q "grid-cols-2 md:grid-cols-4" src/components/drawings/DrawingsSection.tsx && grep -q "includes(profile.global_role)" src/components/drawings/DrawingsSection.tsx && ! grep -qE "'admin'\s*\|\|\s*'pm'\s*\|\|\s*'main_contractor'" src/components/drawings/DrawingsSection.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>DrawingsSection exists; lazy-loads viewer; canUpload uses `['admin','pm','main_contractor'].includes(profile.global_role)` (NOT the buggy shorthand-OR pattern); empty state + grid + search wired; tsc passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Modify ProgressItemCard.tsx to add 圖則 (N) action button + inline section</name>
  <files>src/components/ProgressItemCard.tsx</files>
  <read_first>
    - src/components/ProgressItemCard.tsx (full file — locate the action row at lines 178-236 per RESEARCH.md line 164)
    - src/pages/ProjectDetail.tsx (find the isLeaf detection — search for `isLeaf` or `leaf` or `children.length === 0` in the file; ISSUE-12: the executor reads the file to find the exact lines that derive whether a progress_item is a leaf, then propagates the same logic / variable into ProgressItemCard if not already passed as a prop)
    - src/contexts/DrawingsContext.tsx (Plan 05 — for both `useDrawings` AND the raw `DrawingsContext` named export, which Plan 05 guarantees to exist)
    - src/components/drawings/DrawingsSection.tsx (just-written component)
  </read_first>
  <action>
    Edit src/components/ProgressItemCard.tsx (read existing file first to confirm exact structure):

    1. **isLeaf detection (ISSUE-12 fix):** The exact source of truth for "is this progress_item a leaf" lives in src/pages/ProjectDetail.tsx (or another file in the progress tree-building pipeline). Read that file FIRST and locate the lines that derive leaf status — typical patterns are `item.children?.length === 0`, `!hasChildren`, or a precomputed `isLeaf` field on the item. If ProgressItemCard already receives `isLeaf` as a prop or derives it locally, reuse that. If not, propagate the same computation (do NOT invent a new heuristic). Document in the SUMMARY which file defines isLeaf and where you imported the logic from.

    2. **Import DrawingsContext directly (ISSUE-01 fix — Plan 05 guarantees this named export):**
       ```ts
       import { useContext } from 'react'
       import { DrawingsContext } from '../contexts/DrawingsContext'
       import { DrawingsSection } from './drawings/DrawingsSection'
       ```

    3. Define `useDrawingsOptional` helper at top of the file (or as an inline helper):
       ```ts
       function useDrawingsOptional() {
         return useContext(DrawingsContext)  // null if no DrawingsProvider in scope
       }
       ```
       This is the safe path because ProgressItemCard may render outside ProjectDetail (e.g. Dashboard preview). When no provider is mounted, `drawingsCtx` is null and the 圖則 (N) UI gracefully degrades to count=0 and an inert button (or hides entirely — see step 5).

    4. State: `const [drawingsOpen, setDrawingsOpen] = useState(false)`.

    5. Add a new action button to the leaf action row (alongside 更新 / 指派 / 歷史 / 細項 / 刪除). Compute drawingCount from drawingsCtx if available:
       ```tsx
       const drawingsCtx = useDrawingsOptional()
       const drawingCount = useMemo(
         () => drawingsCtx ? drawingsCtx.drawings.filter(d => d.leaf_item_id === item.id).length : 0,
         [drawingsCtx, item.id]
       )

       {isLeaf && drawingsCtx && (
         <button
           onClick={() => setDrawingsOpen(o => !o)}
           className="px-3 py-2 rounded-lg bg-site-100 text-site-700 hover:bg-site-200 text-sm font-medium"
         >
           圖則 ({drawingCount})
         </button>
       )}
       ```
       When `drawingsCtx` is null (no provider), the button is hidden entirely — this is the "DrawingsSection early-returns when no provider is present" invariant referenced in Plan 06's ISSUE-11 fix.

    6. After the action row, conditionally render the section inline:
       ```tsx
       {isLeaf && drawingsOpen && drawingsCtx && (
         <DrawingsSection leafItemId={item.id} />
       )}
       ```

    7. Do NOT change any other behavior of ProgressItemCard.
  </action>
  <verify>
    <automated>grep -q "DrawingsSection" src/components/ProgressItemCard.tsx && grep -q "圖則" src/components/ProgressItemCard.tsx && grep -q "drawingsOpen" src/components/ProgressItemCard.tsx && grep -q "import \{ DrawingsContext \}" src/components/ProgressItemCard.tsx && grep -q "useDrawingsOptional\|useContext(DrawingsContext)" src/components/ProgressItemCard.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>ProgressItemCard renders 圖則 (N) button on leaf items; toggling shows DrawingsSection inline; isLeaf detection reuses ProjectDetail's source-of-truth logic (documented in SUMMARY); imports raw DrawingsContext from Plan 05 (no fallback path because Plan 05 guarantees the export); tsc passes; non-leaf cards unaffected; cards outside DrawingsProvider hide the 圖則 button silently.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Mount DrawingsProvider in ProjectDetail.tsx</name>
  <files>src/pages/ProjectDetail.tsx</files>
  <read_first>
    - src/pages/ProjectDetail.tsx (entire file — current provider mounting pattern around ProgressProvider + IssuesProvider; per RESEARCH.md line 165, modals are mounted at page level at lines 246-256)
    - src/contexts/DrawingsContext.tsx (exports DrawingsProvider)
  </read_first>
  <action>
    Edit src/pages/ProjectDetail.tsx:

    1. Import: `import { DrawingsProvider } from '../contexts/DrawingsContext'`.

    2. Locate where ProgressProvider + IssuesProvider wrap the page content. Add DrawingsProvider in the same nesting:
       ```tsx
       <ProgressProvider projectId={id}>
         <IssuesProvider projectId={id}>
           <DrawingsProvider projectId={id}>
             <ProjectDetailInner />
           </DrawingsProvider>
         </IssuesProvider>
       </ProgressProvider>
       ```
       Use the same `projectId` variable name already in the file (likely `id` from useParams). Verify by reading file first.

    3. Do NOT add new tabs or routes; the drawings feature is accessed via the inline DrawingsSection inside ProgressItemCard (added in Task 2).

    4. Verify the page still renders by running `npm run build:check`.
  </action>
  <verify>
    <automated>grep -q "DrawingsProvider" src/pages/ProjectDetail.tsx && grep -q "import.*DrawingsProvider.*from '../contexts/DrawingsContext'" src/pages/ProjectDetail.tsx && npx tsc --noEmit && npm run build:check</automated>
  </verify>
  <done>DrawingsProvider mounted; ProjectDetail still compiles and builds under 800 KB entry.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit passes.
- npm run build:check passes (entry chunk <800 KB; viewer-pdf + viewer-zoom chunks present).
- Visual smoke (manual): open the app dev server, navigate to a project with a leaf progress item, confirm 圖則 (0) button appears, tap → empty state + + 新增圖則 visible to PM.
- canUpload uses `.includes(profile.global_role)` — NOT the buggy `'admin' || 'pm' || 'main_contractor'` JavaScript shorthand.
- ProgressItemCard imports raw `DrawingsContext` directly (no defensive workaround — Plan 05 guarantees the export).
</verification>

<success_criteria>
- DRW-01 (upload from leaf), DRW-02 (versioning visible), DRW-03 (supersede flow accessible via menu), DRW-04 (member visibility), DRW-09 (version history modal in viewer), DRW-11 (no hard delete in UI), DRW-14 (sort+search), DRW-15 (role gating UI-side) all satisfied.
- DrawingsProvider scoped to projectId (matches Progress/Issues providers).
- ProgressItemCard gracefully degrades when used outside a DrawingsProvider (button hidden, section never instantiated — supports Plan 06's ISSUE-11 invariant).
- Bundle discipline preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-drawings-on-progress-items/01-07-SUMMARY.md` covering: final wiring approach (inline section vs new route — document Option A choice), provider nesting order, isLeaf detection source (which file/lines), graceful-degradation behavior for ProgressItemCard outside ProjectDetail context.
</output>
