---
phase: 01-drawings-on-progress-items
plan: 09
type: execute
wave: 5
depends_on: [01-01, 01-02, 01-03, 01-04, 01-05, 01-06, 01-07]
files_modified: []
autonomous: false
requirements: [DRW-01, DRW-02, DRW-03, DRW-04, DRW-05, DRW-06, DRW-07, DRW-08, DRW-09, DRW-10, DRW-11, DRW-12, DRW-13, DRW-14, DRW-15]

must_haves:
  truths:
    - "Live Supabase has drawings + drawing_versions tables and project-drawings PRIVATE bucket (re-verified after all dev work)"
    - "On a real iOS device (TestFlight or `cap run ios`), a PM can complete the full upload + view + supersede + withdraw flow against the live Supabase"
    - "On a real Android device (Codemagic build or `cap run android`), the same flow works"
    - "On web (Vercel preview or vite preview), the same flow works"
    - "An authenticated non-member of a project cannot mint a signed URL for any drawing in that project (manual probe via DevTools console using the authenticated client — distinguishes RLS denial from no-auth-session denial)"
    - "A subcontractor on a project sees drawings but NO upload button"
    - "Bundle-size check still passes after all changes (`npm run build:check`)"
    - "Playwright smoke (`npm run test:e2e`) passes one final time"
  artifacts: []
  key_links: []
---

<objective>
End-of-phase verification gate. By Wave 5 all schema, code, components, wiring, and the smoke test are in place. This plan is a SINGLE BLOCKING checkpoint where the developer performs the full Phase 1 success-criteria walkthrough on real devices and confirms green before Phase 1 is declared complete.

Why a separate plan: Plan 01 already pushes the schema once (Wave 1), but additional iterations during Waves 2-4 may add helper updates or fix-forward changes. This plan re-verifies the live DB state, runs the full smoke on web + iOS + Android, and probes the cross-project RLS isolation manually.

Output: Phase 1 verified complete on live Supabase + 3 platforms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-drawings-on-progress-items/01-CONTEXT.md
@.planning/phases/01-drawings-on-progress-items/01-RESEARCH.md
@.planning/phases/01-drawings-on-progress-items/01-01-PLAN.md
@.planning/phases/01-drawings-on-progress-items/01-02-PLAN.md
@.planning/phases/01-drawings-on-progress-items/01-08-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Re-push schema if any helper changed during Waves 2-7</name>
  <what-built>
    Plan 01 pushed v8-drawings.sql at end of Wave 1. If any subsequent wave added new helpers, fixed RLS policies, or extended the migration (e.g. the supersede_drawing_version RPC), the developer must re-paste the file into Supabase SQL Editor.
  </what-built>
  <how-to-verify>
    1. `git log --oneline -- supabase/v8-drawings.sql supabase/tests/rls-smoke.sql` — confirm whether any commits exist AFTER the original Plan 01 push.
    2. If yes: re-paste the latest supabase/v8-drawings.sql into Supabase Dashboard → SQL Editor → Run.
    3. If no: skip to Task 2.
    4. Run the 7 verification queries from Plan 01 Task 4 (tables exist, bucket private, helper present, supersede_drawing_version RPC present, demo_feedback policy admin-only, etc.).
    5. Run supabase/tests/rls-smoke.sql in the SQL Editor — must end with `NOTICE: rls-smoke.sql passed (3 personas verified)`.
  </how-to-verify>
  <resume-signal>Type "schema-ok" once all queries return expected results.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING] Full success-criteria walkthrough on web + iOS + Android</name>
  <what-built>
    All phase 1 code is in place. This checkpoint exercises every Phase 1 success criterion (from ROADMAP.md) on real devices.
  </what-built>
  <how-to-verify>
    For EACH of three platforms — web (vite preview), iOS (cap run ios on simulator or device), Android (cap run android) — perform the following:

    SC1 — Cross-device visibility within 5 seconds (web only required; mobile inheritance acceptable):
    1. Login as the seeded PM on Browser A. Open a project, tap a leaf item's 圖則 button, upload `tests/fixtures/sample-drawing.pdf` with title 'A-101 平面圖'.
    2. Login as a second project member (member with role main_contractor or pm) on Browser B (different profile / incognito). Open the same project + leaf item.
    3. Expect: the thumbnail and viewer for 'A-101 平面圖' is visible to user B within ~5 seconds of upload. (Realtime channel must deliver.)

    SC2 — Role isolation:
    4. Login as a `subcontractor` member on the same project. Open the leaf item's 圖則 section. Expect: drawings visible BUT no `+ 新增圖則` button, no `⋯` overflow on any thumbnail.

    5. **(ISSUE-10 fix — RLS-denial probe distinct from no-auth-session denial)** Login as **user C** — a user who IS authenticated (active Supabase session in the browser) but is NOT a member of Project A. Open the same browser's DevTools console while user C's session is live. Note Project A's path of a known drawing (you may copy a `projectA-path` like `<projectA-uuid>/<drawing-uuid>/v1/drawing.pdf` from user B's earlier upload via inspection of Browser B's Network panel — the path itself isn't secret, the access control is). In the DevTools console of **user C's browser** (where `supabase` is the authenticated client instance), run:
       ```js
       await supabase.storage.from('project-drawings').createSignedUrl('<projectA-path>', 60)
       ```
       **Expect:** the returned object has `error` non-null, indicating RLS denial. The `data` field should be null. This proves RLS rejects the signed-URL mint for an authenticated-but-non-member user (NOT merely a no-auth-session denial — user C is signed in; RLS is doing the work).

    SC3 — Versioning + supersede:
    6. As PM, on the same drawing tap ⋯ → 上載新版本. Upload a second PDF with revision label 'Rev B'.
    7. Expect: the thumbnail now shows revision 'Rev B' with green 現行 badge. Open viewer → tap 📋 版本記錄. Expect: two rows. v2 shows 現行 green; v1 shows 已取代 with strikethrough + effective-from/until dates AND the uploader's display name (NOT a raw UUID — per Plan 06 ISSUE-03 fix). Tap v1 row → viewer swaps to v1. Both versions are openable; nothing was hard-deleted.

    SC4 — Size limits + thumbnails:
    8. Try to upload an 8 MB image. Expect: soft warning toast 「檔案較大，可能會慢」 (full-width comma), upload proceeds, thumbnail (256x256) appears in the grid.
    9. Try to upload a 30 MB file. Expect: hard block alert/toast 「檔案太大 (>25MB)，請壓縮後再上載」 (half-width parens, half-width '>', full-width comma — per Plan 06 ISSUE-04 pin), file NOT uploaded.

    SC5 — Bundle + CI:
    10. Run `npm run build:check` locally. Expect: 0 exit; entry chunk <800 KB; report shows `viewer-pdf`, `viewer-zoom`, `reports-xlsx`, etc. as separate chunks.
    11. Verify Codemagic recent build log included the "Check bundle size" step and it passed.

    Final tests:
    12. Run `npm run test:e2e` one final time — expect: 1 passed.

    DOCUMENT any deviation with screenshots.
  </how-to-verify>
  <resume-signal>Type "phase-1-complete" once all 12 verification steps pass on at least web + one mobile platform (iOS preferred), or paste any failure details.</resume-signal>
</task>

</tasks>

<verification>
- All 5 phase success criteria from ROADMAP.md §Phase 1 verified.
- Live Supabase has the schema (including supersede_drawing_version RPC) and the rls-smoke.sql harness passes.
- Bundle CI guard active in Codemagic.
- Playwright smoke green.
- RLS denial probe (SC2 step 5) uses an authenticated non-member session — distinguishes RLS denial from "no auth session" denial.
</verification>

<success_criteria>
- All 24 Phase 1 requirement IDs (DRW-01..15, INF-01..09) satisfied via prior plans and re-verified here.
- Phase 1 complete; ROADMAP.md can be checked off; STATE.md updated.
</success_criteria>

<output>
After completion, create `.planning/phases/01-drawings-on-progress-items/01-09-SUMMARY.md` covering: which platforms tested (web + iOS + Android — note any skipped), any deviations from success criteria, screenshots referenced, the RLS-denial probe result (error message returned for the authenticated non-member), recommendations for Phase 2.

Also update `.planning/STATE.md` to mark Phase 1 complete and `.planning/ROADMAP.md` to check off the Phase 1 entry.
</output>
