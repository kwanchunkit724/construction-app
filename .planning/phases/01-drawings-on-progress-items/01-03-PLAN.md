---
phase: 01-drawings-on-progress-items
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - ios/App/Podfile.lock
  - android/app/src/main/AndroidManifest.xml
  - capacitor.config.ts
autonomous: false
requirements: [DRW-01]

must_haves:
  truths:
    - "@capacitor/camera@^8 is installed and present in node_modules"
    - "@capacitor/filesystem@^8 is installed and present in node_modules"
    - "npx cap sync ios has run and updated ios/App/Podfile.lock"
    - "npx cap sync android has run and updated android/app/capacitor.build.gradle"
    - "iOS Info.plist still contains NSCameraUsageDescription + NSPhotoLibraryUsageDescription in zh-HK"
    - "Build still succeeds after install (`npm run build`)"
  artifacts:
    - path: package.json
      provides: "Camera + Filesystem plugin dependencies"
      contains: "@capacitor/camera"
  key_links:
    - from: "@capacitor/camera"
      to: "@capacitor/core@^8.3.1"
      via: "peer dep — major versions must match"
      pattern: "@capacitor/camera"
---

<objective>
Install @capacitor/camera and @capacitor/filesystem (both required by D-01 bottom sheet) and run `npx cap sync ios && npx cap sync android` so the native shells include the plugins. This is a Wave 1 task that runs in parallel with Plan 01 (DB) and Plan 02 (bundle), but contains a BLOCKING human checkpoint because `cap sync` modifies generated native files that the developer should review.

Purpose: Without these plugins, the upload sheet (📷 拍攝 / 🖼️ 從相簿選擇 / 📁 從檔案選擇 — D-01) cannot reach native pickers; HTML `<input type="file">` is the only fallback.
Output: Plugins installed + native shells synced + iOS/Android builds still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-drawings-on-progress-items/01-CONTEXT.md
@.planning/phases/01-drawings-on-progress-items/01-RESEARCH.md
@.planning/codebase/STACK.md
@package.json
@capacitor.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install @capacitor/camera and @capacitor/filesystem; run cap sync</name>
  <files>package.json, package-lock.json, ios/App/Podfile.lock, android/app/capacitor.build.gradle</files>
  <read_first>
    - package.json (verify current @capacitor/core version is ^8.3.1 — plugins must match major)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Capacitor Plugin Verification" lines 564-592
    - capacitor.config.ts (current app config)
    - .planning/codebase/STACK.md (Info.plist already has Camera/Photos/Microphone zh-HK strings)
  </read_first>
  <action>
    Run sequentially:

    1. Verify @capacitor/core major: `node -p "require('./package.json').dependencies['@capacitor/core']"` — must output ^8.x. If not 8.x, ABORT and report.

    2. Install (let npm pick the latest 8.x):
       ```
       npm install @capacitor/camera@^8 @capacitor/filesystem@^8
       ```
       Confirm package.json now lists both with version starting `^8.`. If npm errors with `ERESOLVE` or pulls a non-8 version, ABORT and report — do not use --force.

    3. Run iOS sync (must succeed on macOS dev machine; if running on Windows, document and defer the sync to the BLOCKING checkpoint):
       ```
       npx cap sync ios
       ```
       This regenerates `ios/App/Podfile`, runs `pod install`, updates `ios/App/Podfile.lock`. Expect new lines mentioning `CapacitorCamera` and `CapacitorFilesystem`.

    4. Run Android sync:
       ```
       npx cap sync android
       ```
       This updates `android/app/capacitor.build.gradle` and may add to `android/capacitor.settings.gradle`. Manifest merger handles `<uses-permission>` (CAMERA, READ_MEDIA_IMAGES on API 33+) automatically via the plugin's AndroidManifest fragment.

    5. Verify Info.plist (no edit expected — just confirm the existing zh-HK strings cover Camera plugin):
       Run `grep -A1 "NSCameraUsageDescription" ios/App/App/Info.plist` — must show a non-empty zh-HK string. If missing, ABORT (this would be a pre-existing inconsistency to flag, not introduce in this plan).

    6. Confirm build still succeeds:
       ```
       npm run build
       ```
       Must exit 0. (Bundle-size check is a separate concern; Plan 02 owns that.)

    Notes:
    - Do NOT touch capacitor.config.ts — the camera plugin does not require config in v8 (default permissions are fine).
    - Do NOT add `@capacitor/camera` to vite.config.ts manualChunks; the plugin is small and only used inside the lazy-loaded DrawingUploadSheet (Plan 06), which inherits the lazy boundary already.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json').dependencies; if(!p['@capacitor/camera'] || !p['@capacitor/filesystem']) process.exit(1)" && grep -q "CapacitorCamera" ios/App/Podfile.lock 2>/dev/null || (echo 'iOS sync deferred to checkpoint' && true)</automated>
  </verify>
  <done>package.json lists @capacitor/camera@^8 and @capacitor/filesystem@^8; on macOS the ios/App/Podfile.lock includes CapacitorCamera + CapacitorFilesystem; `npm run build` exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Developer confirms native shells build after cap sync</name>
  <what-built>
    Plugins installed; `npx cap sync ios && npx cap sync android` has run (or is about to be run by the developer if Task 1 was on Windows and sync was deferred). Native shells should now include the plugin libraries.
  </what-built>
  <how-to-verify>
    1. (If sync was deferred from Task 1 because of Windows) Run on macOS dev box: `npx cap sync ios && npx cap sync android`.
    2. iOS quick check:
       - `cd ios/App && pod install` (or rely on cap sync's invocation) — must complete with `Installing CapacitorCamera (X.Y.Z)` and `Installing CapacitorFilesystem (X.Y.Z)` lines.
       - Optionally open in Xcode: `npx cap open ios` — verify project loads, scheme builds (Cmd+B) without errors.
    3. Android quick check:
       - `cd android && ./gradlew assembleDebug` (skip if heavy — alternatively just `./gradlew help` to confirm gradle resolves) — must not fail with "could not find capacitor-camera".
    4. Confirm `ios/App/App/Info.plist` still has zh-HK strings for NSCameraUsageDescription and NSPhotoLibraryUsageDescription.
    5. Confirm no rogue changes to capacitor.config.ts (`git diff capacitor.config.ts` should be empty).
  </how-to-verify>
  <resume-signal>Type "synced" once both iOS Podfile.lock and Android build.gradle reflect the new plugins, or paste error output.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| WebView → native Camera plugin | User authorizes camera/photo-library access; OS-mediated |
| WebView → native Filesystem plugin | User picks a file URI; native bridge returns blob to JS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | I (Information disclosure) | Camera plugin without privacy strings | accept | Verified existing Info.plist already has zh-HK NSCameraUsageDescription / NSPhotoLibraryUsageDescription strings (STACK.md). No new permission strings needed in this phase. |
| T-01-12 | E (Elevation of privilege) | Capacitor plugin version mismatch with core | mitigate | Pin both new plugins to ^8 matching @capacitor/core@^8.3.1; abort install if npm resolves a different major. |

</threat_model>

<verification>
- `npm ls @capacitor/camera @capacitor/filesystem` shows both at v8.x with no peer-dep warnings.
- `npm run build` succeeds.
- Developer confirms native syncs (Task 2 checkpoint).
</verification>

<success_criteria>
- Both plugins installed and synced to native shells.
- No regression in iOS or Android build.
- Info.plist unchanged (existing zh-HK strings already cover the new plugins).
</success_criteria>

<output>
After completion, create `.planning/phases/01-drawings-on-progress-items/01-03-SUMMARY.md` covering: exact versions resolved, any warnings from cap sync, confirmation that no AndroidManifest.xml edits were needed beyond merger output.
</output>
