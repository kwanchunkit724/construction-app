---
phase: 01-drawings-on-progress-items
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vite.config.ts
  - scripts/check-bundle-size.cjs
  - package.json
  - codemagic.yaml
  - src/lib/export.ts
  - src/pages/Dashboard.tsx
autonomous: true
requirements: [INF-06, INF-07]

must_haves:
  truths:
    - "Entry chunk `dist/assets/index-*.js` is under 800 KB after build"
    - "No chunk in dist/assets/*.js exceeds 400 KB"
    - "Existing heavy libs (xlsx, jspdf, recharts) emit as separate chunks (reports-xlsx, reports-pdf, charts-recharts)"
    - "src/lib/export.ts is dynamically imported at its callsite (not eagerly imported by Dashboard)"
    - "npm run build:check exits non-zero when any chunk exceeds threshold"
    - "Codemagic runs `node scripts/check-bundle-size.cjs` after Build web app step in all 3 workflows"
  artifacts:
    - path: vite.config.ts
      provides: "manualChunks config for viewer-pdf, viewer-zoom, reports-xlsx, reports-pdf, charts-recharts"
      contains: "manualChunks"
    - path: scripts/check-bundle-size.cjs
      provides: "Pure-node script that fails when entry >800 KB or any chunk >400 KB"
    - path: codemagic.yaml
      provides: "Bundle-size check step inserted into all 3 workflows"
  key_links:
    - from: "src/lib/export.ts"
      to: "Dashboard.tsx and any other callers"
      via: "dynamic await import('../lib/export')"
      pattern: "await import\\(.*export.*\\)"
---

<objective>
Establish the bundle-split + CI guard infrastructure (INF-06, INF-07). The current entry chunk is 1.2 MB per CONCERNS.md — without splitting xlsx/jspdf/recharts out and lazy-loading src/lib/export.ts, the CI guard will trip on first run. This plan lands the Vite manualChunks config, the check-bundle-size.cjs script, the export.ts lazy-import refactor, and the Codemagic integration in ONE PR so main never goes red.

Note: react-pdf and react-zoom-pan-pinch are NOT installed yet (Plan 04 installs them). The manualChunks entries for `viewer-pdf` and `viewer-zoom` are pre-declared so they take effect automatically once those libs land.

Purpose: All later phases (SI/VO, PTW) inherit this discipline. M6 (bundle bloat) is mitigated upstream.
Output: Bundle under 800 KB entry; CI guard active.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-drawings-on-progress-items/01-CONTEXT.md
@.planning/phases/01-drawings-on-progress-items/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@vite.config.ts
@package.json
@codemagic.yaml
@src/lib/export.ts
@src/pages/Dashboard.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add manualChunks to vite.config.ts + create scripts/check-bundle-size.cjs + npm script</name>
  <files>vite.config.ts, scripts/check-bundle-size.cjs, package.json</files>
  <read_first>
    - vite.config.ts (current 22-line file with NO build block)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Vite manualChunks Config" lines 429-492
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Bundle-Size CI Check" lines 496-560
    - package.json (current scripts block: dev, build, preview, cap:sync, cap:open)
  </read_first>
  <action>
    Three coordinated edits:

    A) Edit vite.config.ts — add `build.rollupOptions.output.manualChunks` block per RESEARCH.md §Vite manualChunks Config lines 447-470. Exact entries:
       ```ts
       build: {
         rollupOptions: {
           output: {
             manualChunks: {
               'viewer-pdf':       ['react-pdf', 'pdfjs-dist'],
               'viewer-zoom':      ['react-zoom-pan-pinch'],
               'reports-xlsx':     ['xlsx'],
               'reports-pdf':      ['jspdf', 'jspdf-autotable'],
               'charts-recharts':  ['recharts'],
             },
           },
         },
       },
       ```
       Insert between the `plugins: [react()]` array and `server: { ... }` block. Keep single quotes, no semicolons (per CONVENTIONS.md). The `viewer-pdf` and `viewer-zoom` chunks reference libs not yet installed; Rollup ignores missing-package entries silently, so this is safe and pre-positions them for Plan 04.

    B) Create scripts/check-bundle-size.cjs — exactly the script in RESEARCH.md §Bundle-Size CI Check lines 500-543 (CommonJS, pure Node, no deps). Key behavior: scans dist/assets/*.js; entry chunk (matches /^index-[A-Za-z0-9_-]+\.js$/) limit 800 KB, other chunks limit 400 KB; prints OK/FAIL line per file; exits 1 if any fail. Make executable: `chmod +x scripts/check-bundle-size.cjs` (no-op on Windows; matters on macOS CI).

    C) Edit package.json — add to scripts block:
       ```json
       "build:check": "npm run build && node scripts/check-bundle-size.cjs"
       ```
       Insert after the existing "build" entry. Keep JSON valid (commas).
  </action>
  <verify>
    <automated>grep -q "manualChunks" vite.config.ts && grep -q "viewer-pdf" vite.config.ts && grep -q "reports-xlsx" vite.config.ts && test -f scripts/check-bundle-size.cjs && grep -q "ENTRY_LIMIT" scripts/check-bundle-size.cjs && grep -q "process.exit(1)" scripts/check-bundle-size.cjs && grep -q "build:check" package.json</automated>
  </verify>
  <done>vite.config.ts has the manualChunks block with 5 entries; scripts/check-bundle-size.cjs exists and contains ENTRY_LIMIT/CHUNK_LIMIT constants and exit(1) on failure; package.json has the build:check script.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Lazy-import src/lib/export.ts at callsites (Dashboard.tsx and any other callers)</name>
  <files>src/lib/export.ts, src/pages/Dashboard.tsx</files>
  <read_first>
    - src/lib/export.ts (entire file — currently the consumer of xlsx + jspdf, top-level imports)
    - src/pages/Dashboard.tsx (current static imports + how export is used today)
    - Any other files that `from '../lib/export'` — run grep first:
      `grep -rEn "^\\s*import\\s.+from\\s+['\"][^'\"]*lib/export['\"]" src/`
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md lines 488-492 (ride-along split notes)
    - .planning/codebase/CONVENTIONS.md (Import Organization section — type imports at end)
  </read_first>
  <action>
    First, run `grep -rEn "^\\s*import\\s.+from\\s+['\"][^'\"]*lib/export['\"]" src/` to enumerate all callers (regex handles multi-line imports' first line and tolerates leading whitespace, both quote styles, and any relative path). For EACH caller (most likely just Dashboard.tsx):

    1. Remove the top-level `import { exportToExcel, exportToPdf, ... } from '../lib/export'` line(s).
    2. At each callsite where an export function is invoked (e.g. inside `onClick`, async event handlers), convert to:
       ```ts
       const { exportToExcel } = await import('../lib/export')
       await exportToExcel(...)
       ```
       Use the same function names that currently exist in src/lib/export.ts (do NOT rename — just defer the import).
    3. If a function is invoked synchronously in render (unlikely — but verify), promote to an async event handler. Add a brief loading state if the function call has user-visible latency:
       ```ts
       const [exporting, setExporting] = useState(false)
       async function handleExport() {
         setExporting(true)
         try {
           const { exportToExcel } = await import('../lib/export')
           await exportToExcel(rows)
         } finally {
           setExporting(false)
         }
       }
       ```
       Wire the button to disabled={exporting} per existing Login.tsx pattern (CONVENTIONS.md §Loading States).
    4. Keep src/lib/export.ts itself UNCHANGED in terms of its exports and top-level `import` statements — the xlsx/jspdf eager imports inside export.ts are correct because the entire module is now lazy. Vite + Rollup will split xlsx/jspdf into reports-xlsx/reports-pdf chunks because the only consumers are now dynamic imports.

    Per CONVENTIONS.md: no semicolons, single quotes, type imports at end. Inline Chinese strings if any error toasts added (e.g. "匯出失敗").
  </action>
  <verify>
    <!-- ISSUE-06 fix: tolerate leading whitespace + both quote styles + arbitrary relative path, AND guard against src/ not existing. -->
    <automated>[ -d src ] && ! grep -rEn "^\s*import\s.+from\s+['\"][^'\"]*lib/export['\"]" src/ && grep -rEn "await\s+import\s*\(\s*['\"][^'\"]*lib/export['\"]\s*\)" src/ | grep -q "Dashboard"</automated>
  </verify>
  <done>No top-level static `import ... from '...lib/export'` remains anywhere in src/ (regex tolerates whitespace + multi-line + both quote styles); at least one `await import('../lib/export')` exists in Dashboard.tsx; build still succeeds (`npm run build`).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run npm run build:check, confirm bundle under threshold, wire Codemagic step</name>
  <files>codemagic.yaml</files>
  <read_first>
    - codemagic.yaml (entire file — currently 3 workflows: ios-app-store, ios-testflight, android-internal-test, each with a Build web app step)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Bundle-Size CI Check" lines 551-560
  </read_first>
  <action>
    1. Run `npm run build:check` locally. Inspect dist/assets/index-*.js size. If still >800 KB after Task 1+2, investigate which eager imports remain heavy:
       - Run `npx vite-bundle-visualizer` (optional) or inspect `dist/assets/` listing manually
       - Common culprits: jspdf-autotable transitively pulled in, recharts imported at top of Dashboard
       - If recharts is imported at top of Dashboard.tsx, ALSO lazy-load the chart sub-component: `const RechartsChart = lazy(() => import('../components/DashboardChart'))` (or wherever the recharts <BarChart> lives). Use Suspense fallback.
       - If size still >800 KB, document the residual heavy chunks in the SUMMARY.md and proceed — the threshold can be revisited only with developer approval; do NOT raise ENTRY_LIMIT silently.

    2. Edit codemagic.yaml — for EACH of the 3 workflows (ios-app-store, ios-testflight, android-internal-test), INSERT a new step IMMEDIATELY AFTER the existing "Build web app" step (which runs `npm run build`). Step shape:
       ```yaml
       - name: Check bundle size
         script: node scripts/check-bundle-size.cjs
       ```
       Do NOT use `npm run build:check` here (it would re-run build). The previous step already produced dist/.

    3. Verify YAML indentation matches the existing scripts in the same workflow (2-space indent, `- name:` aligned with sibling steps).

    4. Final local verification: `npm run build:check` exits 0; intentionally bump ENTRY_LIMIT down to e.g. 100*1024 in scripts/check-bundle-size.cjs (don't commit) → run → confirm exit 1 → revert.
  </action>
  <verify>
    <automated>npm run build:check && grep -c "Check bundle size" codemagic.yaml | awk '{exit ($1 < 3)}'</automated>
  </verify>
  <done>`npm run build:check` exits 0 (entry chunk <800 KB, no chunk >400 KB); codemagic.yaml contains the "Check bundle size" step in all 3 workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CI build artifact → end-user device | Oversized bundle = unusable app on HK 4G inside concrete buildings |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | D (DoS via UX) | Entry chunk bloat → cold-load timeout on 3G/4G | mitigate | manualChunks split + 800 KB CI gate; lazy-import export.ts + viewer libs (M6 from PITFALLS). |
| T-01-10 | T (Tampering — silent regression) | Future PR adds heavy lib, no guard | mitigate | Codemagic step `node scripts/check-bundle-size.cjs` runs after Build web app on all 3 release workflows; fails the build before TestFlight/Play upload. |

</threat_model>

<verification>
- `npm run build:check` exits 0 with all chunks listed OK.
- dist/assets/ contains separate chunks for reports-xlsx, reports-pdf, charts-recharts.
- codemagic.yaml has 3 occurrences of "Check bundle size".
- No `import { ... } from '...lib/export'` static import remains anywhere in src/ (regex-verified, tolerating whitespace + both quote styles).
</verification>

<success_criteria>
- Entry chunk <800 KB after build.
- CI guard would block a PR that breaches the budget (verified by intentional bump-and-revert sanity check).
- Codemagic workflows have the check wired into all 3 release pipelines.
- Plan 04 can later add react-pdf/react-zoom-pan-pinch and the manualChunks config already accommodates them.
</success_criteria>

<output>
After completion, create `.planning/phases/01-drawings-on-progress-items/01-02-SUMMARY.md` covering: final chunk sizes (entry + each split), any residual >400 KB chunks and rationale, Codemagic step location verified.
</output>
