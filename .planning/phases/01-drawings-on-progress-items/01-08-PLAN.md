---
phase: 01-drawings-on-progress-items
plan: 08
type: execute
wave: 5
depends_on: [01-07]
files_modified:
  - playwright.config.ts
  - tests/e2e/drawings.spec.ts
  - tests/fixtures/sample-drawing.pdf
  - package.json
  - scripts/seed-demos.js
autonomous: false
requirements: [INF-08]

must_haves:
  truths:
    - "playwright.config.ts exists at repo root with chromium project + iPhone-13 viewport (390x844)"
    - "playwright.config.ts webServer command is `npm run build && npm run preview`; webServer url is http://localhost:5173"
    - "package.json `preview` script is `vite preview --port 5173` (NOT default 4173) — ISSUE-08 fix"
    - "tests/e2e/drawings.spec.ts contains exactly ONE test covering: login as PM → open project → tap leaf 圖則 → upload PDF → see thumbnail → tap → assert pinch-zoom container present"
    - "tests/fixtures/sample-drawing.pdf exists as a small (<100 KB) 1-page PDF"
    - "package.json contains test:e2e and test:e2e:install scripts"
    - "scripts/seed-demos.js ensures at least one PM + project + leaf progress item exists deterministically"
    - "Test passes locally via `npm run test:e2e` (Playwright auto-starts the preview server on port 5173 per webServer block)"
  artifacts:
    - path: playwright.config.ts
      provides: "Playwright setup with auto-started preview server on port 5173"
    - path: tests/e2e/drawings.spec.ts
      provides: "ONE happy-path smoke test"
      contains: "drawing-viewer-zoom"
  key_links:
    - from: "Playwright"
      to: "vite preview --port 5173"
      via: "webServer block in playwright.config.ts (command chains build && preview)"
      pattern: "webServer"
    - from: "drawings.spec.ts"
      to: "seeded PM user via TEST_PM_PHONE / TEST_PM_PASSWORD"
      via: "env vars"
      pattern: "TEST_PM_PHONE"
---

<objective>
Set up Playwright (first test in repo) and ship the Phase 1 share of INF-08: one happy-path smoke test for drawing upload + view. The test is a developer / on-demand check; not yet wired to CI (per D-31 "ONE test only" framing).

Depends on Plan 07 (the user-facing flow must exist). The data-testid `drawing-viewer-zoom` was added by Plan 06.

Purpose: A regression in the upload-or-view path is caught by a single `npm run test:e2e` invocation before any deploy.
Output: Working smoke test that passes locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-drawings-on-progress-items/01-CONTEXT.md
@.planning/phases/01-drawings-on-progress-items/01-RESEARCH.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@package.json
@scripts/seed-demos.js
@src/components/drawings/DrawingViewer.tsx
@src/components/drawings/DrawingsSection.tsx
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create playwright.config.ts + tests/ structure + fixture + npm scripts (with port-5173 alignment)</name>
  <files>playwright.config.ts, tests/e2e/drawings.spec.ts, tests/fixtures/sample-drawing.pdf, package.json</files>
  <read_first>
    - package.json (current scripts; @playwright/test ^1.59.1 already in devDeps; existing `preview` script — check whether it already specifies --port; ISSUE-08: `vite preview` defaults to **4173**, not 5173)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md §"Playwright Smoke Test Scaffolding" lines 674-761
    - src/components/drawings/DrawingViewer.tsx (verify data-testid="drawing-viewer-zoom" present from Plan 06)
    - src/components/drawings/DrawingUploadSheet.tsx (verify button labels: '+ 新增圖則', '從檔案選擇', '上載')
    - src/pages/Login.tsx (verify selectors for 手機號碼 / 密碼 / 登入 labels)
  </read_first>
  <action>
    **ISSUE-08 fix (port alignment) — apply this before anything else:**

    1. Edit package.json's `scripts` block to pin the preview port:
       ```json
       "preview": "vite preview --port 5173"
       ```
       Replace the existing `preview` entry (do not just append). This makes the preview server bind to 5173 instead of vite's default 4173, so Playwright (which expects 5173) actually reaches it.

    2. Create playwright.config.ts at repo root — based on RESEARCH.md §Playwright Smoke Test Scaffolding step 1 (lines 684-708), with these REQUIRED fields:
       - `testDir: './tests/e2e'`
       - `timeout: 60_000`
       - `fullyParallel: false`
       - `use.viewport: { width: 390, height: 844 }` (iPhone 13)
       - `webServer: { command: 'npm run build && npm run preview', url: 'http://localhost:5173', reuseExistingServer: !process.env.CI, timeout: 120_000 }` — the command CHAINS build then preview, so the preview server has fresh dist/ to serve. Using `url` (not `port`) makes Playwright probe the URL until it returns 200.
       - `projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]`

    3. Create tests/e2e/drawings.spec.ts — adapt from RESEARCH.md lines 711-741 BUT verify each selector against the actual implementation. The test should:
       a) Goto '/#/login'
       b) Fill 手機號碼 + 密碼 with process.env.TEST_PM_PHONE / TEST_PM_PASSWORD
       c) Click 登入; wait for /#/home or /#/projects
       d) Navigate to a known seeded project (use `text="Playwright Test Project"` — coordinate with seed in Task 2)
       e) Locate a leaf progress item and tap its 圖則 button (selector: `text=/圖則 \\(\\d+\\)/`)
       f) Click '+ 新增圖則'
       g) Click '從檔案選擇'
       h) `await page.setInputFiles('input[type="file"]', 'tests/fixtures/sample-drawing.pdf')`
       i) Fill 圖則標題 with 'A-101 平面圖'
       j) Click '上載'
       k) `await expect(page.getByText('A-101 平面圖')).toBeVisible({ timeout: 30_000 })`
       l) Click the thumbnail
       m) `await expect(page.locator('[data-testid="drawing-viewer-zoom"]')).toBeVisible()`

    4. Create tests/fixtures/sample-drawing.pdf — minimal 1-page PDF, committed binary:
       - `node -e "const {jsPDF}=require('jspdf');const d=new jsPDF();d.text('Sample Drawing',10,10);require('fs').writeFileSync('tests/fixtures/sample-drawing.pdf',Buffer.from(d.output('arraybuffer')));"`
       - Verify <100 KB and opens as valid PDF.

    5. Add to package.json scripts (alongside the updated `preview`):
       ```json
       "test:e2e": "playwright test",
       "test:e2e:install": "playwright install chromium"
       ```
       Insert after build:check.

    All file content per CONVENTIONS.md.
  </action>
  <verify>
    <!-- ISSUE-08 fix: preview script must specify --port 5173 AND playwright url must be 5173. -->
    <automated>test -f playwright.config.ts && grep -q "testDir.*tests/e2e" playwright.config.ts && grep -q "390" playwright.config.ts && grep -q "localhost:5173" playwright.config.ts && grep -q "npm run build && npm run preview" playwright.config.ts && grep -q "vite preview --port 5173" package.json && test -f tests/e2e/drawings.spec.ts && grep -q "drawing-viewer-zoom" tests/e2e/drawings.spec.ts && grep -q "+ 新增圖則" tests/e2e/drawings.spec.ts && test -f tests/fixtures/sample-drawing.pdf && grep -q "test:e2e" package.json && node -e "const sz=require('fs').statSync('tests/fixtures/sample-drawing.pdf').size; if(sz>100000) process.exit(1)"</automated>
  </verify>
  <done>All artifacts present; package.json `preview` pinned to `vite preview --port 5173`; playwright.config.ts webServer chains build && preview with url http://localhost:5173; PDF fixture <100 KB; selectors in spec match actual Plan 06/07 UI text.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend scripts/seed-demos.js to ensure deterministic PM + project + leaf item</name>
  <files>scripts/seed-demos.js</files>
  <read_first>
    - scripts/seed-demos.js (entire current file — read the existing demo-user creation pattern)
    - supabase/v2-schema.sql + v3-progress-schema.sql (for user_profiles + projects + progress_items column shapes)
    - .planning/phases/01-drawings-on-progress-items/01-RESEARCH.md lines 752-759 (recommendation: option (a) extend seed-demos.js)
  </read_first>
  <action>
    Edit scripts/seed-demos.js to ensure (idempotently) that after the script runs:

    1. A user with `phone='98765432'`, `password='Demo@2026'`, `global_role='pm'` exists. Insert via supabase.auth.admin.createUser then upsert user_profiles. Use the existing service-role pattern.

    2. A project named 'Playwright Test Project' exists and has the seeded PM in `assigned_pm_ids` (or as approved project_member with role='pm').

    3. A leaf progress_items row under that project exists with name 'Test Leaf Item'.

    All operations idempotent. Document env vars at top:
    ```
    // For Playwright tests (tests/e2e/drawings.spec.ts):
    //   TEST_PM_PHONE=98765432
    //   TEST_PM_PASSWORD=Demo@2026
    ```

    Do NOT introduce a new file. Additive changes only.
  </action>
  <verify>
    <automated>grep -q "Playwright Test Project" scripts/seed-demos.js && grep -q "98765432\\|TEST_PM_PHONE" scripts/seed-demos.js && grep -q "Test Leaf Item" scripts/seed-demos.js</automated>
  </verify>
  <done>seed-demos.js idempotently provisions test PM + project + leaf item; constants visible to spec via env vars.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Developer runs the smoke test locally and confirms green</name>
  <what-built>
    Playwright is configured (preview port aligned to 5173); the test, fixture, and seed are in place. The first-ever test in this repo needs a human to verify it runs green against the developer's Supabase instance.
  </what-built>
  <how-to-verify>
    1. Run `node scripts/seed-demos.js` (with VITE_SUPABASE_SERVICE_ROLE_KEY env var set) — verify it completes without errors.
    2. Run `npm run test:e2e:install` once (downloads chromium).
    3. Run `TEST_PM_PHONE=98765432 TEST_PM_PASSWORD=Demo@2026 npm run test:e2e` — expect: 1 passed. The webServer block builds then runs `vite preview --port 5173`; Playwright probes http://localhost:5173 until 200 OK before starting the test.
    4. If the test fails, inspect the trace (playwright-report/) and adjust selectors or labels. Re-run until green.
    5. Confirm runtime <60s end-to-end.
  </how-to-verify>
  <resume-signal>Type "green" once `npm run test:e2e` reports 1 passed, or paste the failing output.</resume-signal>
</task>

</tasks>

<verification>
- Local `npm run test:e2e` reports 1 passed.
- PDF fixture <100 KB and is a valid PDF.
- Spec selectors match actual UI text.
- package.json `preview` script binds vite preview to port 5173 (not vite's default 4173).
- playwright.config.ts webServer url targets http://localhost:5173.
</verification>

<success_criteria>
- INF-08 Phase 1 share delivered (one happy-path smoke test).
- Test infrastructure ready for Phase 2 / Phase 3 to add specs without further setup.
- Seed harness extended without breaking existing demo flows.
- Port alignment: vite preview port (5173) matches playwright.config.ts webServer url.
</success_criteria>

<output>
After completion, create `.planning/phases/01-drawings-on-progress-items/01-08-SUMMARY.md` covering: spec selectors used, seed deltas, runtime (seconds), port-5173 alignment note, any flake observed.
</output>
