---
phase: 02-si-vo
plan: 07
type: execute
wave: 6
depends_on: [02-05, 02-06]
files_modified:
  - src/contexts/VoContext.tsx
  - src/components/vo/VoLineItemRow.tsx
  - src/components/vo/VoLineItemsEditor.tsx
  - src/components/vo/VoSubmitForm.tsx
  - src/components/vo/VoConfirmationScreen.tsx
  - src/components/vo/VoCard.tsx
  - src/components/vo/VoList.tsx
  - src/components/vo/VoApproverBar.tsx
  - src/pages/VoList.tsx
  - src/pages/VoDetail.tsx
  - src/App.tsx
autonomous: false
requirements: [VO-01, VO-02, VO-03, VO-06, VO-07, VO-08, VO-09, VO-10]
notes: "Cohesive feature plan (6 tasks). Executor should write SUMMARY checkpoints after each task to manage context."

must_haves:
  truths:
    - "VoContext per-project state + realtime channel vo-${projectId}; exports useVo() with {vos, versionsByVo, approvalsByVo, canSubmit, createDraftVo(siId), saveVersion, submitVo, approve, requestRevision, reject, adminOverride}"
    - "createDraftVo verifies parent SI is locked client-side (then server enforces VO-01 in submit_vo RPC)"
    - "VoLineItemsEditor renders editable line-item rows: 類別 dropdown (人工/物料/前期費用/暫定), 描述, 數量, 單位, 單價; live local 小計 preview; sticky footer with 經系統核算總額 client preview (formatHKD); + 新增項目 button; swipe-left/✕ to remove"
    - "Currency arithmetic uses src/lib/currency.ts (Math.round(quantity * unit_price_cents) — integer cents throughout, no parseFloat)"
    - "Optional progress_leaf_item_id picker per row: small button '🔗 連結進度' opens modal showing project's progress tree (reuse ProgressContext.items) for leaf-only selection (D-20)"
    - "VoSubmitForm collects description + line_items + creates the VO row via createDraftVo + writes vo_versions via saveVersion + calls submitVo"
    - "VoConfirmationScreen renders post-submit displaying server-confirmed total as '經系統核算總額 HK$X' (VO-06)"
    - "VoApproverBar mirrors SiApproverBar with same 4 actions (D-13); approve/approve_with_edits/request_revision/reject + adminOverride for admins"
    - "VoDetail page shows: header with status pill + VO-NNN + total_amount_cents, tabs 詳情/版本歷史/簽核紀錄, 匯出 PDF button (calls exportVOToPDF from Plan 02-06)"
    - "VoList page with filter pills (全部/待批准/已批准/已退回/已拒絕) + date-range pickers (由/至, yyyy-MM-dd) — VO-10"
    - "On SiDetail page, when SI status='locked' AND no VO exists yet for this SI, show '提出變更指令' button → opens VoSubmitForm; otherwise hide"
    - "Routes added: /project/:id/vo, /project/:id/vo/:voId — mounted under VoProvider"
    - "TS compilation green; bundle CI green (entry <800 KB after jspdf chunk verified lazy-loaded only on Export PDF tap)"
  artifacts:
    - path: src/contexts/VoContext.tsx
      provides: "Per-project VO state mirror of SiContext"
    - path: src/components/vo/VoLineItemsEditor.tsx
      provides: "Mobile-first stacked line-item editor with live client subtotal preview"
    - path: src/components/vo/VoConfirmationScreen.tsx
      provides: "Post-submit confirmation '經系統核算總額 HK$X'"
    - path: src/pages/VoDetail.tsx
      provides: "VO detail with PDF export button + approver bar"
  key_links:
    - from: "VoSubmitForm onSubmit"
      to: "supabase.rpc('submit_vo')"
      via: "VoContext.submitVo"
      pattern: "supabase\\.rpc\\('submit_vo'"
    - from: "VoDetail 匯出 PDF button"
      to: "exportVOToPDF"
      via: "dynamic import on tap"
      pattern: "exportVOToPDF"
    - from: "SiDetail '提出變更指令'"
      to: "VoSubmitForm"
      via: "modal mount when status=locked AND no VO yet"
      pattern: "提出變更指令"
---

<objective>
Build the VO domain UI: context, line-item editor, submission flow with server-confirmed total, approver bar, list with filter, detail with PDF export, and the "raise VO from locked SI" entry point.

Purpose: VO end-user-functional including PDF export.
Output: 11 files; routes wired; bundle still green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-si-vo/02-CONTEXT.md
@.planning/phases/02-si-vo/02-RESEARCH.md
@.planning/phases/02-si-vo/02-04-PLAN.md
@.planning/phases/02-si-vo/02-05-PLAN.md
@.planning/phases/02-si-vo/02-06-PLAN.md
@src/contexts/SiContext.tsx
@src/contexts/ProgressContext.tsx
@src/components/si/SiApproverBar.tsx
@src/components/si/SiDiffCard.tsx
@src/components/si/SiTimeline.tsx
@src/pages/SiDetail.tsx
@src/lib/currency.ts
@src/lib/export.ts
@src/types.ts
@src/App.tsx
@supabase/v9-vo-schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create src/contexts/VoContext.tsx (per-project state mirror of SiContext)</name>
  <files>src/contexts/VoContext.tsx</files>
  <read_first>
    - src/contexts/SiContext.tsx (entire file — mirror shape verbatim with VO substitutions)
    - src/types.ts (VO, VOVersion, VoPayload from Plan 02-06)
    - supabase/v9-vo-schema.sql (next_vo_number RPC, submit_vo RPC)
  </read_first>
  <action>
    Create src/contexts/VoContext.tsx by mirroring SiContext.tsx with these substitutions:

    - `si` / `SI` → `vo` / `VO` throughout
    - Table names: `variation_orders`, `vo_versions` (no protest_comments equivalent)
    - Realtime channel: `vo-${projectId}`
    - RPC names: `next_vo_number`, `submit_vo`, `submit_approval` (with p_doc_type='vo')
    - createDraftVo signature: `(siId: string) => Promise<{ id: string|null; error: string|null }>` — additionally validates: parent SI exists, status='locked', and no VO already exists for this si_id (client convenience; server UNIQUE constraint is authoritative)
    - saveVersion writes to vo_versions; trigger recomputes subtotals + total
    - No protest comments; no addProtest method

    Otherwise identical shape:
    - useVo() hook
    - VoProvider mounting realtime channel
    - canSubmit = role in ['admin','pm','main_contractor'] (MC raises VO per VO-01 narrative)
    - approve/requestRevision/reject/adminOverride call submit_approval with p_doc_type='vo'
    - approve(voId, edits?) writes new vo_versions row when edits provided

    Style: no semicolons, single quotes. tsc green.
  </action>
  <verify>
    <automated>test -f src/contexts/VoContext.tsx && grep -q "export function VoProvider" src/contexts/VoContext.tsx && grep -q "export function useVo" src/contexts/VoContext.tsx && grep -q "supabase.rpc('submit_vo'" src/contexts/VoContext.tsx && grep -q "supabase.rpc('next_vo_number'" src/contexts/VoContext.tsx && grep -q "vo-\\\${projectId}\|vo-\\$\\{projectId\\}" src/contexts/VoContext.tsx && grep -q "p_doc_type.*'vo'\|'vo'.*p_doc_type" src/contexts/VoContext.tsx && grep -q "createDraftVo" src/contexts/VoContext.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>VoContext mirrors SiContext shape; tsc green.</done>
</task>

<task type="auto">
  <name>Task 2: Build VoLineItemRow + VoLineItemsEditor (mobile-first line item UX)</name>
  <files>src/components/vo/VoLineItemRow.tsx, src/components/vo/VoLineItemsEditor.tsx</files>
  <read_first>
    - src/lib/currency.ts (formatHKD, parseHKD, multiplyCents)
    - src/types.ts (VoLineItem, LineItemCategory, LINE_ITEM_CATEGORY_ZH)
    - src/contexts/ProgressContext.tsx (items + isLeaf detection for progress_leaf_item picker — read; D-20)
    - .planning/phases/02-si-vo/02-CONTEXT.md D-18, D-20
    - .planning/phases/02-si-vo/02-RESEARCH.md §8 lines 947-952 (VO line-item UX)
  </read_first>
  <action>
    **src/components/vo/VoLineItemRow.tsx:**
    Props: `{ item: VoLineItem; index: number; onChange: (next: VoLineItem) => void; onRemove: () => void; progressItems: ProgressItem[] }`.

    Mobile (390px) stacked rows:
    ```
    ┌─────────────────────────────┐
    │ #1   [類別: 人工▾]   [🗑]   │
    │ 描述 [_____________________] │
    │ 數量 [____] 單位 [_____]    │
    │ 單價 [HK$ ________]         │
    │ 小計：HK$ X (client preview)│
    │ 🔗 連結進度 (選填)          │
    └─────────────────────────────┘
    ```

    Desktop md: collapsed into table row (one line).

    Currency input: parseHKD on blur → unit_price_cents. Display via formatHKD(unit_price_cents).
    Live subtotal = multiplyCents(quantity, unit_price_cents); render formatHKD.

    🔗 連結進度 button opens modal with leaf progress items from project; select one → onChange with progress_leaf_item_id.

    **src/components/vo/VoLineItemsEditor.tsx:**
    Props: `{ value: VoLineItem[]; onChange: (next: VoLineItem[]) => void; progressItems: ProgressItem[] }`.

    Renders stacked VoLineItemRow list + 新增項目 button at bottom + sticky footer "經系統核算總額 (預覽) HK$X" (sum of subtotals client-side).

    Empty list → empty state "尚未有項目" + 新增項目 button.

    Validation: at least 1 row; each row must have description + quantity > 0 + unit + unit_price_cents > 0.
  </action>
  <verify>
    <automated>test -f src/components/vo/VoLineItemRow.tsx && test -f src/components/vo/VoLineItemsEditor.tsx && grep -q "LINE_ITEM_CATEGORY_ZH" src/components/vo/VoLineItemRow.tsx && grep -q "formatHKD\|multiplyCents" src/components/vo/VoLineItemRow.tsx && grep -q "parseHKD" src/components/vo/VoLineItemRow.tsx && grep -q "🔗 連結進度\|連結進度" src/components/vo/VoLineItemRow.tsx && grep -q "新增項目" src/components/vo/VoLineItemsEditor.tsx && grep -q "經系統核算總額" src/components/vo/VoLineItemsEditor.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Both components render; integer-cent arithmetic only (no parseFloat); progress_leaf_item picker wired; tsc green.</done>
</task>

<task type="auto">
  <name>Task 3: Build VoSubmitForm + VoConfirmationScreen</name>
  <files>src/components/vo/VoSubmitForm.tsx, src/components/vo/VoConfirmationScreen.tsx</files>
  <read_first>
    - src/components/si/SiSubmitForm.tsx (mirror submission flow pattern)
    - src/components/vo/VoLineItemsEditor.tsx (just built)
    - src/contexts/VoContext.tsx (createDraftVo, saveVersion, submitVo)
    - .planning/phases/02-si-vo/02-CONTEXT.md D-19 (VO confirmation screen spec)
  </read_first>
  <action>
    **src/components/vo/VoSubmitForm.tsx:**
    Props: `{ projectId: string; parentSi: SI; onSubmitted: (voId: string, total: number) => void; onCancel: () => void }`.

    Layout:
    - Header: "提出變更指令" + close X + small caption "源於 工地指令 {parentSi.number}"
    - Body:
      - 描述 (textarea, max 4000 chars, required, char counter)
      - VoLineItemsEditor mounted with state `lineItems`
    - Footer: 取消 / 提交 (btn-primary, disabled until description + ≥1 valid line item)

    On 提交:
    ```ts
    setSubmitting(true)
    const { id: voId, error: e1 } = await createDraftVo(parentSi.id)
    if (e1 || !voId) { setError(e1); return }
    const payload: VoPayload = { description, line_items: lineItems, total_amount_cents: 0 /* recomputed by trigger */ }
    const { versionNo, error: e2 } = await saveVersion(voId, payload)
    if (e2) { setError(e2); return }
    const { error: e3 } = await submitVo(voId)
    if (e3) { setError(e3); return }
    // Refetch the VO to get the server-computed total_amount_cents
    await refetch()
    const fresh = (vos.find(v => v.id === voId)) // or refetch single
    onSubmitted(voId, fresh?.total_amount_cents ?? 0)
    ```

    **src/components/vo/VoConfirmationScreen.tsx:**
    Props: `{ voId: string; serverTotal: number; voNumber: string; onClose: () => void }`.
    Renders centred:
    ```
    ✓
    變更指令 VO-001 已提交

    經系統核算總額
    HK$1,234,567.89

    [查看詳情]  [關閉]
    ```
    The total is the SERVER-confirmed value (from refetched VO row). Use formatHKD.

    VO-06 / D-19 exact zh-HK string: `經系統核算總額` followed by formatHKD(serverTotal) in large bold (text-2xl).
  </action>
  <verify>
    <automated>test -f src/components/vo/VoSubmitForm.tsx && test -f src/components/vo/VoConfirmationScreen.tsx && grep -q "提出變更指令" src/components/vo/VoSubmitForm.tsx && grep -q "createDraftVo" src/components/vo/VoSubmitForm.tsx && grep -q "submitVo" src/components/vo/VoSubmitForm.tsx && grep -q "VoLineItemsEditor" src/components/vo/VoSubmitForm.tsx && grep -q "經系統核算總額" src/components/vo/VoConfirmationScreen.tsx && grep -q "formatHKD" src/components/vo/VoConfirmationScreen.tsx && grep -q "text-2xl\|text-3xl\|text-4xl" src/components/vo/VoConfirmationScreen.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>Submit form runs full create → save → submit → refetch → confirmation; confirmation prominently shows server total in zh-HK; tsc green.</done>
</task>

<task type="auto">
  <name>Task 4: Build VoCard + VoList + VoApproverBar</name>
  <files>src/components/vo/VoCard.tsx, src/components/vo/VoList.tsx, src/components/vo/VoApproverBar.tsx</files>
  <read_first>
    - src/components/si/SiCard.tsx + src/components/si/SiList.tsx (mirror shape)
    - src/components/si/SiApproverBar.tsx (mirror approver bar verbatim with VO substitutions)
    - .planning/phases/02-si-vo/02-RESEARCH.md §8 line 971-973 (VO list filter UX)
  </read_first>
  <action>
    **src/components/vo/VoCard.tsx:**
    Mirror SiCard; replace SI with VO; add total_amount_cents in footer rendered via formatHKD (e.g. "HK$1,234,567.89 · 步驟 1/3").

    **src/components/vo/VoList.tsx:**
    Mirror SiList. Add date-range filter pickers (由 / 至, `<input type="date">`) ABOVE the status filter pills (VO-10). Date filter applies to created_at >= from AND created_at <= to.

    **src/components/vo/VoApproverBar.tsx:**
    Mirror SiApproverBar verbatim with `useVo()` instead of `useSi()`. Edit mode for 批准並修改 opens an inline editor with VoLineItemsEditor pre-filled with current version's line_items + description; on save, calls approve(voId, editedPayload).
  </action>
  <verify>
    <automated>test -f src/components/vo/VoCard.tsx && test -f src/components/vo/VoList.tsx && test -f src/components/vo/VoApproverBar.tsx && grep -q "VO_STATUS_ZH" src/components/vo/VoCard.tsx && grep -q "formatHKD" src/components/vo/VoCard.tsx && grep -q "type=\"date\"" src/components/vo/VoList.tsx && grep -q "由\|至" src/components/vo/VoList.tsx && grep -q "useVo()" src/components/vo/VoApproverBar.tsx && grep -q "VoLineItemsEditor" src/components/vo/VoApproverBar.tsx && grep -q "approve_with_edits\|approve(.*editedPayload\|approve(.*edited" src/components/vo/VoApproverBar.tsx && npx tsc --noEmit</automated>
  </verify>
  <done>VO list/card/approver render with VO-specific bits (total, date filter, line-item edit mode); tsc green.</done>
</task>

<task type="auto">
  <name>Task 5: Build VoDetail page with PDF export button + add routes</name>
  <files>src/pages/VoList.tsx, src/pages/VoDetail.tsx, src/App.tsx</files>
  <read_first>
    - src/pages/SiList.tsx + src/pages/SiDetail.tsx (mirror shape)
    - src/lib/export.ts (exportVOToPDF signature from Plan 02-06)
    - src/contexts/VoContext.tsx + src/components/vo/*
    - src/App.tsx (current routes)
  </read_first>
  <action>
    **src/pages/VoList.tsx:** mirror src/pages/SiList.tsx with VoProvider + VoList component. Reads projectId from useParams.

    **src/pages/VoDetail.tsx:** mirror src/pages/SiDetail.tsx with VO substitutions. Differences:
    1. Tabs: 詳情 / 版本歷史 / 簽核紀錄 (no protest tab — VO has no protest).
    2. Header includes total_amount_cents (formatHKD).
    3. Add `匯出 PDF` button (visible when vo.status === 'locked'):
       ```tsx
       const [exporting, setExporting] = useState(false)
       async function handleExport() {
         setExporting(true)
         try {
           // Need: project, current VOVersion, all drawing_versions referenced by parent SI's drawing_version_ids, users by id, approval timeline
           const { exportVOToPDF } = await import('../lib/export')  // ensures lazy chunk
           await exportVOToPDF(project, vo, currentVersion, drawings, usersById, approvalTimeline)
         } catch (e) {
           setError(String(e))
         } finally { setExporting(false) }
       }
       <button onClick={handleExport} disabled={exporting} className="btn-primary">
         {exporting ? '產生中…' : '匯出 PDF'}
       </button>
       ```
    4. approvalTimeline derivation: from approvalsByVo + usersById; format `at` via `new Date(a.created_at).toLocaleString('zh-HK')`.

    **src/App.tsx:**
    Add 2 routes alongside SI routes:
    ```tsx
    <Route path="/project/:id/vo" element={<ProtectedRoute><VoListPage /></ProtectedRoute>} />
    <Route path="/project/:id/vo/:voId" element={<ProtectedRoute><VoDetailPage /></ProtectedRoute>} />
    ```
    Import the page components at top.
  </action>
  <verify>
    <automated>test -f src/pages/VoList.tsx && test -f src/pages/VoDetail.tsx && grep -q "VoProvider" src/pages/VoList.tsx && grep -q "VoProvider" src/pages/VoDetail.tsx && grep -q "匯出 PDF" src/pages/VoDetail.tsx && grep -q "import('../lib/export')\|exportVOToPDF" src/pages/VoDetail.tsx && grep -q "/project/:id/vo" src/App.tsx && grep -q "/project/:id/vo/:voId" src/App.tsx && npx tsc --noEmit && npm run build:check</automated>
  </verify>
  <done>VO routes wired; PDF export triggers dynamic import; build green entry <800 KB; tsc green.</done>
</task>

<task type="auto">
  <name>Task 6: Add '提出變更指令' button to SiDetail when status=locked + no VO yet</name>
  <files>src/pages/SiDetail.tsx</files>
  <read_first>
    - src/pages/SiDetail.tsx (current file from Plan 02-05)
    - src/contexts/VoContext.tsx (useVo to check existing VO for this si_id)
  </read_first>
  <action>
    Edit src/pages/SiDetail.tsx:

    1. Mount VoProvider around the inner content (alongside SiProvider) so the inner component can read existing VOs:
       ```tsx
       <SiProvider projectId={id}>
         <VoProvider projectId={id}>
           <SiDetailInner />
         </VoProvider>
       </SiProvider>
       ```

    2. Inside SiDetailInner, add:
       ```tsx
       const { vos } = useVo()
       const existingVo = vos.find(v => v.si_id === siId)
       const [voFormOpen, setVoFormOpen] = useState(false)
       ```

    3. When `si.status === 'locked' && !existingVo` AND user can submit VO (admin/pm/main_contractor — read from useAuth().profile.global_role), render a button near the top of the detail (e.g. above the 抗議 tab content or in the header area):
       ```tsx
       {si.status === 'locked' && !existingVo && canSubmitVO && (
         <button onClick={() => setVoFormOpen(true)} className="btn-primary">
           提出變更指令
         </button>
       )}
       {voFormOpen && (
         <VoSubmitForm
           projectId={id}
           parentSi={si}
           onSubmitted={(voId, total) => { setVoFormOpen(false); navigate(`/project/${id}/vo/${voId}`) }}
           onCancel={() => setVoFormOpen(false)}
         />
       )}
       ```

    4. If an existing VO is present, show a link instead: `已有變更指令 {existingVo.number}` → navigates to /project/:id/vo/:voId.
  </action>
  <verify>
    <automated>grep -q "VoProvider" src/pages/SiDetail.tsx && grep -q "提出變更指令" src/pages/SiDetail.tsx && grep -q "VoSubmitForm" src/pages/SiDetail.tsx && grep -q "existingVo\|已有變更指令" src/pages/SiDetail.tsx && npx tsc --noEmit && npm run build:check</automated>
  </verify>
  <done>SiDetail surfaces VO entry point when conditions met; existing-VO link otherwise; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: [CHECKPOINT] Visual smoke + PDF export end-to-end</name>
  <what-built>
    VO submission, approval, and PDF export complete on web.
  </what-built>
  <how-to-verify>
    1. `npm run dev`. Login as subcon + approve an SI through to 已鎖定 (use Plan 02-05 flow).
    2. As MC (or admin), open the locked SI. Verify 提出變更指令 button visible.
    3. Tap → VoSubmitForm opens. Fill description, add 3 line items: 人工 10 人日 × HK$200 → subtotal HK$2,000; 物料 1 批 × HK$5,000 → HK$5,000; 暫定 1 批 × HK$1,000 → HK$1,000. Expected client preview total HK$8,000.00.
    4. Tap 提交 → VoConfirmationScreen shows `經系統核算總額 HK$8,000.00`. (Confirm matches client preview to the cent — VO-06 acceptance.)
    5. Approve the VO through all chain steps (MC → PM → 業主 if default D-16 chain).
    6. On VoDetail with status=已鎖定, tap 匯出 PDF. PDF downloads.
    7. Open the PDF. Verify:
       - Header shows 變更指令 VO-001 in Chinese (not empty boxes).
       - Line items table renders Chinese category labels (人工 / 物料 / 暫定).
       - 經系統核算總額 line shows HK$8,000.00 in bold.
       - Approval timeline includes all approvals with zh-HK action labels.
       - At least 1 drawing thumbnail page (if parent SI referenced drawings).
    8. Mobile (390px) check: VoLineItemsEditor stacks correctly; no horizontal scroll.
    9. Run `npm run build:check` once more — entry chunk <800 KB; viewer-pdf chunk present (lazy).
  </how-to-verify>
  <resume-signal>Type "verified" or paste screenshots/issues encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → VoContext mutations | Through Plan 02-06 RLS + submit_vo / submit_approval RPCs |
| client → exportVOToPDF | Same-origin font fetch; signed-URL drawing fetches |
| client total preview vs server total | Server is source of truth; preview labelled 經系統核算總額 (預覽) until confirmation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05b | T (Tampering) | Client claims fake VO total visually | mitigate | VoConfirmationScreen reads serverTotal from refetched VO row (not from form state); discrepancy between preview and confirmation is visible. PDF exports server total only. |
| T-02-03b | T | Concurrent VO approvers | mitigate | submit_approval RPC FOR UPDATE row-lock (Plan 02-04) applies equally to VO. |
| T-02-04d | E | UI shows admin_override to non-admin | mitigate | VoApproverBar checks profile.global_role === 'admin' UI-side; server enforces. |
| T-02-DPDF | I (Information disclosure) | PDF includes drawings user can't view | mitigate | Reuses Plan 02-06 mitigation — createSignedUrl respects bucket RLS; missing drawings silently skipped. |
| T-02-VO-PARENT | T | VO raised from non-locked SI | mitigate | submit_vo RPC checks parent SI lock; client-side guard refuses createDraftVo if parent not locked (but server is authoritative). |
| T-02-FLOAT | T | Floating-point HKD bugs | mitigate | All arithmetic via src/lib/currency.ts integer-cent helpers; no parseFloat on currency strings (P7). |
</threat_model>

<verification>
- 11 files created/edited; tsc green; build:check green.
- Visual smoke (Task 7): VO submission → confirmation → approval → PDF export end-to-end on web.
- PDF renders Chinese characters (not empty boxes); 經系統核算總額 matches server-computed total.
- Bundle entry chunk <800 KB; viewer-pdf chunk lazy-loaded only on 匯出 PDF tap.
</verification>

<success_criteria>
- VO-01..03, 06..10 user-facing flows complete on web; mobile responsive.
- Client preview vs server confirmation discrepancy never silent (server total wins).
- PDF export with Chinese rendering verified.
- SiDetail surfaces the VO entry point cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-si-vo/02-07-SUMMARY.md` covering: VO components, line-item arithmetic test results (e.g. client preview vs server total — confirm matched to the cent), PDF rendering observations (Chinese OK?), bundle size, any zh-HK truncation in line-item rows on 390px.
</output>
