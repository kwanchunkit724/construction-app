---
phase: 02-si-vo
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - package.json
  - package-lock.json
  - ios/App/App/Info.plist
  - android/app/src/main/AndroidManifest.xml
  - capacitor.config.ts
autonomous: false
requirements: [SI-02]

must_haves:
  truths:
    - "@capacitor/geolocation ^8.x installed and listed in package.json dependencies"
    - "Voice recorder path decided per supabase/v9-split/CAPACITOR8-COMPAT.md verdict from Plan 02-01 Task 7: either @capacitor-community/voice-recorder@<version> installed, OR a documented fallback (MediaRecorder web-only + skipped on native; tracked as A1 risk)"
    - "ios/App/App/Info.plist contains NSLocationWhenInUseUsageDescription (zh-HK) AND NSMicrophoneUsageDescription (zh-HK) — verify present, add if missing"
    - "android/app/src/main/AndroidManifest.xml contains <uses-permission android:name=\"android.permission.ACCESS_COARSE_LOCATION\" /> AND <uses-permission android:name=\"android.permission.RECORD_AUDIO\" />"
    - "npx cap sync ios && npx cap sync android complete without error"
    - "diff-match-patch@1.0.5 installed (TypeScript types either bundled or @types/diff-match-patch installed)"
    - "Bundle CI guard still green (entry chunk <800 KB) after npm install and stub import"
  artifacts:
    - path: package.json
      provides: "Three new deps: @capacitor/geolocation, diff-match-patch, and (conditional) @capacitor-community/voice-recorder"
    - path: ios/App/App/Info.plist
      provides: "Permission usage strings in zh-HK"
    - path: android/app/src/main/AndroidManifest.xml
      provides: "Geolocation + audio recording permissions"
  key_links:
    - from: "SiSubmitForm (Plan 02-05) VoiceRecorder + GeoPicker components"
      to: "native plugins"
      via: "import statements"
      pattern: "from '@capacitor/geolocation'"
---

<objective>
Install and configure the native Capacitor plugins SI Submission needs (geolocation for D-09 location capture, voice recorder for D-09 voice memo), plus diff-match-patch for SI version diff card (Plan 02-05). Add platform-specific permission strings. Verify cap sync passes for both iOS and Android.

Purpose: Enable SI submission's optional location + voice features on native + web.
Output: Updated package.json + lockfile + Info.plist + AndroidManifest + cap sync clean.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-si-vo/02-CONTEXT.md
@.planning/phases/02-si-vo/02-RESEARCH.md
@.planning/codebase/STACK.md
@.planning/codebase/INTEGRATIONS.md
@supabase/v9-split/CAPACITOR8-COMPAT.md
@package.json
@ios/App/App/Info.plist
@android/app/src/main/AndroidManifest.xml
@capacitor.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read CAPACITOR8-COMPAT.md verdict and install npm dependencies</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - supabase/v9-split/CAPACITOR8-COMPAT.md (Plan 02-01 Task 7 verdict)
    - package.json (current Capacitor 8 + React version)
    - .planning/phases/02-si-vo/02-RESEARCH.md §3 lines 222-251 (library choices + fallback ordering)
  </read_first>
  <action>
    1. Read CAPACITOR8-COMPAT.md. Identify verdict for voice-recorder:
       - **PASS** → run `npm install @capacitor-community/voice-recorder@latest @capacitor/geolocation@^8 diff-match-patch@^1.0.5 --save` and `npm install --save-dev @types/diff-match-patch`.
       - **FAIL** → run `npm install @capacitor/geolocation@^8 diff-match-patch@^1.0.5 --save` and `npm install --save-dev @types/diff-match-patch` only. Document in summary: "Voice memo uses MediaRecorder web-only; native voice descoped per A1 risk."

    2. Run the install. Then run `npm view @capacitor/geolocation peerDependencies` and `npm view @capacitor-community/voice-recorder peerDependencies` (if installed) once more and append observed versions to the SUMMARY (not the compat file).

    3. Verify lockfile updated:
       ```bash
       grep -E '"@capacitor/geolocation"|"@capacitor-community/voice-recorder"|"diff-match-patch"' package.json
       ```
       Expect at least @capacitor/geolocation and diff-match-patch lines.

    4. Do NOT commit yet — verify cap sync passes first (Task 3).
  </action>
  <verify>
    <automated>grep -q "@capacitor/geolocation" package.json && grep -q "diff-match-patch" package.json && grep -q "@types/diff-match-patch" package.json && test -f package-lock.json</automated>
  </verify>
  <done>npm dependencies installed; lockfile updated; voice-recorder path matches CAPACITOR8-COMPAT verdict; types installed.</done>
</task>

<task type="auto">
  <name>Task 2: Add platform permissions (Info.plist + AndroidManifest.xml)</name>
  <files>ios/App/App/Info.plist, android/app/src/main/AndroidManifest.xml</files>
  <read_first>
    - ios/App/App/Info.plist (read full file; CLAUDE.md notes camera/photos/microphone usage strings already present in zh-HK — verify; add NSLocationWhenInUseUsageDescription if absent)
    - android/app/src/main/AndroidManifest.xml (read full file; check existing <uses-permission> entries)
    - .planning/phases/02-si-vo/02-RESEARCH.md §3 lines 225-227 (permission requirements)
  </read_first>
  <action>
    **iOS (ios/App/App/Info.plist):**
    1. Read existing file. Search for `NSMicrophoneUsageDescription` — CLAUDE.md asserts it's already present in zh-HK; verify.
    2. Search for `NSLocationWhenInUseUsageDescription`. If MISSING, add inside the top-level <dict> (alphabetical / right before NSPhotoLibraryUsageDescription):
       ```xml
       <key>NSLocationWhenInUseUsageDescription</key>
       <string>需要你嘅位置以便在工地指令上記錄施工地點</string>
       ```
    3. If `NSMicrophoneUsageDescription` is somehow missing, add:
       ```xml
       <key>NSMicrophoneUsageDescription</key>
       <string>需要使用麥克風以便為工地指令錄音備忘</string>
       ```
    4. Preserve all existing keys verbatim — Apple compliance is brittle. Do NOT touch UIBackgroundModes, aps-environment, or any existing strings.

    **Android (android/app/src/main/AndroidManifest.xml):**
    5. Read existing file. Inside <manifest> top-level (before <application>), ensure these `<uses-permission>` entries exist (add any that are missing):
       ```xml
       <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
       <uses-permission android:name="android.permission.RECORD_AUDIO" />
       ```
       (Do NOT add ACCESS_FINE_LOCATION — D-09 calls for coarse + non-blocking; FINE triggers stricter Play Store review.)
    6. Preserve INTERNET, FCM, and any other existing permissions.
  </action>
  <verify>
    <automated>grep -q "NSLocationWhenInUseUsageDescription" ios/App/App/Info.plist && grep -q "NSMicrophoneUsageDescription" ios/App/App/Info.plist && grep -q "需要你嘅位置\|需要使用麥克風" ios/App/App/Info.plist && grep -q "android.permission.ACCESS_COARSE_LOCATION" android/app/src/main/AndroidManifest.xml && grep -q "android.permission.RECORD_AUDIO" android/app/src/main/AndroidManifest.xml && ! grep -q "android.permission.ACCESS_FINE_LOCATION" android/app/src/main/AndroidManifest.xml</automated>
  </verify>
  <done>Both Info.plist and AndroidManifest.xml have required zh-HK permission strings and uses-permission entries; FINE_LOCATION not added.</done>
</task>

<task type="auto">
  <name>Task 3: npx cap sync ios + android (verify native projects pick up new plugins)</name>
  <files>capacitor.config.ts</files>
  <read_first>
    - capacitor.config.ts (verify webDir + appId unchanged)
    - .planning/phases/01-drawings-on-progress-items/01-03-PLAN.md (Phase 1 plugin install reference for cap sync output expectations)
  </read_first>
  <action>
    1. Run `npx cap sync ios` — expect success. Plugins listed must include `@capacitor/geolocation` (and voice-recorder if installed). On Windows PowerShell, command stays the same.
    2. Run `npx cap sync android` — expect success.
    3. If either fails with a peer-dep error on voice-recorder Capacitor 8 compat, fall back to the FAIL path:
       - `npm uninstall @capacitor-community/voice-recorder`
       - Update SUMMARY.md noting fallback to web-only MediaRecorder + descoped native voice (A1 risk realised).
       - Re-run `npx cap sync ios` and `npx cap sync android` to confirm geolocation alone syncs cleanly.
    4. Inspect cap sync output and confirm `@capacitor/geolocation` plugin pod (iOS) and gradle line (Android) added.
    5. Do NOT modify capacitor.config.ts unless cap sync errors specifically ask for an update — pluginsiOS / pluginsAndroid sections should remain absent.

    Output of cap sync should be captured in the SUMMARY.
  </action>
  <verify>
    <automated>npx cap sync ios && npx cap sync android</automated>
  </verify>
  <done>Both platform syncs pass; geolocation plugin added to native projects; voice-recorder decision matches CAPACITOR8-COMPAT verdict.</done>
</task>

<task type="auto">
  <name>Task 4: Verify TypeScript compilation + bundle guard still green</name>
  <files>(no file modified — runs npm scripts)</files>
  <read_first>
    - package.json (build scripts)
    - .planning/phases/01-drawings-on-progress-items/01-02-PLAN.md (bundle CI guard threshold)
  </read_first>
  <action>
    1. Run `npx tsc --noEmit` to verify no type errors introduced.
    2. Run `npm run build:check` (Phase 1 bundle CI guard) — entry chunk must still be <800 KB. The new deps are not yet imported by any source file so this should be a no-op increase.
    3. If the bundle check fails, investigate — most likely a transitive dep got pulled in. Report in SUMMARY but do NOT block; Plan 02-04 will add manualChunks if needed.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npm run build:check</automated>
  </verify>
  <done>tsc clean; bundle CI guard green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: [CHECKPOINT] Verify cap sync output on developer machine</name>
  <what-built>
    npm deps installed; native permission strings added; cap sync ran for both platforms. Plugins listed in sync output need a human eye to confirm zh-HK strings render properly and no Xcode/Gradle warning appeared.
  </what-built>
  <how-to-verify>
    1. Open ios/App in Xcode (`npx cap open ios`). Build menu → Clean Build Folder. Then Build for any simulator. Expect: build succeeds. No new warnings about permission strings.
    2. Open android/ in Android Studio (`npx cap open android`). Sync Gradle. Expect: success, no plugin version conflict.
    3. (Optional) Run `npm run dev` and confirm web app still loads at http://localhost:5173.
  </how-to-verify>
  <resume-signal>Type "verified" or paste any warning text encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native plugin → OS permission prompts | OS-mediated; native shell honours zh-HK strings |
| diff-match-patch CPU | Runs in browser; no I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08a | I (Information disclosure) | Microphone access misuse | mitigate | Permission gated by OS (Info.plist usage string + Android RECORD_AUDIO); voice memo upload only to private project-si-vo bucket; signed-URL-only retrieval (Plan 02-04). |
| T-02-08b | I | Location data leakage | mitigate | COARSE permission only (≈3 km accuracy on Android per Google reference); D-09 ships accuracy_m to UI for transparency. |
| T-02-DEP | T (Tampering) | Supply-chain compromise of new npm dep | accept | diff-match-patch is Google-published, dormant since 2018 — low surface area; @capacitor/geolocation is official Ionic family; voice-recorder community plugin risk noted in A1; lockfile pins exact versions. |
| T-02-APP | S (Spoofing) | Apple Play Store reviewer rejects new permission | accept | zh-HK strings clearly describe construction-site use; COARSE not FINE reduces review scrutiny. Document for next App Store submission. |
</threat_model>

<verification>
- npm deps installed; lockfile updated (no missing entries).
- iOS Info.plist + Android Manifest contain required permission strings (grep verified).
- cap sync both platforms passes.
- tsc + build:check pass.
- Manual Xcode + Android Studio open + build (checkpoint Task 5) succeed without warnings.
</verification>

<success_criteria>
- SI-02 native enablement complete (location + voice memo).
- diff-match-patch available for SiContext / diff-card (Plans 02-04 / 02-05).
- No regression to Phase 1 build artefacts.
- Voice-recorder path decision recorded.
</success_criteria>

<output>
After completion, create `.planning/phases/02-si-vo/02-03-SUMMARY.md` covering: voice-recorder verdict (PASS / FAIL with fallback), peerDependency strings observed, any Xcode / Gradle warnings, decision log for Plan 02-04 voice path (native plugin vs. MediaRecorder).
</output>
