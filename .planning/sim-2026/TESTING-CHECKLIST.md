# 測試清單 — Final Upgrade (Wave 1–3) · iOS TestFlight + Android

> 分工：**你（iOS / TestFlight）** + **我（Android / BlueStacks）**。兩邊都行同一張清單。
> 兩邊測完冇問題 → 先推 Wave 4（v74 私有相 bucket + 簽名強制）+ Wave 5（凍結）。
>
> ⚠️ 重要分類：
> - **【伺服器】= 已 LIVE**，任何版本（包括而家手機上嘅舊 build）即刻睇到。
> - **【新 build】= 客戶端改動**，只喺新 TestFlight / 新 APK 先有。舊 build 唔會有。
>
> 帳號（密碼全部 `test1234`）：admin `60000099` · PM `60001001` · 總承建商 `60001003` ·
> 老總/工地主任 `60001002` · 判頭 `60001005` · 工人 `60001006` · 安全主任 `60000004`。
> 測試項目：大地盤 `d0000001…` · 裝修 `d0000002…` · 渠務 `d0000003…` · 維修 `d0000004…`。
> 做完每項剔 ☐→☑，有問題寫低（邊步、見到乜、預期乜）。

---

## A. 伺服器改動（已 LIVE，舊新 build 都應該得）

### A1 ☐ 文件模組已上線【伺服器】
- 邊個：任何已入工地嘅角色（用 PM `60001001`）。
- 點做：登入 → 入大地盤 → 睇側邊欄／工具頁 → 撳「文件」。
- 預期：見到「文件」入口；開到「文件總覽」，有文件（MAT-001 物料送審、MS-001 施工方案），可篩選/搜尋，有「上載文件」掣。
- ⛔ 之前：文件模組係收埋（dark-ship OFF），而家已上線。

### A2 ☐ 機械/表格（地盤表格）已上線【伺服器】
- 邊個：PM 或 admin。
- 點做：入大地盤 → 撳「機械/表格」。
- 預期：見到機械登記冊（EQ-001 塔吊、EQ-002 棚架），有效/到期 KPI 格，「新增機械」「列印全部 QR」。

### A3 ☐ 老總可以寫施工日誌【伺服器】
- 邊個：老總 `60001002`。
- 點做：入大地盤 → 每日日誌 → 填上晝天氣 + 內容 → 提交。
- 預期：寫得入、save 到。非授權角色（如判頭）見到嘅提示係「只有項目經理、工地主任或總承建商成員可以填寫施工日誌」。

### A4 ☐ 老總可以加行事曆事件【伺服器 + 新build客戶端】
- 邊個：老總 `60001002`。
- 點做：入大地盤 → 行事曆 → 加事件（會議/檢查）→ save。
- 預期：加得到。⚠️ 客戶端「+」掣只喺**新 build** 先開俾老總；舊 build 老總見唔到掣（但伺服器已準）。

### A5 ☐ 相片大細/類型限制【伺服器】
- 邊個：工人 `60001006`。
- 點做：報問題時試上載一張超大相（>10MB）或非圖片檔。
- 預期：被拒（太大／類型唔啱）。正常手機相（壓縮後）OK。

### A6 ☐ 刪除帳號唔再殺日誌【伺服器 · 小心做】
- 邊個：admin 開一個**用完即棄**測試帳號，派去一個項目，叫佢寫一篇日誌。
- 點做：用嗰個 throwaway 帳號喺 Profile 撳「刪除帳號」。
- 預期：帳號刪到（合規冇壞），但嗰篇日誌**仍然在**（作者變空白）。
- ⚠️ 唔好攞真人帳號做。

---

## B. 客戶端改動（**只新 build** 先有；舊 build 唔使測）

### B1 ☐ 問題相 — 上載即時顯示【新build】
- 邊個：工人 `60001006`。
- 點做：入大地盤 → 問題 → 報新問題 → 影/揀相 → 提交 → 開返張單。
- 預期：相上載到（已壓縮，慳容量）+ 喺問題詳情同問題卡都顯示到（經簽名 URL）。

### B2 ☐ 舊問題相仍然睇到【新build · 重點】
- 邊個：PM。
- 點做：開一張**舊**問題單（之前有相嗰啲）。
- 預期：舊相照樣顯示（簽名 URL shim 自動處理舊公開連結）。冇爛圖。

### B3 ☐ Demo 頁排版【新build / web】
- 點做：開 `/#/demo`（唔使登入）。
- 預期：價值模組行先，「平台保證（底層）」一段喺**最尾**。

### B4 ☐ 模組開關 = 唯一閘【新build】
- 邊個：admin。
- 點做：管理 → 某項目 → 模組 → 關「文件」→ 用 PM 入嗰個項目睇。再開返。
- 預期：關咗 → 文件入口消失（側邊欄+工具頁）；開返 → 即刻返嚟。冇咗以前嗰層 app_config 旗（dark-ship）。

---

## C. 回歸測試（核心流程冇被改壞）

### C1 ☐ 登入 / 登出 — 各角色登入到，登出乾淨。
### C2 ☐ 進度 — 工人更新派俾自己嘅細項 %；上層大項自動加總。
### C3 ☐ 問題升級鏈 — 判頭報→升級→處理→解決→重開，紀錄齊。
### C4 ☐ SI / VO — 開工地指令、變更指令、簽核鏈行到。
### C5 ☐ PTW — 開動火/高空許可證、安全主任簽核、QR 核實。
### C6 ☐ 物料 — 落單、改狀態（老總/總承建商都入到貨）。
### C7 ☐ 天氣/EOT — 實時 HKO banner 顯示；惡劣天氣字句係通用（唔再淨係講「高空/吊運」）。
### C8 ☐ AI 站長 — 用粵語問「大地盤進度幾多%」，答到。
### C9 ☐ 匯出 — PM 匯出業主版/內部版報告（手機分享/下載到）。

---

## D. 測完之後
兩邊（你 iOS / 我 Android）行完 A+B+C，冇 S1/S2 問題 →
1. **Wave 4** — apply v74（issue-photos 轉私有）+ T2.1（close_out_ptw 強制）+ 開簽名強制旗 #22。
2. **Wave 5** — 最終回歸 + storage 基線 + 打版本 tag → 凍結。

有問題寫喺下面：

| # | 平台 | 項目 | 見到乜 | 預期乜 | 嚴重度 |
|---|------|------|--------|--------|--------|
|   |      |      |        |        |        |
