# CK工程 — 全控件 + 輸出驗證清單 (TEST-PROGRAM 附錄)

> 自動由 codebase 清點(workflow inventory),共 **567** 個控件。
> 配合 [TEST-PROGRAM.md](TEST-PROGRAM.md) 使用。**每一行 = 一個要驗證嘅掣/輸出。**
> 驗法分 UI(撳→睇結果/snapshot)同 REST(打 API assert);輸出(PDF/XLSX/QR/圖表)要開檔睇**內容 + 中文唔亂碼**,再 capture 截圖過目。

## A. 輸出物驗證(PDF / Excel / QR / 圖表 / signed-url)— 共 40 項

逐項:撳掣 → 產生檔/圖 → 開嚟對「預期內容」→ 中文檢查 → 截圖 capture。

| # | 功能 | 螢幕 | 控件 | 類型 | 預期內容 | 點驗 | Capture | 權限 | source |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 進度表 | ExportProgressModal footer | **匯出 Excel button (FileSpreadsheet icon)** | xlsx | Single sheet '進度報告'. Header rows: verdict line (e.g. '港島XX — 整體 63%，落後計劃 7%，5 項要跟進'), project name, generation datetime + period, overall 計劃%/實際%/差距%/behind count, status breakdown counts. Column header row: 分區／編號／名稱／層級／追蹤模式／計劃%／實際%／差距／狀態／計劃開始／計劃完成／備注. Data rows grouped by zone with zone header row (▌ 分區名) + subtotal row (小計, count, 落後N). Item rows indented by depth (全角空格 prefix). Unscheduled leaves show '未排期' for 計劃%/差距. Status column prefixed with mark (⚠/■/✓). Numeric % cells formatted as '0%'. Freeze pane below header. Row outline levels for grouping. Chinese column headers and content must not be garbled (XLSX uses UTF-16 via xlsx library, no font embedding needed). | File downloads; open in Excel/Numbers: '進度報告' sheet present; columns match spec; Chinese text readable; numeric % cells summable; zone grouping present when groupByZone=true | screenshot of downloaded XLSX opened in Excel showing Chinese headers and data rows | ExportProgressModal open; disabled while busy | src/components/ExportProgressModal.tsx:196-199; src/lib/export.ts:362-456 |
| 2 | 進度表 | ExportProgressModal footer | **匯出 PDF button (FileText icon)** | pdf | A4 portrait PDF, JPEG-rendered via html2canvas at 2x scale. PAGE 1 (one-pager, all presets): [1] Verdict banner (coloured red/amber/green): '工地名 — 整體 X%，落後計劃 Y%，N 項要跟進'; [2] KPI cards row: 整體實際 X% / 計劃 Y% / 差距 ±Z% / 本期Δ (when snapshot exists); [3] '各分區進度' zone bars with actual/planned dual-bar and ± labels; [4] '需要關注(N)' list — delayed/blocked/gap<-10 items, max 12 shown, sorted by gap ascending; [5] '未開始 N 項（未列入上表）'. APPENDIX pages (internal preset only): per-zone table with orange header row and columns: 編號／名稱(indented)／實際／計劃／差距／本期(when delta available)／狀態(colour pill)／說明／計劃完成. Left-border colour-coded by status. Level-1 rows bold-700, level-2 bold-600, level-3 normal-400. Footer on each page: 'CK 工程 · 期數' in NotoHK font (Chinese must render; falls back to ASCII if font load fails) + 'P. N / total' in Helvetica. File name: projectName_進度_YYYY-MM-DD.pdf. Chinese text in page body rendered via html2canvas (browser fonts, CJK guaranteed); footer Chinese via embedded NotoSansHK subset TTF. | PDF file opens; page 1 shows verdict banner with Chinese project name; zone bars with dual markers; attention table items in Chinese; internal preset: appendix table pages with 編號/名稱/實際/計劃/差距/狀態 columns; Chinese text readable (not garbled); footer shows 'CK 工程' in CJK font; page numbers correct | preview_screenshot of the rendered PDF page 1 (verdict + zone bars) and page 2+ (appendix table); check footer for CJK rendering | ExportProgressModal open; disabled while busy | src/components/ExportProgressModal.tsx:200-203; src/lib/export.ts:559-652, 468-557 |
| 3 | 問題 | ProjectDetail 問題tab | **匯出 Excel（ExportMenu 下拉項目，問題tab專用）** | xlsx | Sheet1「問題清單」：欄位 編號／位置／狀態／標題／描述／照片數／報告者／報告者角色／當前處理層／解決者／報告時間／解決時間，所有中文欄位及內容必須正確顯示（非亂碼）。Sheet2「處理紀錄」：欄位 編號／問題標題／時間／動作／操作人／內容／由／至，按問題編號再按時間排序，顯示完整升級/解決/重開活動記錄（中文）。 | 開啟下載的 .xlsx，確認 Sheet1 含所有問題行、Sheet2 含全部 comment 行、中文無亂碼、編號格式正確（如 #001） | screenshot Excel 開啟後 Sheet1 及 Sheet2 各一頁 | 無額外角色限制（只在問題 tab 可見） | src/pages/ProjectDetail.tsx:339-361, src/lib/export.ts:664-736 |
| 4 | 問題 | CreateIssueModal | **拍照（Camera icon button）** | image | 已壓縮的現場照片上傳至 Supabase issue-photos 私有 bucket；預覽縮圖顯示於 3-column grid；capturedAt ISO timestamp 及 GPS（capturePhotoGeo）記錄至 photo_meta | 相片縮圖出現在 grid，uploading spinner 後消失，photo counter 增加 | screenshot 照片已加入 grid | photos.length < 6 | src/components/CreateIssueModal.tsx:245-249, 261-268 |
| 5 | 問題 | CreateIssueModal | **從相簿選（ImagePlus icon button）** | image | 同拍照：壓縮照片上傳 issue-photos bucket，縮圖顯示，capturedAt+GPS 記錄 | 相片縮圖出現，counter 更新 | screenshot 照片已加入 grid | photos.length < 6 | src/components/CreateIssueModal.tsx:252-258, 269-275 |
| 6 | 即時問題 | CreateQuickSnagSheet | **拍照（Camera icon button，可選）** | image | 壓縮後照片上傳至 issue-photos bucket；capturedAt + GPS 記錄 | 縮圖出現，counter 增加 | screenshot 照片已加入 grid | photos.length < 6 | src/components/CreateQuickSnagSheet.tsx:224-229, 239 |
| 7 | 即時問題 | CreateQuickSnagSheet | **從相簿選（ImagePlus icon button，可選）** | image | 同拍照路徑 | 縮圖出現，counter 更新 | screenshot 照片已加入 | photos.length < 6 | src/components/CreateQuickSnagSheet.tsx:231-236, 240 |
| 8 | 問題 | IssueDetail | **問題照片（IssuePhoto — <a> 連結，每張照片）** | image | Supabase issue-photos bucket 的 signed URL 圖片，顯示完整解析度現場照片 | 新分頁顯示照片圖像，非 403/空白 | screenshot 新分頁大圖 | issue.photos.length > 0 | src/components/IssuePhoto.tsx:26-30, src/pages/IssueDetail.tsx:227-234 |
| 9 | 問題 | ProjectDetail 問題tab IssuesTab | **IssueCard 封面相片（Camera icon 多張數量徽章）** | image | 私有 bucket 照片 signed URL 縮圖 | 縮圖顯示正確圖片，非 placeholder/broken | screenshot IssueCard 縮圖 | issue.photos.length > 0 | src/components/IssueCard.tsx:22-29 |
| 10 | 工地指令SI | SiSubmitForm — Photo source sheet | **拍攝 button** | image | Camera-captured JPEG/PNG; shown as object-cover thumbnail in 3-col grid | Photo thumbnail appears in grid | Screenshot of photo grid after capture | photoSheetOpen === true | src/components/si/SiSubmitForm.tsx:433-440 |
| 11 | 工地指令SI | SiSubmitForm — Photo source sheet | **從相簿選擇 button** | image | Selected gallery image(s); shown as thumbnails in grid | Thumbnail(s) appear in photo grid | Screenshot of photo grid | photoSheetOpen === true | src/components/si/SiSubmitForm.tsx:441-449 |
| 12 | 工地指令SI | SiSubmitForm — Photo source sheet | **從檔案選擇 button** | image | Selected file image(s) as thumbnails | Thumbnail(s) appear in photo grid | Screenshot of photo grid | photoSheetOpen === true | src/components/si/SiSubmitForm.tsx:450-458 |
| 13 | 變更指令VO | VoDetail 詳情頁 | **匯出 PDF (btn-primary with FileDown icon)** | pdf | Page 1: header '變更指令 {vo.number}', 項目 project.name, 狀態, 提交/鎖定時間, '參考工地指令: {siNumber／—}', description text. autoTable with columns [#, 類別, 描述, 數量, 單位, 單價, 小計] (all in zh-HK), foot row '經系統核算總額' + formatHKD(total_amount_cents) in HKD. Page 2: '簽核紀錄' autoTable [時間, 動作, 處理者, 原因]. Additional pages: '附圖' with drawing thumbnails (6/page, ≤200KB each, fetched via signed URL). Footer: '產生時間：{zh-HK datetime} — 由 CK工程系統產生'. Font: NotoHK Subset — Chinese MUST NOT render as 亂碼. | PDF opens; check page 1 has table with all line items and 經系統核算總額 row; page 2 has 簽核紀錄; Chinese text is legible (not 亂碼); filename matches pattern | preview_screenshot of the rendered PDF (open in browser); verify table rows count matches line_items.length; verify HKD total matches vo.total_amount_cents | only rendered when vo.status === 'locked' | src/pages/VoDetail.tsx:93-126, 185-202; src/lib/export.ts:830-928 |
| 14 | 工作許可證PTW | PtwSubmitForm (modal) — PtwPhotoPicker (PPE相片) | **加入相片 (PPE 相片 camera button)** | image | Compressed JPEG previews of PPE (hard hat/vest/harness) photos; previews are object-URL <img> elements; Chinese label '加入相片 (n/6)' must render; not stored until 提交 pressed | UI: preview tiles appear; button counter increments; file count in state matches previews | screenshot of form with PPE photo preview tiles visible | disabled when files.length >= 6 or busy=true | src/components/ptw/PtwPhotoPicker.tsx:58-66 |
| 15 | 工作許可證PTW | PtwSubmitForm (modal) — PtwPhotoPicker (現場相片) | **加入相片 (現場相片 camera button)** | image | Compressed JPEG previews of site scene photos; Chinese label '加入相片 (n/6)' renders | UI: preview tiles appear in 3-column grid; counter increments | screenshot of form with scene photo preview tiles visible | disabled when files.length >= 6 or busy=true | src/components/ptw/PtwPhotoPicker.tsx:58-66 |
| 16 | 工作許可證PTW | PtwSubmitForm (modal) — WorkerRow | **工人相片 (avatar button per worker row)** | image | Compressed JPEG worker face photo preview (48x48 avatar thumbnail); stored at project-si-vo bucket path {projectId}/ptw/{ptwId}/workers/{workerId}/photo.jpg after submit | UI: avatar shows photo thumbnail instead of + icon after pick | screenshot of worker row with photo thumbnail visible | none — visible for every worker row | src/components/ptw/PtwSubmitForm.tsx:365-375, 344-354 |
| 17 | 工作許可證PTW | PtwDetail | **匯出合規證明 (PDF) (Download icon button)** | pdf | PDF pages: header with '工作許可證 (PTW)' title, PTW serial number, DWSS §3.1.8 reference string (e.g. CK-PTW-000001), status in zh-HK, PTW type in zh-HK, approval chain roles in zh-HK. All Chinese text must render (Traditional Chinese font embedded or system fallback — must NOT show 亂碼). Single document (proof pack). | FILE: open downloaded PDF; verify Chinese characters render; verify DWSS ref string present; verify PTW number matches | preview_screenshot of the rendered PDF showing Chinese title and DWSS reference | none — visible to all users who can view the PTW | src/pages/PtwDetail.tsx:123-136 |
| 18 | 工作許可證PTW | PtwDetail — PtwSignaturePad (close-out modal) | **簽名區 (canvas — touch/mouse draw)** | image | PNG image of hand-drawn signature captured via getTrimmedCanvas().toDataURL('image/png'); base64 stripped of data URI prefix and sent to closeOut(ptw.id, b64) | UI: strokes visible on canvas; confirm button enabled | screenshot of signature pad with drawn signature | modal must be open (showCloseOut===true) | src/components/ptw/PtwSignaturePad.tsx:52-59 |
| 19 | 工作許可證PTW | PtwDetail — PtwQrCard (active, non-expired permit only) | **驗證 QR (QR code display — scannable by inspector)** | qr | QR encodes URL: {origin}/#/verify/{JWT}. JWT payload: { permit_id: UUID, project_id: UUID, ptw_type: string, number: string, iat: Unix timestamp, exp: Unix timestamp }. JWT is server-signed (HS256 secret never leaves server). Scanning after permit expiry shows '已過期'. Scanning when not logged in redirects to /login?next=/verify/:token. Each scan writes one row to permit_scans. | QR: scan with a second device logged in to the app; verify PtwVerify page shows '簽核有效' with correct PTW number and expiry; DB: new row in permit_scans; expired permit shows '已過期' | screenshot of QR card on PtwDetail; screenshot of PtwVerify result page after scan | only rendered when ptw.status === 'active' AND isPtwExpired(ptw) === false; qrToken fetched from server via mintPtwQrToken on mount | src/components/ptw/PtwQrCard.tsx:24-29, src/pages/PtwDetail.tsx:47-53, src/lib/ptw-jwt.ts:16-23 |
| 20 | 機械表格 | EquipmentList | **匯出登記冊** | xlsx | Two sheets: Sheet 1 '機械登記' — columns 編號/名稱/類別/品牌型號/序號/位置/狀態 (all zh-HK text, must NOT be 亂碼). Sheet 2 '檢查狀態' — columns 機械/表格/法定依據/狀態/有效至/最後簽署/簽署人/結果/暫停 plus optional KPI summary row '有效 N · 即將到期 N · 過期 N · 未簽 N · 停用 N' prepended at bottom. All Chinese cell values rendered in UTF-8 via xlsx library. | Open XLSX: confirm 2 sheets with zh-HK column headers and correct Chinese values in cells, no garbled characters | screenshot of downloaded file opened in Excel/Numbers showing both sheets with Chinese text | equipment.length > 0 (button only shown when list is non-empty; exportRegister itself open to any viewer) | src/pages/EquipmentList.tsx:138-150; src/lib/export.ts:1384-1456 |
| 21 | 機械表格 | EquipmentList | **列印全部 QR** | qr | Each QR encodes URL: `${window.location.origin}/#/equipment-verify/<JWT-token>`. Card labels: equipment ref_no (mono) + name_zh (zh-HK bold) + '掃描以核實表格狀態'. Grid is A4 print-ready (2 cols, A6-ish per cell). Chinese text must render correctly in printed output. | Overlay shows grid of QR cards; QR values decode to valid equipment-verify URLs; each card name_zh matches the equipment record | screenshot of the EquipmentQrPrintSheet overlay with QR codes rendered | canManage === true AND equipment.length > 0 | src/pages/EquipmentList.tsx:151-163; src/pages/EquipmentList.tsx:61-75; src/components/equipment/EquipmentQrPrintSheet.tsx |
| 22 | 機械表格 | EquipmentList — EquipmentQrPrintSheet overlay | **列印 / 存 PDF** | pdf | A4 PDF with 2-column grid. Each cell: QRCodeSVG at 160px, ref_no in mono below, name_zh in bold, '掃描以核實表格狀態'. Chinese text (ref_no + name_zh) must render — uses browser system fonts not jsPDF embed, so relies on OS CJK font presence. | Print preview shows 2-col grid; QR codes are scannable; Chinese labels are legible | screenshot of print preview dialog or saved PDF opened showing QR grid | canManage; ready === true (all tokens resolved) | src/components/equipment/EquipmentQrPrintSheet.tsx:51-55 |
| 23 | 機械表格 | EquipmentDetail — equipment header card | **QR (icon + label button)** | qr | QR encodes `${window.location.origin}/#/equipment-verify/<JWT>`. Labels: ref_no (mono), name_zh (zh-HK), '貼於機械上，任何已登入成員掃描即可核實表格狀態。每次掃描會寫入 equipment_scans 審計紀錄。' | QR card visible; QRCodeSVG decoded URL matches /equipment-verify/<token> pattern; ref_no and name_zh shown correctly | screenshot of EquipmentQrCard with rendered QR code | canManage === true | src/pages/EquipmentDetail.tsx:121-129; src/pages/EquipmentDetail.tsx:134-144 |
| 24 | 機械表格 | EquipmentDetail — EquipmentQrCard | **列印 (printer icon button)** | qr | Single-card print sheet: QRCodeSVG at 160px, ref_no, name_zh, '掃描以核實表格狀態'. Same A4 print-CSS layout. | Print sheet overlay shows exactly one QR card with correct labels | screenshot of EquipmentQrPrintSheet with single card | canManage AND token is non-null (button only shown when `token && onPrint`) | src/components/equipment/EquipmentQrCard.tsx:26-29; src/pages/EquipmentDetail.tsx:263-269 |
| 25 | 機械表格 | EquipmentDetail — EquipmentQrPrintSheet (single card) | **列印 / 存 PDF** | pdf | Single QR card on A4: QRCodeSVG, ref_no, name_zh (zh-HK), '掃描以核實表格狀態'. Chinese text via system fonts. | Print preview shows one QR card; label Chinese text legible | screenshot of print preview | ready === true (token resolved) | src/components/equipment/EquipmentQrPrintSheet.tsx:51-55 |
| 26 | 機械表格 | EquipmentDetail — SignSheet — PtwSignaturePad | **確認簽署 (or equivalent confirm button in PtwSignaturePad)** | pdf | A4 PDF with: header = template name_zh; lines for 表格編號/法定依據/項目/機械/位置/檢查結果; section '檢查項目' listing each item with [合格]/[不合格]/[—] prefix, label_zh, optional remark; signature block with drawn PNG image (180x70pt), 合資格人士 name, 證書編號 (if any), 簽署時間 (zh-HK locale), 有效至; footer '產生時間 — 由 CK工程系統產生'. All Chinese text via NotoHK embedded font (must not be 亂碼). | DB: new form_signoffs row exists with correct instance_id, result, signed_by; form_instances.valid_until updated. PDF: open and verify Chinese text, signature image, all fields populated | screenshot of shared/downloaded PDF opened — verify first page shows Chinese header and signature block; also screenshot form card on EquipmentDetail showing updated status badge and SignatureProofCard | credOk === true; step-up grant 'form_signoff'; sign reauth; RPC record_form_signoff server-side re-asserts both | src/pages/EquipmentDetail.tsx:316-358; src/contexts/EquipmentContext.tsx:250-277; src/lib/export.ts:967-1030 |
| 27 | 機械表格 | EquipmentDetail — form instance card — SignatureProofCard | **匯出簽名證明 (PDF)** | pdf | A4 PDF (html2canvas render — uses system CJK fonts Microsoft JhengHei/PingFang HK so ALL Chinese renders correctly without 亂碼): Title '簽名證明 (Signature Proof)'; subtitle 'CK工程 — 電子簽名非否認證明（勞工處用途）'; Sections: 簽署人 (姓名/電話/項目角色); 合資格人士證明 (資格類別/證書編號/有效至, if credential present); 簽署文件 (類型='法定表格'/編號/項目/內容=templateName·result/簽署時間 zh-HK); 身份驗證 (簽署前重新驗證 status); 防篡改記錄 (帳本序號/雜湊/雜湊鏈完整性); 證明聲明 (attestation_zh full text); footer with generation timestamp. | Open PDF: confirm all sections present, Chinese text not garbled (must use html2canvas path not jsPDF glyph subset), attestation_zh text is the zh-HK legal sentence from DB | screenshot of the PDF preview (all sections visible); note html2canvas renders Chinese via system fonts | proof loaded successfully (get_signature_proof RPC returned data); any user who can view the form card | src/components/SignatureProofCard.tsx:79-108; src/lib/export.ts:1060-1111 |
| 28 | 機械表格 | Profile page — CredentialsSection — credential list row | **已上載證明 — thumbnail link (image doc)** | image | Full-size credential image (photo of certificate); viewed in browser native image viewer or new tab | New tab opens with credential image | screenshot of opened image in new tab | c.doc_path non-null; url resolved; not a PDF | src/components/CredentialsSection.tsx:241-244 |
| 29 | 機械表格 | Profile page — CredentialsSection — credential list row | **查看 PDF (PDF doc link)** | pdf | PDF credential document as uploaded by the user; browser PDF viewer renders it | New tab opens PDF viewer with the credential PDF | screenshot of PDF opened in new tab | c.doc_path non-null; url resolved; isPdf === true | src/components/EquipmentList.tsx:0 (CredentialsSection.tsx:246-249) |
| 30 | 文件圖則 | DocumentDetailSheet (from ProjectFiles) | **查看 (per version row)** | signed-url | Signed Supabase Storage URL (project-docs bucket) for the version file; image renders inline or PDF renders via react-pdf; Chinese title visible in viewer header | Viewer opens; image/PDF visible; title + revision label shown in header | screenshot of DocumentViewer with content loaded | any user who can open the document detail sheet | src/pages/ProjectFiles.tsx:889-896 |
| 31 | 文件圖則 | DocumentVersionHistory sheet (文件) | **版本行 (per version button)** | signed-url | Signed Supabase Storage URL for the selected version's file; image/PDF renders; header shows that version's revLabel and status | Viewer header shows selected version revLabel; content changes | screenshot of viewer after version switch | sheet open | src/components/documents/DocumentVersionHistory.tsx:110-119 |
| 32 | 文件圖則 | DocumentsSection (inline) | **DocumentThumbnail (grid tile click)** | signed-url | Signed URL for current version file (image or PDF); Chinese title in viewer header; revision label displayed | Viewer open; content loaded; title + revision label correct | screenshot of viewer | currentVersionFor(d) !== null | src/components/documents/DocumentsSection.tsx:252-258 |
| 33 | 文件圖則 | DocumentsSection inline menu | **查看版本記錄 (menu item)** | signed-url | Signed URL for current version; viewer header shows title + revLabel | Viewer open; version history button accessible | screenshot of viewer | canUpload | src/components/documents/DocumentsSection.tsx:291-299 |
| 34 | 文件圖則 | DrawingsSection (inline) | **DrawingThumbnail (grid tile click)** | signed-url | Signed Supabase Storage URL (project-drawings bucket) for the version file; image or PDF renders; drawing title in header; revision label shown; no 亂碼 | Viewer open; content rendered; title + revLabel correct | screenshot of DrawingViewer with content | currentVersionFor(d) !== null | src/components/drawings/DrawingsSection.tsx:210-216 |
| 35 | 文件圖則 | DrawingsSection inline menu | **查看版本記錄 (menu item)** | signed-url | Signed URL for current version; viewer header shows drawing title + revLabel | Viewer open | screenshot of viewer | canUpload | src/components/drawings/DrawingsSection.tsx:245-251 |
| 36 | 文件圖則 | DrawingVersionHistory sheet | **版本行 (per version button)** | signed-url | Signed Supabase Storage URL (project-drawings bucket) for the selected DrawingVersion; image/PDF rendered; header shows that version's revLabel; no 亂碼 | Viewer header shows selected version revLabel; content changes to that version | screenshot of viewer after version switch | sheet open | src/components/drawings/DrawingVersionHistory.tsx:106-116 |
| 37 | dashboard | WeatherRecord 天氣記錄 | **Excel 匯出** | xlsx | Sheet「天氣EOT記錄」：標題行含項目名、產生時間、EOT總日數；資料行含日期、天氣事件(zh-HK)、天文台站、觸發、關鍵路徑(是/否)、可施工(是/否)、善後日數、申請EOT日數、備註；最後 footer row 顯示總日數。所有中文必須正常顯示（非亂碼）。 | 打開 xlsx，確認 Sheet 名「天氣EOT記錄」，資料行數 = claims.length，EOT 總數正確，中文字可讀 | screenshot of opened xlsx showing Chinese headers and data rows | weatherOn = true AND claims.length > 0（按鈕才顯示）；任何能看此頁的用戶 | src/pages/WeatherRecord.tsx:130, src/lib/export.ts:1259 |
| 38 | dashboard | WeatherRecord 天氣記錄 | **PDF 匯出** | pdf | 標題「惡劣天氣 / 工期延誤 (EOT) 申索報告」；項目名、EOT總日數、CEDD準則說明；autoTable 含列（日期、天氣事件、觸發、關鍵路徑、可施工、善後、EOT日數、備註）；footer row 申請EOT總日數；footer 行「產生時間：...— 由 CK工程系統產生」。全部中文須用 NotoHK 字體正常顯示。 | 打開 PDF，確認標題、表格中文可讀、頁腳日期正確 | preview_screenshot of rendered PDF showing Chinese title and table | weatherOn = true AND claims.length > 0；canManage 角色（pm/main_contractor/subcontractor/general_foreman/admin） | src/pages/WeatherRecord.tsx:131, src/lib/export.ts:1309 |
| 39 | admin | AdminProjects 工地管理 | **匯出 Excel（Download icon）** | xlsx | Sheet「工地清單」：欄位 名稱、分區數、分區（id:name 格式）、指派PM（名字逗號分隔）、建立時間（zh-HK locale）。每個工地一行，中文全部可讀。 | 打開 xlsx，確認 Sheet 名「工地清單」，行數 = projects.length，PM 名字正確，中文可讀 | screenshot of opened xlsx showing all columns and Chinese text | global_role = admin | src/pages/AdminProjects.tsx:55-62, src/lib/export.ts:740 |
| 40 | 問題 | ProjectDetail 頁頭 匯出選單（問題 tab） | **匯出 Excel（問題）** | xlsx | Sheet 1「問題清單」：欄位 編號/位置/狀態/標題/描述/照片數/報告者/報告者角色/當前處理層/解決者/報告時間/解決時間；每個 issue 一行。Sheet 2「處理紀錄」：每個 comment 一行，欄位 編號/問題標題/時間/動作/操作人/內容/由/至。所有中文可讀，前成員顯示「前成員」。 | 打開 xlsx，確認兩個 sheet 存在，資料行數對應 issues/comments 數量，欄位名全中文可讀 | screenshot of opened xlsx with both sheets visible and Chinese content | tab = 'issues' | src/pages/ProjectDetail.tsx:1076-1078, src/lib/export.ts:664 |

---

## B. 全控件逐個驗證(按功能分組)— 共 567 項

### 進度表 (97)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | AssignmentModal — member list | **Member checkbox labels** | other | toggle(selected, setSelected, id) — adds/removes member from assigned[] or delegated[] | Checkbox checks/unchecks; row border changes to safety-500 (assign) or amber-500 (delegate); tab count in header updates | none | AssignmentModal open; members are approved project members | checkbox checked; row has appropriate border colour; tab count reflects selection | screenshot with members selected | src/components/AssignmentModal.tsx:139-170 |
| 2 | AssignmentModal — tabs | **負責人 tab** | tab | setTab('assign') — shows main_contractor + subcontractor member checkboxes | Tab active (white bg, safety-600 text); candidateIds = ownerCandidateIds; assigned[] checkboxes shown | none | AssignmentModal open | tab has bg-white shadow-card style; member list shows main_contractor and subcontractor members | screenshot of AssignmentModal 負責人 tab | src/components/AssignmentModal.tsx:107-114 |
| 3 | AssignmentModal — tabs | **委派判頭/工人 tab** | tab | setTab('delegate') — shows subcontractor + subcontractor_worker member checkboxes | Tab active (amber-700 text); candidateIds = delegateeCandidateIds; delegated[] checkboxes shown | none | AssignmentModal open | tab has bg-white; amber-700 text; delegate member list visible | screenshot of 委派 tab | src/components/AssignmentModal.tsx:116-124 |
| 4 | AssignmentModal (Modal base) | **關閉 modal (X or backdrop)** | button | onClose() → setAssigning(null) | Modal closes without saving | none | modal is open | modal unmounts | screenshot after close | src/components/AssignmentModal.tsx:92 (Modal onClose prop) |
| 5 | AssignmentModal footer | **儲存 button** | button | save() → setAssignment(item.id, assigned, delegated) → updates progress_items.assigned_to + delegated_to; onClose() | Spinner during submit; on success modal closes; item's assigned_to / delegated_to arrays updated in Supabase; assignee pills appear in card expanded detail; canUpdateItem now true for assigned workers | mutation | canEdit; disabled while submitting | progress_items.assigned_to / delegated_to updated; assignee pills visible in card expanded detail | screenshot of expanded card showing assignee pills after save | src/components/AssignmentModal.tsx:95-98, 76-84 |
| 6 | CreateItemModal | **名稱 text input** | other | setTitle(e.target.value) | Title field accepts text input; required for submit | none | always in CreateItemModal | input value updates; empty triggers '請輸入名稱' error on submit | screenshot with name filled | src/components/CreateItemModal.tsx:483-489 |
| 7 | CreateItemModal | **計劃開始 date input** | other | setPlannedStart(e.target.value) | Date picker opens (native); chosen date updates plannedStart; planned % preview auto-calculates | none | always in CreateItemModal | date input updates; plannedPreview % shown in auto-calculated field | screenshot with date chosen | src/components/CreateItemModal.tsx:514 |
| 8 | CreateItemModal | **計劃完成 date input** | other | setPlannedEnd(e.target.value) | Date picker opens (native); chosen date updates plannedEnd; planned % preview auto-calculates; validation: must not be before plannedStart | none | always in CreateItemModal | date input updates; planned % preview reflects both dates vs today | screenshot showing computed planned % in readonly preview | src/components/CreateItemModal.tsx:518 |
| 9 | CreateItemModal — checklist mode sub-form | **工序名 textarea (checklist items, one per line)** | other | setCustomFloors(e.target.value) — shared with floor custom mode; parsed as checklist items | Preview chips show parsed 工序 items; these become floor_labels (checklist mode) | none | trackingMode === 'checklist' | preview shows N checklist item chips | screenshot of checklist items in preview | src/components/CreateItemModal.tsx:563-584 |
| 10 | CreateItemModal — floors auto sub-form | **樓層數 number input** | other | setFloorCount(Math.max(1, Number(value))) | Number of floor labels to generate; preview chips update showing e.g. 1F, 2F... up to floorCount | none | trackingMode === 'floors' && floorMode === 'auto' | floor preview shows correct count of labels | screenshot of preview with N floors | src/components/CreateItemModal.tsx:734-739 |
| 11 | CreateItemModal — floors auto sub-form | **起始（負為地庫）number input** | other | setBaseFloor(Number(value)) | Starting floor; negative = basement (B1, B2…); 0 = GF; preview chips update | none | trackingMode === 'floors' && floorMode === 'auto' | preview shows GF/B1/B2 or 1F/2F based on baseFloor | screenshot of basement floor labels in preview | src/components/CreateItemModal.tsx:742-747 |
| 12 | CreateItemModal — floors custom sub-form | **樓層名稱 textarea (comma/newline separated)** | other | setCustomFloors(e.target.value) — split on comma/newline to get floor labels | Preview chips update as user types; labels will become floor_labels on the item | none | trackingMode === 'floors' && floorMode === 'custom' | preview chips reflect parsed labels | screenshot of custom floor labels preview | src/components/CreateItemModal.tsx:753-759 |
| 13 | CreateItemModal — floors mode sub-form | **自動生成 / 自訂 floor mode toggle** | switcher | setFloorMode('auto') or setFloorMode('custom') | Auto mode: shows 樓層數 + 起始 inputs; Custom mode: shows textarea for comma/newline-separated floor labels; preview chips update | none | trackingMode === 'floors' | selected button has purple border; correct sub-inputs visible | screenshot of auto and custom mode sub-forms | src/components/CreateItemModal.tsx:718-730 |
| 14 | CreateItemModal — peer parent selector | **Peer parent zone checkboxes** | other | togglePeerParent(p.id) — toggles peer parent in selectedParentIds | Source zone always pre-selected (cannot deselect own); other peer zones toggled; items will be added under all selected parents | none | isFirstLevelChildAdd && peerParents.length > 0 | peer checkboxes reflect selectedParentIds | screenshot of peer selector with some peers ticked | src/components/CreateItemModal.tsx:449-479 |
| 15 | CreateItemModal — peer parent selector (first-level child add) | **全選 / 全部取消 for peer parents** | link | togglePeerSelectAll() — selects all peer parent items across zones | All peer parent checkboxes selected/cleared; code map fetched for each | none | isFirstLevelChildAdd && peerParents.length > 0 | all peer parent checkboxes selected | screenshot of peer parent multi-zone selector | src/components/CreateItemModal.tsx:433-440 |
| 16 | CreateItemModal — quantity mode sub-form | **總數量 number input** | other | setQtyTotal(e.target.value) | Numeric input; required for quantity mode; saved as qty_total; validation: must be > 0 | none | trackingMode === 'quantity' | input accepts decimal; empty causes '請輸入有效的總數量' error on submit | screenshot with total quantity filled | src/components/CreateItemModal.tsx:594-601 |
| 17 | CreateItemModal — quantity mode sub-form | **單位 text input (m / m2 / m3 / 個 / 件)** | other | setQtyUnit(e.target.value) | Unit text updates; saved as qty_unit; suggestion chips below provide quick picks | none | trackingMode === 'quantity' | input reflects typed/chip-selected unit | screenshot with unit selected | src/components/CreateItemModal.tsx:603-609 |
| 18 | CreateItemModal — quantity mode sub-form | **Unit suggestion chips (m / m2 / m3 / 個 / 件)** | button | setQtyUnit(u) — quick-sets unit to suggestion | Selected chip highlighted (teal border); unit input updates | none | trackingMode === 'quantity' | active chip has border-teal-500 text-teal-700 font-semibold | screenshot with 'm' chip selected | src/components/CreateItemModal.tsx:613-628 |
| 19 | CreateItemModal — root add only | **範疇 dropdown (未分類 / 大樓 / 外圍)** | menu | setCatDomain(value) — sets category_domain for root 大項 | Dropdown value updates; will be saved as category_domain on the new root item | none | isRootAdd | select element shows chosen option | screenshot of dropdown with selection | src/components/CreateItemModal.tsx:496-499 |
| 20 | CreateItemModal — root add only | **工種 dropdown (未分類 / 土建 / BS)** | menu | setCatStream(value) — sets category_stream for root 大項 | Dropdown value updates; saved as category_stream on new root item | none | isRootAdd | select element shows chosen option | screenshot | src/components/CreateItemModal.tsx:503-507 |
| 21 | CreateItemModal — tracking mode picker | **百分比 / 樓層 / 清單 / 數量 / 單位狀態 mode buttons** | switcher | setTrackingMode(m) — switches tracking mode; shows/hides mode-specific sub-form | Active mode button highlighted (mode-specific colour: orange/purple/teal/rose); sub-form appears for floors/checklist/quantity/unit_status; mode saved on item creation | none | visible modes determined by template.allowedModes per project_type | active button has mode-specific activeClass; sub-form renders correctly for selected mode | screenshot with each mode selected showing sub-form | src/components/CreateItemModal.tsx:538-558 |
| 22 | CreateItemModal — unit_status custom sub-form | **單位名稱 textarea (comma/newline separated)** | other | setCustomFloors(e.target.value) — unit labels parsed and set as floor_labels | Preview chips show parsed unit labels; saved as floor_labels with 'pending' initial state on creation | none | trackingMode === 'unit_status' && unitMode === 'custom' | preview shows parsed unit label chips | screenshot of custom unit labels | src/components/CreateItemModal.tsx:682-690 |
| 23 | CreateItemModal — unit_status grid sub-form | **樓層數 number input** | other | setUnitFloorCount(Math.max(1, Number(value))) | Number of floors for unit grid; preview updates showing F-A, F-B... labels | none | trackingMode === 'unit_status' && unitMode === 'grid' | preview shows correct floor × room combination labels | screenshot of unit grid labels | src/components/CreateItemModal.tsx:657-662 |
| 24 | CreateItemModal — unit_status grid sub-form | **起始（負為地庫）number input** | other | setUnitBaseFloor(Number(value)) | Base floor for unit grid (supports basements); preview updates | none | trackingMode === 'unit_status' && unitMode === 'grid' | preview labels start from correct floor | screenshot | src/components/CreateItemModal.tsx:664-669 |
| 25 | CreateItemModal — unit_status grid sub-form | **每層室數 number input (1-26)** | other | setUnitCount(Math.max(1, Math.min(26, Number(value)))) | Rooms per floor (A-Z, max 26); preview shows correct room labels per floor | none | trackingMode === 'unit_status' && unitMode === 'grid' | preview shows floor×room combinations up to unitCount rooms | screenshot of preview with 8 rooms per floor | src/components/CreateItemModal.tsx:672-677 |
| 26 | CreateItemModal — unit_status mode sub-form | **樓×室 自動 / 自訂 toggle** | switcher | setUnitMode('grid') or setUnitMode('custom') | Grid mode: shows 樓層數 / 起始 / 每層室數 inputs; Custom mode: shows textarea for comma/newline unit labels; preview updates | none | trackingMode === 'unit_status' | selected button has rose border; correct sub-inputs visible | screenshot of grid vs custom sub-form | src/components/CreateItemModal.tsx:638-651 |
| 27 | CreateItemModal — zone multi-select (root add) | **全選 / 全部取消 toggle link** | link | toggleSelectAll() — selects all zones or clears all; selectedZoneIds set | All zone checkboxes checked/unchecked; auto-code previews update for all selected zones | none | isRootAdd && allZones.length > 0 | all checkboxes in zone picker checked/unchecked | screenshot with all zones selected | src/components/CreateItemModal.tsx:396-402 |
| 28 | CreateItemModal — zone multi-select (root add) | **Zone checkbox labels (per zone)** | other | toggleZone(z.id) — toggles zone in selectedZoneIds | Checkbox toggles; selected zones highlighted (white border); code preview updates; item will be inserted into all selected zones on submit | none | isRootAdd | checked zones have border-safety-300 style; codeMap shows code for each | screenshot with subset of zones selected | src/components/CreateItemModal.tsx:404-429 |
| 29 | CreateItemModal (Modal base) | **關閉 modal (X or backdrop)** | button | onClose() → setCreateCtx(null) | Modal closes without saving | none | modal is open | modal unmounts | screenshot after close | src/components/CreateItemModal.tsx:369 (Modal onClose prop) |
| 30 | CreateItemModal footer | **建立 button** | button | onSubmit(e) → validates → Promise.all(addItem per insertTarget) → inserts into progress_items for all selected zones/parents; closes on success (or shows success message for multi-zone) | Spinner during submit; on success: modal closes (single target) or shows '已新增到 N 個位置' success message then closes; new items appear in zone sections; rollups update | mutation | canEdit; disabled while submitting or while code is loading | New progress_items rows in Supabase; items appear in zone section list; zone rollup shows updated leaf count | screenshot of zone section with newly added item visible | src/components/CreateItemModal.tsx:374-376, 295-366 |
| 31 | EditItemModal | **名稱 text input** | other | setTitle(e.target.value) | Name field editable; required; on save updates item title without losing history/children | none | canEdit (from ProjectDetail's onEdit callback); EditItemModal is open | input shows pre-filled current title; can be edited | screenshot of EditItemModal with title | src/components/EditItemModal.tsx:77 |
| 32 | EditItemModal | **計劃開始 date input** | other | setPlannedStart(e.target.value) | Date input editable; auto-planned % preview updates when both dates set | none | EditItemModal open | input pre-filled with current planned_start; preview % updates | screenshot with new start date | src/components/EditItemModal.tsx:83 |
| 33 | EditItemModal | **計劃完成 date input** | other | setPlannedEnd(e.target.value) | Date input editable; validation: must not be before start; planned % preview updates | none | EditItemModal open | input shows current planned_end; preview % updates | screenshot with new end date | src/components/EditItemModal.tsx:87 |
| 34 | EditItemModal — root 大項 only | **範疇 dropdown (未分類 / 大樓 / 外圍)** | menu | setCatDomain(value) | Dropdown value updates; will save category_domain on root 大項 | none | isRoot (item.parent_id === null) | select shows current domain; updated on save | screenshot with domain changed | src/components/EditItemModal.tsx:93-96 |
| 35 | EditItemModal — root 大項 only | **工種 dropdown (未分類 / 土建 / BS)** | menu | setCatStream(value) | Dropdown value updates; will save category_stream on root 大項 | none | isRoot | select shows current stream; updated on save | screenshot with stream changed | src/components/EditItemModal.tsx:100-103 |
| 36 | EditItemModal (Modal base) | **關閉 modal (X or backdrop)** | button | onClose() → setEditing(null) | Modal closes without saving | none | modal is open | modal unmounts | screenshot after close | src/components/EditItemModal.tsx:60 (Modal onClose prop) |
| 37 | EditItemModal footer | **儲存 button** | button | save() → validates title + date order → updateItemMeta(item.id, {title, planned_start, planned_end, category_domain?, category_stream?}) → onClose() | Spinner during submit; on success: modal closes; progress_items.title + planned dates (and category for roots) updated; history row inserted with change_type='meta'; card title and planned % update; rollup recalculates | mutation | canEdit; disabled while submitting | progress_items row updated in Supabase; HistoryModal shows '✎ 編輯' entry with diff (名稱 / 計劃開始 / 計劃完成) | screenshot of updated card title + HistoryModal showing edit | src/components/EditItemModal.tsx:66-68, 42-58 |
| 38 | ExportProgressModal | **業主版（一頁紙）preset button** | button | applyPreset('owner') → setOpts(exportPreset('owner', project)); setActivePreset('owner') | Button highlighted with orange border/bg ring; opts set to: depth=2, statuses=all except not-started, onlyBehind=false, groupByZone=true, audience='owner'; live scope counter updates showing '業主一頁紙' | none | ExportProgressModal open | button has border-safety-500 bg-safety-50 ring-1 ring-safety-300; scope counter shows '業主一頁紙' label | screenshot with 業主版 preset active | src/components/ExportProgressModal.tsx:97 |
| 39 | ExportProgressModal | **內部版（詳細）preset button** | button | applyPreset('internal') → setOpts(exportPreset('internal', project)); setActivePreset('internal') | Button highlighted; opts: depth=3, statuses=all except not-started, onlyBehind=false, audience='internal'; scope counter shows '內部版（詳細）' | none | ExportProgressModal open | button highlighted; scope counter label shows '內部版（詳細）' | screenshot with 內部版 preset active | src/components/ExportProgressModal.tsx:98 |
| 40 | ExportProgressModal | **例外版 preset button** | button | applyPreset('exception') → setOpts(exportPreset('exception', project)); setActivePreset('exception') | Button highlighted; opts: depth=2, statuses=['delayed','blocked'], onlyBehind=true, audience='internal'; scope counter shows only delayed/blocked items | none | ExportProgressModal open | button active; scope counter shows few or 0 items if no delays | screenshot with 例外版 preset active and scope counter | src/components/ExportProgressModal.tsx:100 |
| 41 | ExportProgressModal | **進階設定 toggle disclosure (ChevronDown)** | toggle | setAdvanced(a => !a) — toggles advanced settings panel | Chevron rotates 180°; panel with 分區/深度/狀態/期數 controls appears/disappears | none | ExportProgressModal open | advanced panel div visible/hidden; chevron rotation matches advanced state | screenshot with advanced panel open | src/components/ExportProgressModal.tsx:132-136 |
| 42 | ExportProgressModal — advanced — 分區 | **全選 / 全部取消 zone link** | link | set({ zoneIds: opts.zoneIds.length === allZoneIds.length ? [] : allZoneIds }); setActivePreset('custom') | All zone chips toggled on/off; active preset changes to '自訂' | none | advanced panel open | all zone chips show active/inactive state; scope counter updates | screenshot showing all zones selected or all deselected | src/components/ExportProgressModal.tsx:144-146 |
| 43 | ExportProgressModal — advanced — 分區 | **Zone chips (per zone + 未分區/共用)** | button | toggleZone(z.id) or set({ includeUnzoned: !opts.includeUnzoned }) → setActivePreset('custom') | Chip toggles active state (safety-500 filled vs site-100); scope counter updates; preset changes to 自訂 | none | advanced panel open | chip style reflects inclusion state; scope counter reflects zone filter | screenshot with subset of zones selected | src/components/ExportProgressModal.tsx:148-154 |
| 44 | ExportProgressModal — advanced — 狀態 | **只延誤+阻塞 quick link** | link | set({ statuses: ['delayed', 'blocked'], onlyBehind: false }); setActivePreset('custom') | Status chips: only 延誤 + 阻塞 active; scope counter drops to show only those items | none | advanced panel open | only 延誤/阻塞 chips highlighted; scope counter shows reduced item count | screenshot showing exception-filtered scope | src/components/ExportProgressModal.tsx:172 |
| 45 | ExportProgressModal — advanced — 狀態 | **剔走未開始 quick link** | link | set({ statuses: ALL_STATUSES.filter(s => s !== 'not-started') }); setActivePreset('custom') | Status chips: all except 未開始 active; scope counter excludes not-started items | none | advanced panel open | 未開始 chip is inactive; others active; scope counter updates | screenshot | src/components/ExportProgressModal.tsx:173 |
| 46 | ExportProgressModal — advanced — 狀態 | **全部 quick link** | link | set({ statuses: [...ALL_STATUSES] }); setActivePreset('custom') | All 5 status chips active; scope counter includes all statuses | none | advanced panel open | all status chips have safety-500 bg; scope counter shows all items | screenshot with all statuses selected | src/components/ExportProgressModal.tsx:174 |
| 47 | ExportProgressModal — advanced — 狀態 | **Status chips (已完成 / 進行中 / 延誤 / 阻塞 / 未開始)** | button | toggleStatus(s) → adds/removes status from opts.statuses; setActivePreset('custom') | Chip toggles active/inactive; scope counter live-updates showing qualifying items only | none | advanced panel open | active chips filled safety-500; scope counter matches | screenshot with selective status chips | src/components/ExportProgressModal.tsx:176-180 |
| 48 | ExportProgressModal — advanced — 期數 | **報告期數 free text input** | other | set({ reportPeriod: e.target.value }); setActivePreset('custom') | Text input updates; value used as the period label in file name and PDF footer; also used as the prev-snapshot lookup key for 本期Δ calculation | none | advanced panel open | input value updates; scope counter shows period label in description | screenshot with '2026-W23' entered | src/components/ExportProgressModal.tsx:187-189 |
| 49 | ExportProgressModal — advanced — 層級深度 | **只大項 / 到中項 / 到細項 segmented buttons** | switcher | set({ depth: d }) → ReportDepth 1／2／3; setActivePreset('custom') | Active depth button highlighted (safety-500 bg); number of rows in export changes; scope counter updates | none | advanced panel open | active button has bg-safety-500 text-white; scope reflects depth | screenshot with depth=1 selected | src/components/ExportProgressModal.tsx:161-165 |
| 50 | ExportProgressModal (when activePreset === 'custom') | **還原範本 button (RotateCcw icon)** | button | applyPreset(initial) — restores the last saved preset from localStorage | Active preset reverts to the last remembered preset; opts reset to that preset's values; 自訂 state clears | none | activePreset === 'custom' | preset state reverts; scope counter updates | screenshot with custom→restore transition | src/components/ExportProgressModal.tsx:102-104 |
| 51 | ExportProgressModal backdrop | **Backdrop click (outside modal)** | button | onClose() via outer div onClick handler | Modal closes; stopPropagation on inner div prevents close when tapping inside modal content | none | modal is open | clicking outside white card closes modal; clicking inside does not | screenshot demonstrating backdrop dismiss | src/components/ExportProgressModal.tsx:86-87 |
| 52 | ExportProgressModal footer | **匯出 Excel button (FileSpreadsheet icon)** | button | run('xlsx') → saves preset to localStorage → import('../lib/export') → exportProgressToExcel(project, items, opts) → shareOrDownloadBlob → captureSnapshot; onClose() | Spinner shown; XLSX file generated and downloaded/shared; on native: share sheet opens; on web-mobile: Web Share API; on web-desktop: file download; modal closes on success; alert on failure | xlsx | ExportProgressModal open; disabled while busy | File downloads; open in Excel/Numbers: '進度報告' sheet present; columns match spec; Chinese text readable; numeric % cells summable; zone grouping present when groupByZone=true | screenshot of downloaded XLSX opened in Excel showing Chinese headers and data rows | src/components/ExportProgressModal.tsx:196-199; src/lib/export.ts:362-456 |
| 53 | ExportProgressModal footer | **匯出 PDF button (FileText icon)** | button | run('pdf') → saves preset to localStorage → exportProgressToPDF(project, items, opts) → html2canvas render → jsPDF multi-page → shareOrDownloadBlob → captureSnapshot; onClose() | Spinner shown during render (multi-second); PDF file generated. Owner preset: 1 page with verdict banner + KPI cards + zone bars + attention list. Internal preset: owner one-pager + per-zone appendix table. Exception preset: internal path but only delayed/blocked items. Delivered via share sheet (native) or Web Share/download (web). Modal closes on success; alert on failure | pdf | ExportProgressModal open; disabled while busy | PDF file opens; page 1 shows verdict banner with Chinese project name; zone bars with dual markers; attention table items in Chinese; internal preset: appendix table pages with 編號/名稱/實際/計劃/差距/狀態 columns; Chinese text readable (not garbled); footer shows 'CK 工程' in CJK font; page numbers correct | preview_screenshot of the rendered PDF page 1 (verdict + zone bars) and page 2+ (appendix table); check footer for CJK rendering | src/components/ExportProgressModal.tsx:200-203; src/lib/export.ts:559-652, 468-557 |
| 54 | ExportProgressModal header | **關閉 X button** | button | onClose() → setShowExport(false) | Modal closes; no export triggered | none | modal is open | ExportProgressModal unmounts from DOM | screenshot after close | src/components/ExportProgressModal.tsx:91 |
| 55 | HistoryModal (Modal base) | **關閉 modal (X or backdrop)** | button | onClose() → setHistoryItem(null) | Modal closes | none | modal is open | modal unmounts | screenshot after close | src/components/HistoryModal.tsx:76 (Modal onClose prop) |
| 56 | ProgressItemCard — compact row | **展開/收起（ChevronRight/ChevronDown icon）** | icon-button | onToggle(item.id) — toggles item id in the expanded Set | Parent items: child ProgressItemCards appear/disappear below. Leaf items: expanded detail section (notes, assignees, drawings, materials) appears/disappears | none | always visible on every row | chevron direction changes; child cards or detail section toggles | screenshot of expanded parent showing children, and expanded leaf showing detail | src/components/ProgressItemCard.tsx:198-204 |
| 57 | ProgressItemCard — compact row | **Row title/progress area (tap to toggle expand)** | button | onToggle(item.id) — same as chevron, tap any part of the title/progress area | Same expand/collapse as chevron button | none | always | children appear / detail section opens on tap | screenshot of expanded item | src/components/ProgressItemCard.tsx:207-254 |
| 58 | ProgressItemCard — compact row | **更新（Edit3 icon, orange button）** | icon-button | onUpdate(item) → setUpdating(item) → opens UpdateProgressModal | UpdateProgressModal opens with item's current progress pre-populated; mode-appropriate editor (slider/floors-grid/checklist/quantity/unit_status) shown | modal | canUpdateThis: canEdit OR (item in item.assigned_to OR item.delegated_to for canUpdateItem); isLeaf only | UpdateProgressModal rendered with item.code + item.title in header; correct tracking mode editor shown | screenshot of UpdateProgressModal | src/components/ProgressItemCard.tsx:263-271 |
| 59 | ProgressItemCard — kebab delete confirmation | **刪（confirm delete）** | button | onDelete(item) → requireStepUp('progress_delete') → deleteItem(item.id); closes kebab | Step-up auth prompt appears; on success, item and all descendants deleted from progress_items; row disappears from list; rollups recalculate | mutation | canEdit; requires step-up authentication (progress_delete action) | item row removed from DOM; zone rollup % updates; step-up modal shown before deletion | screenshot after deletion showing item gone + updated rollup | src/pages/ProjectDetail.tsx:464-469; src/components/ProgressItemCard.tsx:306-308 |
| 60 | ProgressItemCard — kebab delete confirmation | **取消（cancel delete）** | button | setConfirmDel(false) — returns to normal kebab menu state | Confirmation UI disappears; normal kebab menu items reappear | none | canEdit (only shown while confirmDel === true) | confirmDel back to false; 刪除 menu item visible again | screenshot of reverted kebab menu | src/components/ProgressItemCard.tsx:309 |
| 61 | ProgressItemCard — kebab menu | **更多（MoreVertical / kebab icon）** | icon-button | setMenuOpen(true) | Dropdown menu appears with context-sensitive options (文件/圖則, 指派, 歷史, 加細項, 編輯, 刪除) | modal | hasMenuActions: canEdit OR (isLeaf && fileCtxPresent) | dropdown div appears; menu items visible based on canEdit and fileCtxPresent | screenshot with kebab menu open | src/components/ProgressItemCard.tsx:274-315 |
| 62 | ProgressItemCard — kebab menu | **指派** | menu | onAssign(item) → setAssigning(item) → opens AssignmentModal; closes kebab | AssignmentModal opens with '負責人' and '委派判頭/工人' tabs; approved project member list shown | modal | canEdit AND isLeaf | AssignmentModal visible; item code+title in header; member checkboxes shown | screenshot of AssignmentModal | src/components/ProgressItemCard.tsx:288-290 |
| 63 | ProgressItemCard — kebab menu | **歷史** | menu | onHistory(item) → setHistoryItem(item) → opens HistoryModal; closes kebab | HistoryModal opens; progress_history rows for this item loaded from Supabase; each row shows actor avatar, timestamp, %, notes, floor chips or unit-state diff chips | modal | canEdit AND isLeaf | HistoryModal renders; history entries shown newest-first; actor names resolved | screenshot of HistoryModal with entries | src/components/ProgressItemCard.tsx:291-293 |
| 64 | ProgressItemCard — kebab menu | **加細項** | menu | onAddChild(item) → setCreateCtx({ parent: item, zone: zoneOf(item, zone) }) → opens CreateItemModal; closes kebab | CreateItemModal opens; parent info shown; level label is '中項' (level 2) or '第 N 層細項'; peer-parent multi-zone picker appears if sibling same-title items exist in other zones | modal | canEdit (any level) | CreateItemModal title shows correct levelLabel; parent code shown in info block | screenshot of CreateItemModal for child add | src/components/ProgressItemCard.tsx:294-296 |
| 65 | ProgressItemCard — kebab menu | **編輯（名稱／日期）** | menu | onEdit(item) → setEditing(item) → opens EditItemModal; closes kebab | EditItemModal opens with current title, planned dates, and (for root items) category domain/stream pre-filled | modal | canEdit (any level) | EditItemModal visible; input fields show item's current values | screenshot of EditItemModal | src/components/ProgressItemCard.tsx:297-299 |
| 66 | ProgressItemCard — kebab menu | **刪除（red danger)** | menu | setConfirmDel(true) — first tap shows inline confirmation | Kebab menu changes to show '確認刪除?' with '刪' and '取消' buttons (red confirmation UI inline) | none | canEdit | confirmDel state true; confirmation row visible with '確認刪除?' text | screenshot of kebab with confirmation state | src/components/ProgressItemCard.tsx:300-302 |
| 67 | ProjectDetail 頁頭 | **匯出 menu（Download icon）** | icon-button | setOpen(true) — 顯示匯出下拉選單 | 下拉選單出現（依目前 tab 顯示：進度tab=「匯出進度報告…」，問題tab=「匯出 Excel」，文書/工具tab=「此頁面冇匯出選項」） | modal | 所有有工地存取權的用戶 | 確認下拉選單出現，選項根據 tab 正確 | screenshot of export dropdown open | src/pages/ProjectDetail.tsx:1062-1086 |
| 68 | ProjectDetail 頁頭 | **刷新（RefreshCw icon）** | icon-button | manualRefresh() → refetch() from ProgressContext | icon 旋轉，進度樹重新載入 | mutation | 所有有工地存取權的用戶 | 確認列表重新載入 | screenshot after refresh | src/pages/ProjectDetail.tsx:363-365 |
| 69 | ProjectDetail 頁頭 匯出選單（進度 tab） | **匯出進度報告…** | menu | setShowExport(true) → 開啟 ExportProgressModal | ExportProgressModal 彈出（含受眾、深度、狀態篩選、報告期數等選項） | modal | tab = 'progress' | 確認 modal 出現，預設選項正確 | screenshot of ExportProgressModal | src/pages/ProjectDetail.tsx:1073-1075 |
| 70 | ProjectDetail 進度 tab | **返回（ChevronLeft）** | icon-button | navigate('/home') | 返回首頁 | navigation | 所有登入用戶 | 確認 URL 為 #/home | screenshot of home page | src/pages/ProjectDetail.tsx:321-323 |
| 71 | ProjectDetail 進度 tab | **進度 tab（ListChecks icon）** | tab | setTab('progress') | 切換到進度表視圖，tab 高亮，底部橙色指示線 | none | 所有用戶（core module，不可禁用） | 確認 tab active 樣式，進度樹顯示 | screenshot of progress tab active | src/pages/ProjectDetail.tsx:372 |
| 72 | ProjectDetail 進度 tab | **類別篩選 chips（全部範疇/大樓/外圍）** | switcher | setCatDomain(k) — 過濾進度樹到選定範疇 | 進度樹只顯示屬於該 category_domain 的 root items | none | items.some(i => i.category_domain ／／ i.category_stream)（有分類的項目才顯示 chips） | 選「大樓」後確認只顯示 category_domain='building' 的大項 | screenshot of filtered progress tree | src/pages/ProjectDetail.tsx:415-417 |
| 73 | ProjectDetail 進度 tab | **領域篩選 chips（所有領域/土建/BS）** | switcher | setCatStream(k) — 過濾進度樹到選定領域 | 進度樹只顯示屬於該 category_stream 的 root items | none | 同上 | 選「BS」後確認只顯示 category_stream='bs' 的大項 | screenshot of filtered by stream | src/pages/ProjectDetail.tsx:418-421 |
| 74 | ProjectDetail 進度 tab — ZoneSection | **加入大項（+ Plus icon）** | button | setCreateCtx({ parent: null, zone }) → 開啟 CreateItemModal | CreateItemModal 彈出，允許新增 root level 進度大項 | modal | canEdit = true（admin / assigned PM / approved pm·main_contractor·subcontractor） | 確認 modal 出現，zone 欄位預填 | screenshot of CreateItemModal | src/pages/ProjectDetail.tsx:459, src/pages/ProjectDetail.tsx:694-700 |
| 75 | ProjectDetail 進度tab — category filter bar | **全部範疇 / 大樓 / 外圍 filter chips** | button | setCatDomain(k) — filters visibleItems and visibleZones to items whose root has matching category_domain | Pill becomes active (safety-500 orange background); zone list and stat tiles re-render showing only items in that domain; empty zones disappear | none | only rendered when items.some(i => i.category_domain ／／ i.category_stream) | active chip has bg-safety-500 text-white; zone sections update to show filtered items only | screenshot of filtered progress list | src/pages/ProjectDetail.tsx:415-417 |
| 76 | ProjectDetail 進度tab — category filter bar | **所有領域 / 土建 / BS filter chips** | button | setCatStream(k) — filters visibleItems and visibleZones to items whose root has matching category_stream | Chip becomes active (site-700 dark background); list re-renders to show only items in that stream; orthogonally composable with domain filter | none | only rendered when items.some(i => i.category_domain ／／ i.category_stream) | active chip has bg-site-700 text-white; zone sections show filtered items | screenshot of stream-filtered list | src/pages/ProjectDetail.tsx:419-421 |
| 77 | ProjectDetail header | **返回（ChevronLeft）** | icon-button | navigate('/home') | Navigate to /home (工地列表) | navigation | always visible | URL changes to /#/home; Home page renders | screenshot of Home page after tap | src/pages/ProjectDetail.tsx:321-323 |
| 78 | ProjectDetail header | **匯出（Download icon）— ExportMenu toggle** | icon-button | setOpen(true) — opens an inline dropdown with context-sensitive export options | Dropdown menu appears with options that vary by active tab (進度 tab shows '匯出進度報告…'; 問題 tab shows '匯出 Excel'; si-vo/tools tabs show '此頁面冇匯出選項') | modal | always visible | dropdown div appears at right-0 top-full position; label matches active tab | screenshot with dropdown open | src/pages/ProjectDetail.tsx:1052-1087 |
| 79 | ProjectDetail header | **刷新（RefreshCw icon）** | icon-button | manualRefresh() → setRefreshing(true) → refetch() → setRefreshing(false) | Spinner animation on the icon while fetching; progress items list re-renders with fresh data from Supabase | mutation | always visible; disabled while refreshing | icon shows animate-spin class during fetch; list updates after fetch completes | screenshot showing spinner during fetch | src/pages/ProjectDetail.tsx:363-365, 166-170 |
| 80 | ProjectDetail header ExportMenu (進度 tab active) | **匯出進度報告…** | menu | onExportProgress() → setShowExport(true); closes dropdown | ExportProgressModal opens as a bottom sheet | modal | only visible when tab === 'progress' | ExportProgressModal element appears in DOM; preset buttons visible | screenshot of ExportProgressModal open | src/pages/ProjectDetail.tsx:1073-1075 |
| 81 | ProjectDetail tab bar | **進度 tab** | tab | setTab('progress') | Progress tab becomes active (orange underline border); progress zone list and stat tiles render | navigation | always visible (core module, cannot be disabled) | tab has border-safety-500 class; progress content section is visible | screenshot of active progress tab | src/pages/ProjectDetail.tsx:372 |
| 82 | UpdateProgressModal | **標記為受阻 toggle** | toggle | setBlockedOn(o => !o) — toggles blocked state; when turned on, reveals reason picker and free text input | Toggle switches to amber; card turns amber bg; reason chip row and free-text input appear; when turned off, reason cleared | none | always visible in UpdateProgressModal | toggle thumb at translate-x-5 position; amber border/bg on card; reason inputs appear | screenshot with blocked toggle on and reason picker visible | src/components/UpdateProgressModal.tsx:406-420 |
| 83 | UpdateProgressModal | **備注 / 說明 textarea** | other | setNotes(e.target.value) | Notes text field accepts multi-line input; submitted with progress update to progress_history | none | always visible in UpdateProgressModal | textarea reflects input; submitted notes appear in HistoryModal | screenshot with notes filled | src/components/UpdateProgressModal.tsx:448-455 |
| 84 | UpdateProgressModal — 受阻 section | **受阻原因 chips (雨天 / 地下水 / 掘路紙 / 物料 / 其他)** | button | setBlockReason(r) — selects a preset block reason | Selected chip gets amber border + text colour; free text input below also updates to the selected value | none | blocked === true (toggle is ON) | selected chip has border-amber-500 bg-white text-amber-700 font-semibold | screenshot showing selected block reason | src/components/UpdateProgressModal.tsx:422-433 |
| 85 | UpdateProgressModal — 受阻 section | **受阻原因 free-text input** | other | setBlockReason(e.target.value) — free text override for block reason | Text input accepts custom reason; submitted as blocked_reason to setBlocked() on save | none | blocked === true | input value reflects typed text | screenshot with custom block reason typed | src/components/UpdateProgressModal.tsx:438-444 |
| 86 | UpdateProgressModal — checklist mode | **Checklist item rows (tap to toggle done)** | button | toggleFloor(label) — adds/removes checklist item label from floorsCompleted | Row turns green with checkbox filled (Check icon); done count / total updates; computed % via floorsToProgress updates; ProgressBar preview updates | none | tracking_mode === 'checklist' | row bg-green-50 border-green-500; checkbox filled; % counter updated | screenshot of checklist with some items ticked | src/components/UpdateProgressModal.tsx:205-230 |
| 87 | UpdateProgressModal — floors mode | **Floor label chips (tap to toggle completed)** | button | toggleFloor(label) — adds/removes floor label from floorsCompleted array | Tapped floor chip turns green (bg-green-500 text-white with check icon); completed count / total updates; computed % recalculates via floorsToProgress; ProgressBar preview updates | none | tracking_mode === 'floors' | floor chip colour changes; completion count updates; ProgressBar reflects new % | screenshot showing some floors completed (green) and some not | src/components/UpdateProgressModal.tsx:243-259 |
| 88 | UpdateProgressModal — percentage mode | **百分比 range slider** | other | setActual(Number(e.target.value)) — 0-100 in steps of 1 | Large % number updates live; ProgressBar preview re-renders; status chip updates (completed/in-progress/delayed/not-started) | none | tracking_mode === 'percentage' | actual state updates; % display and ProgressBar reflect new value | screenshot showing slider at 75% with updated preview | src/components/UpdateProgressModal.tsx:360-369 |
| 89 | UpdateProgressModal — percentage mode | **25% / 50% / 75% / 100% quick-tap chips** | button | setActual(v) — sets actual to 25, 50, 75, or 100 | Selected chip highlighted (safety-600 background); slider jumps to value; % display and ProgressBar preview update; 100% chip → status becomes 已完成 (green) | none | tracking_mode === 'percentage' | active chip has bg-safety-600 text-white; actual state equals chip value | screenshot with 100% chip selected showing green 已完成 status | src/components/UpdateProgressModal.tsx:373-387 |
| 90 | UpdateProgressModal — quantity mode | **減 10 stepper button (Minus icon)** | button | stepQty(-10) — decrements qtyDone by 10, clamped at 0 | qty_done number decreases by 10 (minimum 0); % display and ProgressBar preview update via qtyToProgress | none | tracking_mode === 'quantity' | qtyDone state decreases; computed % updates | screenshot showing updated qty and % | src/components/UpdateProgressModal.tsx:277-283 |
| 91 | UpdateProgressModal — quantity mode | **數量 numeric input field** | other | setQtyDone(e.target.value) — free numeric entry | Input value changes; computed % updates live via qtyToProgress(qtyDone, qty_total) | none | tracking_mode === 'quantity' | input reflects typed value; % preview recalculates | screenshot of filled quantity input | src/components/UpdateProgressModal.tsx:285-296 |
| 92 | UpdateProgressModal — quantity mode | **加 10 stepper button (Plus icon)** | button | stepQty(10) — increments qtyDone by 10, clamped at qty_total | qty_done increases by 10 (capped at qty_total); % display and ProgressBar update | none | tracking_mode === 'quantity' | qtyDone increases; cannot exceed qty_total | screenshot showing incremented qty | src/components/UpdateProgressModal.tsx:297-303 |
| 93 | UpdateProgressModal — unit_status mode | **Unit label chip (per-室 tap to cycle state)** | button | cycleUnit(label) → setLabelStatus advancing that label through UNIT_STATE_ORDER: pending→fixing→fixed→reinspect→signed_off→pending | Chip colour changes to reflect new state (blue=fixing, amber=fixed, purple=reinspect, green=signed_off); 已簽收 and 已修復 counters update; computed % (unitStatusToProgress) updates | none | tracking_mode === 'unit_status' | chip style matches new UnitState; signedOff/fixed counts update; ProgressBar % changes | screenshot showing mixed unit states | src/components/UpdateProgressModal.tsx:329-348 |
| 94 | UpdateProgressModal (Modal base component) | **關閉 modal (X or backdrop tap)** | button | onClose() → setUpdating(null) | Modal closes; no data saved | none | modal is open | modal unmounts from DOM | screenshot showing modal gone | src/components/UpdateProgressModal.tsx:166 (Modal onClose prop) |
| 95 | UpdateProgressModal footer | **儲存更新 button (Send icon)** | button | save() → validates → calls appropriate updateProgress/updateFloors/updateQuantity/updateUnitStatus → optionally setBlocked → onClose() | Spinner during submit; on success: modal closes; progress_items row updated in Supabase; progress_history row inserted; ProgressItemCard shows new %; zone rollup recalculates; realtime subscription fires | mutation | canUpdateThis (same gate as 更新 button); disabled while submitting | Supabase progress_items.actual_progress updated; progress_history has new row; card % changes | screenshot of card after save showing updated % | src/components/UpdateProgressModal.tsx:123-161, 169-171 |
| 96 | ZoneSection — autoZone type (小型工程) | **+ 加入大項 (standalone, no zone header)** | button | onAddRoot() → setCreateCtx({ parent: null, zone }) → opens CreateItemModal | CreateItemModal opens for adding a root item without zone chrome | modal | canEdit AND hideZoneChrome (autoZone project type with ≤1 zone) | CreateItemModal opens; zone info suppressed | screenshot of CreateItemModal | src/pages/ProjectDetail.tsx:692-699 |
| 97 | ZoneSection — zone header card | **+ 加入大項 (within zone header card)** | button | onAddRoot() → setCreateCtx({ parent: null, zone }) → opens CreateItemModal | CreateItemModal opens; title shows '加入 大項'; zone context pre-set; auto-code fetched via next_progress_code RPC | modal | canEdit (admin OR assigned PM OR approved pm/main_contractor/subcontractor membership) | CreateItemModal visible; parent info block shows zone name; code preview shows auto-code | screenshot of CreateItemModal open for root item | src/pages/ProjectDetail.tsx:731-738 |

### 即時問題 (11)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 98 | CreateQuickSnagSheet | **類型 chip（每個 SnagType，如漏水/裂縫/損壞等）** | button | pickType(t) — setSnagType(t)，若用戶未手動改標題則自動填入中文類型名稱作標題 | 選中 chip 變為 safety-500（橙）背景；標題 input 自動更新為該類型中文名 | none | Modal 開啟中 | chip 樣式變化，標題 input 更新 | screenshot chip 選中狀態 | src/components/CreateQuickSnagSheet.tsx:154-167 |
| 99 | CreateQuickSnagSheet | **標題 input** | other | setTitle(value); setTitleTouched(true) — 用戶自定義標題（不再跟隨 chip 自動填入） | 輸入內容反映，titleTouched=true 後 chip 切換不再覆蓋標題 | none | Modal 開啟中 | input value 正確，切換 chip 後不被覆蓋 | screenshot input 填寫後切換 chip | src/components/CreateQuickSnagSheet.tsx:172-178 |
| 100 | CreateQuickSnagSheet | **位置 / 樓層 input** | other | setLocation(value)（maxLength 60） | 輸入內容即時反映 | none | Modal 開啟中 | input value 正確 | screenshot input 已填寫 | src/components/CreateQuickSnagSheet.tsx:183-189 |
| 101 | CreateQuickSnagSheet | **拍照（Camera icon button，可選）** | camera | cameraInputRef.current?.click() — 啟動裝置相機 | 系統相機開啟；拍照後圖片加入 photos 並上傳；縮圖顯示 | image | photos.length < 6 | 縮圖出現，counter 增加 | screenshot 照片已加入 grid | src/components/CreateQuickSnagSheet.tsx:224-229, 239 |
| 102 | CreateQuickSnagSheet | **從相簿選（ImagePlus icon button，可選）** | filepicker | galleryInputRef.current?.click() — 從相簿選取照片（multiple） | 系統相簿開啟；選取後加入 photos 並上傳 | image | photos.length < 6 | 縮圖出現，counter 更新 | screenshot 照片已加入 | src/components/CreateQuickSnagSheet.tsx:231-236, 240 |
| 103 | CreateQuickSnagSheet | **移除相片（X icon，每張縮圖右上角）** | icon-button | removePhoto(localId) — 從 photos 陣列移除 | 縮圖消失，counter 減少 | none | 有至少一張相片 | grid 縮圖消失 | screenshot 移除後 grid | src/components/CreateQuickSnagSheet.tsx:207-212 |
| 104 | CreateQuickSnagSheet | **備註 textarea（可選）** | other | setDescription(value) — 更新備註狀態 | 輸入內容即時反映 | none | Modal 開啟中 | textarea value 正確 | screenshot textarea 已填寫 | src/components/CreateQuickSnagSheet.tsx:244-252 |
| 105 | CreateQuickSnagSheet | **記錄（Zap icon btn-primary footer button）** | button | onSubmit() — 呼叫 createQuickIssue({title, snag_type, location, description, photos})，INSERT issues（is_quick=true, 不觸發 OneSignal push）+ issue_comments 'reported' | Modal 關閉，跳轉至新建即時問題的 IssueDetail 頁 | mutation | myRoleInProject 不為 null；無上傳中或失敗相片 | IssueDetail 顯示「即時問題」Zap badge；問題列表「即時問題」section 新增卡片 | screenshot 新建即時問題 IssueDetail 頁 | src/components/CreateQuickSnagSheet.tsx:108-133, 142-145 |
| 106 | CreateQuickSnagSheet | **關閉 Modal（Modal X / 背景）** | button | close() — reset() + onClose() | Modal 關閉，表單重置 | none | Modal 開啟中 | Modal 不可見 | screenshot Modal 關閉後 | src/components/CreateQuickSnagSheet.tsx:71-74 |
| 107 | IssueDetail | **升級為正式問題（ArrowUp，藍色按鈕）** | button | handleGraduate() — graduateToFormal(issueId)：UPDATE issues SET is_quick=false（觸發 DB guard 分配 issue_no + push）；INSERT comment '已將即時問題升級為正式問題' | 即時問題badge消失，顯示正式編號（如 #005），IssueDetail 刷新；Push 通知發至 handler | mutation | issue.is_quick=true AND isOpen | issue_no 出現（非 null），is_quick badge 消失，活動記錄新增一條系統日誌 | screenshot 升級後 IssueDetail header 顯示編號 | src/pages/IssueDetail.tsx:288-296, 165-171 |
| 108 | ProjectDetail 問題tab IssuesTab | **即時問題（btn-ghost，Zap icon）** | button | setCreateSnagOpen(true) — 開啟 CreateQuickSnagSheet | CreateQuickSnagSheet Modal 開啟，標題「即時問題」 | modal | canReport = !!myRoleInProject（用戶必須是工地成員） | Modal 出現，標題「即時問題」，類型 chips 顯示 | screenshot CreateQuickSnagSheet 開啟後 | src/pages/ProjectDetail.tsx:605-607 |

### 問題 (35)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 109 | CreateIssueModal | **問題標題 input** | other | setTitle(value) — 更新標題文字狀態 | 輸入內容即時反映於 input field | none | Modal 開啟中 | input value 與輸入一致 | screenshot input 已填寫 | src/components/CreateIssueModal.tsx:169-176 |
| 110 | CreateIssueModal | **位置 input** | other | setLocation(value) — 更新位置狀態（maxLength 60） | 輸入內容即時反映 | none | Modal 開啟中 | input value 正確 | screenshot input 已填寫 | src/components/CreateIssueModal.tsx:179-186 |
| 111 | CreateIssueModal | **相關工序 select（可選）** | other | setProgressItemId(value) — 選擇關聯的 leaf progress item | 選項更新，空白選項為「— 無 —」 | none | leafItems.length > 0（工地有 leaf 進度項目） | select 顯示所選 item 的 code+title | screenshot select 已選狀態 | src/components/CreateIssueModal.tsx:190-207 |
| 112 | CreateIssueModal | **拍照（Camera icon button）** | camera | cameraInputRef.current?.click() — 觸發 capture='environment' 的 file input，啟動裝置相機 | 系統相機 UI 開啟；拍照後圖片加入 photos 陣列並即時上傳至 issue-photos bucket | image | photos.length < 6 | 相片縮圖出現在 grid，uploading spinner 後消失，photo counter 增加 | screenshot 照片已加入 grid | src/components/CreateIssueModal.tsx:245-249, 261-268 |
| 113 | CreateIssueModal | **從相簿選（ImagePlus icon button）** | filepicker | galleryInputRef.current?.click() — 觸發 multiple file input（無 capture，從相簿選取） | 系統相簿選擇器開啟；選取後最多補滿至 6 張照片，每張壓縮後上傳 | image | photos.length < 6 | 相片縮圖出現，counter 更新 | screenshot 照片已加入 grid | src/components/CreateIssueModal.tsx:252-258, 269-275 |
| 114 | CreateIssueModal | **移除相片（X icon，每張相片右上角）** | icon-button | removePhoto(localId) — 從 photos 陣列移除，revoke object URL | 該縮圖從 grid 消失；counter 減少 | none | 有至少一張相片 | grid 縮圖消失，counter 更新 | screenshot 移除後 grid | src/components/CreateIssueModal.tsx:230-236 |
| 115 | CreateIssueModal | **詳細描述 textarea** | other | setDescription(value) — 更新描述狀態 | 輸入內容即時反映 | none | Modal 開啟中 | textarea value 正確 | screenshot textarea 已填寫 | src/components/CreateIssueModal.tsx:280-290 |
| 116 | CreateIssueModal | **提交（btn-primary footer button）** | button | onSubmit() — 驗證後呼叫 createIssue(title, description, photoUrls, location, progressItemId)，INSERT 到 issues 表 + issue_comments 'reported' 記錄，記錄 photoMeta | Modal 關閉，跳轉至新建問題的 IssueDetail 頁（/project/:id/issue/:newId） | mutation | 已填標題 + 至少一張相片上傳完成；myRoleInProject 不為 null | 跳轉至 IssueDetail，問題卡片出現於問題列表，Supabase issues 表有新行 | screenshot 新建問題的 IssueDetail 頁 | src/components/CreateIssueModal.tsx:113-148, 156-159 |
| 117 | CreateIssueModal | **關閉 Modal（Modal 背景 / X 按鈕）** | button | close() — reset() + onClose()，清除所有表單狀態 | Modal 關閉，表單重置 | none | Modal 開啟中 | Modal 不可見，問題列表恢復 | screenshot Modal 關閉後 | src/components/CreateIssueModal.tsx:63-66 |
| 118 | IssueDetail | **返回工地（ChevronLeft icon-button）** | icon-button | navigate(`/project/${projectId}`) — 返回工地詳情頁問題 tab | 跳轉回 ProjectDetail，問題 tab 保持選中 | navigation | 所有已登入用戶 | URL 變為 #/project/{id}，問題列表可見 | screenshot 返回後 ProjectDetail 問題 tab | src/pages/IssueDetail.tsx:186-192 |
| 119 | IssueDetail | **相關工序 chip（ListChecks icon，只在 linkedItem 存在時顯示）** | button | navigate(`/project/${projectId}`) — 跳轉回工地進度 tab | 跳轉至 ProjectDetail 進度 tab（不直接 highlight 該進度項目） | navigation | issue.progress_item_id 不為 null，且 Supabase 能解析到 code+title | chip 顯示「相關工序：{code} {title}」；點後跳轉到進度 tab | screenshot chip 及跳轉後頁面 | src/pages/IssueDetail.tsx:253-259 |
| 120 | IssueDetail | **標記為已解決（CheckCircle2，綠色按鈕）** | button | setDialog({type:'resolve'}); setDialogText('') — 開啟解決確認 Modal | Modal 出現，標題「標記為已解決」，含補充說明 textarea | modal | canAct（handler role 匹配 myRole，或 admin/safety_officer/general_foreman）AND isOpen | Modal 出現，標題正確 | screenshot 解決確認 Modal | src/pages/IssueDetail.tsx:273-279 |
| 121 | IssueDetail | **上呈到 X（ArrowUp，橙色按鈕）** | button | setDialog({type:'escalate'}); setDialogText('') — 開啟上呈確認 Modal | Modal 出現，標題「上呈到 {nextRole 中文}」 | modal | canAct AND isOpen AND nextRole 不為 null AND !issue.is_quick（即時問題不可上呈） | Modal 出現，標題含正確的下一層角色中文名 | screenshot 上呈確認 Modal | src/pages/IssueDetail.tsx:280-287 |
| 122 | IssueDetail | **重新開啟（RotateCcw，琥珀色按鈕）** | button | setDialog({type:'reopen'}); setDialogText('') — 開啟重開確認 Modal | Modal 出現，標題「重新開啟此問題」 | modal | !isOpen AND (isReporter OR canAct) | Modal 出現，標題「重新開啟此問題」 | screenshot 重開確認 Modal | src/pages/IssueDetail.tsx:297-304 |
| 123 | IssueDetail | **問題照片（IssuePhoto — <a> 連結，每張照片）** | link | 點擊開啟 signed URL（target='_blank'）— 在新分頁顯示原始大圖 | 新分頁開啟 signed URL 大圖（有效期短）；私有 bucket 的照片正確顯示 | image | issue.photos.length > 0 | 新分頁顯示照片圖像，非 403/空白 | screenshot 新分頁大圖 | src/components/IssuePhoto.tsx:26-30, src/pages/IssueDetail.tsx:227-234 |
| 124 | IssueDetail | **留言 textarea** | other | setCommentText(value) — 更新留言內容狀態 | 輸入內容即時反映 | none | 所有可查看問題的用戶 | textarea value 正確 | screenshot textarea 已填寫 | src/pages/IssueDetail.tsx:327-332 |
| 125 | IssueDetail | **送出留言（Send icon btn-primary）** | button | handleSendComment() — addComment(issueId, commentText)：INSERT issue_comments(action='commented')，清空 textarea | 留言出現在活動記錄列表（realtime subscription 或 refetch），textarea 清空 | mutation | commentText.trim() 非空；非 busy | 新留言 card 出現在活動記錄，帶正確作者名+時間 | screenshot 留言送出後活動記錄 | src/pages/IssueDetail.tsx:335-342, 139-147 |
| 126 | IssueDetail 動作 Modal | **關閉 Modal（X / 背景）** | button | setDialog(null); setDialogText('') | Modal 關閉，不做任何改動 | none | action Modal 開啟中 | Modal 不可見，issue 狀態不變 | screenshot Modal 關閉後 | src/pages/IssueDetail.tsx:349-350 |
| 127 | IssueDetail 動作 Modal（解決/上呈/重開） | **補充說明 textarea** | other | setDialogText(value) — 更新補充說明文字 | 輸入內容反映於 textarea | none | action modal 開啟中 | textarea value 正確 | screenshot textarea 已填寫 | src/pages/IssueDetail.tsx:358-369 |
| 128 | IssueDetail 動作 Modal（解決/上呈/重開） | **確認（btn-primary）** | button | handleAction() — 依 dialog.type 呼叫 escalateIssue / resolveIssue / reopenIssue；UPDATE issues + INSERT issue_comment | Modal 關閉；IssueDetail 即時更新狀態 badge 及活動記錄；上呈→handler badge 更新；解決→狀態變綠；重開→狀態變琥珀 | mutation | 同觸發 Modal 的按鈕條件 | 狀態 pill 更新，活動記錄出現對應系統日誌（上呈至 X / 已解決 / 重新開啟） | screenshot 動作完成後 IssueDetail 狀態 pill + 活動記錄 | src/pages/IssueDetail.tsx:149-163, 352-354 |
| 129 | IssueDetail 問題找不到 fallback | **返回工地（btn-ghost）** | button | navigate(`/project/${projectId}`) — 問題不存在時的 fallback 按鈕 | 跳轉回工地詳情頁 | navigation | issue 或 project 不存在時顯示 | URL 變為 #/project/{id} | screenshot fallback card 及返回後頁面 | src/pages/IssueDetail.tsx:126-129 |
| 130 | ProjectDetail 頁頭 匯出選單（問題 tab） | **匯出 Excel（問題）** | menu | onExportIssuesXlsx → get_issue_actor_profiles RPC + issue_comments fetch → exportIssuesToExcel() | 下載 {project}_問題_{date}.xlsx | xlsx | tab = 'issues' | 打開 xlsx，確認兩個 sheet 存在，資料行數對應 issues/comments 數量，欄位名全中文可讀 | screenshot of opened xlsx with both sheets visible and Chinese content | src/pages/ProjectDetail.tsx:1076-1078, src/lib/export.ts:664 |
| 131 | ProjectDetail 問題 tab | **問題 tab（AlertCircle icon + badge）** | tab | setTab('issues') | 切換到問題視圖，若有開放問題顯示 amber badge 數字 | none | isModuleEnabled('issues') = true | 確認 tab active，問題列表顯示 | screenshot of issues tab with badge | src/pages/ProjectDetail.tsx:373 |
| 132 | ProjectDetail 問題 tab | **報告問題（+ Plus icon）** | button | setCreateIssueOpen(true) → 開啟 CreateIssueModal | CreateIssueModal 彈出 | modal | canReport = !!myRoleInProject（有工地角色的成員） | 確認 modal 出現 | screenshot of CreateIssueModal | src/pages/ProjectDetail.tsx:602 |
| 133 | ProjectDetail 問題 tab | **即時問題（Zap icon）** | button | setCreateSnagOpen(true) → 開啟 CreateQuickSnagSheet | CreateQuickSnagSheet bottom sheet 彈出（即時問題 is_quick=true 流程） | modal | canReport = !!myRoleInProject | 確認 bottom sheet 出現 | screenshot of CreateQuickSnagSheet | src/pages/ProjectDetail.tsx:605 |
| 134 | ProjectDetail 問題tab | **返回首頁（ChevronLeft）** | icon-button | navigate('/home') — 離開工地詳情返回主頁 | 跳轉到 /home 頁面 | navigation | 所有已登入用戶 | URL 變為 #/home，主頁 header 顯示 | screenshot 返回後主頁 | src/pages/ProjectDetail.tsx:322 |
| 135 | ProjectDetail 問題tab | **匯出（Download icon — ExportMenu 觸發器）** | icon-button | setOpen(true) — 展開 ExportMenu 下拉選單 | 顯示下拉選單，包含「匯出 Excel」選項（問題tab時） | modal | 所有已登入用戶（ExportMenu 無角色限制） | 下拉選單出現，包含「匯出 Excel」文字 | screenshot 顯示下拉選單 | src/pages/ProjectDetail.tsx:332-337, 1062-1087 |
| 136 | ProjectDetail 問題tab | **匯出 Excel（ExportMenu 下拉項目，問題tab專用）** | menu | 呼叫 exportIssuesToExcel(project, issues, users, comments) — 動態 import ../lib/export 後產生 XLSX | 瀏覽器觸發 .xlsx 下載（web）；iOS/Android 存至 Documents 並顯示 alert「已儲存」 | xlsx | 無額外角色限制（只在問題 tab 可見） | 開啟下載的 .xlsx，確認 Sheet1 含所有問題行、Sheet2 含全部 comment 行、中文無亂碼、編號格式正確（如 #001） | screenshot Excel 開啟後 Sheet1 及 Sheet2 各一頁 | src/pages/ProjectDetail.tsx:339-361, src/lib/export.ts:664-736 |
| 137 | ProjectDetail 問題tab | **刷新（RefreshCw icon）** | icon-button | manualRefresh() — 呼叫 ProgressContext.refetch() + 期間顯示 animate-spin | 進度樹重新從 Supabase 拉取，列表更新；圖示旋轉後停止 | none | 所有已登入用戶 | 圖示旋轉後停止，問題列表顯示最新資料 | screenshot 刷新後列表 | src/pages/ProjectDetail.tsx:363-365, 166-170 |
| 138 | ProjectDetail 問題tab | **進度 tab** | tab | setTab('progress') | 顯示進度樹視圖，問題/文書等隱藏 | navigation | 所有用戶（core tab，永遠顯示） | 進度tab active 樣式（safety-500 bottom border），進度列表可見 | screenshot tab 切換後進度視圖 | src/pages/ProjectDetail.tsx:372 |
| 139 | ProjectDetail 問題tab | **問題 tab（含 open issue 徽章）** | tab | setTab('issues') | 顯示問題 tab 內容：報告問題/即時問題按鈕 + 問題卡片列表；徽章數字顯示 open 正式問題數 | navigation | isModuleEnabled('issues') 為 true 時顯示 | 問題 tab active；問題列表（處理中/即時問題/已解決 sections）可見 | screenshot 問題 tab 已選狀態 | src/pages/ProjectDetail.tsx:373, 107 |
| 140 | ProjectDetail 問題tab IssuesTab | **報告問題（btn-primary）** | button | setCreateIssueOpen(true) — 開啟 CreateIssueModal | CreateIssueModal 開啟，標題「報告新問題」 | modal | canReport = !!myRoleInProject（用戶必須是工地成員） | Modal 出現，標題「報告新問題」 | screenshot CreateIssueModal 開啟後 | src/pages/ProjectDetail.tsx:602-604, 580-586 |
| 141 | ProjectDetail 問題tab IssuesTab | **IssueCard（Link — 任何問題卡片）** | link | navigate 到 /project/:id/issue/:issueId | 跳轉到 IssueDetail 頁面，顯示問題詳情 | navigation | 所有可見問題列表的用戶 | URL 變為 #/project/{id}/issue/{issueId}，問題詳情頁顯示 | screenshot IssueDetail 頁面 | src/components/IssueCard.tsx:15-71 |
| 142 | ProjectDetail 問題tab IssuesTab | **IssueCard 封面相片（Camera icon 多張數量徽章）** | other | 視覺資訊元件（非互動）— 顯示第一張照片縮圖 + 相片數量 | 縮圖顯示（IssuePhoto signed URL），多張時右下角顯示 Camera icon + 數量 | image | issue.photos.length > 0 | 縮圖顯示正確圖片，非 placeholder/broken | screenshot IssueCard 縮圖 | src/components/IssueCard.tsx:22-29 |
| 143 | ProjectDetail tab bar | **問題 tab (with badge count)** | tab | setTab('issues') | Issues tab becomes active; issue list renders; amber badge shows open formal issue count | navigation | isModuleEnabled('issues') — hidden when issues module off | tab active; IssuesTab content rendered; badge count matches openIssueCount | screenshot of issues tab with badge | src/pages/ProjectDetail.tsx:373 |

### 工地指令SI (53)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 144 | ProjectDetail 文書 tab | **文書 tab（FileCheck2 icon）** | tab | setTab('si-vo') | 切換到 SiVoSwitcher 視圖，顯示 SI/VO/PTW/文件/機械 選項 card | none | showSiVoTab = isModuleEnabled('si') ／／ isModuleEnabled('vo') ／／ isModuleEnabled('ptw') ／／ isModuleEnabled('documents') ／／ isModuleEnabled('equipment') | 確認 tab active，SiVoSwitcher cards 顯示 | screenshot of si-vo tab with switcher cards | src/pages/ProjectDetail.tsx:374 |
| 145 | ProjectDetail 文書 tab SiVoSwitcher | **工地指令 card（navigate to SI）** | button | navigate to /project/:id/si | 導航到 SI 列表頁 | navigation | isModuleEnabled('si') = true | 確認 URL 為 #/project/:id/si | screenshot of SI list | src/pages/ProjectDetail.tsx:796-809 |
| 146 | ProjectDetail tab bar | **文書 tab** | tab | setTab('si-vo') | 文書 tab active; SiVoSwitcher renders showing SI/VO/PTW/文件/機械表格 nav cards | navigation | isModuleEnabled('si') ／／ isModuleEnabled('vo') ／／ isModuleEnabled('ptw') ／／ isModuleEnabled('documents') ／／ isModuleEnabled('equipment') | tab active; SiVoSwitcher renders with relevant nav card links | screenshot of 文書 tab content | src/pages/ProjectDetail.tsx:375 |
| 147 | SiApproverBar — EditPayloadModal | **取消 button** | button | Calls onClose() → setModal(null) | EditPayloadModal dismissed; no edit saved | none | disabled while busy === true | Modal gone; SI content unchanged | Screenshot after dismiss | src/components/si/SiApproverBar.tsx:385-387 |
| 148 | SiApproverBar — EditPayloadModal | **批准並修改 button (in EditPayloadModal)** | button | Calls runAction → requireStepUp('approval') → approve(si.id, edited); saves new version with modified title/description then approves | Step-up prompt fires; on success new version created with edited payload; SI status advances; modal closes | mutation | edited.title.trim>0 AND edited.description.trim>0 AND !busy; step-up action_class='approval' | 版本歷史 tab shows new version; 簽核紀錄 shows approve_with_edits action | Screenshot of 版本歷史 showing new version + 簽核紀錄 showing approve_with_edits | src/components/si/SiApproverBar.tsx:388-395 |
| 149 | SiApproverBar — EditPayloadModal (批准並修改) | **標題 input** | other | Updates edited.title in local state; maxLength 120 | Title field editable; submit enabled when both title and description non-empty | none | modal === 'edits' | Input accepts typing; content shown in field | Screenshot of filled title field | src/components/si/SiApproverBar.tsx:358-364 |
| 150 | SiApproverBar — EditPayloadModal (批准並修改) | **描述 textarea** | other | Updates edited.description; maxLength 4000 | Description editable | none | modal === 'edits' | Textarea shows edited content | Screenshot of filled textarea | src/components/si/SiApproverBar.tsx:365-371 |
| 151 | SiApproverBar — ReasonModal | **取消 button** | button | Calls onClose() → setModal(null) | Modal dismissed; no action taken | none | disabled while busy === true | Modal gone; SI status unchanged | Screenshot after dismiss | src/components/si/SiApproverBar.tsx:311-313 |
| 152 | SiApproverBar — ReasonModal | **提交 button (in ReasonModal)** | button | Calls runAction → requireStepUp('approval') → on 退回: requestRevision(si.id, reason); on 拒絕: reject(si.id, reason); on admin: adminOverride(si.id, reason) | Step-up prompt fires; on success SI status updates (revision_requested / rejected / admin_override logged); modal closes | mutation | reason.trim().length >= 10 AND !busy; step-up action_class='approval' | SI status badge changes; 簽核紀錄 tab shows new entry with reason text | Screenshot of updated status + timeline entry with reason | src/components/si/SiApproverBar.tsx:314-324 |
| 153 | SiApproverBar — ReasonModal (退回/拒絕/管理員介入) | **原因 textarea** | other | Sets reason state; submit enabled when reason.trim().length >= 10 | Char count shown; submit button activates at 10 chars | none | ReasonModal open | Submit button becomes active; count shown | Screenshot of filled textarea with counter | src/components/si/SiApproverBar.tsx:293-303 |
| 154 | SiDetail — 抗議 tab (ProtestCommentBar, status=locked) | **抗議意見 textarea** | other | Sets body state for protest text | Text input fills; Submit button activates when body.trim() non-empty | none | si.status === 'locked' AND profile != null | Textarea accepts input; send button becomes enabled | Screenshot of filled textarea | src/components/si/ProtestCommentBar.tsx:87-93 |
| 155 | SiDetail — 抗議 tab (ProtestCommentBar) | **提交 button (Send icon)** | button | Calls addProtest(si.id, text); inserts protest_comment row via SiContext.addProtest | New comment appended to sorted list; textarea cleared; toast/error shown on failure | mutation | si.status === 'locked' AND body.trim() non-empty AND !busy AND profile != null | New comment card visible in list with author name + timestamp | Screenshot of list with new protest comment | src/components/si/ProtestCommentBar.tsx:94-103 |
| 156 | SiDetail — 版本歷史 tab (VersionsPane) | **vN → vM compare button** | button | Calls setComparePair({oldId, newId}); renders SiDiffCard above version list | SiDiffCard appears showing title/description/photo/geo diff between selected versions; active compare button turns safety-600 | none | versions.length >= 2 | SiDiffCard visible with diff highlights (green inserts, red deletes) | Screenshot of SiDiffCard with diff highlighted | src/pages/SiDetail.tsx:439-463 |
| 157 | SiDetail — 詳情 tab (DetailPane) — voice section | **語音備忘 audio player (controls)** | other | Browser-native <audio controls> plays signed-URL voice recording | Audio plays; standard browser audio controls (play/pause/scrub/volume) visible | none | current version payload has voice_path != null AND signed URL resolved | Audio plays when play pressed; waveform/timeline scrubber visible | Screenshot of audio player in playing state | src/pages/SiDetail.tsx:366-370 |
| 158 | SiDetail — header | **返回列表 button (ChevronLeft)** | button | navigate(`/project/${projectId}/si`) | Navigates back to SiList page | navigation | none | URL changes to /project/:id/si; SiList rendered | Screenshot of SiList | src/pages/SiDetail.tsx:153-159 |
| 159 | SiDetail — header (not-found state) | **返回列表 button (ChevronLeft, not-found fallback)** | button | navigate(`/project/${projectId}/si`) | Navigates to SiList | navigation | si === undefined (SI not found) | SiList page rendered | Screenshot of SiList | src/pages/SiDetail.tsx:134-142 |
| 160 | SiDetail — header (status=locked) | **VO number link button (e.g. 'VO-001 →')** | button | navigate(`/project/${projectId}/vo/${vo.id}`) | Navigates to VoDetail page for the related VO | navigation | si.status === 'locked' AND relatedVos.length > 0 | VoDetail page loads for clicked VO | Screenshot of VoDetail page | src/pages/SiDetail.tsx:187-193 |
| 161 | SiDetail — header (status=locked) | **就此工地指令提出變更指令 button** | button | Sets voFormOpen=true; mounts VoSubmitForm modal with parentSi=si | VoSubmitForm modal opens pre-linked to this SI | modal | si.status === 'locked' AND canSubmitVO (from VoContext) | VoSubmitForm modal visible with SI reference shown | Screenshot of open VoSubmitForm | src/pages/SiDetail.tsx:197-206 |
| 162 | SiDetail — SiApproverBar (sticky bottom, status=in_review, admin outside active role) | **管理員介入 button (purple, ShieldAlert icon)** | button | Calls openReason('reason-admin'); opens ReasonModal with title '管理員介入' | ReasonModal opens; admin can enter reason and force override | modal | !canAct AND isAdmin AND si.status === 'in_review' | Purple button visible for admin; ReasonModal opens on tap | Screenshot of 管理員介入 button + ReasonModal | src/components/si/SiApproverBar.tsx:204-214 |
| 163 | SiDetail — SiApproverBar (sticky bottom, status=in_review, role holder) | **批准 button (green, Check icon)** | button | Calls requireStepUp('approval') → step-up modal if needed → approve(si.id) RPC | Step-up prompt fires (password/biometric/SMS); on success SI advances in chain or status→approved/locked; bar re-evaluates visibility | mutation | canAct: si.status==='in_review' AND isRoleHolder (or optionalUser match); step-up action_class='approval' | SI status badge changes; approval record in 簽核紀錄 tab | Screenshot of updated status badge + timeline entry | src/components/si/SiApproverBar.tsx:169-174 |
| 164 | SiDetail — SiApproverBar (sticky bottom, status=in_review, role holder) | **批准並修改 button (blue, Edit3 icon)** | button | Calls openEdits(); sets modal='edits'; mounts EditPayloadModal pre-filled with current version payload | EditPayloadModal opens with editable 標題 and 描述 fields | modal | canAct === true | EditPayloadModal visible; fields show current SI title/description | Screenshot of open EditPayloadModal | src/components/si/SiApproverBar.tsx:175-182 |
| 165 | SiDetail — SiApproverBar (sticky bottom, status=in_review, role holder) | **退回 button (amber, CornerUpLeft icon)** | button | Calls openReason('reason-revision'); opens ReasonModal with title '退回 (要求修訂)' | ReasonModal appears requiring ≥10 char reason before submit | modal | canAct === true | ReasonModal visible with correct title; submit disabled until 10 chars typed | Screenshot of open ReasonModal | src/components/si/SiApproverBar.tsx:183-191 |
| 166 | SiDetail — SiApproverBar (sticky bottom, status=in_review, role holder) | **拒絕 button (red, X icon)** | button | Calls openReason('reason-reject'); opens ReasonModal with title '拒絕' | ReasonModal appears requiring ≥10 char reason | modal | canAct === true | ReasonModal visible with '拒絕' title | Screenshot of ReasonModal | src/components/si/SiApproverBar.tsx:192-200 |
| 167 | SiDetail — tab strip | **詳情 tab** | tab | Sets tab='detail'; renders DetailPane | Detail pane shows title, description, drawing refs, photos, voice audio player, map tile | none | always visible | DetailPane content visible; tab underline on 詳情 | Screenshot of detail tab | src/pages/SiDetail.tsx:244-260 |
| 168 | SiDetail — tab strip | **版本歷史 tab** | tab | Sets tab='versions'; renders VersionsPane | VersionsPane shows version compare buttons and list of version cards | none | always visible | VersionsPane renders; version cards visible | Screenshot of versions tab | src/pages/SiDetail.tsx:244-260 |
| 169 | SiDetail — tab strip | **簽核紀錄 tab** | tab | Sets tab='approvals'; renders SiTimeline | SiTimeline shows chronological approval action list with actor names, action badges, reasons | none | always visible | Approval timeline renders with APPROVAL_ACTION_ZH labels | Screenshot of approvals tab | src/pages/SiDetail.tsx:244-260 |
| 170 | SiDetail — tab strip | **抗議 tab** | tab | Sets tab='protest'; renders ProtestCommentBar | Protest thread and input shown | none | si.status === 'locked' only (tab only added to array when locked) | 抗議 tab visible only on locked SIs; ProtestCommentBar renders | Screenshot of protest tab on a locked SI | src/pages/SiDetail.tsx:116-125 |
| 171 | SiList (SiListPage) | **新增 (+ button in header)** | button | Sets creating=true, mounts SiSubmitForm modal | SiSubmitForm bottom-sheet/modal appears; user can fill in new SI | modal | canSubmit: admin OR assigned PM OR approved membership with role in (pm, main_contractor, subcontractor) — SiContext.canSubmit. Button hidden otherwise. | Screenshot: SiSubmitForm modal is visible with 標題 / 描述 / 圖則參照 fields | Screenshot of the open modal | src/components/si/SiList.tsx:65-73 |
| 172 | SiList (SiListPage) | **全部 / 待批准 / 已批准 / 已退回 / 已拒絕 filter pills** | switcher | Sets filter state; filters visible sis by matching SiStatus values | List re-renders showing only SIs matching the chosen status group; active pill turns safety-orange | none | none (always visible) | Screenshot: list content changes; active pill bg-safety-600 text-white | Screenshot of list after each filter tap | src/components/si/SiList.tsx:77-95 |
| 173 | SiList (SiListPage) | **搜尋標題… input** | other | Sets search state with 200ms debounce; filters list by title substring or SI number | List narrows to SIs whose title or number includes the typed string | none | none (always visible) | Screenshot: list shows only matching SI cards after typing | Screenshot of narrowed list | src/components/si/SiList.tsx:98-107 |
| 174 | SiList (SiListPage) | **SiCard (tappable list item)** | button | Calls onOpen(si.id) → navigate(`/project/${projectId}/si/${si.id}`) | Navigation to SiDetail page for the selected SI | navigation | none (always tappable) | URL changes to /project/:id/si/:siId; SiDetail page renders | Screenshot of SiDetail page after tap | src/components/si/SiCard.tsx:47-82 |
| 175 | SiList (SiListPage) — empty state | **新增 (+ button in empty-state center)** | button | Calls onNew() → sets creating=true, mounts SiSubmitForm modal | SiSubmitForm modal appears (same as header button) | modal | canSubmit === true AND sis.length === 0 (empty state) | Screenshot: modal visible after tap when list is empty | Screenshot of the open modal | src/components/si/SiList.tsx:121-131 |
| 176 | SiSubmitForm — Photo source sheet | **拍攝 button** | camera | Triggers cameraInputRef.current.click(); hidden <input type=file accept=image/* capture=environment> | Native camera opens; captured photo appended to photos array and shown as thumbnail; sheet closes | image | photoSheetOpen === true | Photo thumbnail appears in grid | Screenshot of photo grid after capture | src/components/si/SiSubmitForm.tsx:433-440 |
| 177 | SiSubmitForm — Photo source sheet | **從相簿選擇 button** | filepicker | Triggers galleryInputRef.current.click(); hidden <input type=file accept=image/* multiple> | Photo gallery opens; selected photo(s) appended to photos array; sheet closes | image | photoSheetOpen === true | Thumbnail(s) appear in photo grid | Screenshot of photo grid | src/components/si/SiSubmitForm.tsx:441-449 |
| 178 | SiSubmitForm — Photo source sheet | **從檔案選擇 button** | filepicker | Triggers fileInputRef.current.click(); hidden <input type=file accept=image/* multiple> | File picker opens; selected image file(s) appended; sheet closes | image | photoSheetOpen === true | Thumbnail(s) appear in photo grid | Screenshot of photo grid | src/components/si/SiSubmitForm.tsx:450-458 |
| 179 | SiSubmitForm — Photo source sheet | **取消 button (in photo sheet)** | button | Sets photoSheetOpen=false; dismisses sheet without picking any photo | Photo source sheet closes; no new photo added | none | photoSheetOpen === true | Sheet dismissed; photo grid unchanged | Screenshot after dismiss | src/components/si/SiSubmitForm.tsx:459-463 |
| 180 | SiSubmitForm modal | **關閉 (X icon in header)** | icon-button | Calls onCancel() → sets creating=false, unmounts modal | Modal dismisses; user returns to SiList | none | none | Screenshot: modal gone, SiList visible | Screenshot after dismiss | src/components/si/SiSubmitForm.tsx:225-232 |
| 181 | SiSubmitForm modal | **標題 text input** | other | Sets title state; maxLength 120 | Character counter updates (e.g. '5/120'); value stored in form state | none | none | Counter text below input reflects typed length | Screenshot of filled field | src/components/si/SiSubmitForm.tsx:241-250 |
| 182 | SiSubmitForm modal | **描述 textarea** | other | Sets description state; maxLength 4000 | Character counter updates; value stored | none | none | Counter text below textarea reflects length | Screenshot of filled textarea | src/components/si/SiSubmitForm.tsx:256-266 |
| 183 | SiSubmitForm modal — 位置 section (GeoPicker, denied state) | **重試 link** | link | Calls acquire() again to re-request geolocation | State → 'resolving'; second attempt to get GPS fix | none | state === 'denied' (permission refused or timeout) | Spinner shown again; success shows tile preview | Screenshot after retry | src/components/si/GeoPicker.tsx:91-101 |
| 184 | SiSubmitForm modal — 位置 section (GeoPicker, empty state) | **加入位置 (選填) button** | other | Calls acquire(); calls Geolocation.getCurrentPosition({enableHighAccuracy:false, timeout:8000}); state → 'resolving' | Spinner + '正在取得位置…' shown; on success state → 'resolved' with OSM tile map preview and coordinates | none | state === 'empty' | OSM tile image + coordinates appear after success | Screenshot of resolved geo preview | src/components/si/GeoPicker.tsx:65-73 |
| 185 | SiSubmitForm modal — 位置 section (GeoPicker, resolved state) | **清除位置 button (X icon)** | button | Calls clear(); calls onChange(null); state → 'empty' | Map tile preview removed; GeoPicker returns to empty state button | none | state === 'resolved' | Map preview gone; 加入位置 button reappears | Screenshot of empty state after clear | src/components/si/GeoPicker.tsx:55-59 and :139-146 |
| 186 | SiSubmitForm modal — 相片 section | **+ (add photo) button** | button | Sets photoSheetOpen=true; opens photo source bottom-sheet | Photo source sheet slides up with 拍攝 / 從相簿選擇 / 從檔案選擇 options | modal | none | Screenshot: photo source sheet visible | Screenshot of open sheet | src/components/si/SiSubmitForm.tsx:374-381 |
| 187 | SiSubmitForm modal — 相片 section | **× (remove photo) button on photo thumbnail** | icon-button | Calls removePhoto(idx); filters photo out of photos array | Photo thumbnail disappears from the grid | none | Visible only when at least one photo has been added | Screenshot: thumbnail gone after tap | Screenshot after removal | src/components/si/SiSubmitForm.tsx:363-371 |
| 188 | SiSubmitForm modal — 圖則參照 section | **Drawing row expand/collapse toggle (clicking drawing row header)** | button | Toggles expandedDrawingId; shows/hides version list for that drawing | Version sub-list appears/disappears under the drawing row | none | none (always visible when drawings exist) | Screenshot: version list shown/hidden after tap | Screenshot of expanded drawing row | src/components/si/SiSubmitForm.tsx:287-317 |
| 189 | SiSubmitForm modal — 圖則參照 section | **Drawing checkbox (select/deselect drawing version pin)** | toggle | Calls toggleVersionPin(current_version_id) to add/remove from pickedVersionIds | Checkbox ticked/unticked; drawing row bg changes to safety-50 when active; SI submit requires ≥1 pinned | none | none | Checkbox checked state + row bg-safety-50 | Screenshot of checked row | src/components/si/SiSubmitForm.tsx:303-316 |
| 190 | SiSubmitForm modal — 圖則參照 section (expanded) | **Version selector row (vN button inside expanded drawing)** | button | Calls pickVersionForDrawing(drawingId, versionId); replaces any other version of same drawing in picked set | Selected version row highlighted bg-safety-100; previously picked version of same drawing deselected | none | Drawing must be expanded (expandedDrawingId === d.id) | Screenshot: selected row has bg-safety-100 + tick mark | Screenshot of version picker row | src/components/si/SiSubmitForm.tsx:322-339 |
| 191 | SiSubmitForm modal — 語音備忘 section (VoiceRecorder, idle state) | **錄製語音備忘 (選填) button** | voice | Calls startRecording(); requests microphone permission (native: VoiceRecorder plugin; web: getUserMedia); begins recording; state → 'recording' | Red pulsing dot + timer 0:00/2:00 appears; recording in progress | none | state === 'idle'; microphone permission must be granted | Recording indicator visible; timer ticking | Screenshot of recording state | src/components/si/VoiceRecorder.tsx:213-224 |
| 192 | SiSubmitForm modal — 語音備忘 section (VoiceRecorder, playback state) | **播放 / 暫停 button** | button | Calls togglePlayback(); plays or pauses the hidden <audio> element | Audio plays/pauses; label toggles between 播放 and 暫停 | none | state === 'playback' AND playbackUrl != null | Label changes; audio audible | Screenshot showing 暫停 label while playing | src/components/si/VoiceRecorder.tsx:246-259 |
| 193 | SiSubmitForm modal — 語音備忘 section (VoiceRecorder, playback state) | **重錄 button (Trash2 icon)** | button | Calls reset(); clears blob, resets state to 'idle', calls onRecorded(null) | Playback UI replaced with 錄製語音備忘 button; blob cleared | none | state === 'playback' | VoiceRecorder shows idle state again | Screenshot of idle state after reset | src/components/si/VoiceRecorder.tsx:261-270 |
| 194 | SiSubmitForm modal — 語音備忘 section (VoiceRecorder, recording state) | **停止 button** | button | Calls finishRecording(); stops MediaRecorder/native recorder; creates Blob; state → 'playback' | Recording stops; playback controls (播放/重錄) appear; blob passed to parent via onRecorded | none | state === 'recording' | Playback row visible with 播放 + 重錄 buttons | Screenshot of playback state | src/components/si/VoiceRecorder.tsx:233-243 |
| 195 | SiSubmitForm modal footer | **取消 button** | button | Calls onCancel(); sets creating=false; unmounts SiSubmitForm | Modal closes; SiList shown; no SI created | none | disabled while submitting === true | Modal gone; SiList visible | Screenshot after cancel | src/components/si/SiSubmitForm.tsx:399-405 |
| 196 | SiSubmitForm modal footer | **提交 button** | button | Calls onSubmit(): createDraftSi() → uploadSiPhotos() → recordPhotoMeta() → uploadSiVoice() → saveVersion() → submitSi(); navigates to SiDetail on success | SI created in DB with status='submitted'; navigates to /project/:id/si/:siId; realtime updates SiList | mutation | canSubmit: title.trim>0 AND description.trim>0 AND pickedVersionIds.length>0 AND !submitting | New SI appears in SiList with 已提交 badge; SiDetail page loads | Screenshot of SiDetail page post-submit + SiList showing new entry | src/components/si/SiSubmitForm.tsx:148-213 and :407-415 |

### 變更指令VO (49)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 197 | 新增VO選擇器 (picking bottom-sheet) | **＋ 獨立變更指令（不引用工地指令）** | button | setParentSiId(null); setPicking(false); setFormOpen(true) | 選擇器關閉；VoSubmitForm以parentSi=undefined開啟，標題顯示「獨立變更（不引用工地指令）」 | modal | visible always within picker; canSubmit required to open picker | VoSubmitForm header shows '獨立變更（不引用工地指令）' | screenshot of VoSubmitForm | src/pages/VoList.tsx:54-60 |
| 198 | 新增VO選擇器 (picking bottom-sheet) | **引用已鎖定SI 行項目按鈕** | button | setParentSiId(s.id); setPicking(false); setFormOpen(true) | 選擇器關閉；VoSubmitForm以parentSi=selected SI開啟，標題顯示「引用工地指令 {s.number}」 | modal | 只在有status==='locked'的SI時才顯示行；canSubmit required | VoSubmitForm header contains the SI number in font-mono span | screenshot of VoSubmitForm with SI reference shown | src/pages/VoList.tsx:61-77 |
| 199 | 新增VO選擇器 (picking bottom-sheet) | **取消** | button | setPicking(false) | 底部彈出選擇器關閉；返回列表 | none | always visible within picker | picker overlay disappears; VoList visible | screenshot after dismiss | src/pages/VoList.tsx:79-86 |
| 200 | EditPayloadModal (批准並修訂) | **描述 textarea** | other | updateDescription(v) → setPayload({...payload, description: v}); maxLength 4000 | 描述欄位更新；ok重算 | none | always visible in EditPayloadModal | textarea reflects typed text | screenshot of edited description | src/components/vo/VoApproverBar.tsx:329-332, 353-358 |
| 201 | EditPayloadModal (批准並修訂) | **VoLineItemsEditor (新增/刪除/編輯行項目)** | other | updateLineItems(next) → setPayload({...payload, line_items: next}); same sub-controls as in VoSubmitForm | 行項目清單更新；validateLineItems重算；ok=true when description non-empty AND no lineItemError | none | always visible in EditPayloadModal | line item list reflects edits; total preview recalculates | screenshot of editor with edited rows | src/components/vo/VoApproverBar.tsx:360-370 |
| 202 | EditPayloadModal (批准並修訂) | **取消** | button | onClose() → setModal(null) | EditPayloadModal關閉；VO狀態不變 | none | disabled while busy | modal disappears; VO status unchanged | screenshot after dismiss | src/components/vo/VoApproverBar.tsx:379-381 |
| 203 | EditPayloadModal (批准並修訂) | **批准並修訂 (confirm button)** | button | onSubmit(payload) → requireStepUp('approval') → approve(vo.id, editedPayload) — saves new version AND approves in one RPC | Modal關閉；新版本保存（版本歷史新增條目）；VO前進審批步驟；若最終步則status→approved/locked | mutation | disabled when description empty OR lineItemError non-null OR busy | 版本歷史 tab shows new version v{n+1}; 簽核紀錄 shows '批准並修訂' action; status badge updated | screenshot of 版本歷史 tab with new version entry + updated status badge | src/components/vo/VoApproverBar.tsx:382-391 |
| 204 | ProjectDetail 文書 tab SiVoSwitcher | **變更指令 card（navigate to VO）** | button | navigate to /project/:id/vo | 導航到 VO 列表頁 | navigation | isModuleEnabled('vo') = true | 確認 URL 為 #/project/:id/vo | screenshot of VO list | src/pages/ProjectDetail.tsx:810-824 |
| 205 | ReasonModal (退回/拒絕/管理員介入) | **原因 textarea** | other | setReason(e.target.value); displays char count vs MIN_REASON=10 | 字數計數即時更新；提交按鈕在 reason.trim().length >= 10 時才啟用 | none | always visible in ReasonModal | counter shows '{n}/10 chars min'; submit enabled only when >= 10 | screenshot of modal with typed reason | src/components/vo/VoApproverBar.tsx:280-290 |
| 206 | ReasonModal (退回/拒絕/管理員介入) | **取消** | button | onClose() → setModal(null) | ReasonModal關閉；VO狀態不變 | none | disabled while busy | modal disappears; approverBar still visible; VO status unchanged | screenshot after dismiss | src/components/vo/VoApproverBar.tsx:299-301 |
| 207 | ReasonModal (退回/拒絕/管理員介入) | **提交** | button | requireStepUp('approval') → requestRevision(vo.id, reason) OR reject(vo.id, reason) OR adminOverride(vo.id, reason) | RPC執行；modal關閉；VO status更新（退回→revision_requested，拒絕→rejected，介入→next step or forced）；簽核紀錄新增條目 | mutation | disabled when reason.trim().length < 10 OR busy | VO status badge changes accordingly; 簽核紀錄 tab shows new entry with actor_name, action_zh, reason | screenshot of updated status badge + 簽核紀錄 entry | src/components/vo/VoApproverBar.tsx:302-312 |
| 208 | VoApproverBar (sticky bottom bar on VoDetail) | **批准 (green button)** | button | requireStepUp('approval') → approve(vo.id) RPC call (no payload edit) | 步驟升審核；若為最終步則VO status→'approved'/'locked'；bar hides or refreshes; step-up dialog may appear first | mutation | canAct = (vo.status==='in_review') AND (isRoleHolder OR optionalUser===me OR isAdmin); shown only when canAct=true | VO status badge changes; approval entry added to 簽核紀錄 tab | screenshot of updated status badge + 簽核紀錄 tab entry | src/components/vo/VoApproverBar.tsx:156-164 |
| 209 | VoApproverBar (sticky bottom bar on VoDetail) | **批准並修訂 (blue Edit3 button)** | button | openEdits() → copies latestVersion.payload to edited state → opens EditPayloadModal | EditPayloadModal opens pre-filled with current description and line items for editing before approval | modal | canAct = true (same as 批准) | EditPayloadModal shows '批准並修訂' header; description and line items pre-populated | screenshot of EditPayloadModal | src/components/vo/VoApproverBar.tsx:165-172 |
| 210 | VoApproverBar (sticky bottom bar on VoDetail) | **退回 (amber CornerUpLeft button)** | button | openReason('reason-revision') → opens ReasonModal with title '退回 (要求修訂)' | ReasonModal opens for entering revision reason (min 10 chars) | modal | canAct = true | ReasonModal appears with title '退回 (要求修訂)' and reason textarea | screenshot of ReasonModal | src/components/vo/VoApproverBar.tsx:173-182 |
| 211 | VoApproverBar (sticky bottom bar on VoDetail) | **拒絕 (red X button)** | button | openReason('reason-reject') → opens ReasonModal with title '拒絕' | ReasonModal opens for entering rejection reason (min 10 chars) | modal | canAct = true | ReasonModal appears with title '拒絕' and reason textarea | screenshot of ReasonModal | src/components/vo/VoApproverBar.tsx:183-192 |
| 212 | VoApproverBar (sticky bottom bar on VoDetail) | **管理員介入 (purple ShieldAlert button)** | button | openReason('reason-admin') → opens ReasonModal with title '管理員介入' | ReasonModal opens for entering admin-override reason (min 10 chars) | modal | shown only when isAdmin=true AND canAct=false AND vo.status==='in_review' (admin who is not the designated role-holder for this step) | ReasonModal appears with title '管理員介入' | screenshot of ReasonModal | src/components/vo/VoApproverBar.tsx:193-204 |
| 213 | VoConfirmationScreen 提交確認 | **關閉** | button | onClose() → setConfirmation(null) | 確認彈窗關閉；返回VoList列表（新VO以狀態「已提交」顯示） | none | always visible | confirmation overlay disappears; VoList shows new card | screenshot of VoList with new VO card | src/components/vo/VoConfirmationScreen.tsx:45-50 |
| 214 | VoConfirmationScreen 提交確認 | **查看詳情** | button | onViewDetail(voId) → setConfirmation(null); navigate(`/project/${projectId}/vo/${voId}`) | 確認彈窗關閉；導航至新VO的VoDetail頁 | navigation | visible only when onViewDetail prop is provided (always provided from VoListInner) | URL = /project/:id/vo/:voId; VoDetail shows matching number + server total | screenshot of VoDetail page | src/components/vo/VoConfirmationScreen.tsx:52-59; src/pages/VoList.tsx:116-120 |
| 215 | VoDetail 詳情頁 | **返回列表 (ChevronLeft link)** | button | navigate(`/project/${projectId}/vo`) | 導航至VoList頁 | navigation | always visible; also shown on error state when VO not found | URL = /project/:id/vo; VoList renders | screenshot of VoList | src/pages/VoDetail.tsx:153-160 |
| 216 | VoDetail 詳情頁 | **匯出 PDF (btn-primary with FileDown icon)** | button | handleExport() → fetches SI drawing versions → dynamic import exportVOToPDF → generates and downloads PDF blob named {project}_{vo.number}_{date}.pdf | Button label changes to '產生中…'; PDF file auto-downloads; button returns to '匯出 PDF' | pdf | only rendered when vo.status === 'locked' | PDF opens; check page 1 has table with all line items and 經系統核算總額 row; page 2 has 簽核紀錄; Chinese text is legible (not 亂碼); filename matches pattern | preview_screenshot of the rendered PDF (open in browser); verify table rows count matches line_items.length; verify HKD total matches vo.total_amount_cents | src/pages/VoDetail.tsx:93-126, 185-202; src/lib/export.ts:830-928 |
| 217 | VoDetail 詳情頁 | **「詳情」tab** | tab | setTab('detail') | DetailPane renders: description card + line items table + 經系統核算總額 total row | none | always visible | DetailPane with description and line items visible; tab has active underline (border-safety-600) | screenshot of detail tab | src/pages/VoDetail.tsx:206-228 |
| 218 | VoDetail 詳情頁 | **「版本歷史」tab** | tab | setTab('versions') | VersionsPane renders: list of VOVersion cards sorted newest-first showing v{version_no}, created_at, item count, total HKD | none | always visible | each version card shows 'v{n}' label and total in formatHKD; if no versions, shows '尚未有版本' | screenshot of versions tab | src/pages/VoDetail.tsx:206-228 |
| 219 | VoDetail 詳情頁 | **「簽核紀錄」tab** | tab | setTab('approvals') | SiTimeline renders approval events sorted chronologically with actor_name, action_zh (from APPROVAL_ACTION_ZH), timestamp, reason | none | always visible | SiTimeline shows each approval action in correct chronological order with zh-HK locale timestamp | screenshot of approvals tab | src/pages/VoDetail.tsx:206-228 |
| 220 | VoLineItemRow (inside VoLineItemsEditor) | **類別 select dropdown** | menu | update('category', value) — options: labour/material/preliminaries/contingency | 行項目category更新為選定值（顯示 勞工/物料/臨時工程/應急費用 等ZH標籤） | none | always visible per row | select shows selected ZH label; LINE_ITEM_CATEGORY_ZH maps correctly | screenshot of open dropdown | src/components/vo/VoLineItemRow.tsx:48-57 |
| 221 | VoLineItemRow (inside VoLineItemsEditor) | **移除項目 (Trash2 icon-button)** | icon-button | onRemove() → parent calls removeAt(i) → filters out this row | 行從列表中移除；總額預覽重算 | none | always visible per row | row count decreases by 1; total preview updates | screenshot showing row removed and updated total | src/components/vo/VoLineItemRow.tsx:58-66 |
| 222 | VoLineItemRow (inside VoLineItemsEditor) | **描述 text input** | other | update('description', value) — maxLength 200 | li.description更新；驗證錯誤（如已存在）消除 | none | always visible per row | input value reflects typed text; validateLineItems no longer returns error for this row if non-empty | screenshot of filled description field | src/components/vo/VoLineItemRow.tsx:69-79 |
| 223 | VoLineItemRow (inside VoLineItemsEditor) | **數量 number input** | other | update('quantity', Number(e.target.value)) → also recomputes subtotal_cents = multiplyCents(qty, unit_price_cents) | 數量更新；小計預覽即時重算顯示 | none | always visible per row | 小計預覽 = quantity × unit_price (in HKD); total preview updates | screenshot showing updated subtotal | src/components/vo/VoLineItemRow.tsx:83-93 |
| 224 | VoLineItemRow (inside VoLineItemsEditor) | **單位 text input** | other | update('unit', value) — maxLength 20 | li.unit更新；validateLineItems重算 | none | always visible per row | input value reflects typed unit; no unit validation error once non-empty | screenshot of filled unit field | src/components/vo/VoLineItemRow.tsx:94-105 |
| 225 | VoLineItemRow (inside VoLineItemsEditor) | **單價 (HK$) text input (with onBlur commit)** | other | onChange: setPriceText; onBlur: commitPrice() → parseHKD(priceText) → update('unit_price_cents', cents) → subtotal recomputed | blur後單價格式化為兩位小數；小計及總額預覽重算 | none | always visible per row | after blur: priceText shows 'x.xx'; subtotal = qty × price | screenshot after blur showing formatted price and updated subtotal | src/components/vo/VoLineItemRow.tsx:109-123 |
| 226 | VoLineItemRow (inside VoLineItemsEditor) | **🔗 連結進度 link button** | button | setPickerOpen(true) → opens ProgressLeafPicker bottom-sheet | 底部彈出顯示所有進度葉節點項目；已連結者顯示code+title | modal | always visible per row (optional field) | ProgressLeafPicker modal appears with '連結進度項目' heading | screenshot of picker | src/components/vo/VoLineItemRow.tsx:132-139 |
| 227 | VoLineItemRow → ProgressLeafPicker | **進度葉節點 行項目按鈕** | button | onPick(l.id) → update('progress_leaf_item_id', pid); setPickerOpen(false) | 選擇器關閉；連結按鈕文字更新為「🔗 連結進度 · {code} {title}」 | none | visible for each leaf progress item; shown only when leaves.length > 0 | link button label shows selected code+title; item has active highlight bg-safety-100 | screenshot of updated link button label | src/components/vo/VoLineItemRow.tsx:195-210 |
| 228 | VoLineItemRow → ProgressLeafPicker | **清除連結** | button | onPick(null) → update('progress_leaf_item_id', null); setPickerOpen(false) | 連結清除；連結按鈕文字回到「🔗 連結進度 (選填)」 | none | shown only when selectedId is non-null | link button shows '(選填)'; progress_leaf_item_id=null in payload | screenshot of link button after clear | src/components/vo/VoLineItemRow.tsx:183-190 |
| 229 | VoLineItemRow → ProgressLeafPicker | **關閉 (X icon-button)** | icon-button | onClose() → setPickerOpen(false) | 選擇器關閉；連結不變 | none | always visible in picker header | picker overlay disappears; link button unchanged | screenshot after close | src/components/vo/VoLineItemRow.tsx:178-179 |
| 230 | VoLineItemsEditor (inside VoSubmitForm / EditPayloadModal) | **新增項目 (primary button — empty state)** | button | addRow() → onChange([...value, makeBlankItem()]) | 空白VoLineItemRow card以category='labour'、全空欄位出現 | none | shown only when value.length === 0 | one VoLineItemRow card appears in editor | screenshot of editor with first blank row | src/components/vo/VoLineItemsEditor.tsx:41-56 |
| 231 | VoLineItemsEditor (inside VoSubmitForm / EditPayloadModal) | **新增項目 (ghost button — when rows exist)** | button | addRow() → appends makeBlankItem() to list | 新空白行在最後追加；總額預覽不變（小計為0） | none | shown only when value.length > 0 | row count increases by 1; new row has empty fields | screenshot of editor with new blank row | src/components/vo/VoLineItemsEditor.tsx:70-79 |
| 232 | VoList 列表頁 (VoList component) | **新增 (+ button)** | button | sets picking=true → opens SI picker bottom-sheet | 底部彈出「新增變更指令」選擇器，列出獨立VO及所有已鎖定SI供選擇 | modal | canSubmit (VoContext) — hidden when user lacks submit permission | snapshot shows picker modal with h3 text '新增變更指令' | screenshot of picker bottom-sheet overlay | src/components/vo/VoList.tsx:77-85 |
| 233 | VoList 列表頁 (VoList component) | **日期篩選「由」date input** | other | sets from state → filters vos list to created_at >= selected date | 列表即時過濾，只顯示在所選日期或之後建立的VO | none | always visible | visible VoCard count changes; all shown cards have created_at >= from date | screenshot of filtered list | src/components/vo/VoList.tsx:90-97 |
| 234 | VoList 列表頁 (VoList component) | **日期篩選「至」date input** | other | sets to state → filters vos list to created_at <= end-of-day of selected date | 列表即時過濾，只顯示在所選日期或之前建立的VO | none | always visible | visible VoCard count changes; all shown cards have created_at <= to date + 86399999ms | screenshot of filtered list | src/components/vo/VoList.tsx:98-106 |
| 235 | VoList 列表頁 (VoList component) | **狀態篩選「全部」pill** | button | setFilter('all') → shows all VOs regardless of status | 篩選藥丸高亮變為橙色；列表顯示全部VO | none | always visible | pill has bg-safety-600 class; all VOs in context appear in list | screenshot of list with active pill highlighted | src/components/vo/VoList.tsx:111-129 |
| 236 | VoList 列表頁 (VoList component) | **狀態篩選「待批准」pill** | button | setFilter('pending') → matches status submitted／in_review | 列表只顯示狀態為「已提交」或「審核中」的VO | none | always visible | every visible VoCard has status badge '已提交' or '審核中' | screenshot of filtered list | src/components/vo/VoList.tsx:11-16, 111-129 |
| 237 | VoList 列表頁 (VoList component) | **狀態篩選「已批准」pill** | button | setFilter('approved') → matches status approved／locked | 列表只顯示狀態為「已批准」或「已鎖定」的VO | none | always visible | every visible VoCard has status badge '已批准' or '已鎖定' | screenshot of filtered list | src/components/vo/VoList.tsx:11-16, 111-129 |
| 238 | VoList 列表頁 (VoList component) | **狀態篩選「已退回」pill** | button | setFilter('returned') → matches status revision_requested | 列表只顯示狀態為「已退回」的VO | none | always visible | every visible VoCard has status badge '已退回' | screenshot of filtered list | src/components/vo/VoList.tsx:11-16, 111-129 |
| 239 | VoList 列表頁 (VoList component) | **狀態篩選「已拒絕」pill** | button | setFilter('rejected') → matches status rejected | 列表只顯示狀態為「已拒絕」的VO | none | always visible | every visible VoCard has status badge '已拒絕' | screenshot of filtered list | src/components/vo/VoList.tsx:11-16, 111-129 |
| 240 | VoList 列表頁 (VoList component) | **搜尋編號 text input** | other | debounced 200ms → filters vos where vo.number includes search string (case-insensitive) | 200ms後列表只顯示編號包含搜尋字串的VO | none | always visible | all visible VoCards have vo.number containing the typed string | screenshot of search results | src/components/vo/VoList.tsx:131-141 |
| 241 | VoList 列表頁 (VoList component) | **VoCard 卡片 (tap)** | button | onTap(vo.id) → navigate(`/project/${projectId}/vo/${voId}`) | 導航至該VO的詳情頁 VoDetail | navigation | always visible for any VO in the list | URL changes to /project/:id/vo/:voId; VoDetail header shows matching vo.number | screenshot of VoDetail page after navigation | src/components/vo/VoCard.tsx:46-88; src/pages/VoList.tsx:36-38 |
| 242 | VoSubmitForm 提交表單 | **關閉 (X icon-button)** | icon-button | onCancel() → setFormOpen(false); setParentSiId(null) | 表單bottom-sheet關閉；返回列表 | none | always visible in form header | form overlay disappears; VoList visible | screenshot after close | src/components/vo/VoSubmitForm.tsx:97-104 |
| 243 | VoSubmitForm 提交表單 | **描述 textarea** | other | setDescription(e.target.value) — maxLength 4000 | 字符計數器即時更新；canSubmit重算 | none | always visible | counter shows '{length}/4000'; submit button enabled when description.trim().length > 0 AND lineItems valid | screenshot of filled form | src/components/vo/VoSubmitForm.tsx:110-120 |
| 244 | VoSubmitForm 提交表單 | **取消** | button | onCancel() → setFormOpen(false); setParentSiId(null) | 表單關閉；返回列表 | none | always visible; disabled while submitting | form overlay disappears | screenshot | src/components/vo/VoSubmitForm.tsx:145-151 |
| 245 | VoSubmitForm 提交表單 | **提交** | button | onSubmit() → createDraftVo → saveVersion → submitVo → refetch → onSubmitted(voId, serverTotal) | 表單關閉；VoConfirmationScreen以server-computed total顯示；VO在列表中出現狀態「已提交」 | mutation | disabled unless description.trim().length > 0 AND validateLineItems === null AND !submitting | VoConfirmationScreen shows vo.number + server total HKD; DB row in variation_orders with status='submitted' | screenshot of VoConfirmationScreen showing '已提交' and HKD total | src/components/vo/VoSubmitForm.tsx:153-162 |

### 工作許可證PTW (47)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 246 | ProjectDetail 文書 tab SiVoSwitcher | **工作許可證 card（navigate to PTW）** | button | navigate to /project/:id/ptw | 導航到 PTW 列表頁 | navigation | isModuleEnabled('ptw') = true（showPtw） | 確認 URL 為 #/project/:id/ptw | screenshot of PTW list | src/pages/ProjectDetail.tsx:825-840 |
| 247 | PtwDetail | **返回列表 (ChevronLeft link)** | button | navigate('/project/:id/ptw') — go back to PtwList | PtwList page loads | navigation | none | UI: PtwList page with filter bar visible | screenshot of PtwList after navigation | src/pages/PtwDetail.tsx:101-108 |
| 248 | PtwDetail | **匯出合規證明 (PDF) (Download icon button)** | button | exportComplianceProofPack({docKindZh:'工作許可證 (PTW)', docNumber, dwssRefStr, statusZh, detailZh, chainRolesZh}) — generates PDF via jspdf | PDF file downloaded/opened in browser; contains PTW compliance proof document with DWSS reference, PTW number, status, type, chain roles in Chinese | pdf | none — visible to all users who can view the PTW | FILE: open downloaded PDF; verify Chinese characters render; verify DWSS ref string present; verify PTW number matches | preview_screenshot of the rendered PDF showing Chinese title and DWSS reference | src/pages/PtwDetail.tsx:123-136 |
| 249 | PtwDetail — 火警監察 section (hot_work + active only) | **開始 30 分鐘火警監察 (button)** | button | handleStartFireWatch() → startFireWatch(ptw.id) → PtwContext.startFireWatch RPC → sets fire_watch_started_at on permit | Button disappears; countdown timer appears '還需 XX 分 XX 秒'; DB column fire_watch_started_at set to current timestamp; 30-minute countdown ticks down every second | mutation | only rendered when ptw.status === 'active' AND ptw.ptw_type === 'hot_work' AND ptw.fire_watch_started_at is null; disabled when online===false | UI: countdown timer visible and ticking; DB: permits_to_work.fire_watch_started_at is set; '關閉許可證' button absent until 30 min elapsed | screenshot showing countdown timer after pressing button | src/pages/PtwDetail.tsx:261-264, 80-85 |
| 250 | PtwDetail — 火警監察 section (hot_work + active, fire-watch complete) | **關閉許可證 (button — inside hot_work fire-watch section)** | button | setShowCloseOut(true) — opens close-out signature modal | Modal '關閉工作許可證' appears with PtwSignaturePad inside | modal | only rendered when hotWorkFireWatchEligible(ptw) === true (hot_work type, fire_watch_started_at set, >= 30 min elapsed); disabled when online===false | UI: Modal with title '關閉工作許可證' and signature pad canvas visible | screenshot of close-out signature modal | src/pages/PtwDetail.tsx:271-276 |
| 251 | PtwDetail — PtwApproverBar — 拒絕 modal | **確認 (button in reject modal)** | button | handleReasonAction() → requireStepUp('approval') → reject(ptw.id, reason) RPC → PTW status → 'rejected' | PTW status changes to '已拒絕' (red pill); approver bar disappears; modal closes; PTW is terminal | mutation | reason.trim().length >= 10; requireStepUp must pass; disabled during submitting | UI: status pill shows '已拒絕' with bg-red-50 text-red-600; DB: permits_to_work.status = 'rejected' | screenshot of PtwDetail with '已拒絕' status pill | src/components/ptw/PtwApproverBar.tsx:135-158 |
| 252 | PtwDetail — PtwApproverBar — 拒絕 modal | **取消 (button in reject modal)** | button | setActiveAction(null); setReason(''); setError(null) | Modal closes; reason cleared; PTW unchanged | none | none | UI: modal absent; PTW status unchanged | screenshot of PtwDetail with modal closed | src/components/ptw/PtwApproverBar.tsx:245-248 |
| 253 | PtwDetail — PtwApproverBar — 退回 modal | **原因 (textarea)** | other | setReason(value) — stores revision reason text | Text stored in state; char counter shown; confirm button enabled when length >= 10 | none | none | UI: text appears; confirm button enables at 10+ chars | screenshot with reason typed | src/components/ptw/PtwApproverBar.tsx:235-239 |
| 254 | PtwDetail — PtwApproverBar — 退回 modal | **確認 (button in revision modal)** | button | handleReasonAction() → requireStepUp('approval') → requestRevision(ptw.id, reason) RPC → PTW status → 'revision_requested' | PTW status changes to '已退回'; approver bar disappears (status no longer in_review); modal closes; submitter can re-edit and resubmit | mutation | reason.trim().length >= 10; requireStepUp must pass; disabled during submitting | UI: PTW status pill shows '已退回'; DB: permits_to_work.status = 'revision_requested' | screenshot of PtwDetail with '已退回' status pill | src/components/ptw/PtwApproverBar.tsx:135-158 |
| 255 | PtwDetail — PtwApproverBar — 退回 modal | **取消 (button in revision modal)** | button | setActiveAction(null); setReason(''); setError(null) | Modal closes; reason cleared; PTW unchanged | none | none | UI: modal absent; PTW status unchanged | screenshot of PtwDetail with modal closed | src/components/ptw/PtwApproverBar.tsx:245-248 |
| 256 | PtwDetail — PtwApproverBar — 管理員指派 modal | **確認 (button in admin_override modal)** | button | handleReasonAction() → requireStepUp('approval') → adminOverride(ptw.id, reason) RPC → advances chain step bypassing role check | Chain step advanced by admin; PTW may become 'active' if it was the final step; modal closes; approval timeline updated | mutation | reason.trim().length >= 10; requireStepUp must pass; admin only; safety_officer step cannot be overridden (server rejects) | UI: step appears in 簽核紀錄; PTW status may advance; DB: permit_approvals row with action_type 'admin_override' | screenshot of updated 簽核紀錄 after admin override | src/components/ptw/PtwApproverBar.tsx:135-158 |
| 257 | PtwDetail — PtwApproverBar — 管理員指派 modal | **取消 (button in admin_override modal)** | button | setActiveAction(null); setReason(''); setError(null) | Modal closes; PTW unchanged | none | none | UI: modal absent | screenshot of PtwDetail with modal closed | src/components/ptw/PtwApproverBar.tsx:245-248 |
| 258 | PtwDetail — PtwApproverBar — 簽署批准 modal — PtwSignaturePad | **簽名區 (canvas — approval signature draw)** | signpad | Draw signature on react-signature-canvas; on confirm: requireStepUp('approval') → requireSignReauth() → approve(ptw.id) RPC → record_ptw_signoff(p_ptw_id, p_signature_b64) RPC | Step-up auth prompt shown first; then sign re-auth password prompt; on success PTW advances chain step (or becomes 'active' if last step); signoff record created in permit_signoffs; SignatureProofCard appears in approval timeline; modal closes | mutation | requires step-up (requireStepUp) and sign re-auth (requireSignReauth); server: record_ptw_signoff asserts assert_sign_reauth; role checked again server-side | UI: approval step added to 簽核紀錄 list with '✓ 簽名'; PTW status may advance; DB: permit_approvals + permit_signoffs rows created | screenshot of approval timeline showing new signed step | src/components/ptw/PtwApproverBar.tsx:105-133 |
| 259 | PtwDetail — PtwApproverBar — 簽署批准 modal — PtwSignaturePad | **清除 (Eraser button — approval signature pad)** | button | handleClear() — clears approval signature canvas | Canvas wiped blank | none | disabled during submitting | UI: canvas blank after press | screenshot of blank canvas | src/components/ptw/PtwSignaturePad.tsx:64-71 |
| 260 | PtwDetail — PtwApproverBar — 簽署批准 modal — PtwSignaturePad | **確認簽名 (Check button — approval signature pad)** | button | handleSubmit() → validates not empty → onSign(b64) → handleSign(b64) in PtwApproverBar → requireStepUp → requireSignReauth → approve + record_ptw_signoff RPCs | PTW chain step advances; if final step PTW becomes 'active'; signoff recorded; modal closes | mutation | disabled when submitting; shows '請先簽名' if empty | UI: step appears in 簽核紀錄 with '✓ 簽名' marker; status may change to '生效中' | screenshot of updated 簽核紀錄 section | src/components/ptw/PtwSignaturePad.tsx:72-83, src/components/ptw/PtwApproverBar.tsx:105-133 |
| 261 | PtwDetail — PtwApproverBar — 簽署批准 modal — PtwSignaturePad | **取消 / 關閉 modal (X icon on pad header or modal onClose)** | icon-button | setActiveAction(null) — closes sign modal without action | Modal closes; PTW unchanged | none | none | UI: modal absent; PTW status unchanged | screenshot of PtwDetail with bar visible and modal gone | src/components/ptw/PtwApproverBar.tsx:212 |
| 262 | PtwDetail — PtwApproverBar (admin only, in_review, requiredRole !== 'safety_officer') | **管理員指派 (Shield button)** | button | setActiveAction('admin_override') — opens reason textarea modal | Modal '管理員指派' appears with textarea for reason (min 10 chars) | modal | isAdmin===true AND ptw.status === 'in_review' AND chain step requiredRole !== 'safety_officer' (safety officer steps are server-enforced non-overridable); disabled when online===false | UI: modal with '管理員指派' title visible (only for admin accounts) | screenshot of admin override modal (admin login required) | src/components/ptw/PtwApproverBar.tsx:196-209 |
| 263 | PtwDetail — PtwApproverBar (in_review only) | **簽署批准 (Check button)** | button | setActiveAction('sign') — opens PtwSignaturePad modal for approval signature | Modal '簽署批准' appears with PtwSignaturePad inside showing title '簽名以批准呢張工作許可證' | modal | canAct===true (admin OR role holder for current chain step OR delegate); ptw.status === 'in_review'; disabled when online===false; bar hidden entirely if status !== 'in_review' | UI: modal with signature pad visible | screenshot of approval signature modal | src/components/ptw/PtwApproverBar.tsx:165-173 |
| 264 | PtwDetail — PtwApproverBar (in_review only) | **退回 (ArrowLeftToLine button)** | button | setActiveAction('revision') — opens reason textarea modal | Modal '退回' appears with textarea for reason (min 10 chars) | modal | canAct===true; ptw.status === 'in_review'; disabled when online===false | UI: modal with '退回' title and textarea visible | screenshot of revision modal | src/components/ptw/PtwApproverBar.tsx:175-183 |
| 265 | PtwDetail — PtwApproverBar (in_review only) | **拒絕 (XIcon button)** | button | setActiveAction('reject') — opens reason textarea modal | Modal '拒絕' appears with textarea for reason (min 10 chars) | modal | canAct===true; ptw.status === 'in_review'; disabled when online===false | UI: modal with '拒絕' title and textarea visible | screenshot of rejection modal | src/components/ptw/PtwApproverBar.tsx:184-193 |
| 266 | PtwDetail — PtwQrCard (active, non-expired permit only) | **驗證 QR (QR code display — scannable by inspector)** | qr | Display only — QRCodeSVG renders URL: {window.location.origin}/#/verify/{token}; token is a server-minted JWT (mintPtwQrToken RPC → mint_ptw_jwt SECURITY DEFINER); scanning navigates inspector to PtwVerify page | 208x208px SVG QR code visible on screen; scanning with a logged-in device navigates to /verify/:token and triggers verifyPtwQrToken RPC (verify_ptw_jwt); scan is written to permit_scans audit table; PtwVerify shows '簽核有效' card with permit number, type, issue time, expiry | qr | only rendered when ptw.status === 'active' AND isPtwExpired(ptw) === false; qrToken fetched from server via mintPtwQrToken on mount | QR: scan with a second device logged in to the app; verify PtwVerify page shows '簽核有效' with correct PTW number and expiry; DB: new row in permit_scans; expired permit shows '已過期' | screenshot of QR card on PtwDetail; screenshot of PtwVerify result page after scan | src/components/ptw/PtwQrCard.tsx:24-29, src/pages/PtwDetail.tsx:47-53, src/lib/ptw-jwt.ts:16-23 |
| 267 | PtwDetail — PtwSignaturePad (close-out modal) | **簽名區 (canvas — touch/mouse draw)** | signpad | react-signature-canvas draw — user draws signature with finger/stylus/mouse on canvas (penColor #0f172a, h-44 sm:h-56); touch-none class prevents scroll-interference | Signature strokes drawn in dark navy on white canvas; canvas stores vector points; isEmpty() returns false once drawn | image | modal must be open (showCloseOut===true) | UI: strokes visible on canvas; confirm button enabled | screenshot of signature pad with drawn signature | src/components/ptw/PtwSignaturePad.tsx:52-59 |
| 268 | PtwDetail — PtwSignaturePad (close-out modal) | **清除 (Eraser button on signature pad)** | button | handleClear() — padRef.current.clear(); setError(null) | Signature canvas wiped blank; error message (if any) cleared | none | disabled when submitting===true | UI: canvas is blank after press | screenshot of blank canvas after clear | src/components/ptw/PtwSignaturePad.tsx:64-71 |
| 269 | PtwDetail — PtwSignaturePad (close-out modal) | **確認簽名 (Check button on signature pad)** | button | handleSubmit() — validates canvas not empty → getTrimmedCanvas().toDataURL('image/png') → strip prefix → call onSign(b64) → handleCloseOutSign(b64) → closeOut(ptw.id, b64) RPC | PTW status changes to 'closed_out'; modal closes; PTW detail shows '已完工' status pill; QR card disappears; '關閉許可證' button disappears | mutation | disabled when submitting===true; shows '處理中...' during submission; shows '請先簽名' error if canvas empty | UI: status pill shows '已完工'; DB: permits_to_work.status = 'closed_out'; signoff record created | screenshot of PtwDetail after close-out showing '已完工' status | src/components/ptw/PtwSignaturePad.tsx:72-83, src/pages/PtwDetail.tsx:87-95 |
| 270 | PtwDetail — PtwSignaturePad (close-out modal) | **取消 (X icon button on signature pad header)** | icon-button | onCancel() → setShowCloseOut(false) — closes modal without submitting | Close-out modal dismisses; PTW status unchanged | none | only rendered when onCancel prop is provided (always true from PtwDetail) | UI: modal no longer visible; PTW status unchanged | screenshot of PtwDetail with modal closed | src/components/ptw/PtwSignaturePad.tsx:44-47 |
| 271 | PtwDetail (non-hot_work active permit) | **關閉許可證 (button — outside fire-watch section)** | button | setShowCloseOut(true) — opens close-out signature modal | Modal '關閉工作許可證' appears with PtwSignaturePad inside | modal | only rendered when ptw.status === 'active' AND ptw.ptw_type !== 'hot_work'; disabled when online===false | UI: Modal with title '關閉工作許可證' and signature pad canvas visible | screenshot of close-out signature modal | src/pages/PtwDetail.tsx:281-287 |
| 272 | PtwList | **新增** | button | setShowForm(true) — opens PtwSubmitForm modal | PtwSubmitForm modal slides up; user can fill in PTW details | modal | canSubmit from PtwContext (admin OR approved project member with submitting role); rendered only when canSubmit === true | UI: modal with title '新增工作許可證' is visible on screen | screenshot of opened PtwSubmitForm modal | src/pages/PtwList.tsx:54 |
| 273 | PtwList | **狀態篩選器 (全部/草稿/簽核中/生效中/已完工/已過期/已退回/已拒絕)** | switcher | setFilter(value) — filters ptws by effectivePtwStatus | Card grid updates immediately to show only PTWs matching selected status pill; active pill turns safety-500 orange | none | none — visible to all authenticated users | UI: only cards whose effectivePtwStatus matches filter value are rendered; selected pill has bg-safety-500 class | screenshot showing filtered card list and highlighted pill | src/pages/PtwList.tsx:68-82 |
| 274 | PtwList | **搜尋編號 (text input)** | other | setSearch(value) — filters ptws by ptw.number substring match | Card grid updates in real time to show only cards whose number contains the typed string (case-insensitive) | none | none | UI: typing a PTW number substring leaves only matching cards visible | screenshot of filtered results with search term in input | src/pages/PtwList.tsx:86-92 |
| 275 | PtwList — PtwCard | **PTW 卡片 (整張卡片可點擊)** | link | React Router Link to /project/:id/ptw/:ptwId | Navigates to PtwDetail page for that permit | navigation | none — all authenticated members with project access see cards | UI: PtwDetail page loads with correct PTW number in title bar | screenshot of PtwDetail page after tap | src/components/ptw/PtwCard.tsx:47 |
| 276 | PtwSubmitForm (modal) | **許可證類型選擇按鈕 (熱工作/高空工作/吊運/密閉空間/挖掘/電氣/棚架/升降機)** | switcher | handleTypeChange(t) — sets ptwType state and resets checklist to checklistTemplate(t) | Selected type button gets border-safety-500 highlight; checklist section below resets to the type-specific zh-HK safety items; disabled types ('敬請期待') show greyed button and cannot be selected | none | PTW_TYPE_V1 list controls which types are enabled; v1 enables hot_work, work_at_height, lifting, confined_space, excavation, electrical, scaffold, lift | UI: selected button has border-2 border-safety-500 class; checklist items refresh to match type | screenshot of form with selected type highlighted and checklist updated | src/components/ptw/PtwSubmitForm.tsx:185-203 |
| 277 | PtwSubmitForm (modal) | **工作描述 (textarea)** | other | setDescription(value) — updates description state; char counter updates | Character counter at bottom-right updates (n/4000); text stored in form state | none | none | UI: text appears in textarea; counter reflects length | screenshot showing textarea with text and counter | src/components/ptw/PtwSubmitForm.tsx:208-218 |
| 278 | PtwSubmitForm (modal) | **安全核對清單項目 (每個核對項目均為可點擊按鈕)** | toggle | toggleCheck(key) — flips PtwChecklistItem.value between true and false | Item background changes green (checked) or amber/white (unchecked); green circle ✓ appears; all required (*) items must be checked before 提交 is enabled | none | none | UI: checked item has border-green-300 bg-green-50 class; unchecked required has border-amber-200 bg-amber-50; submit disabled if any required unchecked | screenshot of checklist with mix of checked/unchecked items | src/components/ptw/PtwSubmitForm.tsx:227-262 |
| 279 | PtwSubmitForm (modal) | **加入工人 (button)** | button | addWorkerRow() — appends {name:'', phone:'', photo:null} to workers array | New empty worker row appears below existing rows | none | none | UI: workers list gains one additional row | screenshot showing additional worker row | src/components/ptw/PtwSubmitForm.tsx:285-288 |
| 280 | PtwSubmitForm (modal) | **取消 (button)** | button | onClose() — closes modal without saving | Modal closes; form state is discarded; PtwList is unchanged | none | none | UI: modal no longer visible; PtwList shows same cards as before | screenshot of PtwList after cancel | src/components/ptw/PtwSubmitForm.tsx:317 |
| 281 | PtwSubmitForm (modal) | **提交 (submit button)** | button | handleSubmit() — sequential: createDraft → uploadPpePhotos → uploadScenePhotos → saveVersion → addWorker(s) → uploadWorkerPhoto(s) → submit(ptw_id); triggers PTW chain review | Progress messages shown ('建立草稿...', '上載 PPE 相片...', '儲存版本...', '提交簽核...'); on success modal closes and new PTW card appears in list with status '簽核中'; on error red banner shown | mutation | disabled when: description is empty, any required checklist item unchecked, validWorkers is empty, or online===false | UI: new PTW card appears in PtwList with status pill '簽核中'; DB row inserted in permits_to_work + permit_versions + permit_workers | screenshot of PtwList showing new card + screenshot of progress message during submit | src/components/ptw/PtwSubmitForm.tsx:78-171, 318-325 |
| 282 | PtwSubmitForm (modal) — PtwPhotoPicker (現場相片) | **加入相片 (現場相片 camera button)** | camera | inputRef.current.click() — same as PPE picker but for scenePhotos state | Native camera/file picker opens; images compressed; preview tiles appear for scene photos (work area + surroundings + fire extinguisher/signage); up to 6 max | image | disabled when files.length >= 6 or busy=true | UI: preview tiles appear in 3-column grid; counter increments | screenshot of form with scene photo preview tiles visible | src/components/ptw/PtwPhotoPicker.tsx:58-66 |
| 283 | PtwSubmitForm (modal) — PtwPhotoPicker (現場相片) | **刪除相片 (X icon on scene photo tile)** | icon-button | removeAt(idx) — removes scene photo from files array | Scene photo tile disappears; counter decrements | none | only visible when at least one scene photo added | UI: tile count decreases | screenshot after deletion showing reduced tile count | src/components/ptw/PtwPhotoPicker.tsx:83-90 |
| 284 | PtwSubmitForm (modal) — PtwPhotoPicker (PPE相片) | **加入相片 (PPE 相片 camera button)** | camera | inputRef.current.click() — opens hidden file input (accept=image/*, capture=environment, multiple) → compressImages → append to ppePhotos state | Native camera/file picker opens; selected images compressed to ~1920px JPEG 0.82; preview tiles appear in 3-column grid below (up to 6 max); button shows count (n/6) | image | disabled when files.length >= 6 or busy=true | UI: preview tiles appear; button counter increments; file count in state matches previews | screenshot of form with PPE photo preview tiles visible | src/components/ptw/PtwPhotoPicker.tsx:58-66 |
| 285 | PtwSubmitForm (modal) — PtwPhotoPicker (PPE相片) | **刪除相片 (X icon on each preview tile)** | icon-button | removeAt(idx) — removes photo from files array and revokes object-URL | Tile disappears from preview grid; button counter decrements; photo removed from submission payload | none | only visible when at least one PPE photo has been added | UI: tile count decreases by 1; specific tile no longer in grid | screenshot before and after deletion showing tile count change | src/components/ptw/PtwPhotoPicker.tsx:83-90 |
| 286 | PtwSubmitForm (modal) — WorkerRow | **工人相片 (avatar button per worker row)** | camera | fileRef.current.click() — opens hidden input (accept=image/*, capture=user/selfie); compressImage(maxEdge=1280, quality=0.78) → stored as worker.photo File | Front-facing camera opens (selfie mode on mobile); compressed image shown as preview in 48x48 avatar; stored for upload on submit | image | none — visible for every worker row | UI: avatar shows photo thumbnail instead of + icon after pick | screenshot of worker row with photo thumbnail visible | src/components/ptw/PtwSubmitForm.tsx:365-375, 344-354 |
| 287 | PtwSubmitForm (modal) — WorkerRow | **移除相片 (button shown when worker.photo is set)** | button | clearPhoto() — sets worker.photo=null, revokes object-URL, resets preview | Avatar reverts to + icon; photo removed from worker payload | none | only rendered when worker.photo !== null | UI: avatar shows + icon again; '移除相片' button disappears | screenshot showing avatar reverted to placeholder | src/components/ptw/PtwSubmitForm.tsx:399-403, 356-360 |
| 288 | PtwSubmitForm (modal) — WorkerRow | **工人姓名 (text input)** | other | updateWorker(idx, 'name', value) — updates worker name in state | Name stored in form state; at least one valid name required for submit to enable | none | none | UI: text appears; submit enables once description + checklist + worker name all satisfied | screenshot of worker row with name entered | src/components/ptw/PtwSubmitForm.tsx:386-390 |
| 289 | PtwSubmitForm (modal) — WorkerRow | **電話 (optional text input)** | other | updateWorker(idx, 'phone', value) — updates worker phone in state | Phone stored in form state (optional); passed to addWorker RPC | none | none | UI: text appears in phone field | screenshot of worker row with phone entered | src/components/ptw/PtwSubmitForm.tsx:391-396 |
| 290 | PtwSubmitForm (modal) — WorkerRow | **刪除工人 (Trash2 icon button per row)** | icon-button | removeWorker(idx) — removes worker at index from workers array | Worker row disappears; if only 1 row remains this button is hidden (canDelete=false) | none | only rendered when workers.length > 1 (canDelete prop) | UI: row count decreases; button absent when only 1 row remains | screenshot before and after deletion showing row count change | src/components/ptw/PtwSubmitForm.tsx:404-412 |
| 291 | PtwVerify | **查看詳情 (button on valid QR verify result)** | button | navigate('/project/:project_id/ptw/:permit_id') — navigates to PtwDetail using IDs decoded from JWT payload | PtwDetail page for the scanned permit loads | navigation | only rendered after successful QR verification (payload !== null AND no error); user must be logged in (unauthenticated users are redirected to /login first) | UI: PtwDetail page loads with correct PTW number | screenshot of PtwDetail after navigating from verify page | src/pages/PtwVerify.tsx:98-104 |
| 292 | PtwVerify (error state) | **返回首頁 (ArrowLeft button on verification failure)** | button | navigate('/home') — navigates to Home page | Home page loads | navigation | only rendered when verification error occurred (error !== null) | UI: Home page visible after press | screenshot of Home page after navigation | src/pages/PtwVerify.tsx:51-57 |

### 機械表格 (47)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 293 | EquipmentDetail | **← 機械登記冊 (back link)** | button | navigate(`/project/${projectId}/equipment`) | Navigates back to EquipmentList | navigation | any project member | URL changes to /project/:id/equipment; EquipmentList renders | screenshot of EquipmentList after navigation | src/pages/EquipmentDetail.tsx:95-100 |
| 294 | EquipmentDetail — AddFormModal | **法定表格 (select dropdown)** | menu | setTemplateId(value) — selects from available FormTemplates: options show `{code} — {name_zh}` | Selected template stored in state | none | AddFormModal is open (canManage) | Select shows chosen template code and zh-HK name | screenshot of modal with template selected | src/pages/EquipmentDetail.tsx:489-492 |
| 295 | EquipmentDetail — AddFormModal | **加入 (submit)** | button | submit() → addInstance(equipmentId, templateId) → supabase.from('form_instances').insert(…) | New form_instances row created; modal closes; EquipmentDetail shows new form card with template name, status badge, and 簽署 button | mutation | canManage; RLS re-checks on form_instances INSERT | New form card appears in EquipmentDetail instance list; DB row present in form_instances | screenshot of EquipmentDetail with newly added form card | src/pages/EquipmentDetail.tsx:474-481; src/contexts/EquipmentContext.tsx:219-248 |
| 296 | EquipmentDetail — AddFormModal | **取消** | button | onClose() → setShowAddForm(false) | Modal closes without changes | none | AddFormModal is open | Modal disappears | screenshot | src/pages/EquipmentDetail.tsx:499 |
| 297 | EquipmentDetail — equipment header card | **QR (icon + label button)** | button | setShowQr(v => !v) — toggles EquipmentQrCard visibility; on first show triggers mintEquipmentQrToken(equipmentId) via useEffect | EquipmentQrCard section appears below header. QRCodeSVG renders after token is minted (short loading spinner first). Shows ref_no + name_zh + attestation note. | qr | canManage === true | QR card visible; QRCodeSVG decoded URL matches /equipment-verify/<token> pattern; ref_no and name_zh shown correctly | screenshot of EquipmentQrCard with rendered QR code | src/pages/EquipmentDetail.tsx:121-129; src/pages/EquipmentDetail.tsx:134-144 |
| 298 | EquipmentDetail — EquipmentQrCard | **列印 (printer icon button)** | button | onPrint() → setShowPrint(true) → renders EquipmentQrPrintSheet with single card | EquipmentQrPrintSheet overlay opens with one QR card for this equipment | qr | canManage AND token is non-null (button only shown when `token && onPrint`) | Print sheet overlay shows exactly one QR card with correct labels | screenshot of EquipmentQrPrintSheet with single card | src/components/equipment/EquipmentQrCard.tsx:26-29; src/pages/EquipmentDetail.tsx:263-269 |
| 299 | EquipmentDetail — EquipmentQrPrintSheet (single card) | **列印 / 存 PDF** | button | window.print() | Browser print dialog; A4 single-cell QR card printed/saved | pdf | ready === true (token resolved) | Print preview shows one QR card; label Chinese text legible | screenshot of print preview | src/components/equipment/EquipmentQrPrintSheet.tsx:51-55 |
| 300 | EquipmentDetail — EquipmentQrPrintSheet (single card) | **關閉 (X)** | button | onClose() → setShowPrint(false) | Print sheet closes; EquipmentDetail remains | none | print sheet is open | Overlay gone; EquipmentDetail visible | screenshot after close | src/components/equipment/EquipmentQrPrintSheet.tsx:57-62 |
| 301 | EquipmentDetail — form instance card | **加入表格 (add form instance)** | button | setShowAddForm(true) → renders AddFormModal | AddFormModal opens with a select of available FormTemplates not yet attached to this equipment | modal | canManage AND availableTemplates.length > 0 | Modal heading '加入表格' visible; select populated with template code + name | screenshot of open AddFormModal | src/pages/EquipmentDetail.tsx:149-153 |
| 302 | EquipmentDetail — form instance card | **簽署 (enabled — credOk === true)** | button | setSigning(inst) → opens SignSheet modal for this FormInstance | SignSheet modal opens showing: checklist items, result picker, '下一步：簽名' button | modal | credOk === true (user has a verified UserCredential matching template.required_credential); otherwise the button is rendered disabled | SignSheet modal visible with template checklist items; header shows template name | screenshot of open SignSheet | src/pages/EquipmentDetail.tsx:203-210 |
| 303 | EquipmentDetail — form instance card | **簽署 (disabled — credOk === false)** | button | disabled — no handler; renders with cursor-not-allowed and amber warning text explaining the credential requirement | Button is visible but greyed out; amber banner shows '你未有已核實的「…」資格。請於「個人」頁上載證書並由管理員 / PM / 安全主任核實後再簽署。' | none | credOk === false (user lacks a verified credential for the template's required_credential type) | Button is in disabled state (opacity/cursor); amber warning text rendered below | screenshot of disabled 簽署 button with amber text | src/pages/EquipmentDetail.tsx:211-224 |
| 304 | EquipmentDetail — form instance card | **返回登記冊 (error / not-found state)** | button | navigate(`/project/${projectId}/equipment`) — shown when equipment record not found | Navigates to EquipmentList | navigation | eq === undefined after loading (equipment not found) | URL changes to /project/:id/equipment | screenshot of EquipmentList | src/pages/EquipmentDetail.tsx:81-86 |
| 305 | EquipmentDetail — form instance card — SignatureProofCard | **匯出簽名證明 (PDF)** | button | handleExport() → exportSignatureProofPdf(…) from src/lib/export.ts — renders HTML body via html2canvas + jsPDF → shareOrDownloadBlob | PDF file downloaded/shared. Filename: '簽名證明_<docNumber>_YYYYMMDD.pdf' | pdf | proof loaded successfully (get_signature_proof RPC returned data); any user who can view the form card | Open PDF: confirm all sections present, Chinese text not garbled (must use html2canvas path not jsPDF glyph subset), attestation_zh text is the zh-HK legal sentence from DB | screenshot of the PDF preview (all sections visible); note html2canvas renders Chinese via system fonts | src/components/SignatureProofCard.tsx:79-108; src/lib/export.ts:1060-1111 |
| 306 | EquipmentDetail — form instance card — SignatureProofCard | **簽名證明 integrity badge (記錄完整 / 記錄異常)** | other | display-only — shows tamper_evidence.integrity.intact; green badge if intact, red if broken | Green '記錄完整' badge or red '記錄異常' badge; if broken, additional red error text showing integrity reason | none | proof loaded; any viewer | Badge color matches integrity.intact boolean; error text visible when broken | screenshot of SignatureProofCard with badge | src/components/SignatureProofCard.tsx:138-145 |
| 307 | EquipmentDetail — SignSheet — PtwSignaturePad | **Signature canvas (draw area)** | signpad | User draws signature on canvas with finger/stylus; stored as base64 PNG in PtwSignaturePad component | Ink stroke appears on canvas; onSign callback can be triggered | none | showPad === true (SignSheet flow) | Drawing strokes appear on canvas | screenshot of signature pad with drawn signature | src/pages/EquipmentDetail.tsx:362-369 |
| 308 | EquipmentDetail — SignSheet — PtwSignaturePad | **確認簽署 (or equivalent confirm button in PtwSignaturePad)** | button | handleSign(signatureB64) → requireStepUp('form_signoff') → requireSignReauth() → supabase.rpc('record_form_signoff', {p_instance_id, p_result, p_payload, p_signature_b64}) → shareFormSignoffPdf(…) → modal closes | Step-up auth dialog + password re-auth dialog appear. On success: form_signoffs row created; form_instances.valid_until updated; PDF replica auto-generated and shared/downloaded; modal closes. If result='fail': equipment suspended + push notification to safety officer/PM. | pdf | credOk === true; step-up grant 'form_signoff'; sign reauth; RPC record_form_signoff server-side re-asserts both | DB: new form_signoffs row exists with correct instance_id, result, signed_by; form_instances.valid_until updated. PDF: open and verify Chinese text, signature image, all fields populated | screenshot of shared/downloaded PDF opened — verify first page shows Chinese header and signature block; also screenshot form card on EquipmentDetail showing updated status badge and SignatureProofCard | src/pages/EquipmentDetail.tsx:316-358; src/contexts/EquipmentContext.tsx:250-277; src/lib/export.ts:967-1030 |
| 309 | EquipmentDetail — SignSheet — PtwSignaturePad | **取消 (cancel signature)** | button | onCancel() → setShowPad(false) — returns to checklist view without signing | Signature pad dismissed; checklist view shown again; no RPC called | none | showPad === true | Checklist view re-appears with previously entered values intact | screenshot of checklist view after cancel | src/pages/EquipmentDetail.tsx:364-366 |
| 310 | EquipmentDetail — SignSheet modal | **下一步：簽名** | button | proceedToSign() — validates required checklist items are not null → setShowPad(true) → renders PtwSignaturePad | If validation passes: signature pad appears. If required items blank: error '請完成所有必填項目：…' shown. | modal | SignSheet is open | Signature canvas visible; or error message shown listing incomplete required items | screenshot of signature pad, or error banner | src/pages/EquipmentDetail.tsx:304-314; src/pages/EquipmentDetail.tsx:452-454 |
| 311 | EquipmentDetail — SignSheet modal | **Modal close (X / backdrop)** | button | onClose() → setSigning(null) | SignSheet modal dismissed; no signoff recorded | none | SignSheet is open | Modal closes; form card unchanged | screenshot of EquipmentDetail with no modal | src/pages/EquipmentDetail.tsx:362 |
| 312 | EquipmentDetail — SignSheet modal — checklist | **合格 (per-item pass toggle)** | button | setItem(c.key, true) — marks this checklist item as passed | Button turns green (bg-green-100 text-green-700 border-green-300); item stored as true in values state | none | SignSheet is open | Button background turns green for the tapped item | screenshot of checklist with at least one item marked 合格 | src/pages/EquipmentDetail.tsx:388-396 |
| 313 | EquipmentDetail — SignSheet modal — checklist | **不合格 (per-item fail toggle)** | button | setItem(c.key, false) — marks this checklist item as failed | Button turns red (bg-red-50 text-red-600 border-red-300); item stored as false | none | SignSheet is open | Button background turns red for the tapped item | screenshot of checklist with at least one item marked 不合格 | src/pages/EquipmentDetail.tsx:396-407 |
| 314 | EquipmentDetail — SignSheet modal — checklist | **備註 input (per checklist item)** | other | setRemarks(prev => ({ ...prev, [c.key]: e.target.value })) — stores optional remark per item | Text entered is stored in remarks state and will be included in the signoff payload and PDF replica | none | SignSheet is open | Text typed appears in input; remark appears in generated PDF checklist section | screenshot of input with text, then PDF showing remark in checklist row | src/pages/EquipmentDetail.tsx:409-415 |
| 315 | EquipmentDetail — SignSheet modal — result picker | **整體結果 segmented picker (合格 / 有備注合格 / 不合格)** | switcher | setResult(r) — sets FormSignoffResult to 'pass' ／ 'pass_with_remarks' ／ 'fail' | Selected result button highlighted; if 'fail' selected, red warning '標記不合格後，此機械會即時標記為「停用」，並通知安全主任及 PM' appears | none | SignSheet is open | Active result button visually highlighted; 'fail' triggers visible warning text | screenshot with '不合格' selected showing red warning | src/pages/EquipmentDetail.tsx:422-444 |
| 316 | EquipmentList | **返回工地** | button | navigate(`/project/${projectId}`) — navigates back to ProjectDetail | User is navigated to the parent ProjectDetail page | navigation | any authenticated member with access to the project | URL changes to /project/:id; ProjectDetail renders | screenshot of the ProjectDetail page after navigation | src/pages/EquipmentList.tsx:94-97 |
| 317 | EquipmentList | **新增機械** | button | setShowAdd(true) → renders AddEquipmentModal | AddEquipmentModal slides up (bottom-sheet on mobile, centered modal on sm+) | modal | canManage === true (admin OR assigned PM OR approved pm/main_contractor/safety_officer member) | Modal heading '新增機械' is visible; kind select + name input rendered | screenshot of open AddEquipmentModal | src/pages/EquipmentList.tsx:124-134 |
| 318 | EquipmentList | **匯出登記冊** | button | calls exportRegister() → EquipmentContext.exportRegister → importable exportEquipmentRegister(…) from src/lib/export.ts → shareOrDownloadBlob with .xlsx | XLSX file is downloaded (web) or shared via native share sheet (mobile). Filename pattern: <projectName>_機械表格_YYYYMMDD.xlsx | xlsx | equipment.length > 0 (button only shown when list is non-empty; exportRegister itself open to any viewer) | Open XLSX: confirm 2 sheets with zh-HK column headers and correct Chinese values in cells, no garbled characters | screenshot of downloaded file opened in Excel/Numbers showing both sheets with Chinese text | src/pages/EquipmentList.tsx:138-150; src/lib/export.ts:1384-1456 |
| 319 | EquipmentList | **列印全部 QR** | button | printAllQr() → mintEquipmentQrToken per equipment item in parallel (supabase.rpc('mint_equipment_jwt')) → setPrintCards → renders EquipmentQrPrintSheet overlay | Full-screen print overlay opens showing a 2-column grid of QR cards, one per equipment item. Each card shows QRCodeSVG + ref_no + name_zh. When all tokens are ready, 列印 / 存 PDF button becomes enabled. | qr | canManage === true AND equipment.length > 0 | Overlay shows grid of QR cards; QR values decode to valid equipment-verify URLs; each card name_zh matches the equipment record | screenshot of the EquipmentQrPrintSheet overlay with QR codes rendered | src/pages/EquipmentList.tsx:151-163; src/pages/EquipmentList.tsx:61-75; src/components/equipment/EquipmentQrPrintSheet.tsx |
| 320 | EquipmentList — AddEquipmentModal | **類別 (select dropdown)** | menu | setKind(value) — updates EquipmentKind state. Options: scaffold/棚架, excavation/挖掘工程, lifting_appliance/起重機械, swp/懸吊工作台, other/其他 | Selected kind is stored in form state; shown in select field | none | AddEquipmentModal is open (canManage) | Select shows the chosen zh-HK label | screenshot of modal with a specific kind selected | src/pages/EquipmentList.tsx:290-294 |
| 321 | EquipmentList — AddEquipmentModal | **新增 (submit)** | button | submit() → validates name_zh → supabase.rpc('next_equipment_ref') then supabase.from('equipment_register').insert(…) → refetch | New equipment row inserted in equipment_register; modal closes; equipment grid now shows new card with auto-generated ref_no (EQ-001 etc) | mutation | canManage; RLS + RPC re-check can_edit_project_progress server-side | New equipment card appears in grid with correct ref_no and name_zh; DB row present in equipment_register | screenshot of updated equipment grid with new card | src/pages/EquipmentList.tsx:265-273; src/contexts/EquipmentContext.tsx:185-217 |
| 322 | EquipmentList — AddEquipmentModal | **取消** | button | onClose() — calls setShowAdd(false) | Modal closes; no changes saved | none | AddEquipmentModal is open | Modal disappears; equipment list unchanged | screenshot showing modal is gone | src/pages/EquipmentList.tsx:318 |
| 323 | EquipmentList — AddEquipmentModal | **X 關閉 (icon button)** | icon-button | onClose() — same as 取消 | Modal closes without saving | none | AddEquipmentModal is open | Modal disappears | screenshot showing modal is gone | src/pages/EquipmentList.tsx:282 |
| 324 | EquipmentList — equipment card | **機械卡片 (equipment grid card)** | button | navigate(`/project/${projectId}/equipment/${eq.id}`) — navigates to EquipmentDetail | EquipmentDetail page loads for the selected equipment item | navigation | any project member who can view the list | URL changes to /project/:id/equipment/:equipmentId; EquipmentDetail header shows correct ref_no and name_zh | screenshot of EquipmentDetail page after navigation | src/pages/EquipmentList.tsx:187-233 |
| 325 | EquipmentList — EquipmentQrPrintSheet overlay | **列印 / 存 PDF** | button | window.print() — triggers browser print dialog with print-CSS applied (A4, 10mm margins, no-print elements hidden) | Browser print dialog opens; user can print to paper or save to PDF. Output: A4 grid of QR cards with equipment labels. disabled until all tokens minted (ready === true). | pdf | canManage; ready === true (all tokens resolved) | Print preview shows 2-col grid; QR codes are scannable; Chinese labels are legible | screenshot of print preview dialog or saved PDF opened showing QR grid | src/components/equipment/EquipmentQrPrintSheet.tsx:51-55 |
| 326 | EquipmentList — EquipmentQrPrintSheet overlay | **關閉 (X)** | button | onClose() → setPrintCards(null) in EquipmentList | Print sheet overlay dismissed; returns to EquipmentList | none | EquipmentQrPrintSheet overlay is open | Overlay disappears; equipment list visible | screenshot showing overlay gone | src/components/equipment/EquipmentQrPrintSheet.tsx:57-62 |
| 327 | EquipmentList — stat tiles (dashboard header) | **有效 / 即將到期 / 過期 / 未簽 / 停用 stat tiles** | other | display-only — shows counts from get_forms_dashboard RPC; no tap action/navigation | Five colored tiles showing integer counts for each FormStatus bucket | none | dashboard non-null (canManage not required; all members who can view see it) | Screenshot shows 5 tiles with correct colors (green/amber/red/site/red-dark) and non-negative integers | screenshot of the EquipmentList stat-tile row | src/pages/EquipmentList.tsx:107-115 |
| 328 | EquipmentList — VerifyCredentialsPanel | **核實 (per-credential button)** | button | verify(c) → requireStepUp('membership') → verifyCredential(c.id) (supabase.rpc('verify_user_credential')) → reload pending list | Step-up dialog appears; on success, credential's verified_at is set; the credential row disappears from pending list (or panel hides if none remain) | mutation | canManage === true (admin/PM/safety_officer); panel only renders when pending.length > 0 | DB row user_credentials.verified_at is now non-null; pending panel count decrements | screenshot showing credential row removed from panel after verification | src/components/VerifyCredentialsPanel.tsx:54-64; src/components/VerifyCredentialsPanel.tsx:100-109 |
| 329 | EquipmentVerify (QR scan deep link) | **返回首頁 (error state)** | button | navigate('/home') | Navigates to /home when QR token verification failed | navigation | error !== null after verifyEquipmentQrToken fails | URL changes to /home; Home page renders | screenshot of Home page | src/pages/EquipmentVerify.tsx:81-88 |
| 330 | EquipmentVerify (QR scan deep link) — form instance list | **去簽署 (per-due-form CTA)** | button | navigate(`/project/${projectId}/equipment/${result.equipment_id}`) — deep links to EquipmentDetail to sign the overdue form | EquipmentDetail page opens for the equipment; user can tap 簽署 on the due form | navigation | instance status in ['expiring','expired','missing','suspended'] AND projectId resolved | URL changes to /project/:id/equipment/:equipmentId; correct form card visible | screenshot of EquipmentDetail showing the form needing signature | src/pages/EquipmentVerify.tsx:145-160 |
| 331 | Profile page — CredentialsSection | **上載證書 (open add form)** | button | setShowAdd(true); setError('') — expands the credential add form inline | Inline credential upload form expands: credential type select, 證書名稱 input, 證書編號, 到期日, 發證機構, file picker | none | !showAdd; any authenticated user (own credentials) | Add form fields visible | screenshot of expanded add credential form | src/components/CredentialsSection.tsx:130-135 |
| 332 | Profile page — CredentialsSection — add form | **證書類別 (select dropdown)** | menu | setCredType(e.target.value) — selects from CREDENTIAL_TYPE_ZH keys (合資格人員/驗機師/etc.) | Selected credential type stored in state | none | add form is expanded | Select shows chosen zh-HK label | screenshot of select with a type chosen | src/components/CredentialsSection.tsx:140-145 |
| 333 | Profile page — CredentialsSection — add form | **選擇檔案 (file picker for credential document)** | filepicker | pickDoc(file) → uploadCredentialDoc(file, profile.id) → supabase.storage upload to credentials bucket → stores path | File upload in progress (Spinner + '上載中…'); on success: green badge '已上載：<filename>' appears; X button to remove shows. Accepts image/* and application/pdf. | mutation | add form is expanded; not currently uploading | Green '已上載' badge shows filename; doc_path state non-null | screenshot of green upload badge after successful upload | src/components/CredentialsSection.tsx:181-194 |
| 334 | Profile page — CredentialsSection — add form | **移除檔案 (X icon on uploaded doc badge)** | icon-button | setDocPath(null); setDocName('') — clears the uploaded file reference | Green badge disappears; file picker button reappears | none | docPath is non-null | Badge gone; file picker visible | screenshot after removal | src/components/CredentialsSection.tsx:171-178 |
| 335 | Profile page — CredentialsSection — add form | **上載 (submit credential)** | button | submit() → addMyCredential({user_id, credential_type, cert_name_zh, cert_no, issuer, valid_until, doc_path}) → supabase insert into user_credentials → reload list | New UserCredential row inserted with verified_at = null (pending); form collapses; credential list shows new entry with '待核實' amber badge | mutation | certName non-empty; not uploading; not saving | New credential row in credential list with '待核實' badge; DB row in user_credentials with verified_at = null | screenshot of credential list showing new '待核實' entry | src/components/CredentialsSection.tsx:59-77 |
| 336 | Profile page — CredentialsSection — add form | **取消 (close add form)** | button | setShowAdd(false); setError('') | Add form collapses; credential list shown | none | add form is expanded | Form hidden; list visible | screenshot | src/components/CredentialsSection.tsx:200-204 |
| 337 | Profile page — CredentialsSection — credential list row | **已上載證明 — thumbnail link (image doc)** | link | Opens signed URL in new tab (supabase.storage.createSignedUrl on credential doc bucket) — shows image thumbnail inline; click opens full image | New browser tab opens with the uploaded credential image | image | c.doc_path non-null; url resolved; not a PDF | New tab opens with credential image | screenshot of opened image in new tab | src/components/CredentialsSection.tsx:241-244 |
| 338 | Profile page — CredentialsSection — credential list row | **查看 PDF (PDF doc link)** | link | Opens signed URL in new tab for PDF credential document | New browser tab opens with the PDF credential document | pdf | c.doc_path non-null; url resolved; isPdf === true | New tab opens PDF viewer with the credential PDF | screenshot of PDF opened in new tab | src/components/EquipmentList.tsx:0 (CredentialsSection.tsx:246-249) |
| 339 | ProjectDetail 文書 tab SiVoSwitcher | **機械 / 表格 card（navigate to equipment）** | button | navigate to /project/:id/equipment | 導航到機械登記 / 表格頁面 | navigation | showEquipment = isModuleEnabled('equipment') AND (admin / assigned PM / approved pm·main_contractor·safety_officer) | 確認 URL 為 #/project/:id/equipment | screenshot of equipment page | src/pages/ProjectDetail.tsx:855-870 |

### 每日日誌 (18)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 340 | DailyEdit | **返回日誌列表 (ChevronLeft)** | button | navigate(`/project/${projectId}/daily`) | Navigates back to DailyList without saving | navigation | Always visible | URL changes to /project/:id/daily; list renders | screenshot of DailyList after tap | src/pages/DailyEdit.tsx:209-217 |
| 341 | DailyEdit | **複製琴日 (Copy icon)** | button | copyYesterday() — fetchMyDailyFor(yesterdayHKT()) then seedFrom(y); window.confirm if form has content | Form fields overwritten with yesterday's progress_item_ids, freeform_items, notes; shows amber banner if yesterday has no entry | mutation | canAuthorDaily (admin OR assigned PM OR approved membership in pm/main_contractor/general_foreman) | Checkboxes and text fields reflect yesterday's data; or amber '琴日冇你嘅日誌' notice appears | screenshot of form after copy showing pre-checked items | src/pages/DailyEdit.tsx:225-234 |
| 342 | DailyEdit | **搜尋進度項目 (Search input)** | other | setItemSearch(e.target.value) — filters visibleItems by code/title | Checkbox list filters to matching items in real-time | none | Always visible | Only items matching query appear in list | screenshot of filtered checkbox list | src/pages/DailyEdit.tsx:262-273 |
| 343 | DailyEdit | **進度項目 checkbox (per item)** | toggle | toggleItem(it.id) — adds or removes item id from selectedItemIds Set | Checkbox state flips; selected count updates; completeness meter reflects | none | Always visible in edit form | Checkbox checked/unchecked; count badge increments/decrements | screenshot showing checked items and updated count | src/pages/DailyEdit.tsx:278-306 |
| 344 | DailyEdit | **其他事項 — 移除此項 (Trash2 icon per row)** | icon-button | removeFreeformRow(i) — filters freeform array; resets to [''] if last row | That freeform text row is removed from the list | none | Always visible in edit form | Row disappears from the freeform list | screenshot of freeform list after removal | src/pages/DailyEdit.tsx:322-330 |
| 345 | DailyEdit | **新增一項 (Plus icon, freeform)** | button | addFreeformRow() — appends empty string to freeform array | New empty text input row appears in 其他事項 section | none | Always visible in edit form | New empty input row rendered below existing rows | screenshot showing additional input row | src/pages/DailyEdit.tsx:333-339 |
| 346 | DailyEdit | **備註 (textarea)** | other | setNotes(e.target.value) | Notes field updates; completeness meter 備註 pill turns green when non-empty | none | Always visible | Text appears in textarea; completeness pill state changes | screenshot of textarea with text and green 備註 pill | src/pages/DailyEdit.tsx:344-356 |
| 347 | DailyEdit | **取消 (sticky bottom bar)** | button | navigate(`/project/${projectId}/daily`) | Discards all edits; returns to DailyList | navigation | Always visible; disabled while submitting | URL returns to /project/:id/daily; no data saved | screenshot of DailyList after cancel | src/pages/DailyEdit.tsx:406-413 |
| 348 | DailyEdit | **儲存 (sticky bottom bar, btn-primary)** | button | onSave() — upsertMyDaily({...selectedItemIds, freeform_items, notes, weather:'晴', ...}); navigates to DailyList on success | Daily row upserted in DB; redirects to DailyList; new/updated card visible | mutation | canAuthorDaily; disabled while submitting or dateBlocked; shows spinner on submitting | DailyList shows the entry; supabase dailies table has the row for today | screenshot of DailyList showing saved card with correct fields | src/pages/DailyEdit.tsx:415-424 |
| 349 | DailyList | **返回項目** | button | navigate(`/project/${projectId}`) | Navigates back to ProjectDetail page | navigation | Always visible | URL changes to /project/:id; ProjectDetail renders | screenshot of ProjectDetail after tap | src/pages/DailyList.tsx:134-141 |
| 350 | DailyList | **日期 (date picker input)** | other | setSelectedDate(e.target.value ／／ today) — refetches dailies for chosen date via DailiesContext | List updates to show that date's dailies; max is today | mutation | Always visible | Daily cards update; heading changes to selected date | screenshot of list after date change | src/pages/DailyList.tsx:150-157 |
| 351 | DailyList | **返回今日** | button | setSelectedDate(today) — resets context date to todayHKT() | Date picker resets to today; list shows today's dailies | mutation | Only shown when selectedDate !== today | Date picker shows today's date; today's entries displayed | screenshot showing today's date selected | src/pages/DailyList.tsx:158-167 |
| 352 | DailyList | **編輯我嘅日誌 (Pencil icon)** | button | navigate(`/project/${projectId}/daily/edit`) | Navigates to DailyEdit page pre-seeded with the user's existing today row | navigation | Only shown on today's view AND profile.id === daily.user_id (own row) | DailyEdit page renders with existing data pre-filled | screenshot of DailyEdit with pre-populated fields | src/pages/DailyList.tsx:239-250 |
| 353 | DailyList | **填寫今日日誌 (FAB, Plus icon)** | fab | navigate(`/project/${projectId}/daily/edit`) | Navigates to DailyEdit page (blank form for new entry) | navigation | isToday && canAuthorDaily (admin OR assigned PM OR approved membership in pm/main_contractor/general_foreman) && !myDaily (no existing entry today) | DailyEdit page renders with empty fields | screenshot of blank DailyEdit form | src/pages/DailyList.tsx:258-267 |
| 354 | ProjectDetail 工具 tab | **工具 tab（Wrench icon）** | tab | setTab('tools') | 切換到 ToolsSwitcher 視圖，顯示天氣/日誌/物料/行事曆/聯絡人 cards | none | showToolsTab = isModuleEnabled('weather') ／／ isModuleEnabled('materials') ／／ isModuleEnabled('contacts') ／／ isModuleEnabled('timetable') ／／ isModuleEnabled('dailies') | 確認 tab active，ToolsSwitcher cards 顯示 | screenshot of tools tab | src/pages/ProjectDetail.tsx:375 |
| 355 | ProjectDetail 工具 tab ToolsSwitcher | **天氣記錄 card（navigate to weather）** | button | navigate to /project/:id/weather | 導航到 WeatherRecord 頁面 | navigation | isModuleEnabled('weather') AND profile?.global_role === 'admin'（天氣記錄為 admin only） | 確認 URL 為 #/project/:id/weather | screenshot of weather record page | src/pages/ProjectDetail.tsx:887-901 |
| 356 | ProjectDetail 工具 tab ToolsSwitcher | **每日日誌 card（navigate to daily）** | button | navigate to /project/:id/daily | 導航到每日日誌頁 | navigation | isModuleEnabled('dailies') = true | 確認 URL 為 #/project/:id/daily | screenshot of daily log page | src/pages/ProjectDetail.tsx:903-917 |
| 357 | ProjectDetail tab bar | **工具 tab** | tab | setTab('tools') | 工具 tab active; ToolsSwitcher renders showing 天氣/每日日誌/物料/行事曆/聯絡人 nav cards | navigation | isModuleEnabled('weather') ／／ isModuleEnabled('materials') ／／ isModuleEnabled('contacts') ／／ isModuleEnabled('timetable') ／／ isModuleEnabled('dailies') | tab active; ToolsSwitcher renders with relevant nav cards | screenshot of 工具 tab content | src/pages/ProjectDetail.tsx:375 |

### 物料 (14)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 358 | MaterialForm modal (create) | **提交** | button | handleSubmit() — createMaterial({name, unit, qty_needed, item_ids, planned_arrival_at, notes, urgent}); onSaved() on success | New material row created in DB; modal closes; new card appears in MaterialList | mutation | canSubmit (name and unit non-empty, qty > 0, not submitting); disabled otherwise | New card with correct name/status in list; supabase materials table has new row | screenshot of MaterialList showing new card | src/components/material/MaterialForm.tsx:176-185 |
| 359 | MaterialForm modal (create/edit) | **取消** | button | onClose() — sets creating/editing state to false/null; closes modal | Modal closes; no data saved | none | Always visible; disabled while submitting | Modal no longer rendered; list unchanged | screenshot of MaterialList after cancel with no new row | src/components/material/MaterialForm.tsx:168-175 |
| 360 | MaterialForm modal (create/edit) | **急件 toggle button** | toggle | setUrgent(v => !v) — toggles urgent flag; visual changes between '一般' and '急件 ✓' | Button turns red with '急件 ✓' text when active; grey '一般' when inactive | none | Always visible in MaterialForm | Button color and text reflect urgent state correctly | screenshot of toggle in both states | src/components/material/MaterialForm.tsx:253-265 |
| 361 | MaterialForm modal (create/edit) | **搜尋進度項目 (Search input in 用於進度項目)** | other | setItemSearch(e.target.value) — filters filteredItems by code/title | Item picker list filters in real-time | none | Always visible in MaterialForm | Only matching items shown in scroll list | screenshot of filtered item picker | src/components/material/MaterialForm.tsx:271-279 |
| 362 | MaterialForm modal (create/edit) | **進度項目 row toggle (per item in picker)** | button | toggleItem(i.id) — adds/removes item id from selectedItemIds array | Row highlighted in safety-100; check icon appears; count in section header increments | none | Always visible in item picker | Row styling changes; count badge updates; Check icon appears/disappears | screenshot of picker with one item selected vs deselected | src/components/material/MaterialForm.tsx:292-319 |
| 363 | MaterialForm modal (edit) | **儲存** | button | handleSubmit() — updateMaterial(material.id, {...}); onSaved() on success | Material row updated in DB; modal closes; card reflects new values | mutation | canSubmit (name and unit non-empty, qty > 0, not submitting); disabled otherwise | Card in list shows updated name/qty/notes; supabase row updated | screenshot of updated card in MaterialList | src/components/material/MaterialForm.tsx:176-185 |
| 364 | MaterialList | **篩選標籤 — 全部 / 已申請 / 部分到貨 / 已齊料 / 逾期** | switcher | setFilter(f.key) — filters visible materials array | Card list updates to show only materials matching selected status/late flag | none | Always visible; 5 pills horizontally scrollable | Only cards with matching status badge visible; active pill styled with safety-600 background | screenshot of filtered list with active pill highlighted | src/pages/MaterialList.tsx:194-213 |
| 365 | MaterialList | **加物料 (FAB, Plus icon)** | fab | setCreating(true) — opens MaterialForm in create mode | MaterialForm modal appears with blank fields | modal | canManage (admin OR assigned PM OR approved membership in pm/main_contractor/general_foreman/subcontractor) | Modal title shows '新增物料'; all fields empty | screenshot of blank MaterialForm modal | src/pages/MaterialList.tsx:248-258 |
| 366 | MaterialList — MaterialCard | **入貨 (Truck icon, btn-primary per card)** | button | onReceive(m) — sets receiving state; opens MaterialReceiveModal | MaterialReceiveModal appears overlaid, showing material name + current arrived/needed qty | modal | canMutate (isSupervisor OR profile.id === m.requested_by) AND m.status !== 'arrived' | Modal rendered with material.name and qty fields visible | screenshot of MaterialReceiveModal open | src/pages/MaterialList.tsx:111-119 |
| 367 | MaterialList — MaterialCard | **編輯 (Pencil icon per card)** | button | onEdit(m) — sets editing state; opens MaterialForm in edit mode | MaterialForm modal opens pre-populated with material's name, unit, qty, planned_arrival_at, urgent, item_ids, notes | modal | canMutate (isSupervisor OR profile.id === m.requested_by) | Modal title shows '編輯物料'; fields pre-filled | screenshot of MaterialForm modal with pre-filled data | src/pages/MaterialList.tsx:121-128 |
| 368 | MaterialList — MaterialCard | **刪除 (Trash2 icon per card)** | button | handleDelete(m) — window.confirm then deleteMaterial(m.id); updates list on success | Confirm dialog appears; on confirm, material row deleted from DB and removed from list | mutation | canMutate (isSupervisor OR profile.id === m.requested_by) | Card disappears from list; supabase materials table row gone | screenshot of list after deletion confirming card removed | src/pages/MaterialList.tsx:129-137 |
| 369 | MaterialReceiveModal | **取消** | button | onClose() — sets receiving state to null; closes modal | Modal closes; no change to qty_arrived | none | Always visible; disabled while submitting | Modal no longer rendered; card qty unchanged | screenshot of MaterialList after cancel | src/components/material/MaterialReceiveModal.tsx:64-70 |
| 370 | MaterialReceiveModal | **確認入貨** | button | handleSubmit() — receiveMaterial(material.id, qty); increments qty_arrived in DB; onDone() on success | qty_arrived increases by entered qty; status badge on card updates (partial/arrived); modal closes | mutation | validQty (qty > 0) && !submitting | Card qty display and status badge updated; supabase materials row shows new qty_arrived; preview preview in modal matched actual result | screenshot of card after 入貨 showing updated qty and status badge | src/components/material/MaterialReceiveModal.tsx:71-81 |
| 371 | ProjectDetail 工具 tab ToolsSwitcher | **物料 card（navigate to materials）** | button | navigate to /project/:id/materials | 導航到物料頁 | navigation | isModuleEnabled('materials') = true | 確認 URL 為 #/project/:id/materials | screenshot of materials page | src/pages/ProjectDetail.tsx:918-932 |

### 行事曆 (12)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 372 | EventForm modal (create/edit) | **類型 (select dropdown)** | menu | setEventType(e.target.value as EventType) — updates eventType state | Selected event type reflected in the dropdown; affects what is stored as event_type | none | Always visible in EventForm | Dropdown shows chosen type label (from EVENT_TYPE_ZH map) | screenshot of open dropdown with options visible | src/components/event/EventForm.tsx:161-169 |
| 373 | EventForm modal (create/edit) | **建立 / 儲存 (submit button)** | button | onSubmit(e) — validates title and starts_at; createEvent or updateEvent; onClose() on success | Event created/updated in DB; modal closes; new/updated entry appears in timetable grouped by date | mutation | Not disabled unless submitting/deleting; validation errors shown inline if title or starts_at missing | Entry with correct title and time appears under correct date group in timetable | screenshot of timetable showing new/updated event entry card | src/components/event/EventForm.tsx:137-145 |
| 374 | EventForm modal (create/edit) | **Modal close (X icon via Modal component onClose)** | icon-button | onClose() — setFormOpen(false); setEditing(null) | Modal closes without saving | none | Always visible (Modal component renders X) | Modal not rendered; timetable unchanged | screenshot after close showing timetable without modal | src/components/event/EventForm.tsx:121-124 |
| 375 | EventForm modal (edit) | **刪除** | button | onDelete() — window.confirm then deleteEvent(event.id); onClose() on success | Confirm dialog appears; event row deleted from DB; modal closes; entry disappears from timetable | mutation | isEdit (edit mode only); disabled while deleting or submitting | Timetable entry gone; supabase events table row deleted | screenshot of timetable after deletion confirming entry removed | src/components/event/EventForm.tsx:128-136 |
| 376 | ProjectDetail 工具 tab ToolsSwitcher | **行事曆 card（navigate to timetable）** | button | navigate to /project/:id/timetable | 導航到行事曆頁 | navigation | isModuleEnabled('timetable') = true | 確認 URL 為 #/project/:id/timetable | screenshot of timetable page | src/pages/ProjectDetail.tsx:933-947 |
| 377 | TimetablePage | **上週 (ChevronLeft, date range shift)** | button | shiftWeek(-1) — moves rangeFrom and rangeTo back 7 days; refetches timetable RPC | Date range shifts back one week; grouped day list updates | mutation | Always visible | Date inputs show previous week dates; timetable entries for that week shown | screenshot of timetable showing previous week dates | src/pages/TimetablePage.tsx:186-194 |
| 378 | TimetablePage | **本週** | button | resetThisWeek() — calls defaultHktWeekRange(); setRange(from, to) | Date range resets to current HKT week; list shows current week entries | mutation | Always visible | Date inputs reflect current Monday–Sunday; current week entries displayed | screenshot with current week dates in pickers | src/pages/TimetablePage.tsx:195-200 |
| 379 | TimetablePage | **下週 (ChevronRight, date range shift)** | button | shiftWeek(1) — moves rangeFrom and rangeTo forward 7 days; refetches timetable RPC | Date range shifts forward one week; grouped day list updates | mutation | Always visible | Date inputs show next week dates; timetable entries for that week shown | screenshot of timetable showing next week dates | src/pages/TimetablePage.tsx:201-210 |
| 380 | TimetablePage | **由 (date picker — range start)** | other | onFromChange(v) — localDateInputToIso(v); setRange(iso, rangeTo) | rangeFrom updates; list refetches and shows entries from new start date | mutation | Always visible | Date input reflects chosen date; entries grouped from that date | screenshot with new start date in picker and updated entry list | src/pages/TimetablePage.tsx:212-221 |
| 381 | TimetablePage | **至 (date picker — range end)** | other | onToChange(v) — localDateInputToIso(v, true); setRange(rangeFrom, iso) | rangeTo updates; list refetches and shows entries up to new end date | mutation | Always visible | Date input reflects chosen date; entries grouped up to that date | screenshot with new end date in picker and updated entry list | src/pages/TimetablePage.tsx:222-230 |
| 382 | TimetablePage | **新增事件 (FAB, Plus icon)** | fab | setEditing(null); setFormOpen(true) — opens EventForm in create mode | EventForm modal opens with blank fields, title '新增事件' | modal | canWrite (admin OR assigned-PM OR approved membership in pm/general_foreman/main_contractor) | EventForm modal renders with empty inputs | screenshot of blank EventForm modal | src/pages/TimetablePage.tsx:294-304 |
| 383 | TimetablePage — event entry card | **編輯 (Pencil icon on event-type entry)** | icon-button | openEditFor(entry) — finds matching Event by ref_id; setEditing(ev); setFormOpen(true) | EventForm modal opens pre-filled with event's title, type, starts_at, ends_at, location, description | modal | entry.source === 'event' AND canMutateEvent (profile.global_role admin/pm OR ev.created_by === profile.id) | EventForm modal renders; fields show existing event data | screenshot of EventForm modal with pre-filled event data | src/pages/TimetablePage.tsx:269-280 |

### 聯絡人 (15)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 384 | AddContactModal (create) | **加入** | button | handleSubmit() — validates name/trade/phone; createContact({name, trade, phone, notes}); onClose() on success | New contact row created in DB; modal closes; new card appears in ContactList | mutation | Not disabled unless saving; validation errors shown inline if required fields empty | New card with correct name, trade badge, phone link in list | screenshot of ContactList showing new contact card | src/components/contact/AddContactModal.tsx:128-134 |
| 385 | AddContactModal (create/edit) | **關閉 (X icon-button in modal header)** | icon-button | onClose() — setModalOpen(false); setEditing(null) | Modal closes; no data saved | none | Always visible | Modal not rendered; contact list unchanged | screenshot of ContactList after close with modal gone | src/components/contact/AddContactModal.tsx:58-60 |
| 386 | AddContactModal (create/edit) | **取消** | button | onClose() — setModalOpen(false); setEditing(null) | Modal closes; no data saved | none | Always visible; disabled while saving | Modal not rendered; contact list unchanged | screenshot of ContactList after cancel | src/components/contact/AddContactModal.tsx:122-127 |
| 387 | AddContactModal (edit) | **更新** | button | handleSubmit() — validates name/trade/phone; updateContact(initial.id, payload); onClose() on success | Contact row updated in DB; modal closes; card reflects updated values | mutation | Not disabled unless saving; validation errors shown inline if required fields empty | Card shows updated name/trade/phone; supabase contacts row updated | screenshot of ContactList showing updated contact card | src/components/contact/AddContactModal.tsx:128-134 |
| 388 | ContactList | **返回工地 (ChevronLeft)** | button | navigate(`/project/${projectId}`) | Navigates back to ProjectDetail page | navigation | Always visible | URL changes to /project/:id; ProjectDetail renders | screenshot of ProjectDetail after tap | src/pages/ContactList.tsx:63-68 |
| 389 | ContactList | **新增 (Plus icon, btn-primary header button)** | button | openCreate() — setEditing(null); setModalOpen(true) | AddContactModal opens in create mode with blank fields | modal | canManage (from ContactsContext — admin OR approved membership supervisor role) | Modal renders with title '新增聯絡人' and empty inputs | screenshot of blank AddContactModal | src/pages/ContactList.tsx:73-79 |
| 390 | ContactList | **搜尋 (Search input — 搜尋名/行頭/電話)** | other | setQuery(e.target.value) — filters contacts by name, trade, phone in real-time | Contact card list updates immediately to only show matching contacts | none | Always visible | Only cards matching query shown; empty state shown if no match | screenshot of filtered list | src/pages/ContactList.tsx:82-91 |
| 391 | ContactList | **全部 (trade filter pill)** | button | setTradeFilter(null) — clears trade filter | All contacts shown regardless of trade; pill styled with safety-500 | none | Shown only when trades.length > 0 | All contact cards visible; '全部' pill active | screenshot with '全部' active and full list visible | src/pages/ContactList.tsx:95-104 |
| 392 | ContactList | **行頭 filter pill (per unique trade, dynamic)** | button | setTradeFilter(t) — filters contacts to matching trade | Only contacts with that trade value shown; pill styled with safety-500 | none | Shown only when trades.length > 0; one pill per unique trade value in data | Only cards with matching trade badge shown | screenshot with trade pill active and filtered list | src/pages/ContactList.tsx:105-118 |
| 393 | ContactList — contact card | **電話號碼 link (Phone icon + number, tel: href)** | link | href={`tel:${c.phone.replace(/\s+/g,'')}`} — native phone dialer intent | Device opens phone dialer pre-filled with the contact's number; on mobile triggers a call | none | Always visible for all roles (read-only action) | Phone dialer opens on device with correct number; on web browser shows tel: prompt | screenshot of dialer opening with number pre-filled | src/pages/ContactList.tsx:147-151 |
| 394 | ContactList — contact card | **編輯 (Edit3 icon-button per card)** | icon-button | openEdit(c) — setEditing(c); setModalOpen(true) | AddContactModal opens in edit mode with contact's name, trade, phone, notes pre-filled | modal | canManage; shown only when confirmDeleteId !== c.id | Modal title shows '編輯聯絡人'; fields show contact's current data | screenshot of AddContactModal with pre-filled contact data | src/pages/ContactList.tsx:175-181 |
| 395 | ContactList — contact card | **刪除 (Trash2 icon-button per card — first tap)** | icon-button | setConfirmDeleteId(c.id) — shows inline confirm buttons replacing edit/delete icons | Confirm delete UI appears on that card (確認刪除 + 取消 buttons replace icons) | none | canManage; shown when confirmDeleteId !== c.id | Card shows 確認刪除 and 取消 buttons; edit/delete icons hidden | screenshot of card in confirm-delete state | src/pages/ContactList.tsx:182-188 |
| 396 | ContactList — contact card (confirm delete state) | **確認刪除** | button | handleDelete(c.id) — deleteContact(id); setConfirmDeleteId(null) | Contact row deleted from DB; card removed from list | mutation | canManage; shown when confirmDeleteId === c.id | Card disappears from list; supabase contacts table row deleted | screenshot of list after deletion showing card removed | src/pages/ContactList.tsx:160-166 |
| 397 | ContactList — contact card (confirm delete state) | **取消 (inline confirm cancel)** | button | setConfirmDeleteId(null) — reverts card to normal edit/delete icon state | Card returns to normal; edit/delete icon buttons reappear; contact not deleted | none | canManage; shown when confirmDeleteId === c.id | Card shows edit/delete icons again; contact still in list | screenshot of card back in normal state | src/pages/ContactList.tsx:167-173 |
| 398 | ProjectDetail 工具 tab ToolsSwitcher | **聯絡人 card（navigate to contacts）** | button | navigate to /project/:id/contacts | 導航到聯絡人頁 | navigation | isModuleEnabled('contacts') = true | 確認 URL 為 #/project/:id/contacts | screenshot of contacts page | src/pages/ProjectDetail.tsx:948-962 |

### 文件圖則 (88)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 399 | DocumentDetailSheet (from ProjectFiles) | **關閉 (X icon)** | icon-button | close() → onClearDeepLink() clears ?item/?doc search params + onClose() sets detail to null | Sheet dismissed; URL params cleared if present | none | sheet open | Sheet no longer visible; URL no longer contains ?doc or ?item | screenshot after dismiss | src/pages/ProjectFiles.tsx:770-778 |
| 400 | DocumentDetailSheet (from ProjectFiles) | **設定 / 更改 死線 (inline link button)** | button | setDueEditOpen(true) — shows date input + 儲存/取消 row inline | Date input field appears with current value pre-filled | none | canEditDue = canReview OR doc.created_by === profile.id | Date input visible in 送審死線 row | screenshot of inline edit | src/pages/ProjectFiles.tsx:795-802 |
| 401 | DocumentDetailSheet (from ProjectFiles) | **儲存 (due date)** | button | saveDue() → setReviewDueDate(doc.id, dueInput) RPC via DocumentsContext | review_due_date updated on documents row; date label refreshes; edit row closes | mutation | canEditDue | Displayed due date matches saved value; no error shown | screenshot of updated dead line label | src/pages/ProjectFiles.tsx:736-742, 814-820 |
| 402 | DocumentDetailSheet (from ProjectFiles) | **取消 (due date edit)** | button | setDueEditOpen(false) | Date input row collapses; no change saved | none | dueEditOpen === true | Date input no longer visible | screenshot | src/pages/ProjectFiles.tsx:822-828 |
| 403 | DocumentDetailSheet (from ProjectFiles) | **重新送審** | button | setResubmitOpen(true) → DocumentUploadSheet with existingDocumentId + suggestedRevisionLabel + rejectionNote | Upload sheet opens pre-filled with next version number and previous rejection reason banner | modal | allowVersionUpload AND currentVersion.status === 'rejected' | Sheet heading '上載新版本'; rejection note banner visible in red | screenshot of upload sheet with rejection banner | src/pages/ProjectFiles.tsx:837-845 |
| 404 | DocumentDetailSheet (from ProjectFiles) | **上載新版本** | button | setUploadVersionOpen(true) → DocumentUploadSheet with existingDocumentId | Upload sheet opens in new-version mode (heading '上載新版本') | modal | allowVersionUpload = canUpload AND (doc.document_type !== 'drawing' OR canUploadDrawingType) | Upload sheet visible with '上載新版本' heading | screenshot | src/pages/ProjectFiles.tsx:847-856 |
| 405 | DocumentDetailSheet (from ProjectFiles) | **查看 (per version row)** | button | setViewing(v) → opens DocumentViewer for that version | Full-screen DocumentViewer loads signed URL for the chosen version | signed-url | any user who can open the document detail sheet | Viewer opens; image/PDF visible; title + revision label shown in header | screenshot of DocumentViewer with content loaded | src/pages/ProjectFiles.tsx:889-896 |
| 406 | DocumentDetailSheet (from ProjectFiles) | **撤回 (per version row)** | button | window.confirm → requireStepUp('document') → withdrawVersion(v.id) | Confirm dialog; step-up auth prompt; on confirm version status becomes 'withdrawn'; version pill updates to 已撤回 | mutation | canWithdraw(v) = v.submitted_by === profile.id OR profile.global_role === 'admin'; status not already withdrawn/superseded | Version pill shows 已撤回 (red crossed-out); event timeline entry '已撤回' appears | screenshot of version list after withdrawal | src/pages/ProjectFiles.tsx:897-905, 703-716 |
| 407 | DocumentDetailSheet (from ProjectFiles) | **DocumentReviewBar — 批核** | button | requireStepUp('document') → reviewVersion(version.id, 'approve') | Step-up auth prompt; on success version status → 'approved'; pill turns green 已批准 | mutation | canReview AND version.status === 'submitted' | Version pill shows 已批准 (green); audit timeline shows '已批准' entry | screenshot of version pill + timeline after approval | src/components/documents/DocumentReviewBar.tsx:33-39, 63-71 |
| 408 | DocumentDetailSheet (from ProjectFiles) | **DocumentReviewBar — 拒絕** | button | setRejectOpen(true) — opens rejection-note modal | Rejection note modal appears with required textarea | modal | canReview AND version.status === 'submitted' | Modal visible with 拒絕原因（必填） textarea | screenshot of rejection modal | src/components/documents/DocumentReviewBar.tsx:73-83 |
| 409 | DocumentReviewBar — 拒絕文件 modal | **確認拒絕** | button | reject() → requireStepUp('document') → reviewVersion(version.id, 'reject', note) | Step-up auth; on success version status → 'rejected'; pill turns red 已拒絕; modal closes; review_note saved | mutation | canReview; note.trim() must be non-empty | Version pill shows 已拒絕; timeline entry '已拒絕' with note; rejection modal closed | screenshot of version pill + review note | src/components/documents/DocumentReviewBar.tsx:42-58, 131-143 |
| 410 | DocumentReviewBar — 拒絕文件 modal | **取消 (拒絕 modal)** | button | setRejectOpen(false) | Modal dismissed; no change | none | rejectOpen === true | Rejection modal no longer visible | screenshot | src/components/documents/DocumentReviewBar.tsx:124-130 |
| 411 | DocumentReviewBar — 拒絕文件 modal | **關閉 (X icon in reject modal)** | icon-button | setRejectOpen(false) | Modal dismissed; no change | none | rejectOpen === true; !busy | Modal not visible | screenshot | src/components/documents/DocumentReviewBar.tsx:105-112 |
| 412 | DocumentsSection (inline under ProgressItemCard) | **在文件總覽開啟 (ExternalLink icon link)** | link | Link to /project/:id/files?item=<leafItemId> → navigates to ProjectFiles with that group scrolled into view | Browser navigates to ProjectFiles page; 按進度項目 view is forced; matched group scrolls into view | navigation | files_enabled ON | URL is /project/:id/files?item=<id>; group header visible and scrolled to | screenshot of ProjectFiles with item group visible | src/components/documents/DocumentsSection.tsx:162-167 |
| 413 | DocumentsSection (inline) | **上載文件 (header button)** | button | setUploadSheet({ mode: 'new' }) → DocumentUploadSheet with progressItemId pre-linked | DocumentUploadSheet opens; 進度項目 picker hidden (pre-linked) | modal | canUpload | Sheet open; no item picker visible | screenshot of upload sheet | src/components/documents/DocumentsSection.tsx:168-176 |
| 414 | DocumentsSection (inline) | **上載文件 (empty-state button)** | button | setUploadSheet({ mode: 'new' }) | DocumentUploadSheet opens | modal | canUpload AND itemDocuments.length === 0 | Sheet visible | screenshot | src/components/documents/DocumentsSection.tsx:236-245 |
| 415 | DocumentsSection (inline) | **類型 chips — 全部 / 物料送審 / 施工方案 / 圖則 / 檢驗記錄 / 其他** | button | setTypeFilter(t) — filters itemDocuments by document_type | Grid narrows to matching documents; active chip darkens | none | files_enabled ON | Only documents of the selected type shown in grid | screenshot of filtered grid | src/components/documents/DocumentsSection.tsx:188-213 |
| 416 | DocumentsSection (inline) | **搜尋文件名稱 (text input)** | other | setSearchInput → debounced setSearch → filters by title | Grid filters to matching documents | none | files_enabled ON | Visible thumbnails match search term | screenshot | src/components/documents/DocumentsSection.tsx:215-228 |
| 417 | DocumentsSection (inline) | **DocumentThumbnail (grid tile click)** | button | openViewer(d) → setViewing({document, version}) → lazy-loads DocumentViewer | Full-screen DocumentViewer opens for that document's current version | signed-url | currentVersionFor(d) !== null | Viewer open; content loaded; title + revision label correct | screenshot of viewer | src/components/documents/DocumentsSection.tsx:252-258 |
| 418 | DocumentsSection (inline) | **更多動作 (MoreVertical icon — per tile)** | icon-button | setOpenMenuId(d.id) — toggles context menu for that document | Dropdown menu appears with 上載新版本 / 查看版本記錄 / 撤回 (conditional) | modal | canUpload | Menu visible with correct items | screenshot of open menu | src/components/documents/DocumentsSection.tsx:260-270 |
| 419 | DocumentsSection (inline) | **DocumentReviewBar — 批核 (inline under thumbnail)** | button | requireStepUp('document') → reviewVersion(version.id, 'approve') | Step-up auth; version approved; review bar disappears from tile | mutation | canReview AND v.status === 'submitted' | Review bar removed; tile status badge changes to 已批准 | screenshot of tile after approval | src/components/documents/DocumentsSection.tsx:316-317 |
| 420 | DocumentsSection (inline) | **DocumentReviewBar — 拒絕 (inline under thumbnail)** | button | setRejectOpen(true) — opens rejection-note modal | Rejection note modal appears | modal | canReview AND v.status === 'submitted' | Rejection modal visible | screenshot | src/components/documents/DocumentsSection.tsx:316-317 |
| 421 | DocumentsSection inline menu | **上載新版本 (menu item)** | menu | setUploadSheet({ mode: 'version', documentId: d.id }) + setOpenMenuId(null) | DocumentUploadSheet opens in version mode for that document | modal | canUpload AND (d.document_type !== 'drawing' OR canUploadDrawingType) | Sheet heading '上載新版本' | screenshot of upload sheet | src/components/documents/DocumentsSection.tsx:279-290 |
| 422 | DocumentsSection inline menu | **查看版本記錄 (menu item)** | menu | openViewer(d) + setOpenMenuId(null) → opens DocumentViewer → user can open version history from there | DocumentViewer opens for the current version; 版本記錄 button available in header | signed-url | canUpload | Viewer open; version history button accessible | screenshot of viewer | src/components/documents/DocumentsSection.tsx:291-299 |
| 423 | DocumentsSection inline menu | **撤回 (menu item)** | menu | onWithdraw(v) → window.confirm → withdrawVersion(v.id) via DocumentsContext | Confirm dialog; on confirm version withdrawn; thumbnail status pill updates to 已撤回 | mutation | canWithdraw(v) = v.submitted_by === profile.id OR admin; status not withdrawn/superseded | Tile status pill shows 已撤回 | screenshot of tile after withdrawal | src/components/documents/DocumentsSection.tsx:300-309 |
| 424 | DocumentUploadSheet (文件 — new document) | **關閉 (X icon)** | icon-button | close() → reset() + onClose(); blocked while isUploading | Sheet dismissed; slots/form cleared | none | !isUploading | Sheet not visible | screenshot | src/components/documents/DocumentUploadSheet.tsx:312-320 |
| 425 | DocumentUploadSheet (文件) | **📷 拍攝** | camera | onTakePhoto() → Capacitor Camera.getPhoto({source: Camera}) on native; falls back to cameraInputRef.current.click() on web | Camera opens; captured photo added to file slots list | none | canUpload | New file slot appears with photo filename and size | screenshot of file slot list | src/components/documents/DocumentUploadSheet.tsx:182-199, 333-339 |
| 426 | DocumentUploadSheet (文件) | **🖼️ 從相簿選擇** | camera | onPickFromAlbum() → Capacitor Camera.getPhoto({source: Photos}) on native; falls back to galleryInputRef.current.click() on web | Photo picker opens; chosen photo added to file slots | none | canUpload | File slot appears with picked photo | screenshot of file slot | src/components/documents/DocumentUploadSheet.tsx:201-218, 341-348 |
| 427 | DocumentUploadSheet (文件) | **📁 從檔案選擇** | filepicker | fileInputRef.current.click() → hidden <input accept='image/*,application/pdf' multiple> → onPickFiles | OS file picker opens; selected files (image or PDF, up to 5) added to slots | none | canUpload | File slots appear for each selected file | screenshot of file slot list | src/components/documents/DocumentUploadSheet.tsx:349-355 |
| 428 | DocumentUploadSheet (文件) | **類型 select (物料送審 / 施工方案 / 圖則 / 檢驗記錄 / 其他)** | menu | setDocType(value) — changes document_type; updates doc_number prefix preview | Selected type label shown; prefix hint below field updates (MAT/MS/DWG/INS/DOC) | none | !isNewVersion; 圖則 option only if canUploadDrawingType | Prefix label below field matches type (e.g. MAT- for 物料送審) | screenshot of select + prefix hint | src/components/documents/DocumentUploadSheet.tsx:386-402 |
| 429 | DocumentUploadSheet (文件) | **進度項目 select** | menu | setPickedItemId(value) — links document to a leaf progress item | Selected item code + title shown; document will attach to that item on upload | none | showItemPicker = !progressItemId AND !isNewVersion AND leafItems.length > 0 | Chosen item shown in select; uploaded document appears under that item's group | screenshot | src/components/documents/DocumentUploadSheet.tsx:403-420 |
| 430 | DocumentUploadSheet (文件) | **文件名稱 text input** | other | setTitle(value) | Title field updates; required for new document upload (validated on submit) | none | !isNewVersion | Title field accepts up to 120 chars | screenshot of filled field | src/components/documents/DocumentUploadSheet.tsx:422-431 |
| 431 | DocumentUploadSheet (文件) | **送審死線 date input** | other | setReviewDueDate(value) | Date field set; passed to uploadDocument as reviewDueDate | none | !isNewVersion | Field accepts a date value | screenshot | src/components/documents/DocumentUploadSheet.tsx:433-440 |
| 432 | DocumentUploadSheet (文件) | **版本標籤 text input** | other | setRevisionLabel(value) | Revision label field updated; passed as revisionLabel on upload | none | always visible in sheet | Field accepts up to 16 chars | screenshot | src/components/documents/DocumentUploadSheet.tsx:445-455 |
| 433 | DocumentUploadSheet (文件) | **移除 file slot (X per file row)** | icon-button | removeSlot(s.id) — removes that file from the upload queue | File row disappears from slots list | none | s.status !== 'uploading' | File no longer shown in slots list | screenshot | src/components/documents/DocumentUploadSheet.tsx:502-510 |
| 434 | DocumentUploadSheet (文件) | **取消** | button | close() → reset() + onClose() | Sheet dismissed; form reset | none | !isUploading | Sheet not visible | screenshot | src/components/documents/DocumentUploadSheet.tsx:525-530 |
| 435 | DocumentUploadSheet (文件) | **上載 (submit button)** | button | onSubmit() → uploadDocument() or uploadVersion() via DocumentsContext → Supabase Storage PUT + DB insert | Progress bar per file; on success slot shows 已上載; sheet closes; DocumentsContext realtime triggers list refresh | mutation | slots.length > 0 AND !slots.every(s => s.tooLarge) AND !submitting AND !isUploading | New document/version appears in the grid or list; no error shown | screenshot of updated document list / version list | src/components/documents/DocumentUploadSheet.tsx:224-299, 531-545 |
| 436 | DocumentVersionHistory sheet (文件) | **關閉 (X icon)** | icon-button | onClose() → setHistoryOpen(false) in DocumentViewer | Version history sheet closes; returns to viewer | none | sheet open | History sheet not visible; viewer visible | screenshot | src/components/documents/DocumentVersionHistory.tsx:84-93 |
| 437 | DocumentVersionHistory sheet (文件) | **版本行 (per version button)** | button | onSelect(v) → onSelectVersion(v) in DocumentViewer + onClose() | Sheet closes; DocumentViewer switches to show the selected version (new signed URL fetched) | signed-url | sheet open | Viewer header shows selected version revLabel; content changes | screenshot of viewer after version switch | src/components/documents/DocumentVersionHistory.tsx:110-119 |
| 438 | DocumentViewer (文件) | **📋 版本記錄 (History icon button)** | icon-button | setHistoryOpen(true) → renders DocumentVersionHistory sheet | 版本記錄 bottom sheet slides up showing all versions with status badges | modal | DocumentViewer open | Sheet shows version list with v1/v2 numbers, status badges, submitter names | screenshot of version history sheet | src/components/documents/DocumentViewer.tsx:99-107 |
| 439 | DocumentViewer (文件) | **關閉 (X icon)** | icon-button | onClose() — unmounts DocumentViewer | Viewer closes; returns to calling screen | none | DocumentViewer open | DocumentViewer no longer visible | screenshot | src/components/documents/DocumentViewer.tsx:109-115 |
| 440 | DocumentViewer (文件) | **DocumentReviewBar — 批核 (in viewer footer)** | button | requireStepUp('document') → reviewVersion(version.id, 'approve') | Step-up auth; version approved; footer bar disappears (status no longer submitted) | mutation | canReview AND version.status === 'submitted' | Review bar vanishes; version status updated in DocumentVersionHistory list | screenshot of viewer after approval | src/components/documents/DocumentViewer.tsx:183-185 |
| 441 | DocumentViewer (文件) | **DocumentReviewBar — 拒絕 (in viewer footer)** | button | setRejectOpen(true) — opens rejection-note modal over the viewer | Rejection note modal appears at z-[70] | modal | canReview AND version.status === 'submitted' | Modal visible on top of viewer | screenshot | src/components/documents/DocumentViewer.tsx:183-185 |
| 442 | DocumentViewer (文件) — PDF only | **上一頁 (PdfPageNavigator ChevronLeft)** | icon-button | onChange(current - 1) → setPageNum(pageNum - 1) in DocumentViewer | Previous PDF page renders in TransformWrapper | none | isPdf AND totalPages > 1 AND current > 1 | Page counter shows decremented number; PDF page changes | screenshot of viewer on page N-1 | src/components/drawings/PdfPageNavigator.tsx:15-23 |
| 443 | DocumentViewer (文件) — PDF only | **下一頁 (PdfPageNavigator ChevronRight)** | icon-button | onChange(current + 1) → setPageNum(pageNum + 1) | Next PDF page renders | none | isPdf AND totalPages > 1 AND current < total | Page counter increments; PDF page changes | screenshot of viewer on page N+1 | src/components/drawings/PdfPageNavigator.tsx:25-33 |
| 444 | DocumentViewer (文件) — zoom/pan | **Pinch-zoom / scroll-wheel zoom** | other | react-zoom-pan-pinch TransformWrapper handles wheel (step 0.2) + pinch (step 5); scale 0.5–6× | Content zooms in/out smoothly; double-tap resets to 1× | none | signedUrl loaded AND no urlError | Content visually scales; double-tap resets scale to 1 | screenshot at zoomed state | src/components/documents/DocumentViewer.tsx:131-138 |
| 445 | DocumentViewer (文件) — zoom/pan | **雙擊重設縮放** | other | TransformWrapper doubleClick={{ mode: 'reset' }} → scale back to initialScale 1 | View resets to 1× scale and centered position | none | signedUrl loaded | After zoom, double-tap returns to default scale | screenshot after double-tap | src/components/documents/DocumentViewer.tsx:132-135 |
| 446 | DrawingsSection (inline under ProgressItemCard) | **新增圖則 (header button)** | button | setUploadSheet({ mode: 'new' }) → DrawingUploadSheet with leafItemId | DrawingUploadSheet opens with heading '新增圖則' | modal | canUpload = admin OR assigned PM OR approved membership pm/main_contractor | Upload sheet visible with '新增圖則' heading | screenshot | src/components/drawings/DrawingsSection.tsx:157-164 |
| 447 | DrawingsSection (inline) | **新增圖則 (empty-state button)** | button | setUploadSheet({ mode: 'new' }) | DrawingUploadSheet opens | modal | canUpload AND itemDrawings.length === 0 | Sheet visible | screenshot | src/components/drawings/DrawingsSection.tsx:195-202 |
| 448 | DrawingsSection (inline) | **搜尋圖則標題 (text input)** | other | setSearchInput → debounced setSearch → filters itemDrawings by title | Grid narrows to matching drawings | none | always | Visible thumbnails match search term | screenshot | src/components/drawings/DrawingsSection.tsx:173-185 |
| 449 | DrawingsSection (inline) | **DrawingThumbnail (grid tile click)** | button | openViewer(d) → setViewing({drawing, version}) → lazy-loads DrawingViewer | Full-screen DrawingViewer opens for that drawing's current version | signed-url | currentVersionFor(d) !== null | Viewer open; content rendered; title + revLabel correct | screenshot of DrawingViewer with content | src/components/drawings/DrawingsSection.tsx:210-216 |
| 450 | DrawingsSection (inline) | **更多動作 (MoreVertical icon — per tile)** | icon-button | setOpenMenuId(d.id) — toggles context menu | Dropdown menu appears: 上載新版本 / 查看版本記錄 / 撤回 (conditional) | modal | canUpload | Menu visible | screenshot of open menu | src/components/drawings/DrawingsSection.tsx:218-229 |
| 451 | DrawingsSection inline menu | **上載新版本 (menu item)** | menu | setUploadSheet({ mode: 'version', drawingId: d.id }) + setOpenMenuId(null) | DrawingUploadSheet opens in version mode with heading '上載新版本' | modal | canUpload | Sheet heading '上載新版本' | screenshot | src/components/drawings/DrawingsSection.tsx:239-244 |
| 452 | DrawingsSection inline menu | **查看版本記錄 (menu item)** | menu | openViewer(d) + setOpenMenuId(null) → DrawingViewer opens | DrawingViewer opens; version history accessible via 📋 button | signed-url | canUpload | Viewer open | screenshot of viewer | src/components/drawings/DrawingsSection.tsx:245-251 |
| 453 | DrawingsSection inline menu | **撤回 (menu item)** | menu | onWithdraw(v) → window.confirm → withdrawVersion(v.id) via DrawingsContext | Confirm dialog; on confirm version status → 'withdrawn'; tile status updates to 已撤回 | mutation | canWithdraw(v) = v.uploaded_by === profile.id OR admin; status not withdrawn | Tile status badge shows 已撤回 (red line-through) | screenshot of tile | src/components/drawings/DrawingsSection.tsx:253-264 |
| 454 | DrawingUploadSheet | **關閉 (X icon)** | icon-button | close() → reset() + onClose(); blocked while isUploading | Sheet dismissed; slots/form cleared | none | !isUploading | Sheet not visible | screenshot | src/components/drawings/DrawingUploadSheet.tsx:248-256 |
| 455 | DrawingUploadSheet | **📷 拍攝** | camera | onTakePhoto() → Capacitor Camera on native; cameraInputRef fallback on web | Camera opens; captured photo appended to file slots | none | canUpload | New file slot with photo appears | screenshot of slot | src/components/drawings/DrawingUploadSheet.tsx:118-136, 261-267 |
| 456 | DrawingUploadSheet | **🖼️ 從相簿選擇** | camera | onPickFromAlbum() → Capacitor Camera Photos source; galleryInputRef fallback | Photo picker opens; chosen photo appended to slots | none | canUpload | File slot appears | screenshot | src/components/drawings/DrawingUploadSheet.tsx:138-155, 268-276 |
| 457 | DrawingUploadSheet | **📁 從檔案選擇** | filepicker | fileInputRef.current.click() → hidden <input accept='image/*,application/pdf' multiple> | OS file picker; selected files added to slots (max 5) | none | canUpload | Slot entries appear for each selected file | screenshot of slots | src/components/drawings/DrawingUploadSheet.tsx:277-284 |
| 458 | DrawingUploadSheet | **圖則標題 text input** | other | setTitle(value) | Title field updates; required for new drawing (validated on submit) | none | !isNewVersion | Field accepts up to 120 chars | screenshot | src/components/drawings/DrawingUploadSheet.tsx:312-325 |
| 459 | DrawingUploadSheet | **版本標籤 text input** | other | setRevisionLabel(value) | Revision label updated; passed as revisionLabel on upload | none | always visible | Accepts up to 16 chars | screenshot | src/components/drawings/DrawingUploadSheet.tsx:326-335 |
| 460 | DrawingUploadSheet | **移除 file slot (X per file row)** | icon-button | removeSlot(s.id) | File row removed from slot list | none | s.status !== 'uploading' | Slot not visible | screenshot | src/components/drawings/DrawingUploadSheet.tsx:383-390 |
| 461 | DrawingUploadSheet | **取消** | button | close() → reset() + onClose() | Sheet dismissed; form cleared | none | !isUploading | Sheet not visible | screenshot | src/components/drawings/DrawingUploadSheet.tsx:406-411 |
| 462 | DrawingUploadSheet | **上載 (submit button)** | button | onSubmit() → uploadDrawing() or uploadVersion() via DrawingsContext → Supabase Storage PUT + DB insert | Progress bar per file; all slots show 已上載; sheet closes; grid refreshes via realtime | mutation | slots.length > 0 AND !slots.every(s => s.tooLarge) AND !submitting | New drawing/version tile appears in DrawingsSection grid | screenshot of updated grid | src/components/drawings/DrawingUploadSheet.tsx:161-233, 413-426 |
| 463 | DrawingVersionHistory sheet | **關閉 (X icon)** | icon-button | onClose() → setHistoryOpen(false) in DrawingViewer | Version history sheet closes; viewer visible | none | sheet open | Sheet not visible | screenshot | src/components/drawings/DrawingVersionHistory.tsx:84-93 |
| 464 | DrawingVersionHistory sheet | **版本行 (per version button)** | button | onSelect(v) → onSelectVersion(v) in DrawingViewer; onClose() | Sheet closes; DrawingViewer switches to selected version (new signed URL fetched, page reset to 1) | signed-url | sheet open | Viewer header shows selected version revLabel; content changes to that version | screenshot of viewer after version switch | src/components/drawings/DrawingVersionHistory.tsx:106-116 |
| 465 | DrawingViewer | **📋 版本記錄 (History icon button)** | icon-button | setHistoryOpen(true) → renders DrawingVersionHistory sheet | 版本記錄 sheet slides up showing all versions with status badges | modal | DrawingViewer open | Sheet shows version rows with status (現行/已取代/已撤回), dates, uploader names | screenshot of version history sheet | src/components/drawings/DrawingViewer.tsx:94-103 |
| 466 | DrawingViewer | **關閉 (X icon)** | icon-button | onClose() — unmounts DrawingViewer | Viewer closes; returns to DrawingsSection | none | DrawingViewer open | Viewer not visible | screenshot | src/components/drawings/DrawingViewer.tsx:105-111 |
| 467 | DrawingViewer — PDF only | **上一頁 (PdfPageNavigator ChevronLeft)** | icon-button | onChange(current - 1) → setPageNum(pageNum - 1) | Previous PDF page shown | none | isPdf AND totalPages > 1 AND current > 1 | Page counter decrements; PDF page changes | screenshot on page N-1 | src/components/drawings/PdfPageNavigator.tsx:15-23 |
| 468 | DrawingViewer — PDF only | **下一頁 (PdfPageNavigator ChevronRight)** | icon-button | onChange(current + 1) → setPageNum(pageNum + 1) | Next PDF page shown | none | isPdf AND totalPages > 1 AND current < total | Page counter increments; PDF page changes | screenshot on page N+1 | src/components/drawings/PdfPageNavigator.tsx:25-33 |
| 469 | DrawingViewer — zoom/pan | **Pinch-zoom / scroll-wheel zoom** | other | TransformWrapper wheel (step 0.2) + pinch (step 5); scale 0.5–6× | Content scales smoothly; double-tap resets to 1× | none | signedUrl loaded AND no urlError | Content zooms; double-tap resets | screenshot at zoomed state | src/components/drawings/DrawingViewer.tsx:126-133 |
| 470 | DrawingViewer — zoom/pan | **雙擊重設縮放** | other | TransformWrapper doubleClick={{ mode: 'reset' }} | Scale resets to 1× | none | signedUrl loaded | Scale returns to 1 after double-tap | screenshot after double-tap | src/components/drawings/DrawingViewer.tsx:128-131 |
| 471 | PendingReviews 待我審批 | **刷新 (RefreshCw icon)** | icon-button | manualRefresh() → supabase.rpc('list_my_pending_reviews') re-fetched | Icon spins; list updates with latest pending reviews across all projects | mutation | files_enabled ON (FilesGate); canReview role | List count updates; newly submitted documents appear | screenshot of updated list | src/pages/PendingReviews.tsx:65-69, 88-96 |
| 472 | PendingReviews 待我審批 | **待審文件行 (PendingReview card button)** | button | navigate(`/project/${r.project_id}/files?doc=${r.document_id}`) — deep-link to that project's register | Navigates to ProjectFiles page; DocumentDetailSheet auto-opens for that document | navigation | files_enabled ON; reviewer role | URL is /project/:id/files?doc=<id>; document detail sheet open showing that document | screenshot of ProjectFiles with open detail sheet | src/pages/PendingReviews.tsx:121-126 |
| 473 | ProgressItemCard — kebab menu | **圖則 (n) or 文件 (n)** | menu | setDrawingsOpen(o => !o); if (!isOpen) onToggle(item.id); setMenuOpen(false) — toggles DrawingsSection or DocumentsSection in expanded detail | Item expands (if not already); DrawingsSection (drawings mode) or DocumentsSection (files mode) appears inline; shows upload + thumbnail list for this leaf | modal | isLeaf && fileCtxPresent (DrawingsContext or DocumentsContext mounted); visible to all roles who can see the item | DrawingsSection / DocumentsSection renders inside expanded detail area; drawing/document count badge reflects actual count | screenshot of expanded leaf with drawings/documents section open | src/components/ProgressItemCard.tsx:285-287 |
| 474 | ProjectDetail 文書 tab SiVoSwitcher | **文件 card（navigate to files）** | button | navigate to /project/:id/files | 導航到文件 / Documents 頁面 | navigation | isModuleEnabled('documents') = true（showFiles） | 確認 URL 為 #/project/:id/files | screenshot of files page | src/pages/ProjectDetail.tsx:841-854 |
| 475 | ProjectFiles 文件總覽 | **返回 (ChevronLeft)** | icon-button | navigate(`/project/${projectId}`) — goes back to ProjectDetail | User navigated back to ProjectDetail page | navigation | files_enabled module ON (FilesGate in App.tsx) | URL changes to /project/:id; ProjectDetail header visible | screenshot of ProjectDetail page after tap | src/pages/ProjectFiles.tsx:345-351 |
| 476 | ProjectFiles 文件總覽 | **刷新 (RefreshCw icon)** | icon-button | manualRefresh() → refetch() on DocumentsContext — re-queries documents + versions from Supabase | Icon spins briefly; document list reloads with latest data | mutation | files_enabled ON | Check that spinner shows then stops; new/updated documents appear | screenshot of list after refresh | src/pages/ProjectFiles.tsx:315-319, 366-374 |
| 477 | ProjectFiles 文件總覽 | **搜尋 標題 / 編號 (text input)** | other | setSearchInput → debounced setSearch (200ms) → filters filteredDocuments by title/doc_number | Document list narrows to matches; empty state shown if no match | none | files_enabled ON | Visible document rows match the typed term in title or doc_number | screenshot of filtered list | src/pages/ProjectFiles.tsx:385-393 |
| 478 | ProjectFiles 文件總覽 | **類型 chip — 全部** | button | setTypeFilter('all') — clears type filter | All document types shown; chip turns dark background | none | files_enabled ON | typeFilter chip '全部' has dark bg; all types visible in list | screenshot of chip row + list | src/pages/ProjectFiles.tsx:399 |
| 479 | ProjectFiles 文件總覽 | **類型 chips — 物料送審 / 施工方案 / 圖則 / 檢驗記錄 / 其他** | button | setTypeFilter(t) — filters filteredDocuments to matching document_type | Only documents of that type shown; chip activates | none | files_enabled ON | Each row's type badge matches the selected chip | screenshot of filtered list with active chip | src/pages/ProjectFiles.tsx:400-404 |
| 480 | ProjectFiles 文件總覽 | **狀態 chip — 全部** | button | setStatusFilter('all') — clears status filter | All statuses shown; chip activates | none | files_enabled ON | Status chip '全部' is active; all statuses visible | screenshot | src/pages/ProjectFiles.tsx:410 |
| 481 | ProjectFiles 文件總覽 | **狀態 chips — 待審 / 已批准 / 已拒絕** | button | setStatusFilter(s) — filters to displayStatusOf(d) === s | Only documents with that review status shown | none | files_enabled ON | All visible document pills match selected status | screenshot | src/pages/ProjectFiles.tsx:411-416 |
| 482 | ProjectFiles 文件總覽 | **檢視 selector (按進度項目 / 按類型 / 按狀態)** | switcher | setView(value) — changes grouping mode; regrouping is instant | Document list regroups by chosen dimension; section headers change | none | files_enabled ON | Section headers match selected view: item codes for 按進度項目, type names for 按類型, status names for 按狀態 | screenshot of each grouping | src/pages/ProjectFiles.tsx:422-434 |
| 483 | ProjectFiles 文件總覽 | **文件行 (DocumentRow card button)** | button | setDetail(d) — opens DocumentDetailSheet for the tapped document | DocumentDetailSheet modal slides up with version list, review controls, audit timeline | modal | files_enabled ON; any authenticated user with project access | Sheet header shows document title + DWSS ref; version list rendered | screenshot of open sheet | src/pages/ProjectFiles.tsx:556-595 |
| 484 | ProjectFiles 文件總覽 | **上載文件 FAB (+)** | fab | setUploadOpen(true) — opens DocumentUploadSheet | DocumentUploadSheet bottom sheet opens | modal | canUpload (DocumentsContext: admin OR assigned PM OR approved member with pm/main_contractor role) | Upload sheet visible with file-pick buttons | screenshot of upload sheet | src/pages/ProjectFiles.tsx:486-495 |
| 485 | ProjectFiles 文件總覽 | **上載文件 (empty-state button)** | button | setUploadOpen(true) — same as FAB, shown only in empty-state card | DocumentUploadSheet opens | modal | canUpload AND documents.length === 0 | Upload sheet visible | screenshot of upload sheet | src/pages/ProjectFiles.tsx:444-452 |
| 486 | ProjectFiles 文件總覽 — 不存在工地 error state | **回首頁 (button)** | button | navigate('/home') | Navigates to Home page | navigation | project not found in ProjectsContext (projectId param invalid) | URL is /home; Home page rendered | screenshot of Home page | src/pages/ProjectFiles.tsx:323-330 |

### dashboard (14)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 487 | Dashboard 儀表板 | **工地進度列（每個工地 card）** | link | navigate to /project/:id | 導航到該工地的 ProjectDetail 頁面 | navigation | admin 見全部；approved member / assigned PM 只見自己工地（visibleProjects filter） | 確認 URL 變成 #/project/:id，頁頭顯示工地名稱 | screenshot of ProjectDetail header with project name | src/pages/Dashboard.tsx:213-235 |
| 488 | Dashboard 儀表板 最近動態 | **動態列（有 link 的事件）** | link | navigate to ev.link (/project/:id or /project/:id/issue/:issueId) | 導航到相關問題或工地頁面 | navigation | 只對有 ev.link 的 ActivityEvent 項目（issue_created / issue_resolved / progress_updated） | 確認 URL 變換，頁面標題對應 | screenshot showing navigated page | src/pages/Dashboard.tsx:311-314 |
| 489 | WeatherRecord 天氣記錄 | **返回（ChevronLeft）** | icon-button | navigate back to /project/:id | 返回工地 ProjectDetail 頁面 | navigation | 所有用戶 | 確認 URL 為 #/project/:id | screenshot of ProjectDetail | src/pages/WeatherRecord.tsx:125 |
| 490 | WeatherRecord 天氣記錄 | **Excel 匯出** | button | runExport('xlsx') → exportWeatherEotToExcel() | 下載 / 分享 {project}_天氣EOT_{date}.xlsx；native 顯示 alert 確認儲存路徑 | xlsx | weatherOn = true AND claims.length > 0（按鈕才顯示）；任何能看此頁的用戶 | 打開 xlsx，確認 Sheet 名「天氣EOT記錄」，資料行數 = claims.length，EOT 總數正確，中文字可讀 | screenshot of opened xlsx showing Chinese headers and data rows | src/pages/WeatherRecord.tsx:130, src/lib/export.ts:1259 |
| 491 | WeatherRecord 天氣記錄 | **PDF 匯出** | button | runExport('pdf') → exportWeatherEotToPDF() | 下載 / 分享 {project}_天氣EOT_{date}.pdf；native 觸發系統分享表單 | pdf | weatherOn = true AND claims.length > 0；canManage 角色（pm/main_contractor/subcontractor/general_foreman/admin） | 打開 PDF，確認標題、表格中文可讀、頁腳日期正確 | preview_screenshot of rendered PDF showing Chinese title and table | src/pages/WeatherRecord.tsx:131, src/lib/export.ts:1309 |
| 492 | WeatherRecord 天氣記錄 — 每個日期 card | **記錄此日 / 編輯申索** | button | startEdit(date) — 展開 inline 表單（trigger, 關鍵路徑, 可施工 checkboxes, 善後日數, EOT日數, 備註） | 日期 card 展開 inline 編輯表單 | modal | canManage = true（pm/main_contractor/subcontractor/general_foreman/admin，且 weatherOn） | 確認 inline 表單出現，input 預填現有 claim 資料 | screenshot of expanded edit form | src/pages/WeatherRecord.tsx:163-164 |
| 493 | WeatherRecord 天氣記錄 — inline 編輯表單 | **觸發原因 input** | other | 更新 editing.trigger 文字 | input 值改變 | none | canManage = true，isEditing = true | 確認 input value 更新 | screenshot of filled input | src/pages/WeatherRecord.tsx:168 |
| 494 | WeatherRecord 天氣記錄 — inline 編輯表單 | **關鍵路徑 checkbox** | toggle | 更新 editing.on_critical_path boolean | checkbox 狀態切換 | none | canManage = true，isEditing = true | 確認 checkbox 狀態 | screenshot of checkbox state | src/pages/WeatherRecord.tsx:170 |
| 495 | WeatherRecord 天氣記錄 — inline 編輯表單 | **本可施工 checkbox** | toggle | 更新 editing.ready_to_work boolean | checkbox 狀態切換 | none | canManage = true，isEditing = true | 確認 checkbox 狀態 | screenshot of checkbox state | src/pages/WeatherRecord.tsx:171 |
| 496 | WeatherRecord 天氣記錄 — inline 編輯表單 | **善後日數 number input** | other | 更新 editing.tidy_days | 數字 input 值改變 | none | canManage = true，isEditing = true | 確認 input value | screenshot of filled number input | src/pages/WeatherRecord.tsx:174 |
| 497 | WeatherRecord 天氣記錄 — inline 編輯表單 | **申請 EOT 日數 number input** | other | 更新 editing.claim_days | 數字 input 值改變 | none | canManage = true，isEditing = true | 確認 input value | screenshot of filled number input | src/pages/WeatherRecord.tsx:175 |
| 498 | WeatherRecord 天氣記錄 — inline 編輯表單 | **備註 input** | other | 更新 editing.note 文字 | input 值改變 | none | canManage = true，isEditing = true | 確認 input value | screenshot of filled input | src/pages/WeatherRecord.tsx:177 |
| 499 | WeatherRecord 天氣記錄 — inline 編輯表單 | **儲存** | button | saveEdit() → supabase.from('project_weather_claims').upsert(...) | claim upsert 成功，表單收起，reload 資料，totalDays 更新 | mutation | canManage = true，isEditing = true | 確認 project_weather_claims 表對應 hkt_date 有新 / 更新 row；UI 顯示更新後的 claim 資料 | screenshot of updated claim row with new claim_days displayed | src/pages/WeatherRecord.tsx:179, src/pages/WeatherRecord.tsx:90-103 |
| 500 | WeatherRecord 天氣記錄 — inline 編輯表單 | **取消** | button | setEditing(null) — 關閉表單 | inline 表單收起，日期 card 恢復只讀顯示 | none | canManage = true，isEditing = true | 確認表單不再顯示 | screenshot of collapsed date card | src/pages/WeatherRecord.tsx:180 |

### home (7)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 501 | Home 首頁 | **待我審批 tile（PendingReviewsTile）** | link | navigate to /reviews | 導航到跨工地文件審批列表頁 | navigation | 只在 list_my_pending_reviews RPC 返回 count > 0 時顯示（文件模組啟用、用戶有待審批文件） | 確認 URL 為 #/reviews，頁面顯示待審批文件列表 | screenshot of reviews page | src/pages/Home.tsx:276-289 |
| 502 | Home 首頁 | **申請中 badge（pending memberships 數量）** | other | 顯示待審批申請數量；非可點擊 | amber badge 顯示數字 | none | myPending > 0 | 確認 badge 數字等於 pending memberships 數 | screenshot of home 我的工地 section header | src/pages/Home.tsx:211-215 |
| 503 | Home 首頁 工地 card | **日誌 pill（SitePills — 日誌狀態）** | other | 顯示今日日誌是否已填（get_my_site_status RPC 結果）；非可點擊 | 綠色 CheckCircle = 今日已填日誌；amber XCircle = 未填 | none | canAuthorDaily(profile, memberships, projects, projectId) = true 才顯示（pm/main_contractor/subcontractor/general_foreman/admin） | 確認 pill 顏色與 daily_done 狀態一致 | screenshot of home project card pills | src/pages/Home.tsx:50-56 |
| 504 | Home 首頁 工地 card | **進度 pill（SitePills — 進度狀態）** | other | 顯示今日是否已更新進度（progress_today）；非可點擊 | 綠色 CheckCircle = 已更新；灰色 Clock = 未更新 | none | canEditProgressClient(...) = true（pm/main_contractor/subcontractor/general_foreman/admin/assigned PM） | 確認 pill 狀態與 RPC 返回值一致 | screenshot of home project card pills | src/pages/Home.tsx:57-63 |
| 505 | Home 首頁 工地 card | **新文件 pill（SitePills — doc_24h）** | other | 顯示過去 24h 有新文件上傳；非可點擊 | 藍色 FileText pill = 有新文件 | none | doc_24h = true（由 RPC 返回，任何 approved member 可見） | 確認 pill 出現當 doc_24h=true | screenshot of home project card with 新文件 pill | src/pages/Home.tsx:64-70 |
| 506 | Home 首頁 我的工地 | **工地 card（每個項目）** | link | navigate to /project/:id | 導航到該工地的 ProjectDetail 頁面 | navigation | 只顯示 approved memberships + assigned PM 的工地；admin 見全部 | 確認 URL 為 #/project/:id | screenshot of ProjectDetail | src/pages/Home.tsx:231-252 |
| 507 | Home 首頁 最近決定 banner | **工地申請已通過 / 已拒絕 banner** | other | 顯示 24h 內的申請結果；非可點擊 | 綠色 approved / 紅色 rejected banner，顯示工地名及時間 | none | membership 在過去 24h 內有 approved/rejected 決定 | 確認 banner 顏色與狀態一致，時間正確 | screenshot of home page recent decisions | src/pages/Home.tsx:177-205 |

### admin (28)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 508 | AdminProjectChains 步驟 ChainStepRow | **上移（ChevronUp）** | icon-button | move(idx, -1) — 與上一步驟互換位置 | 步驟位置上移，step_order 重新編號 | none | canEdit = true，isFirst = false | 確認步驟順序改變 | screenshot of reordered steps | src/pages/AdminProjectChains.tsx:103-108 |
| 509 | AdminProjectChains 步驟 ChainStepRow | **下移（ChevronDown）** | icon-button | move(idx, 1) — 與下一步驟互換位置 | 步驟位置下移 | none | canEdit = true，isLast = false | 確認步驟順序改變 | screenshot of reordered steps | src/pages/AdminProjectChains.tsx:103-108 |
| 510 | AdminProjectChains 步驟 ChainStepRow | **移除步驟（X）** | icon-button | removeStep(idx) — 從 workingSteps 刪除此步驟 | 步驟行消失，其餘步驟 step_order 重新編號 | none | canEdit = true，workingSteps.length > 1（canRemove） | 確認步驟列表少一行 | screenshot of step list after removal | src/pages/AdminProjectChains.tsx:119-122 |
| 511 | AdminProjectChains 簽核流程設定 | **返回管理（ArrowLeft）** | button | navigate('/admin') | 導航回 AdminProjects 頁面 | navigation | global_role = admin | 確認 URL 為 #/admin | screenshot of AdminProjects | src/pages/AdminProjectChains.tsx:145-149 |
| 512 | AdminProjectChains 簽核流程設定 | **文書類型 tab（工地指令/變更指令/工作許可證）** | tab | setActiveTab(t) — 切換顯示對應文書類型的簽核流程 | 選中 tab 高亮，下方顯示該文書類型的簽核步驟列表 | none | global_role = admin 或本項目 PM | 確認 tab 高亮，步驟列表對應文書類型 | screenshot of active tab with steps | src/pages/AdminProjectChains.tsx:160-175 |
| 513 | AdminProjectChains 簽核流程設定 | **加入步驟** | button | addStep() — 在 workingSteps 末尾加入預設 main_contractor 步驟 | 步驟列表末尾新增一個步驟行（ChainStepRow），步驟數 + 1 | none | canEdit = true（admin 或本項目 PM） | 確認步驟列表多一行 | screenshot of step list with new row | src/pages/AdminProjectChains.tsx:217-221 |
| 514 | AdminProjectChains 簽核流程設定 | **預設範本** | button | loadDefaults() — 將 workingSteps 替換為 DEFAULTS[activeTab] | 步驟列表重設為預設（SI: MC→PM；VO: MC→PM→業主；PTW: 安全主任→MC） | none | canEdit = true | 確認步驟行數和角色與 DEFAULTS 一致 | screenshot of reset step list | src/pages/AdminProjectChains.tsx:222-227 |
| 515 | AdminProjectChains 簽核流程設定 | **儲存（Save icon）** | button | handleSave() → requireStepUp('approval') → saveChain(activeTab, workingSteps) | 成功：toast「已儲存簽核流程」；失敗：toast 顯示錯誤訊息；disabled when !dirty | mutation | canEdit = true，workingSteps.length >= 1，dirty = true，需 StepUp 驗證 | 確認 approval_chain_steps 表已更新；toast 顯示成功訊息 | screenshot of success toast after save | src/pages/AdminProjectChains.tsx:228-236, src/pages/AdminProjectChains.tsx:128-139 |
| 516 | AdminProjectModules 模組設定 | **返回管理（ArrowLeft）** | button | navigate('/admin') | 導航回 AdminProjects 頁面 | navigation | global_role = admin | 確認 URL 為 #/admin | screenshot of AdminProjects page | src/pages/AdminProjectModules.tsx:77-80 |
| 517 | AdminProjectModules 模組設定 | **模組 toggle 按鈕（每個非 core 模組）** | toggle | toggle(m.key, !enabled) → set_project_module RPC(p_project_id, p_module_key, p_enabled) | 按鈕狀態切換（已啟用綠/已關閉灰），toast 顯示「已啟用/關閉「模組名」」；disabled 顯示「...」期間 | mutation | global_role = admin；core 模組（進度）顯示「核心」標籤，不可切換 | 確認 DB project_modules 表對應 module_key 的 enabled 值已更新；toast 顯示正確中文訊息 | screenshot of module row with new toggle state and toast | src/pages/AdminProjectModules.tsx:120-130, src/pages/AdminProjectModules.tsx:57-71 |
| 518 | AdminProjects 工地管理 | **新增工地項目** | button | setCreateOpen(true) → 開啟 CreateProjectModal | CreateProjectModal 彈出 | modal | global_role = admin（AppLayout 已 ProtectedRoute 限制） | 確認 modal 出現，含工地名稱、分區等欄位 | screenshot of CreateProjectModal | src/pages/AdminProjects.tsx:49-53 |
| 519 | AdminProjects 工地管理 | **匯出 Excel（Download icon）** | icon-button | handleExport() → admin_list_user_profiles RPC → exportProjectsToExcel() | 下載 工地清單_{date}.xlsx | xlsx | global_role = admin | 打開 xlsx，確認 Sheet 名「工地清單」，行數 = projects.length，PM 名字正確，中文可讀 | screenshot of opened xlsx showing all columns and Chinese text | src/pages/AdminProjects.tsx:55-62, src/lib/export.ts:740 |
| 520 | AdminProjects 工地管理 | **刷新（RefreshCw icon）** | icon-button | manualRefresh() → refetch() from ProjectsContext | icon 旋轉動畫，完成後工地列表刷新 | mutation | global_role = admin | 確認列表重新載入，refreshing 狀態 false | screenshot of refreshed project list | src/pages/AdminProjects.tsx:64-69 |
| 521 | AdminProjects 工地管理 — 每個工地 card | **指派 PM** | button | setAssigning(p) → 開啟 AssignPMModal | AssignPMModal 彈出，顯示該工地的 PM 分配介面 | modal | global_role = admin | 確認 modal 出現，工地名稱正確 | screenshot of AssignPMModal | src/pages/AdminProjects.tsx:120-123 |
| 522 | AdminProjects 工地管理 — 每個工地 card | **簽核流程** | link | navigate to /admin/projects/:id/chains | 導航到 AdminProjectChains 頁面 | navigation | global_role = admin | 確認 URL 為 #/admin/projects/:id/chains | screenshot of AdminProjectChains page | src/pages/AdminProjects.tsx:125-132 |
| 523 | AdminProjects 工地管理 — 每個工地 card | **模組** | link | navigate to /admin/projects/:id/modules | 導航到 AdminProjectModules 頁面 | navigation | global_role = admin | 確認 URL 為 #/admin/projects/:id/modules | screenshot of AdminProjectModules page | src/pages/AdminProjects.tsx:133-140 |
| 524 | AdminProjects 工地管理 — 每個工地 card | **刪除（Trash2 icon）** | icon-button | setConfirmDelId(p.id) — 顯示確認按鈕 | 「確認刪除」和「取消」按鈕出現替代原刪除按鈕 | none | global_role = admin | 確認確認按鈕出現 | screenshot of confirm delete buttons | src/pages/AdminProjects.tsx:155-162 |
| 525 | AdminProjects 工地管理 — 確認刪除 | **確認刪除** | button | deleteProject(p.id) → supabase delete，setConfirmDelId(null) | 工地從列表移除；toast 或靜默成功 | mutation | global_role = admin，confirmDelId = p.id | 確認工地不再在列表中；DB 中 projects 表無此 id | screenshot of project list after deletion (project absent) | src/pages/AdminProjects.tsx:141-146 |
| 526 | AdminProjects 工地管理 — 確認刪除 | **取消（刪除確認）** | button | setConfirmDelId(null) — 收起確認按鈕 | 確認按鈕消失，回復正常按鈕組 | none | global_role = admin，confirmDelId = p.id | 確認 Trash2 按鈕重新出現 | screenshot of restored card buttons | src/pages/AdminProjects.tsx:147-151 |
| 527 | AdminUsers 用戶管理 | **搜尋 input（姓名 / 手機 / 公司）** | other | setSearch(value) — 即時過濾用戶列表 | 列表即時收窄至匹配用戶 | none | global_role = admin | 輸入姓名片段後確認列表只顯示匹配用戶 | screenshot of filtered user list | src/pages/AdminUsers.tsx:107-113 |
| 528 | AdminUsers 用戶管理 | **刷新（RefreshCw icon）** | icon-button | manualRefresh() → admin_list_user_profiles RPC | icon 旋轉，列表刷新顯示最新用戶資料 | mutation | global_role = admin | 確認列表重新載入 | screenshot of refreshed user list | src/pages/AdminUsers.tsx:114-120 |
| 529 | AdminUsers 用戶管理 | **角色過濾 chips（全部/管理員/PM/…）** | switcher | setRoleFilter(f.value) — 過濾用戶列表 | 列表即時收窄到所選角色，選中 chip 變 orange 底色，每個 chip 括號內顯示該角色用戶數 | none | global_role = admin；chips: 全部/管理員/項目經理/工地主任/老總/分判/分判工人/業主/安全主任/地盤主任（9 個 chip） | 點擊「項目經理」chip，確認只顯示 pm 角色用戶 | screenshot of filtered list with active chip highlighted | src/pages/AdminUsers.tsx:125-139 |
| 530 | AdminUsers 用戶管理 — 每個用戶 card | **編輯角色** | button | setEditing(u) → 開啟 EditRoleModal | EditRoleModal 彈出，顯示用戶名稱、電話、現有角色 | modal | global_role = admin；disabled when u.id === profile.id（不能改自己） | 確認 modal 出現，用戶名稱正確，角色選項包含 8 個全域角色 | screenshot of EditRoleModal | src/pages/AdminUsers.tsx:166-171 |
| 531 | AdminUsers 用戶管理 — 每個用戶 card | **查看待處理簽核** | button | setViewingInFlight(u) → 開啟 InFlightApprovalsModal | InFlightApprovalsModal 彈出，顯示該用戶的待處理 SI/VO/PTW 審批 | modal | global_role = admin | 確認 modal 出現，userId 為選定用戶 | screenshot of InFlightApprovalsModal | src/pages/AdminUsers.tsx:173-180 |
| 532 | EditRoleModal（AdminUsers） | **角色選擇按鈕（8個：管理員/PM/地盤主任/老總/安全主任/分判/分判工人/業主）** | switcher | setRole(r)；若 r !== 'main_contractor' 同時 setSubRole(null) | 所選角色按鈕高亮（orange border），若選 main_contractor 顯示職位子選項 | none | EditRoleModal 開啟時 | 確認選中按鈕有 border-safety-500 樣式，main_contractor 才顯示職位選項 | screenshot of EditRoleModal with role selected | src/pages/AdminUsers.tsx:252-264 |
| 533 | EditRoleModal（AdminUsers） | **儲存** | button | save() → requireStepUp('membership') → admin_update_user_role RPC(p_target, p_global_role, p_sub_role) | 成功：modal 關閉，列表刷新，用戶角色已更新；若 requireStepUp 失敗則無操作 | mutation | global_role = admin；需通過 StepUp 驗證（step_up_enforced flag） | 確認 user_profiles 表該用戶 global_role/sub_role 已更新，列表刷新後顯示新角色 chip | screenshot of updated user row with new role chip | src/pages/AdminUsers.tsx:219-232, src/pages/AdminUsers.tsx:241 |
| 534 | EditRoleModal（AdminUsers） | **關閉 modal（onClose / X）** | button | setEditing(null) — 關閉 modal | modal 消失，不作任何保存 | none | EditRoleModal 開啟時 | 確認 modal 不再顯示，用戶資料未變 | screenshot of closed modal | src/pages/AdminUsers.tsx:189 |
| 535 | EditRoleModal（AdminUsers）— main_contractor 選中時 | **職位選擇（工程師/工頭/安全）** | switcher | setSubRole(s) — 設定 sub_role | 選中職位高亮 | none | role = 'main_contractor' | 確認選中職位有 orange border | screenshot of sub-role selection | src/pages/AdminUsers.tsx:269-282 |

### assistant (7)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 536 | AssistantPanel AI助理 | **問 AI 站長… input（Enter 鍵送出）** | other | onKeyDown Enter → send() — 發送訊息到 ai-assistant Edge Function（SSE stream） | 用戶訊息 bubble 出現，AI 開始串流回覆（助理 bubble 逐字出現），tool 狀態提示顯示（查緊進度表… 等） | none | aiEnabled（assistant module on + feature flag）；!busy && !pending | 確認用戶 bubble 出現，AI bubble 開始顯示文字，tool status label 閃現後消失 | screenshot of chat with user and assistant bubbles | src/components/assistant/AssistantPanel.tsx:181 |
| 537 | AssistantPanel AI助理 | **傳送（Send icon button）** | icon-button | send() — 發送 input 文字到 ai-assistant Edge Function | 同 Enter 鍵效果：用戶 bubble，AI 串流回覆 | none | !busy && !pending && input.trim() !== '' | 確認 AI 有回覆訊息 | screenshot of chat with assistant response | src/components/assistant/AssistantPanel.tsx:184 |
| 538 | AssistantPanel AI助理 — 待確認動作 card | **確認（Check icon）** | button | confirmAction() → requireStepUp(pending.step_up_class) → 發送 {confirm: {action_id, tool_use_id, args_hash}} 到 Edge Function 執行動作 | pending card 消失，AI 執行動作並串流結果回覆（如更新進度、新增物料等）；若 step_up 失敗顯示「二步驗證未完成，已取消動作」 | mutation | pending !== null；risk='destructive' 時需先輸入「刪除」確認；有 step_up_class 時需 StepUp | 確認 AI 執行動作後相關 DB 資料已改變，AI bubble 顯示執行結果 | screenshot of AI response after confirmation | src/components/assistant/AssistantPanel.tsx:166, src/components/assistant/AssistantPanel.tsx:106-128 |
| 539 | AssistantPanel AI助理 — 待確認動作 card | **取消（X icon）** | button | cancelAction() — setPending(null)，AI 回覆「好，唔做住。」 | pending card 消失，AI bubble 顯示「好，唔做住。」 | none | pending !== null，!busy | 確認 pending card 不再顯示，AI 有「好，唔做住。」回覆 | screenshot showing cancelled action message | src/components/assistant/AssistantPanel.tsx:167, src/components/assistant/AssistantPanel.tsx:130-133 |
| 540 | AssistantPanel AI助理 — 待確認動作 card（risk=destructive） | **輸入「刪除」確認 text input** | other | setDelConfirm(value) — 鍵入「刪除」才解鎖確認按鈕 | 當 delConfirm.trim() === '刪除' 時確認按鈕解除 disabled | none | pending.risk === 'destructive' | 確認空白時確認 disabled；輸入「刪除」後啟用 | screenshot of confirm input with delete typed | src/components/assistant/AssistantPanel.tsx:163 |
| 541 | ProjectDetail 助理 tab | **助理 tab（Bot icon）** | tab | setTab('assistant') | 切換到 AssistantPanel 視圖；panel 保持 MOUNTED（對話不重置） | none | showAssistantTab = aiEnabled AND isModuleEnabled('assistant') | 確認 tab active，聊天介面顯示 | screenshot of assistant tab with chat panel | src/pages/ProjectDetail.tsx:376 |
| 542 | ProjectDetail tab bar | **助理 tab** | tab | setTab('assistant') | 助理 tab active; AssistantPanel mounts (lazy Suspense); chat interface visible | navigation | aiEnabled && isModuleEnabled('assistant') | tab active; AssistantPanel rendered (not display:none) | screenshot of 助理 tab | src/pages/ProjectDetail.tsx:376 |

### nav (25)

| # | 螢幕 | 控件 | 類型 | 動作 | 預期結果 | 輸出 | 權限/條件 | 點驗 | Capture | source |
|---|---|---|---|---|---|---|---|---|---|---|
| 543 | AppLayout header（所有 AppLayout 頁面） | **教學 HelpButton（pill）** | button | 開啟教學 modal / overlay（HelpButton component） | 教學面板顯示 | modal | 所有登入用戶 | 確認教學面板出現 | screenshot of help panel | src/components/AppLayout.tsx:35 |
| 544 | BottomNav（手機底部導航） | **首頁 tab** | link | navigate to /home | 導航到 Home，icon 變 orange | navigation | 所有登入用戶（手機，md 以下顯示） | 確認 URL 為 #/home，首頁 icon active | screenshot of home with bottom nav active | src/components/BottomNav.tsx:19 |
| 545 | BottomNav（手機底部導航） | **工地 tab** | link | navigate to /projects | 導航到 Projects 列表頁 | navigation | 所有登入用戶 | 確認 URL 為 #/projects | screenshot of projects with bottom nav | src/components/BottomNav.tsx:20 |
| 546 | BottomNav（手機底部導航） | **管理 tab** | link | navigate to /admin | 導航到 AdminProjects 頁面 | navigation | global_role = admin | 確認 URL 為 #/admin | screenshot of admin page with bottom nav | src/components/BottomNav.tsx:23 |
| 547 | BottomNav（手機底部導航） | **個人 tab** | link | navigate to /profile | 導航到個人資料頁 | navigation | 所有登入用戶 | 確認 URL 為 #/profile | screenshot of profile with bottom nav | src/components/BottomNav.tsx:24 |
| 548 | BottomNav（手機底部導航）— 在工地 scope 內 | **簽核 tab（SignoffNavLink）** | link | navigate to /project/:id/si | 導航到 SI 列表頁 | navigation | projectId 存在 AND (isModuleEnabled('si') OR isModuleEnabled('vo') OR isModuleEnabled('ptw')) | 確認 URL 為 #/project/:id/si，簽核 tab active | screenshot of SI list with bottom nav | src/components/BottomNav.tsx:66-70 |
| 549 | Sidebar（桌面導航） | **首頁 nav link** | link | navigate to /home | 導航到 Home 頁面，nav item 高亮 | navigation | 所有登入用戶（md+ 桌面顯示） | 確認 URL，nav item active 樣式 | screenshot of sidebar with home item active | src/components/Sidebar.tsx:47 |
| 550 | Sidebar（桌面導航） | **儀表板 nav link** | link | navigate to /dashboard | 導航到 Dashboard 頁面 | navigation | isAdmin OR isPM（assigned PM of any project） | 確認 URL 為 #/dashboard | screenshot of Dashboard | src/components/Sidebar.tsx:48 |
| 551 | Sidebar（桌面導航） | **工地 nav link** | link | navigate to /projects | 導航到 Projects 列表頁 | navigation | 所有登入用戶 | 確認 URL 為 #/projects | screenshot of Projects page | src/components/Sidebar.tsx:49 |
| 552 | Sidebar（桌面導航） | **管理 nav link** | link | navigate to /admin | 導航到 AdminProjects 頁面 | navigation | global_role = admin | 確認 URL 為 #/admin | screenshot of admin page | src/components/Sidebar.tsx:52 |
| 553 | Sidebar（桌面導航） | **用戶 nav link** | link | navigate to /admin/users | 導航到 AdminUsers 頁面 | navigation | global_role = admin | 確認 URL 為 #/admin/users | screenshot of admin users page | src/components/Sidebar.tsx:53 |
| 554 | Sidebar（桌面導航） | **資料完整性 nav link** | link | navigate to /admin/integrity | 導航到資料完整性頁面 | navigation | global_role = admin | 確認 URL 為 #/admin/integrity | screenshot of integrity page | src/components/Sidebar.tsx:54 |
| 555 | Sidebar（桌面導航） | **教學 nav link** | link | navigate to /help | 導航到教學頁面 | navigation | 所有登入用戶 | 確認 URL 為 #/help | screenshot of help page | src/components/Sidebar.tsx:57 |
| 556 | Sidebar（桌面導航） | **個人 nav link** | link | navigate to /profile | 導航到個人資料頁 | navigation | 所有登入用戶 | 確認 URL 為 #/profile | screenshot of profile page | src/components/Sidebar.tsx:58 |
| 557 | Sidebar（桌面導航）— 用戶 card | **登出（LogOut icon）** | icon-button | signOut() from AuthContext — pushLogoutUser() + supabase.auth.signOut() | 清除 onesignal_id，登出並導航到 /login | navigation | 已登入用戶（Sidebar 中的 user card） | 確認 URL 為 #/login，session 已清除 | screenshot of login page after signout | src/components/Sidebar.tsx:100-106 |
| 558 | Sidebar（桌面導航）— 在工地 scope 內 | **工地指令 nav link** | link | navigate to /project/:id/si | 導航到 SI 列表頁 | navigation | isModuleEnabled('si') = true，在 /project/:id 路徑下 | 確認 URL 為 #/project/:id/si | screenshot of SI list | src/components/Sidebar.tsx:146 |
| 559 | Sidebar（桌面導航）— 在工地 scope 內 | **變更指令 nav link** | link | navigate to /project/:id/vo | 導航到 VO 列表頁 | navigation | isModuleEnabled('vo') = true | 確認 URL 為 #/project/:id/vo | screenshot of VO list | src/components/Sidebar.tsx:147 |
| 560 | Sidebar（桌面導航）— 在工地 scope 內 | **工作許可證 nav link** | link | navigate to /project/:id/ptw | 導航到 PTW 列表頁 | navigation | isModuleEnabled('ptw') = true | 確認 URL 為 #/project/:id/ptw | screenshot of PTW list | src/components/Sidebar.tsx:148 |
| 561 | Sidebar（桌面導航）— 在工地 scope 內 | **天氣記錄 nav link** | link | navigate to /project/:id/weather | 導航到 WeatherRecord 頁面 | navigation | isModuleEnabled('weather') = true | 確認 URL 為 #/project/:id/weather | screenshot of WeatherRecord page | src/components/Sidebar.tsx:149 |
| 562 | Sidebar（桌面導航）— 在工地 scope 內 | **機械/表格 nav link** | link | navigate to /project/:id/equipment | 導航到 EquipmentList 頁面 | navigation | showEquipment = true（admin / assigned PM / approved pm·main_contractor·safety_officer）AND isModuleEnabled('equipment') | 確認 URL 為 #/project/:id/equipment | screenshot of equipment list | src/components/Sidebar.tsx:150 |
| 563 | Sidebar（桌面導航）— 在工地 scope 內 | **每日日誌 nav link** | link | navigate to /project/:id/daily | 導航到 DailyLog 頁面 | navigation | isModuleEnabled('dailies') = true | 確認 URL 為 #/project/:id/daily | screenshot of daily log page | src/components/Sidebar.tsx:151 |
| 564 | Sidebar（桌面導航）— 在工地 scope 內 | **物料 nav link** | link | navigate to /project/:id/materials | 導航到 Materials 頁面 | navigation | isModuleEnabled('materials') = true | 確認 URL 為 #/project/:id/materials | screenshot of materials page | src/components/Sidebar.tsx:152 |
| 565 | Sidebar（桌面導航）— 在工地 scope 內 | **行事曆 nav link** | link | navigate to /project/:id/timetable | 導航到 Timetable 頁面 | navigation | isModuleEnabled('timetable') = true | 確認 URL 為 #/project/:id/timetable | screenshot of timetable page | src/components/Sidebar.tsx:153 |
| 566 | Sidebar（桌面導航）— 在工地 scope 內 | **聯絡人 nav link** | link | navigate to /project/:id/contacts | 導航到 Contacts 頁面 | navigation | isModuleEnabled('contacts') = true | 確認 URL 為 #/project/:id/contacts | screenshot of contacts page | src/components/Sidebar.tsx:154 |
| 567 | Sidebar（桌面導航）— 在工地 scope 內 | **文件 nav link** | link | navigate to /project/:id/files | 導航到 Files/Documents 頁面 | navigation | isModuleEnabled('documents') = true | 確認 URL 為 #/project/:id/files | screenshot of files page | src/components/Sidebar.tsx:155 |

